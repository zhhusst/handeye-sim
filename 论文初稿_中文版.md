整体逻辑：

\[
\text{工程问题：依赖专家采集}
\rightarrow
\text{已有思路：自动规划与NBV}
\rightarrow
\text{科学障碍：二维轮廓使这些方法无法直接迁移}
\rightarrow
\text{本文方法}
\]

有几个地方需要收紧：

## 一、当前逻辑中需要调整的四点

### 1. 不要把应用动机只归结为“容易振动”

“焊接场景容易振动，所以需要快速标定”有些单薄。正常工作中的振动未必会改变刚性安装的手眼关系。更准确的应用动机是：

> 焊接现场中的碰撞、维护拆装、传感器重装、支架变形和长期热—机械漂移可能改变手眼外参，因此需要定期或按需重新标定。传统现场标定依赖人工示教，重复部署成本较高。

振动可以保留，但应当属于外参变化的原因之一，而不是唯一原因。

### 2. 不能说“这些标定方法都依赖人工经验”

这个表述太绝对。已经存在半自动甚至全自动方法，例如2025年的六位姿单平面方法，以及基于相机图像的自动焊接视觉标定。

建议改成：

> 尽管现有研究已经在简化标定物和提高求解稳定性方面取得了显著进展，但多数面向机器人携带二维轮廓仪的标定方法，仍采用人工示教、预设轨迹或随机采集获得多位姿观测。

这样既指出问题，又不否定已有自动化研究。

### 3. 必须区分两类“线激光传感器”

这是引言中非常重要的一点：

- 本文对象：工业二维激光轮廓仪，只输出厂家内标定后的 \(x-z\) 公制轮廓；
- [自动路径规划论文](/workspace/docs/A_Fast_Calibration_of_Laser_Vision_Robotic_Welding_Systems_Using_Automatic_Path_Planning.pdf)：线结构光视觉传感器，保留相机图像，可以识别棋盘格、图像中心和激光条纹。

如果统一叫“线激光传感器”，审稿人可能会认为后者已经解决了本文的问题。引言开头就应明确本文传感器的数据接口。

### 4. 三个研究缺口需要重新排列

你现在写的是：

1. 没有初始多视角；
2. 未来测量可能不存在；
3. 联合信息不等于手眼信息。

这三点本身正确，但缺少了最基础的“观测模型缺口”，导致第一条贡献“联合模型”没有直接对应的问题。

建议改为：

1. **观测建模缺口**：二维轮廓没有已知标志点的2D–3D对应关系，也不能从简单平板的单次扫描获得完整目标位姿，因此相机主动标定的重投影残差和候选Jacobian无法直接使用。
2. **冷启动缺口**：现有主动标定通常把若干具有非平行旋转轴的初始观测作为输入，但二维轮廓系统在手眼关系和标定物位姿均不准确时，难以自动获得这些初始观测。
3. **主动选位缺口**：未来轮廓是否存在取决于激光平面能否同时截到两条有限边；同时，平板几何必须联合估计，最大化联合信息不一定最大化手眼信息。

这样就与三条贡献严格一一对应。

## 二、建议采用的完整引言逻辑

### 第一段：应用背景与现场需求

机器人二维激光轮廓仪用于焊缝定位、轨迹跟踪和三维测量。为了将轮廓点变换到机器人基坐标系，必须准确标定轮廓仪与机器人法兰之间的手眼关系。现场碰撞、维护拆装、传感器重装和长期机械漂移可能造成外参变化，因此需要方便、快速且可重复的现场重标定。

这里首先建立“为什么需要反复标定”。

### 第二段：二维轮廓的特殊困难与现有求解方法

明确二维轮廓仪只输出

\[
p^S=[x,0,z]^\mathrm T,
\]

不包含图像纹理，也无法直接观察棋盘格图案。随后介绍现有方法：

- 复杂三维标定物：球、圆柱、多平面、四平面量具；
- 简单标定物：单平面、普通直边；
- 数学方法：平面约束、边缘共线约束、两阶段或联合优化。

这一段的结论不是说这些方法“不准确”，而是：

> 现有工作主要解决了“使用什么几何约束求解手眼参数”的问题。

### 第三段：由求解问题转向数据采集问题

接着提出工程缺口：

> 即使求解器已经给定，标定质量仍高度依赖采集位姿的分布。多数二维轮廓仪方法仍通过人工示教、随机运动或固定轨迹获得标定数据。

人工经验采集存在两个问题：

1. 为保证激励充分，往往采集大量位姿，增加时间和机器人运动成本；
2. 位姿数量多不等于信息充分。低旋转激励、重复视角和偏置分布会使方程病态；在噪声、离群点和模型误差存在时，增加低质量观测甚至可能降低实际估计质量。

这里不要简单写“位姿越多反而越差”，而要加上噪声、离群点和模型误差这一现实条件。

### 第四段：已有的两条自动化路线

这里分别介绍两条路线，不要混为一类。

第一条是自动路径规划：

- 自动路径规划论文通过棋盘格中心、相机光轴约束和图像反馈保持目标可见；
- 解决了传统人工示教问题；
- 但依赖完整相机图像。
- 该方法解决了“如何自动生成可见且具有较大运动差异的标定轨迹”，但位姿质量仍由预设几何约束和经验规则评价，尚未根据当前标定状态定量选择增量信息最大的后续位姿。
 
第二条是主动标定：

- Yang 2023根据Fisher信息预测不同候选位姿的参数不确定性下降；
- 证明信息增益NBV优于随机和最大距离策略；
- 但它以已知三维标志点和可检测图像投影为观测基础，并把初始多视角作为输入。

第四段可以这样写：

为减少人工示教，现有研究主要沿两条路线实现标定数据的自动采集。第一类方法通过视觉反馈和运动约束自动规划标定轨迹。例如，Yang等利用棋盘格中心、相机光轴以及旋转和距离约束，使机器人在保持标定板可见的同时自动采集具有较大姿态差异的观测。该方法使用能够获取完整棋盘格图像的相机—激光视觉传感器，而商用二维激光轮廓仪虽然可能提供调试视频或轮廓强度数据，其成像和输出接口主要面向激光三角测量，不能直接提供等价的棋盘格特征观测。此外，该方法使用预设几何约束和运动选择规则生成可行且多样的轨迹，但没有根据当前参数不确定性判断哪个可行位姿能够带来最大的新增标定信息。第二类方法将主动视觉引入手眼标定。Yang等根据候选位姿的Fisher信息预测参数不确定性下降，并通过NBV选择信息增益最大的后续位姿。然而，该方法仍依赖已知三维标志点及其图像投影，并将初始多视角观测作为输入，不能直接应用于只提供二维公制轮廓的激光轮廓仪。

这一段的结论应当是：

> 自动路径规划和信息驱动NBV已经证明，机器人可以自主采集标定数据；但它们的观测和初始化条件并不适用于仅输出二维公制轮廓的传感器。

### 第五段：指出直接迁移失败的三个原因

可以写成一段连续逻辑：

> 然而，将上述方法直接用于二维激光轮廓仪存在三个困难。首先，简单平板的单次二维轮廓既不提供已知标志点对应关系，也不能恢复完整标定物位姿，使相机主动标定使用的重投影模型和候选观测Jacobian无法直接构造。其次，信息驱动的候选选择依赖一个可靠初解，而现有方法通常假设若干初始多视角已经给定，二维轮廓系统则面临“需要初解才能规划、需要规划才能获得初解”的循环依赖。最后，候选位姿即使具有较高理论信息，也可能无法使激光平面同时截取两条有限物理边；同时，联合估计中的平板几何属于干扰变量，最大化联合状态信息并不必然最大化手眼参数信息。

这段是整篇引言最重要的部分。

## 三、核心科学问题

随后自然提出：

> 因此，本文研究的问题是：在精确手眼关系和标定物位姿未知、传感器仅输出无纹理二维公制轮廓的条件下，如何从一次有效观察自主建立可观初解，并选择能够稳定产生有效轮廓且最大化手眼参数信息的后续机器人位姿？

更短的版本是：

> 如何在二维轮廓部分观测条件下实现自启动、观测可行的主动手眼标定？

“减少人工经验依赖”是工程目标，“部分观测下如何自启动和主动选位”是科学问题。

## 四、三条贡献应严格对应三个缺口

### 贡献一：解决观测建模缺口

> 提出相邻双边轮廓联合标定模型，同时利用稠密轮廓共面约束和两条固定物理边的断点约束，联合估计手眼变换与未知平板几何，并通过变量投影消除公共角点，使不依赖图案和单帧完整目标位姿的联合求解与信息计算成为可能。

### 贡献二：解决冷启动缺口

> 提出单次有效双边观察驱动的自举初始化方法，在不依赖精确手眼关系和已知平板位姿进行三维目标预测的情况下，通过局部法兰运动、实际轮廓反馈和多轴旋转激励自动建立初始观测集及可靠联合初解。

注意写“单次有效观察”，不要把“六个种子”写成创新本体。

### 贡献三：解决主动选位缺口

> 提出观测可行且面向手眼边际信息的NBV方法，显式预测激光平面与两条有限边的交点及其有效概率，并通过Schur边际化消除平板几何信息，只选择能够产生有效双边轮廓并提高目标手眼参数信息的后续位姿。

### 三条贡献的通俗版本

其实三条贡献可以概括成三句话：

1.让二维轮廓能用于主动标定。
2.让系统能从一个初始观察自己起步。
3.让机器人每次去既看得到、又最值得采的位置。

对应到论文贡献，可以写得简洁一些。

#### 贡献一：把一次轮廓中的信息充分用起来
> 提出双边轮廓联合标定模型，一次扫描同时利用平板轮廓和两条相邻边的断点，在不识别棋盘格、也不要求单帧获得完整平板位姿的情况下，联合求解手眼关系和平板几何。

它解决的是：
二维轮廓没有图案和完整目标位姿，怎样建立可用于求解和主动选位的标定模型？

#### 贡献二：让系统从一个位姿自己开始

> 提出单初始观察自举方法。操作者只需找到一个能够稳定看到两条边的位姿，机器人随后根据实际轮廓反馈自动采集具有多轴旋转差异的初始数据，并建立主动标定所需的初解。

它解决的是：
NBV需要初解，但初解需要多个好位姿，这些初始位姿怎样在没有准确手眼关系时自动获得？

#### 贡献三：选择既看得到又最有用的下一位姿
> 提出面向二维轮廓的主动选位方法：先排除不能稳定看到两条边的候选，再从剩余位姿中选择对手眼参数新增信息最大的一个，并根据剩余信息收益自动决定是否继续采集。

它解决的是：
理论上信息大的位姿可能根本采不到轮廓；能够采到轮廓的位姿，也不一定是当前对手眼标定最有价值的位姿。

Schur补、变量投影、Sigma点和D-optimal这些术语不必塞进贡献句子，放到方法部分解释即可。贡献列表首先要让审稿人一眼看懂“做了什么、解决了什么”。

最终，三条贡献可以进一步压缩为：

> 本文首先建立了不依赖图案和单帧完整目标位姿的双边轮廓联合模型；随后使机器人能够从一个有效观察自主获得标定初解；最后在保证双边轮廓可观的前提下，持续选择对手眼参数最有价值的后续位姿。

## 五、最终逻辑链

你现在的思路经过上述调整后，可以压缩成：

\[
\begin{aligned}
&\text{现场需要频繁、快速重标定}\\
\Downarrow\quad\\
&\text{现有二维轮廓标定仍依赖人工、随机或固定轨迹采集}\\
\Downarrow\quad\\
&\text{自动路径规划和相机NBV表明自主采集是可行方向}\\
\Downarrow\quad\\
&\text{但二维部分观测破坏了其观测模型、初始化和候选预测前提}\\
\Downarrow\quad\\
&\text{科学问题：部分观测下如何自启动并主动选择有效高信息位姿}\\
\Downarrow\quad\\
&\text{双边联合模型＋自举初始化＋观测可行边际NBV}\\
\Downarrow\quad\\
&\text{实现降低专家位姿设计依赖的自动手眼标定。}
\end{aligned}
\]

最关键的修改是：不要只写“二维轮廓缺少初始多视角”，而要先建立“二维轮廓缺少可直接用于主动标定的观测模型”，这样第一条贡献才能成为整篇论文的理论起点。 

---

# 六、方法原理与代码实现对应

本章按照论文“方法”部分的写法组织，但在每个关键公式后增加“代码对应”。这样阅读时可以先理解数学对象和算法目的，再跳到当前真实实现核对。文中行号对应2026年8月4日工作区中的当前版本；以后如果修改了代码，函数名仍可作为稳定入口，行号可能随之移动。

需要先说明一个边界：本章描述的是**当前代码实际执行的方法**，不是只按理想算法重写。凡是论文核心方法、工程安全机制和尚未完成充分消融验证的策略，均会明确区分。

## 6.1 问题定义、前提条件与标定输出

### 6.1.1 坐标系与待估手眼变换

定义四个坐标系：

- $B$：机器人基坐标系；
- $F$：机器人法兰坐标系；
- $S$：二维激光轮廓仪坐标系；
- $P$：带直角平板的局部坐标系。

本文要估计的是传感器坐标系到法兰坐标系的刚体变换

$$
X\triangleq{}^{F}T_{S}
=
\begin{bmatrix}
R_X&t_X\\
0&1
\end{bmatrix}\in SE(3),
$$

其中 $R_X\in SO(3)$ 为手眼旋转，$t_X\in\mathbb R^3$ 为手眼平移。机器人在第 $i$ 个观测时刻提供法兰位姿

$$
T_i\triangleq{}^{B}T_{F_i}
=
\begin{bmatrix}
R_i&t_i\\
0&1
\end{bmatrix}.
$$

因此传感器在基坐标系中的位姿为

$$
{}^{B}T_{S_i}=T_iX,
$$

传感器坐标系中的一点 $p^S$ 变换到机器人基坐标系后为

$$
q_i^B(p^S;X)
=R_iR_Xp^S+t_i+R_it_X.
\tag{1}
$$

式（1）是稠密轮廓平面残差、双边断点残差和未来轮廓预测共同使用的坐标变换。

> 代码对应：位姿和观测数据结构见 [models.py：FlangePose，第31行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:31) 与 [models.py：Measurement，第45行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:45)；$SO(3)$指数映射和齐次变换见 [geometry.py：so3_exp，第13行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/geometry.py:13) 与 [geometry.py：make_transform，第67行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/geometry.py:67)。式（1）在残差构造中的直接实现见 [corner_projection.py：第34—40行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:34)。

### 6.1.2 当前方法真正需要的先验

当前方法并不是“完全没有任何先验”的无条件标定，它需要：

1. 轮廓仪已经完成厂家内部标定，能够输出公制 $X_S-Z_S$ 轮廓点 $p^S=[x,0,z]^T$；
2. 机器人能够给出与每帧轮廓同步的法兰位姿 $T_i$；
3. 已知平板两条目标边的长度 $L_u,L_v$ 及其相邻、正交的拓扑关系，但不需要预先测量平板在机器人基坐标系中的位姿；
4. 操作者给出一个能稳定观察到两条相邻物理边的初始位姿；
5. 12-DOF-V2非线性求解器仍需要一个粗略手眼安装值作为数值初值，但不要求人工精确测量。当前仿真压力配置故意使用 $15^\circ/200\,\mathrm{mm}$ 的粗初值误差。

这里必须区分两件事：**种子运动的生成不依赖手眼初值和平板位姿**；但是种子采完后的非线性联合求解需要粗安装值作为迭代初值。因此，论文中应写“无需精确人工初始测量”，不宜写“无需任何手眼先验”。

配置文件中的 board.corner 和 board.rotation 用于建立仿真场景真值，并不作为12-DOF-V2的已知输入。主动标定节点实际只读取两条边长，平板角点和方向由种子数据联合估计。

> 代码对应：粗手眼初值进入流水线见 [pipeline.py：第35—64行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:35)；仿真对粗初值施加 $15^\circ/200\,\mathrm{mm}$ 扰动见 [active_calibration_sim_node.py：第667—681行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:667)；当前配置值见 [calibration.yaml：第3—5行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:3)。

## 6.2 方法总流程

整个方法可以概括为

$$
\begin{aligned}
&\text{一次人工确认的有效双边观察}\\
&\rightarrow\text{局部法兰旋转与真实轮廓反馈}\\
&\rightarrow\text{六个具有多轴旋转激励的物理种子}\\
&\rightarrow\text{12-DOF-V2手眼—平板联合初解}\\
&\rightarrow\text{有限边未来轮廓预测与可行性筛选}\\
&\rightarrow\text{手眼边际信息增益NBV}\\
&\rightarrow\text{规划、执行、真实观测和滚动更新}\\
&\rightarrow\text{信息饱和后自动停止。}
\end{aligned}
\tag{2}
$$

对应的算法主循环为：

1. 接收原始轮廓、独立提取两个物理断点，并与同时间戳法兰位姿配对；
2. 检查初始观察是否落在保守工作包络内；
3. 保存参考种子，并围绕法兰局部 $x/y$ 轴自动获得其余种子；
4. 使用所有种子计算联合初解，并做真值无关的重采样稳定性检查；
5. 基于当前手眼和平板估计生成候选法兰位姿；
6. 预测候选能否同时截到两条有限边，并过滤传感器域、IK和碰撞不可行候选；
7. 对剩余候选计算手眼边际信息增益，选择最优可行候选；
8. MoveIt规划并执行，静止后采集一批同步轮廓；
9. 用新观测试求解，验收后提交，否则回退并重选；
10. 若剩余相对信息增益连续多轮低于阈值，则输出最后一次已验收解。

> 代码对应：不含机器人运动的算法状态机见 [pipeline.py：PipelineStage，第18行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:18) 和 [pipeline.py：ActiveCalibrationPipeline，第27行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:27)；ROS/Gazebo中的实际外层状态机见 [active_calibration_sim_node.py：_tick，第560行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:560)。这种分层意味着，将来接真实Gocator时可以保留核心求解与NBV，只替换消息和机器人执行适配层。

## 6.3 从原始二维轮廓到固定物理身份的双边断点

### 6.3.1 输入不是仿真真值断点

仿真和未来实机的共同输入均为有序公制轮廓

$$
\mathcal Q_i^S=\{p_{ij}^S=[x_{ij},0,z_{ij}]^T\}_{j=1}^{N_i}.
$$

当前标定链不直接使用Gazebo中的几何真值端点。独立节点从 `/gocator/profile` 接收 `PointCloud2`，提取端点后发布 `/calibration/endpoints`。该节点不订阅机器人状态，也不订阅仿真真值，因此同一个检测器可以接仿真发布器或真实Gocator驱动。

> 代码对应：ROS无关的检测器入口见 [endpoint_detector.py：ProfileEndpointDetector.detect，第90行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:90)；ROS适配器及话题参数见 [profile_endpoint_detector_node.py：第59—67行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_endpoint_detector_node.py:59)，实际回调链见 [profile_endpoint_detector_node.py：_callback，第209行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_endpoint_detector_node.py:209)。

### 6.3.2 间断分段、稳健直线拟合与半采样间距外推

检测过程依次执行：

1. 删除非有限点和孤立尖峰；
2. 估计相邻采样点的名义间距 $\Delta_s$；
3. 当相邻点距离超过
   $$
   \tau_g=\max(\tau_{\mathrm{abs}},k_g\Delta_s)
   $$
   时切断轮廓，形成若干连续段；
4. 对每个满足点数和长度要求的连续段做总最小二乘直线拟合；
5. 用垂线残差的中位数绝对偏差
   $$
   \hat\sigma_r=\max(1.4826\operatorname{MAD}(r),\sigma_{\min})
   $$
   去除离群点后再拟合；
6. 将拟合直线上最小和最大投影分别向外延长半个采样间距，得到断点
   $$
   \hat e_1=c+(s_{\min}-0.5\Delta_s)d,
   \qquad
   \hat e_2=c+(s_{\max}+0.5\Delta_s)d.
   \tag{3}
   $$

半采样间距外推用于补偿离散采样只落在真实边界内部造成的系统性收缩偏差。若两个候选连续段得分过于接近，则拒绝该帧，不强行猜测。

> 代码对应：名义采样间距和分段阈值见 [endpoint_detector.py：第127—142行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:127)；间断分段见 [endpoint_detector.py：_contiguous_segments，第160行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:160)；TLS和MAD筛选见 [endpoint_detector.py：第172—203行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:172)；式（3）的半间距外推见 [endpoint_detector.py：第209—224行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:209)；残差、端点不确定度和置信度见 [endpoint_detector.py：第232—266行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/perception/endpoint_detector.py:232)。参数入口见 [calibration.yaml：第62—81行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:62)。

### 6.3.3 为什么必须保持物理边身份

平板两条相邻边在模型中承担不同约束。因此，第一断点不能在某帧表示 $E_u$，下一帧又表示 $E_v$。设上一帧已标记端点为 $(e_u^-,e_v^-)$，当前无标签候选为 $(e_1,e_2)$，比较

$$
d_{\mathrm{direct}}
=\|e_1-e_u^-\|^2+\|e_2-e_v^-\|^2,
$$

$$
d_{\mathrm{swap}}
=\|e_2-e_u^-\|^2+\|e_1-e_v^-\|^2.
$$

选择代价较小的关联；若两种代价过于接近，则认为身份含糊并拒绝该帧。初次启动时只需配置一次“采样顺序中的第一个端点属于哪条物理边”，随后都按时间连续性追踪。

固定身份不等于固定深度顺序。合法观测可以满足 $z(e_v)>z(e_u)$，也可以满足相反顺序。当前初始检查使用

$$
|z(e_v)-z(e_u)|\ge\Delta z_{\min},
$$

而不是带符号不等式。

> 代码对应：直接/交换匹配与含糊拒绝见 [endpoint_tracker.py：EndpointTracker.match，第16行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/endpoint_tracker.py:16)；初次标签绑定和后续追踪见 [profile_endpoint_detector_node.py：第223—237行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_endpoint_detector_node.py:223)；绝对深度差判断见 [initial_pose.py：第68—95行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/initial_pose.py:68)。

## 6.4 双边角点联合几何模型

### 6.4.1 有限平板与两条目标边

用公共角点 $C\in\mathbb R^3$ 和旋转矩阵

$$
R_P=[u\;v\;n]\in SO(3),\qquad n=u\times v
$$

描述平板，其中 $u,v$ 为两条相邻边方向，$n$ 为板法向。已知边长为 $L_u,L_v$，平板区域为

$$
\mathcal P=\{C+au+bv\mid0\le a\le L_u,\;0\le b\le L_v\}.
$$

两条具有固定物理身份的目标边为

$$
E_u=\{C+au\mid0\le a\le L_u\},
$$

$$
E_v=\{C+bv\mid0\le b\le L_v\}.
$$

每帧数据写为

$$
D_i=(T_i,\mathcal Q_i^S,p_{u,i}^S,p_{v,i}^S),
$$

其中两个断点分别属于 $E_u,E_v$。

> 代码对应：平板的 $C,R_P,L_u,L_v$ 数据模型和方向属性见 [models.py：BoardModel，第71行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:71)；单帧数据转为求解器字段见 [models.py：Measurement.as_solver_dict，第61行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:61)。

### 6.4.2 稠密轮廓共面残差

利用式（1）把第 $i$ 帧第 $j$ 个轮廓点变换到基坐标系：

$$
q_{ij}=R_iR_Xp_{ij}^S+t_i+R_it_X.
$$

它应位于平板平面上，因此

$$
n^T(q_{ij}-C)=0.
$$

代码使用等价反号形式

$$
r_{p,ij}
=\sqrt{\frac{w_p}{N_i}}\,n^T(C-q_{ij}).
\tag{4}
$$

除以 $\sqrt{N_i}$ 的作用是：一帧轮廓的采样点数增加时，不应仅因采样更密就成比例压倒断点约束。

### 6.4.3 两个断点的相邻边残差

对于 $E_u$ 上的端点，存在标量 $a_i$ 使

$$
q_{u,i}=C+a_i u.
$$

因此它既位于平板平面内，又不含 $v$ 方向分量：

$$
r_{u,e}=\sqrt{w_e}\,v^T(C-q_{u,i}),
\qquad
r_{u,n}=\sqrt{w_n}\,n^T(C-q_{u,i}).
\tag{5}
$$

同理，$E_v$ 上的端点满足

$$
r_{v,e}=\sqrt{w_e}\,u^T(C-q_{v,i}),
\qquad
r_{v,n}=\sqrt{w_n}\,n^T(C-q_{v,i}).
\tag{6}
$$

式（4）主要约束“所有点落在同一平面”；式（5）和式（6）进一步约束两个断点分别落在两条固定相邻边上，从而补充普通单平面约束难以提供的平面内方向信息。这就是“双边角点模型提高单次位姿信息密度”的具体含义。

当前配置并非三个残差等权，而是

$$
w_p=1,\qquad w_e=0.25,\qquad w_n=0.25.
$$

该权重用于缓和断点噪声大于普通轮廓点噪声的问题。严格地说，只有当权重与测量协方差相匹配时，后续 $J^TJ$ 才能解释为严格Fisher信息；当前实现更准确的称呼是“加权Gauss–Newton信息近似”。

> 代码对应：式（4）的逐帧归一化和平面行见 [corner_projection.py：第34—42行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:34)；式（5）见 [corner_projection.py：第44—50行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:44)；式（6）见 [corner_projection.py：第53—59行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:53)；求解器接收三种权重见 [twelve_dof_v2.py：第63—79行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:63)；当前数值见 [calibration.yaml：第181—193行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:181)。

## 6.5 12-DOF-V2联合求解与变量投影

### 6.5.1 “12自由度”和“9维优化”并不矛盾

联合未知量包括：

- 手眼旋转 $\omega_X$：3维；
- 手眼平移 $t_X$：3维；
- 平板方向 $\omega_P$：3维；
- 公共角点 $C$：3维。

物理未知量共12维。但在固定前三项时，全部残差对 $C$ 都是线性的，所以非线性优化状态只保留

$$
x_9=
\begin{bmatrix}
\omega_X\\t_X\\\omega_P
\end{bmatrix}\in\mathbb R^9,
$$

并令

$$
R_X=\operatorname{Exp}(\omega_X),
\qquad
R_P=\operatorname{Exp}(\omega_P).
$$

因此该求解器应准确表述为：**12个几何自由度，9维非线性优化，3维公共角点解析投影。**

> 代码对应：9维状态的拆分见 [corner_projection.py：第23—27行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:23)；求解器初始状态 $x_0$ 的拼接见 [twelve_dof_v2.py：第102—114行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:102)。

### 6.5.2 公共角点的变量投影

固定 $x_9$ 后，每条残差都可以写成

$$
a_k(x_9)^TC-b_k(x_9).
$$

把所有行堆叠得到

$$
A(x_9)C\approx b(x_9).
$$

给定 $x_9$ 的条件最优角点为

$$
C^\star(x_9)
=\arg\min_C\|A(x_9)C-b(x_9)\|_2^2
=A(x_9)^+b(x_9),
\tag{7}
$$

并要求 $\operatorname{rank}(A)=3$。将式（7）代回得到约化残差

$$
r(x_9)=A(x_9)C^\star(x_9)-b(x_9).
\tag{8}
$$

最终求解

$$
\hat x_9
=\arg\min_{x_9}\frac12\|r(x_9)\|_2^2.
\tag{9}
$$

变量投影的意义不只是把维数从12降到9。更重要的是，每当 $x_9$ 在优化或NBV数值微分中发生扰动时，公共角点都会根据全部数据重新求得条件最优值，不会被错误地当作固定常数。

> 代码对应：构造 $A,b$ 见 [corner_projection.py：build_corner_system，第11行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:11)；式（7）及秩返回见 [corner_projection.py：solve_corner，第67行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:67)；式（8）见 [residual.py：variable_projection_residual，第13行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/residual.py:13)；式（9）的TRF最小二乘见 [twelve_dof_v2.py：第112—124行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:112)。

### 6.5.3 数值初值、可观测性和收敛条件

给定粗手眼初值后，代码先把各帧两个断点变换到基坐标系，分别拟合两组端点轨迹，得到平板 $u,v$ 方向初值并正交化。求解结束后计算变量投影约化Jacobian

$$
J=\frac{\partial r}{\partial x_9},
$$

当前用中心差分计算。为避免弧度和平移米数的量纲差异直接影响奇异值，定义状态尺度矩阵

$$
S=\operatorname{diag}
(10^\circ,10^\circ,10^\circ,
0.1,0.1,0.1,
10^\circ,10^\circ,10^\circ),
$$

并使用无量纲Jacobian

$$
\widetilde J=JS.
$$

当前求解结果只有同时满足以下条件才标为收敛：优化器成功、$\operatorname{rank}(\widetilde J)=9$、条件数不超过配置上限。

代码还构造局部协方差近似。设约化残差维数为 $m$。虽然数值优化变量只有9维，公共角点3维也由同一批残差拟合，因此残差方差的自由度按12个拟合参数扣除：

$$
\hat\sigma_r^2
=\max\left(
\frac{r^Tr}{\max(m-9-3,1)},
\sigma_{\mathrm{floor}}^2
\right).
$$

在无量纲状态中，

$$
\Sigma_{\widetilde x}
=\hat\sigma_r^2
\left(\widetilde J^T\widetilde J+\lambda_\Sigma I\right)^+,
$$

再映射回原量纲：

$$
\Sigma_{x9}=S\Sigma_{\widetilde x}S^T.
$$

这是局部线性化和当前加权残差下的近似协方差，不等于已经校准的真实置信区间。现有审计也已取消“仅靠协方差达到精度阈值便停止”的决策权；它目前主要用于候选状态扰动和日志诊断。

> 代码对应：从两组断点拟合平板方向初值见 [twelve_dof_v2.py：_initial_board_rotation，第38行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:38)；中心差分Jacobian见 [residual.py：numerical_jacobian，第26行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/residual.py:26)；状态尺度和无量纲Jacobian见 [information.py：第8—37行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:8)；协方差及12参数自由度见 [information.py：covariance_from_jacobian，第55行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:55)；奇异值、秩和条件数见 [information.py：observability，第99行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:99)；求解器调用这些诊断见 [twelve_dof_v2.py：第156—169行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:156)，最终收敛判断见 [twelve_dof_v2.py：第186—203行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:186)。

## 6.6 从一个有效观察自举获得联合初解

### 6.6.1 冷启动矛盾及解决思路

主动标定需要初解才能预测未来观测和计算候选信息；但可靠初解又需要若干具有旋转差异的观测。如果让操作者人工示教这些初始位姿，自动化问题并没有真正解决。

当前方法绕开这一循环的关键是：在种子阶段不预测平板的三维位姿，而只在机器人已知的法兰局部坐标中做小幅旋转，并用**实际采到的双边轮廓**判断该运动能否继续。也就是说，种子阶段依赖的是“运动后的真实反馈”，而不是“不可靠初解下的未来几何预测”。

### 6.6.2 初始观察的静态充分条件

操作者只需把机器人移动到一个满足保守工作包络的位姿。当前静态检查使用：

- 两个断点均处于安全测量域；
- 轮廓中点横向偏移 $|x_{\mathrm{mid}}|$ 足够小；
- 工作深度 $z_{\mathrm{mid}}$ 位于给定范围；
- 两端到安全域边界具有足够余量；
- 轮廓长度位于质量范围；
- $|z(e_v)-z(e_u)|$ 大于阈值，符号不限；
- 关节距离极限有余量；
- 局部 $\pm x/\pm y$ 小旋转中至少若干方向存在IK解。

这些条件是工程上的**保守充分条件**，不是12-DOF-V2的必要数学条件。当前各阈值尚不能在论文中宣称为理论最优工作包络。

> 代码对应：静态判据数据结构见 [initial_pose.py：InitialPoseCriteria，第16行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/initial_pose.py:16)，完整判定见 [initial_pose.py：assess_initial_pose，第58行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/initial_pose.py:58)；局部四方向IK试探见 [seed_collection_node.py：_local_ik_coverage，第851行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:851)；当前阈值见 [calibration.yaml：第135—152行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:135)。

### 6.6.3 星形局部旋转种子

保存参考位姿后，默认目标由“参考位姿 + 五条星形分支”组成：

$$
\mathcal S_0=
\{I,R_x(+\theta),R_x(-\theta),R_y(+\theta),R_y(-\theta),R_x(+\theta)R_y(+\theta)\}.
\tag{10}
$$

对当前法兰旋转 $R_{BF}$，局部小旋转命令为

$$
R_{BF}^{\mathrm{next}}
=R_{BF}\operatorname{Exp}(s\,\delta\,e_a),
\tag{11}
$$

其中 $a\in\{x,y\}$、$s\in\{-1,+1\}$、$\delta$ 为微旋转步长。右乘表示绕法兰局部轴旋转。式（11）不含 $X$ 和平板位姿，因此不会形成“先知道手眼才能采初解”的循环。

每次微旋转执行后，系统重新读取真实轮廓：若双边仍安全，则继续累积旋转；若接近测量域边界，则停止继续旋转、尝试平移居中或退回上一安全位姿。当前代码还包含符号/顺序回退和部分姿态保存，用于困难初始位姿下的工程恢复。

> 代码对应：默认五个参考后目标见 [rotation_scheduler.py：star_rotation_plan，第15行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/rotation_scheduler.py:15)；不依赖手眼的自适应回退分支说明见 [rotation_scheduler.py：第26—42行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/rotation_scheduler.py:26)；式（11）见 [seed_collection_node.py：第1158—1179行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1158)；运动后的真实双边检查见 [seed_collection_node.py：第1181—1206行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1181)；失败降步长、回退和partial分支见 [seed_collection_node.py：_rollback，第1404行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1404)。

### 6.6.4 多帧稳健聚合：降低断点随机误差，但不伪造几何激励

每个种子位姿静止后采集一批同步帧。对第 $m$ 帧构造四维断点特征

$$
y_m=[x_{u,m},z_{u,m},x_{v,m},z_{v,m}]^T.
$$

逐坐标计算中位数和MAD尺度，再定义整帧稳健距离

$$
d_m=\max_k
\frac{|y_{m,k}-\operatorname{median}(y_{:,k})|}
{\max(1.4826\operatorname{MAD}(y_{:,k}),\sigma_{\min})}.
$$

当 $d_m\le3.5$ 时保留该帧。这样一帧中只要任意一个断点坐标异常，整帧就被拒绝，避免把不同帧中的端点随意拼接。

保留下来的多帧最终聚合为**一个物理姿态观测**：旋转在 $SO(3)$ 切空间取均值，平移和断点取均值，轮廓点合并后限量采样。多帧的作用是降低同一静止位姿的随机测量误差，不应被当成多个独立机器人姿态去夸大可观测性。

> 代码对应：整帧四坐标MAD筛选见 [multiframe.py：robust_endpoint_inliers，第35行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/multiframe.py:35)；种子批采集入口见 [seed_collection_node.py：_begin_seed_capture，第1526行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1526)，逐帧保存同步法兰位姿与轮廓见 [seed_collection_node.py：_collect_seed_frame，第1550行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1550)，MAD验收见 [seed_collection_node.py：第1582—1618行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1582)；物理组的 $SO(3)$/平移/断点聚合见 [dataset_io.py：aggregate_seed_group，第52行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/dataset_io.py:52)。当前18帧是配置选择，不是理论常数，见 [calibration.yaml：第120—128行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:120)。

### 6.6.5 初解验收

六个物理种子先通过旋转差异检查，再进入12-DOF-V2。求解结果必须满秩且条件数合格。随后，在每个固定种子组内部有放回重采样帧，重新聚合六个物理观测并重复求解，比较各次结果与完整批次初解的旋转和平移差异。其目的不是利用仿真真值评价“准不准”，而是判断初解是否过度依赖偶然采到的某几帧。

> 代码对应：种子旋转Gram矩阵和最小两两角度见 [seed_observability.py：rotation_diversity，第10行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/seed_observability.py:10)；保存物理种子前的旋转分离检查见 [seed_collection_node.py：第1631—1648行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1631)；加载六组并联合初始化见 [active_calibration_sim_node.py：第653—765行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:653)；真值无关的组内bootstrap见 [initial_validation.py：bootstrap_initial_stability，第73行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/initial_validation.py:73)，初解不稳定时阻止NBV见 [active_calibration_sim_node.py：第808—830行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:808)。

### 6.6.6 可选动态预检的准确定位

动态预检有 `auto`、`always` 和 `off` 三种模式。它在正式种子采集前试走法兰局部 $\pm x/\pm y$ 小旋转，用真实轮廓检查哪些方向仍能保持双边可见，并可据此调整种子分支顺序。

这个模块不参与残差、不改变待估状态，也不是论文的核心理论贡献。它是减少困难初始位姿下无效运动的工程安全机制；即使关闭独立预检，正式种子运动仍会在每一步执行双边反馈检查和回退。

> 代码对应：三模式决策见 [initial_pose.py：dynamic_preflight_decision，第141行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/seed_collection/initial_pose.py:141)；节点决定执行或跳过预检见 [seed_collection_node.py：第1064—1089行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1064)；配置入口见 [calibration.yaml：第153—172行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:153)。

## 6.7 NBV候选的闭式构造

### 6.7.1 从两条有限边内部选择目标交点

联合初解给出 $\hat X,\hat C,\hat u,\hat v,\hat n$。在两条相邻边内部选择

$$
Q_u=\hat C+a\hat u,
\qquad
Q_v=\hat C+b\hat v,
$$

其中 $a,b$ 与边端保留安全距离。目标轮廓方向为

$$
d=\frac{Q_v-Q_u}{\|Q_v-Q_u\|},
$$

板内且与该方向正交的方向为

$$
m=\frac{\hat n\times d}{\|\hat n\times d\|}.
$$

候选倾角 $\alpha$ 和分支 $s\in\{-1,+1\}$ 定义激光平面法向

$$
\ell=\cos\alpha\,\hat n+s\sin\alpha\,m.
\tag{12}
$$

在当前传感器约定中，$y_S=\ell$。令零滚转方向 $x_0=d$、$z_0=x_0\times\ell$，再用 $\psi$ 调整激光平面内姿态：

$$
x_S=\cos\psi\,x_0+\sin\psi\,z_0,
$$

$$
z_S=-\sin\psi\,x_0+\cos\psi\,z_0.
$$

给定工作距离 $d_w$，传感器原点为

$$
t_{BS}^c=\frac{Q_u+Q_v}{2}-d_wz_S.
$$

由此得到名义传感器候选 ${}^BT_S^c$。

> 代码对应：式（12）和传感器坐标轴构造见 [candidate_generator.py：_sensor_transform，第13行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/candidate_generator.py:13)；$a,b,\alpha,\psi,d_w,s$ 的笛卡尔积见 [candidate_generator.py：第58—93行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/candidate_generator.py:58)；当前候选网格参数见 [calibration.yaml：第211—218行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:211)。

### 6.7.2 候选必须转成固定法兰指令

机器人真正执行的不是传感器位姿，而是法兰位姿。利用当前手眼估计生成

$$
{}^BT_F^c
= {}^BT_S^c({}^F\hat T_S)^{-1}.
\tag{13}
$$

式（13）一旦生成，在这个候选的有效性传播和信息评分过程中保持不变。否则，如果每次扰动手眼状态都重新生成“完美补偿后的法兰位姿”，就等于假设机器人预先知道真实手眼关系，会错误高估候选可控性并改变真实实验条件。

> 代码对应：式（13）见 [candidate_generator.py：第108—125行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/candidate_generator.py:108)；状态样本下用固定法兰命令重新计算传感器位姿见 [profile_predictor.py：predict_candidate，第96行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/profile_predictor.py:96)。

## 6.8 未来双边轮廓预测与候选可观性

### 6.8.1 激光平面与两条有限边解析求交

对于一个固定候选法兰命令和某个状态样本，传感器位姿记为 $(R,t)$，激光平面法向为

$$
\ell=Re_y.
$$

激光平面满足

$$
\ell^T(q-t)=0.
$$

它与两条目标边的交点坐标分别为

$$
\lambda_u=\frac{\ell^T(t-C)}{\ell^Tu},
\qquad
\lambda_v=\frac{\ell^T(t-C)}{\ell^Tv}.
\tag{14}
$$

候选有效必须满足：

1. $|\ell^Tu|$ 和 $|\ell^Tv|$ 不能过小，否则激光平面与目标边近似平行；
2. $\lambda_u,\lambda_v$ 落在有限边段内部，并距离边端保留余量；
3. 两交点变换到传感器坐标系后均位于安全测量域；
4. 两端之间的轮廓长度落在质量范围内。

与只判断棋盘格中心是否在图像中的方法不同，这里必须显式判断“激光平面能否同时截到两条指定的有限物理边”。这正是二维轮廓主动标定不能直接照搬相机NBV的关键原因。

> 代码对应：式（14）、近平行检查和有限边段检查见 [profile_predictor.py：第15—55行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/profile_predictor.py:15)；交点变到传感器系、ROI与长度检查见 [profile_predictor.py：第56—93行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/profile_predictor.py:56)；安全域和硬有效域的梯形模型见 [models.py：TrapezoidDomain，第108行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:108) 与 [models.py：SensorROI，第142行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:142)。

### 6.8.2 当前实现的Sigma点有效概率

当前局部估计写为

$$
x_9\sim\mathcal N(\hat x_9,\Sigma_{x9}).
$$

代码沿协方差九个特征方向生成中心点和正负扰动，共 $2\times9+1=19$ 个样本。对每个样本：

1. 用全部已有真实观测重新投影公共角点 $C^\star$；
2. 保持式（13）的法兰命令不变；
3. 重新计算式（14）并执行有限边、ROI、长度与退化检查。

用等权通过比例定义

$$
P_{\mathrm{valid}}(c)
=\frac1{19}\sum_{s=1}^{19}\mathbf 1
[c\text{在样本}s\text{下有效}].
\tag{15}
$$

当前先用 $P_{\mathrm{valid}}\ge0.8$ 作硬过滤，再比较信息增益。需要注意，式（15）是当前工程中的局部鲁棒性近似，不是经过完整概率校准的Unscented变换概率；现有消融审计把该模块标为 `UNPROVEN`。因此论文核心可以稳妥地表述为“显式有限边和测量域可观性预测”，Sigma点概率应在补齐A/B实验后再决定是否作为主要贡献。

> 代码对应：Sigma点生成见 [validity.py：sigma_points，第26行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/validity.py:26)；每个样本重新求角点并预测见 [validity.py：第76—108行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/validity.py:76)；式（15）见 [validity.py：第109—118行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/validity.py:109)；评分中的0.8硬门槛见 [scoring.py：第40—53行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/scoring.py:40)；证据边界见 [方法有效性消融审计.md：第23—34行](/workspace/docs/方法有效性消融审计.md:23)。

## 6.9 只针对手眼参数的信息增益

### 6.9.1 为什么不能直接最大化9维联合信息

约化状态 $x_9$ 中仍包含平板方向 $\omega_P$。如果直接比较整个9维信息，某个候选可能主要把平板方向估得更准，却没有显著改善真正关心的手眼参数。

用无量纲Jacobian构造Gauss–Newton信息矩阵

$$
H_9=\widetilde J^T\widetilde J
=
\begin{bmatrix}
H_{XX}&H_{XP}\\
H_{PX}&H_{PP}
\end{bmatrix},
$$

其中 $X$ 对应手眼6维，$P$ 对应平板方向3维。通过Schur补消去平板方向，得到手眼有效信息

$$
H_X^{\mathrm{eff}}
=H_{XX}
-H_{XP}(H_{PP}+\lambda I)^+H_{PX}.
\tag{16}
$$

公共角点 $C$ 已经在残差层通过变量投影消去；式（16）只消除剩余的平板方向。这是两层不同的干扰变量处理，不能混为一谈。

> 代码对应：无量纲Jacobian见 [information.py：scaled_jacobian，第31行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:31)；式（16）见 [information.py：effective_handeye_information，第40行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:40)。

### 6.9.2 固定虚拟观测与D-optimal增益

在当前解 $\hat x_9$ 处，用所有真实观测计算当前手眼信息 $H_{X,k}^{\mathrm{eff}}$。对候选 $c$，把其固定法兰命令和名义未来轮廓作为一条虚拟物理观测加入数据集，再次计算增广信息 $H_{X,k+c}^{\mathrm{eff}}$。候选的D-optimal增益定义为

$$
\Delta I_X(c)
=\frac12\left[
\log\det(H_{X,k+c}^{\mathrm{eff}}+\epsilon I)
-\log\det(H_{X,k}^{\mathrm{eff}}+\epsilon I)
\right].
\tag{17}
$$

虽然实际NBV静止后会采集多帧，但这些帧主要用于压低同一姿态的随机测量噪声，并不提供新的机器人姿态激励。因此候选评分只加入一条代表性虚拟物理观测，而不是把相同候选重复15次来夸大几何信息。

当前候选排序流程是：先排除名义几何无效和 $P_{\mathrm{valid}}$ 不足的候选，再按式（17）比较信息。ROS执行层还定义关节距离

$$
d_q=\|q_c-q_k\|_2,
$$

以及运动感知效用

$$
U(c)=\frac{\Delta I_X(c)}{1+w_md_q},
\tag{18}
$$

用于在高信息候选中减少过大的机器人运动。式（18）是效率导向的工程排序项，当前也属于尚待独立消融确认的机制；式（17）才是论文NBV的理论核心。

> 代码对应：当前Jacobian和手眼信息见 [scoring.py：第30—37行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/scoring.py:30)；固定虚拟法兰/轮廓和增广Jacobian见 [scoring.py：第54—67行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/scoring.py:54)；式（17）见 [information.py：information_gain，第85行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:85)，评分记录与排序见 [scoring.py：第68—86行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/scoring.py:68)；式（18）及执行层最终排序见 [active_calibration_sim_node.py：第858—877行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:858)。

## 6.10 可行性级联、机器人执行与同步采集

### 6.10.1 为什么“信息最大”不能直接执行

信息增益只评价参数敏感性，不保证机器人能到达或传感器能获得真实双边轮廓。当前实现按以下级联过滤：

1. 候选传感器是否处于平板允许侧；
2. 名义端点是否具有足够ROI余量；
3. 数值IK是否存在；
4. 单关节变化和总关节距离是否在限制内；
5. 是否与已提交NBV过于接近；
6. 解析未来双边预测及有效概率是否通过；
7. MoveIt目标状态是否碰撞；
8. MoveIt是否能规划出碰撞自由路径。

只有通过这些检查的候选才会执行。论文中可以把1、2、6概括为“观测可行性”，把3—5、7、8概括为“机器人运动可行性”，不必把每个阈值写成创新点。

> 代码对应：候选生成、均匀限量和评分调用见 [pipeline.py：rank_candidates，第198行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:198)；板侧、ROI、IK、关节增量和近重复过滤见 [active_calibration_sim_node.py：_candidate_kinematic_filter，第899行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:899)；目标碰撞检查见 [active_calibration_sim_node.py：第937—1011行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:937)；MoveIt规划请求和路径结果见 [active_calibration_sim_node.py：第1013—1084行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:1013)。

### 6.10.2 同时间戳法兰位姿与轮廓

每条残差默认 $T_i$ 和 $D_i$ 描述同一物理时刻。如果机器人仍在运动，却把轮廓与另一个时刻的法兰位姿配对，就会形成无法通过多帧平均消除的系统误差。

当前适配层用消息时间戳作为键，只在原始轮廓、检测端点和法兰位姿三者具有相同时间戳时接收一帧。实机迁移时必须由Gocator触发/时间戳与机器人控制器时间建立等价同步关系；仅仅在ROS回调中取“最近位姿”不够。

> 代码对应：原始轮廓、端点和法兰位姿缓存见 [active_calibration_sim_node.py：第419—491行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:419)；三者同键配对见 [active_calibration_sim_node.py：_try_accept_matched_frame，第493行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:493)；种子节点使用同一协议见 [seed_collection_node.py：_try_accept_matched_frame，第553行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:553)。

## 6.11 真实观测驱动的事务化滚动更新

候选到达并稳定后，系统不是直接相信预测轮廓，而是采集真实同步批次，并执行以下步骤：

1. 用四坐标MAD拒绝异常帧；
2. 检查所有保留帧端点均位于硬有效域；
3. 将保留帧划分为拟合组和只用于诊断的留出组；
4. 把拟合组聚合为一个新的物理NBV观测；
5. 用“全部已提交观测 + 新观测”重新运行12-DOF-V2；
6. 检查新解收敛、满秩，且相对上一解没有超过事务跳变上限；
7. 全部通过才提交新解、增加NBV计数并拉黑已用候选；否则恢复上一机器人位姿并重选。

因此，滚动更新不是用NBV预测值直接修正参数，而是NBV只决定“去哪里采”；最终参数更新始终由真实轮廓重新求解得到。

留出几何分数当前只用于日志诊断，不参与停止和“历史最佳结果”回滚。原因是已有审计表明该内部残差分数与仿真真值误差的相关性不足。当前最终结果是最后一次事务验收通过的滚动解。

> 代码对应：NBV批次采集触发见 [active_calibration_sim_node.py：第570—605行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:570)；真实批次MAD、有效域和拟合/留出划分见 [active_calibration_sim_node.py：_commit_observation，第1112行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:1112)；试求解、更新跳变检查和提交见 [pipeline.py：append_nbv_batch，第256行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:256)；失败回退和候选拉黑见 [active_calibration_sim_node.py：_rollback_after_failure，第1403行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:1403)；留出残差只作诊断见 [pipeline.py：第164—184行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:164) 和 [validation.py：held_out_geometric_metrics，第24行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/validation.py:24)。

## 6.12 自适应停止与输出

令第一次得到的最佳可行候选信息增益为 $\Delta I_{\mathrm{ref}}$，第 $k$ 轮剩余最佳增益为 $\Delta I_k^\star$。当前相对增益为

$$
\rho_k=\frac{\Delta I_k^\star}{\Delta I_{\mathrm{ref}}}.
\tag{19}
$$

当满足以下条件时认为信息饱和：

1. 已提交至少3个主动NBV；
2. 手眼有效信息矩阵满秩，即 $\operatorname{rank}(H_X^{\mathrm{eff}})=6$；
3. $\rho_k<0.05$ 连续出现3轮。

此外保留总物理位姿数上限作为紧急保护。控制台中的 `maximum_nbv_poses=0` 表示不设置用户NBV预算，由上述自适应策略停止；并不表示无限制运行，因为内部仍有总位姿保护。

> 代码对应：式（19）、连续低增益计数和停止条件见 [stopping.py：StopPolicy，第10行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/stopping.py:10)；流水线向停止器传递的是“六个物理种子 + 已提交NBV数”见 [pipeline.py：check_stop，第356行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:356)；自适应停止在每轮排序后执行见 [active_calibration_sim_node.py：第882—897行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:882)；用户NBV预算为0的语义见 [active_calibration_sim_node.py：第99—101行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:99)；当前5%、3轮和26总位姿配置见 [calibration.yaml：第238—247行](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:238)。

## 6.13 论文核心与工程实现的边界

为了避免方法章节再次变得臃肿，建议按下表决定哪些内容进入论文主线。

| 层级 | 内容 | 在论文中的位置 | 当前证据状态 |
|---|---|---|---|
| 核心方法 | 双边稠密平面 + 两条固定物理边断点联合模型 | 方法主体、贡献一 | 残差移除消融支持保留 |
| 核心方法 | 单初始观察下的局部法兰多轴种子自举 | 方法主体、贡献二 | 总体自动种子链已实现；具体分支策略仍需消融 |
| 核心方法 | 有限边未来轮廓解析预测 | 方法主体、贡献三 | 是二维轮廓可观性建模的必要组成 |
| 核心方法 | 变量投影 + Schur边际手眼信息 + D-optimal NBV | 方法主体、贡献三 | 主动NBV总体历史对照支持保留 |
| 鲁棒测量 | 原始轮廓分段、半间距断点外推、多帧平均 | 方法实现或实验设置 | 已有消融支持保留 |
| 工程保障 | 初始工作包络、动态预检、居中伺服、partial分支 | 实现细节或补充材料 | 多数仍为 `UNPROVEN`，不宜包装成理论贡献 |
| 工程保障 | IK、MoveIt、运动代价、近重复位姿过滤、事务回退 | 系统实现 | 有用，但多数具体策略仍待A/B测试 |
| 停止策略 | 最小NBV数 + 相对信息饱和 + 总位姿保护 | 方法末尾 | 当前5%/连续3轮回归支持保留 |
| 诊断项 | 协方差、留出几何残差、仿真真值误差 | 实验与日志 | 不再拥有停止或历史最佳选择权 |

证据状态的详细出处见 [方法有效性消融审计.md：机制表，第9行](/workspace/docs/方法有效性消融审计.md:9)。`KEEP`只表示在当前仿真数据域已有可重复证据，并不等于已经完成实机验证。

## 6.14 建议的代码阅读顺序

如果目的是“边看原理边真正读懂程序”，不建议从两个一千多行的ROS节点开读。更高效的顺序是：

1. 先看 [models.py：第31行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/models.py:31)，弄清一帧位姿、测量、平板、候选和结果分别保存什么；
2. 再看 [corner_projection.py：第11行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/corner_projection.py:11)，把式（4）—式（7）与每一行 `rows/rhs` 对上；
3. 看 [residual.py：第13行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/residual.py:13) 和 [twelve_dof_v2.py：第87行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/solvers/twelve_dof_v2.py:87)，理解9维优化与3维角点投影；
4. 看 [candidate_generator.py：第58行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/candidate_generator.py:58) 和 [profile_predictor.py：第15行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/profile_predictor.py:15)，理解一个候选怎样从板上两点变成固定法兰命令；
5. 看 [information.py：第40行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/v2_backend/information.py:40) 与 [scoring.py：第14行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/nbv/scoring.py:14)，理解为什么优化的是手眼边际信息而不是联合9维信息；
6. 看 [pipeline.py：第121行](/workspace/ros2_ws/src/handeye_sim_bridge/calibration_pipeline/pipeline.py:121)，沿“种子初始化—候选排序—NBV试提交—停止”读核心状态变化；
7. 再看 [seed_collection_node.py：第1038行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1038)，理解自动种子运动怎样调用上述数据结构；
8. 最后看 [active_calibration_sim_node.py：第653行](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_sim_node.py:653)，把联合初始化、候选评分、MoveIt执行、真实批次和滚动更新串起来。

用一句话概括当前代码实现：

> 系统先从一个真实有效的双边轮廓出发，用不依赖精确手眼和平板位姿的局部法兰运动自动建立多轴种子；再以双边角点联合模型求得手眼和平板初解；随后只在能够同时观察两条有限边且机器人可达的候选中，选择对手眼边际信息增益最大的下一位姿，并用真实同步轮廓反复滚动求解直至信息饱和。
