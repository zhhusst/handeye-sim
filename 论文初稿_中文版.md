# 线激光双边角点主动手眼标定方法原理

> 本文档仅说明当前已经用于真机实验的标定方法，不讨论论文要解决的问题、相关工作、创新性表述、实验结果或结论。文中的坐标系、状态量、公式和算法顺序均以当前代码为准。
>
> 当前真机默认方法为：原始二维轮廓与机器人位姿同步 → ROI 引导的双断点检测与 CSRT 关联跟踪 → 六物理位姿自举采集 → 多初值平面 12-DOF-V2 预求解 → 三阶共享形貌联合求解 → 观测可行性约束下的手眼信息增益 NBV → 事务式滚动更新 → 自适应停止。

## 1. 坐标系、变换约定与统一符号

### 1.1 坐标系

全文只使用以下四个坐标系：

| 符号 | 含义 |
|---|---|
| \(\mathcal B\) | 机器人基坐标系（base） |
| \(\mathcal F\) | 机器人末端法兰坐标系（flange） |
| \(\mathcal S\) | 二维激光轮廓仪坐标系（sensor） |
| \(\mathcal P\) | 标定件局部参考坐标系（plate） |

齐次变换

\[
{}^{\mathcal A}\mathbf T_{\mathcal D}
=
\begin{bmatrix}
{}^{\mathcal A}\mathbf R_{\mathcal D} & {}^{\mathcal A}\mathbf t_{\mathcal D}\\
\mathbf 0^\mathsf T & 1
\end{bmatrix}
\tag{1}
\]

表示把坐标系 \(\mathcal D\) 中的点变换到坐标系 \(\mathcal A\)。因此

\[
{}^{\mathcal A}\mathbf p
=
{}^{\mathcal A}\mathbf R_{\mathcal D}\,{}^{\mathcal D}\mathbf p
+{}^{\mathcal A}\mathbf t_{\mathcal D}.
\tag{2}
\]

待标定的眼在手上外参定义为

\[
\mathbf X
\triangleq{}^{\mathcal F}\mathbf T_{\mathcal S}
=
\begin{bmatrix}
\mathbf R_X & \mathbf t_X\\
\mathbf 0^\mathsf T&1
\end{bmatrix}.
\tag{3}
\]

第 \(i\) 个物理观测位姿的机器人正运动学结果为

\[
\mathbf T_i
\triangleq{}^{\mathcal B}\mathbf T_{\mathcal F,i}
=
\begin{bmatrix}
\mathbf R_i&\mathbf t_i\\
\mathbf 0^\mathsf T&1
\end{bmatrix}.
\tag{4}
\]

于是该位姿下传感器相对基坐标系的位姿为

\[
{}^{\mathcal B}\mathbf T_{\mathcal S,i}
=\mathbf T_i\mathbf X.
\tag{5}
\]

式（5）的乘法顺序是全文所有测量变换、候选预测和机器人指令反算的统一基础。

### 1.2 旋转参数化

对旋转向量 \(\boldsymbol\phi\in\mathbb R^3\)，定义

\[
\mathbf R=\operatorname{Exp}(\boldsymbol\phi^\wedge),
\qquad
\boldsymbol\phi=\operatorname{Log}(\mathbf R)^\vee,
\tag{6}
\]

其中 \((\cdot)^\wedge\) 把三维向量变为反对称矩阵，\((\cdot)^\vee\) 是其逆运算。代码中所有旋转优化量均使用旋转向量，而不是直接优化九个矩阵元素。

### 1.3 标定件局部几何

标定件所选物理角点在基坐标系中的位置记为

\[
\mathbf C\in\mathbb R^3.
\tag{7}
\]

从该角点引出的两条相邻物理边方向分别为 \(\mathbf u\) 和 \(\mathbf v\)，参考法向为 \(\mathbf n\)。三者组成右手正交坐标系

\[
\mathbf R_P=
\begin{bmatrix}
\mathbf u&\mathbf v&\mathbf n
\end{bmatrix}
=\operatorname{Exp}(\boldsymbol\phi_P^\wedge),
\qquad
\mathbf n=\mathbf u\times\mathbf v.
\tag{8}
\]

两条已知有限边记为

\[
\mathcal E_u=\{\mathbf C+a\mathbf u\mid 0\le a\le L_u\},
\qquad
\mathcal E_v=\{\mathbf C+b\mathbf v\mid 0\le b\le L_v\},
\tag{9}
\]

其中 \(L_u,L_v\) 是配置给定的标定件尺寸。当前真机配置为 \(L_u=0.20\ \mathrm m\)、\(L_v=0.15\ \mathrm m\)。

### 1.4 观测下标

- \(i=1,\ldots,M\)：物理机器人位姿编号；六种子结束时 \(M=6\)。
- \(f=1,\ldots,F_i\)：同一物理位姿下的同步帧编号。
- \(j=1,\ldots,N_{if}\)：一帧轮廓中被选为标定件表面的点编号。
- \(c\)：NBV 候选编号。
- \(s\)：不确定性传播样本编号。

传感器在第 \((i,f)\) 帧输出的表面轮廓点、\(u\) 边断点和 \(v\) 边断点分别记为

\[
{}^{\mathcal S}\mathbf p_{ifj},
\qquad
{}^{\mathcal S}\mathbf e_{u,if},
\qquad
{}^{\mathcal S}\mathbf e_{v,if}.
\tag{10}
\]

二维轮廓仪的测量点满足 \({}^{\mathcal S}y=0\)。下标 \(u,v\) 表示断点所属的物理边，不表示断点在图像中的左右顺序。

### 1.5 三种状态向量

为了避免“12-DOF”与共享形貌联合状态混淆，全文明确区分：

1. 平面模型的外层九维非线性状态

\[
\mathbf x_9=
\begin{bmatrix}
\boldsymbol\phi_X^\mathsf T&
\mathbf t_X^\mathsf T&
\boldsymbol\phi_P^\mathsf T
\end{bmatrix}^{\mathsf T}\in\mathbb R^9.
\tag{11}
\]

2. 12-DOF 几何状态

\[
\mathbf x_{12}=
\begin{bmatrix}
\mathbf x_9^\mathsf T&\mathbf C^\mathsf T
\end{bmatrix}^{\mathsf T}\in\mathbb R^{12}.
\tag{12}
\]

3. 共享形貌联合状态

\[
\boldsymbol\theta=
\begin{bmatrix}
\mathbf x_{12}^\mathsf T&\boldsymbol\beta^\mathsf T
\end{bmatrix}^{\mathsf T}
\in\mathbb R^{12+K},
\tag{13}
\]

其中 \(\boldsymbol\beta\in\mathbb R^K\) 是所有观测共同使用的固定形貌系数。当前三阶 Legendre 模型有 \(K=7\)，所以实际联合优化状态为 19 维。“共享形貌 12-DOF-V2”表示在 12 个几何自由度上增加形貌干扰参数，并不表示整个优化仍只有 12 维。

## 2. 方法总体流程

当前真机方法按以下顺序执行：

1. Gocator 发布原始二维轮廓；单位适配节点将毫米转换为米，并统一传感器坐标轴方向。
2. 同步节点为每帧轮廓寻找时间最近且静止的机器人关节状态，通过正运动学得到同时间戳的 \({}^{\mathcal B}\mathbf T_{\mathcal F}\)。
3. 操作者只设置一个参考观察，使目标表面弦段进入 RViz 紫色 ROI；系统检测并锁定两个物理断点。
4. 系统围绕参考法兰位姿执行局部星形微旋转，并用双特征平移伺服维持断点中心与断点间距，自动形成六个物理种子位姿。
5. 每个物理位姿采集一批静止同步帧，经帧级异常值剔除后聚合为一个物理观测。
6. 先用平面模型和四个离散旋转初值搜索合理解域，再以最优合理平面解初始化共享形貌联合求解。
7. 对六种子初解执行帧内自助重采样稳定性检查，并据此修正初解协方差。
8. 根据当前标定件局部参考系生成有限边双交点候选；对每个候选预测未来轮廓、传播联合状态不确定性，并计算边缘化后的手眼信息增益。
9. 将通过观测、运动学和碰撞检查的最高效用候选交给机器人执行。
10. 机器人停稳后采集新批次；只有当实测双边观测与试求解均通过时才提交该 NBV，否则回退且保持原结果不变。
11. 每次成功提交后重新估计 \(\boldsymbol\theta\)、重新生成候选并重新评分，直到信息增益饱和或触发紧急位姿上限。

这个流程包含两个不同的闭环：六种子阶段是“无需手眼初解的实测轮廓反馈闭环”；NBV 阶段是“当前联合估计驱动的预测—执行—验证—更新闭环”。

## 3. 原始轮廓、断点与同步观测

### 3.1 原始二维轮廓的度量化

Gocator 原始工程坐标以毫米表示。当前真机适配将其缩放为米，并按安装约定执行

\[
(x,y,z)_{\rm raw}\longmapsto
10^{-3}(x,-y,-z)_{\mathcal S}.
\tag{14}
\]

对二维轮廓，校准模型实际使用

\[
{}^{\mathcal S}\mathbf p=[x,0,z]^\mathsf T.
\tag{15}
\]

该步骤只做单位和坐标约定转换，不生成断点，也不加入人为端点噪声。

### 3.2 初始表面线段检测

操作者首先将目标表面弦段移入可视化模板。检测器在按 \(x\) 排序的轮廓中，找到最接近期望中点 \(x_0\) 的点作为生长起点。向左右两侧逐点扩展；当已有点拟合直线

\[
z=kx+d
\tag{16}
\]

后，新点仅在满足

\[
|z_j-(kx_j+d)|\le \tau_r
\tag{17}
\]

时被纳入表面集合。首次超过残差阈值的位置被视为表面与邻接结构的断裂边界。左右生长边界投影到最终拟合直线上，形成当前帧的两个实测断点。

初始匹配还检查断点与紫色模板的端点距离、弦方向差以及弦中点法向偏差。连续若干帧稳定后，系统才允许锁定。紫色模板只用于选择“是哪一块表面、哪一对边”，不进入手眼标定残差。

### 3.3 CSRT 的作用边界

锁定后，系统围绕两个断点分别初始化一个 CSRT 矩形跟踪器。为避免不同帧按各自范围缩放而造成虚假运动，轮廓始终映射到固定度量栅格：

\[
u_{\rm px}=\frac{x-x_{\min}}{r},
\qquad
v_{\rm px}=\frac{z_{\max}-z}{r},
\tag{18}
\]

其中 \(r\) 是固定的米/像素分辨率。

CSRT 输出的矩形中心只提供下一帧的局部关联先验。系统仍会在当前原始度量轮廓上重新执行式（16）—（17）的几何检测，并把当前实测线段边界作为断点。因此：

- CSRT 像素中心不是标定测量值；
- 进入求解器的端点始终来自当前帧原始轮廓；
- 跟踪器负责保持物理边身份，几何检测负责给出度量坐标。

如果连续跟踪失败，系统进入有界重捕获状态：以最后一次有效断点或模型预测断点为锚点，在扩大的局部区域中重新检测；只有断点位移、弦长和连续稳定帧均通过后，才重建 CSRT 跟踪器。

六种子运动期间采用连续跟踪。进入 NBV 后，不继续沿用种子阶段的运动轨迹外推，而是在机器人到达预测位姿前，把模型预测的断点对作为一次性关联先验。

### 3.4 轮廓与机器人位姿同步

设轮廓时间戳为 \(t_p\)，缓存中的机器人关节时间戳为 \(t_q^{(k)}\)。同步节点选择

\[
k^*=\arg\min_k|t_q^{(k)}-t_p|.
\tag{19}
\]

仅当时间偏差不超过阈值、机器人连续静止样本数量足够且估计最大关节速度低于阈值时，该轮廓才被接受。随后通过机器人正运动学计算

\[
\mathbf T_{if}=\operatorname{FK}(\mathbf q_{if}),
\tag{20}
\]

并给该法兰位姿赋予与轮廓完全相同的时间戳。当前实现只使用机器人停稳后的观测，不使用运动过程中的轮廓。

### 3.5 静止批次的帧级异常值剔除

每个物理位姿默认采集 18 帧。对第 \(f\) 帧构造端点特征

\[
\mathbf y_f=
\begin{bmatrix}
e_{u,x}&e_{u,z}&e_{v,x}&e_{v,z}
\end{bmatrix}^{\mathsf T}.
\tag{21}
\]

逐维中位数与稳健尺度为

\[
\widetilde{\mathbf y}=\operatorname{median}_f(\mathbf y_f),
\qquad
\boldsymbol\sigma_{\rm MAD}
=\max\!\left(1.4826\operatorname{median}_f
|\mathbf y_f-\widetilde{\mathbf y}|,\ \sigma_{\min}\right).
\tag{22}
\]

定义一帧的联合稳健距离

\[
d_f=\max_k
\frac{|y_{f,k}-\widetilde y_k|}{\sigma_{{\rm MAD},k}}.
\tag{23}
\]

当前采用 \(d_f\le3.5\) 作为内点条件；六种子每个位姿至少需要 14 个内点。

### 3.6 一个批次聚合为一个物理观测

多帧用于降低随机测量噪声，但不能伪装成多个不同观测几何。对物理位姿 \(i\) 的内点帧，以第一帧旋转 \(\mathbf R_{i1}\) 为参考，聚合位姿为

\[
\overline{\mathbf R}_i
=\mathbf R_{i1}
\operatorname{Exp}\!\left[
\frac{1}{F_i}\sum_{f=1}^{F_i}
\operatorname{Log}(\mathbf R_{i1}^{\mathsf T}\mathbf R_{if})
\right],
\tag{24}
\]

\[
\overline{\mathbf t}_i
=\frac{1}{F_i}\sum_{f=1}^{F_i}\mathbf t_{if},
\quad
\overline{\mathbf e}_{u,i}
=\frac{1}{F_i}\sum_{f=1}^{F_i}\mathbf e_{u,if},
\quad
\overline{\mathbf e}_{v,i}
=\frac{1}{F_i}\sum_{f=1}^{F_i}\mathbf e_{v,if}.
\tag{25}
\]

各帧表面点合并后均匀降采样到最多 200 点。由此，六种子解算使用的是六个聚合物理位姿，而不是 \(6\times18\) 个独立位姿。

## 4. 双边角点与共享形貌联合模型

### 4.1 观测点变换到基坐标系

对任一传感器点 \({}^{\mathcal S}\mathbf p\)，由式（2）—（5）得到

\[
{}^{\mathcal B}\mathbf q_{ifj}
=\mathbf R_{if}
(\mathbf R_X{}^{\mathcal S}\mathbf p_{ifj}+\mathbf t_X)
+\mathbf t_{if}.
\tag{26}
\]

为简化后文，把同一变换写成

\[
\mathbf q=\mathbf R_{BS,i}{}^{\mathcal S}\mathbf p+\mathbf t_{BS,i},
\quad
\mathbf R_{BS,i}=\mathbf R_i\mathbf R_X,
\quad
\mathbf t_{BS,i}=\mathbf t_i+\mathbf R_i\mathbf t_X.
\tag{27}
\]

### 4.2 理想平面双边约束

若标定件表面为理想平面，则所有表面点满足

\[
r_{p,ifj}=\mathbf n^\mathsf T(\mathbf q_{ifj}-\mathbf C)=0.
\tag{28}
\]

属于 \(\mathcal E_u\) 的断点必须同时位于参考表面和 \(u\) 边的法向截面内：

\[
\mathbf v^\mathsf T(\mathbf q_{u,if}-\mathbf C)=0,
\qquad
\mathbf n^\mathsf T(\mathbf q_{u,if}-\mathbf C)=0.
\tag{29}
\]

属于 \(\mathcal E_v\) 的断点满足

\[
\mathbf u^\mathsf T(\mathbf q_{v,if}-\mathbf C)=0,
\qquad
\mathbf n^\mathsf T(\mathbf q_{v,if}-\mathbf C)=0.
\tag{30}
\]

式（28）提供密集表面约束；式（29）—（30）提供两条有身份的相邻边约束。二者共用同一个角点 \(\mathbf C\) 和同一个局部坐标系 \(\mathbf R_P\)。

### 4.3 共享形貌模型

真实标定件可能相对其平均参考平面存在固定的、平滑的面外偏差。定义归一化局部坐标

\[
\xi(\mathbf q)=\frac{\mathbf u^\mathsf T(\mathbf q-\mathbf C)}{L_u},
\qquad
\eta(\mathbf q)=\frac{\mathbf v^\mathsf T(\mathbf q-\mathbf C)}{L_v}.
\tag{31}
\]

形貌高度表示为

\[
h_{\boldsymbol\beta}(\xi,\eta)
=\boldsymbol\Phi(\xi,\eta)^\mathsf T\boldsymbol\beta.
\tag{32}
\]

当前采用归一化 Legendre 乘积基。对总阶数 \(m=2,\ldots,d\)，包含所有 \(r+s=m\) 的项

\[
\Phi_{rs}(\xi,\eta)
=\frac{P_r(2\xi-1)P_s(2\eta-1)}{c_{rs}},
\qquad r+s\in\{2,\ldots,d\},
\tag{33}
\]

其中 \(c_{rs}\) 使每个基函数在 \([0,1]^2\) 上具有单位均方尺度。形貌系数个数为

\[
K=\sum_{m=2}^{d}(m+1)
=\frac{(d+1)(d+2)}{2}-3.
\tag{34}
\]

当前 \(d=3\)，故 \(K=7\)。

常数项没有被加入，因为它与 \(\mathbf C\) 沿 \(\mathbf n\) 的平移不可区分；一阶项没有被加入，因为它与参考平面绕 \(\mathbf u,\mathbf v\) 的微小倾斜不可区分。删除这三类基函数是消除显式 gauge freedom 的必要参数化约束。

共享形貌表面为

\[
\mathcal S_P(\boldsymbol\theta)=
\left\{
\mathbf C+L_u\xi\mathbf u+L_v\eta\mathbf v
+h_{\boldsymbol\beta}(\xi,\eta)\mathbf n
\mid 0\le\xi,\eta\le1
\right\}.
\tag{35}
\]

这里的“共享”是指同一个 \(\boldsymbol\beta\) 被所有物理位姿共同使用。若每个位姿各估一套形貌，外参误差就可能被每帧独立形貌吸收，手眼关系将失去约束。

### 4.4 共享形貌残差

任一点相对共享表面的有符号残差为

\[
r_s(\mathbf q;\boldsymbol\theta)
=\mathbf n^\mathsf T(\mathbf q-\mathbf C)
-h_{\boldsymbol\beta}
\bigl(\xi(\mathbf q),\eta(\mathbf q)\bigr).
\tag{36}
\]

第 \(i\) 个物理位姿的残差向量由以下部分组成：

\[
\mathbf r_i(\boldsymbol\theta)=
\begin{bmatrix}
\sqrt{w_p/N_i}\,r_s(\mathbf q_{i1})\\
\vdots\\
\sqrt{w_p/N_i}\,r_s(\mathbf q_{iN_i})\\
\sqrt{w_e}\,\mathbf v^\mathsf T(\mathbf q_{u,i}-\mathbf C)\\
\sqrt{w_s}\,r_s(\mathbf q_{u,i};\boldsymbol\theta)\\
\sqrt{w_e}\,\mathbf u^\mathsf T(\mathbf q_{v,i}-\mathbf C)\\
\sqrt{w_s}\,r_s(\mathbf q_{v,i};\boldsymbol\theta)
\end{bmatrix}.
\tag{37}
\]

表面点权重除以 \(N_i\)，使一个采样更密的位姿不会仅因点数更多而压倒其他物理位姿。当前权重为 \(w_p=1\)、\(w_e=0.25\)、\(w_s=0.25\)。

## 5. 平面 12-DOF-V2 预求解

共享形貌联合问题非线性较强。当前真机系统先求解理想平面模型，用它确定正确解域，再进入共享形貌细化。

### 5.1 为什么角点可以在内层线性求解

给定 \(\mathbf x_9\) 后，\(\mathbf R_X,\mathbf t_X,\mathbf u,\mathbf v,\mathbf n\) 都已确定，所有变换后的 \(\mathbf q\) 也已确定。式（28）可改写为

\[
\mathbf n^\mathsf T\mathbf C
=\mathbf n^\mathsf T\mathbf q.
\tag{38}
\]

同理，式（29）—（30）的每个标量约束均可写为

\[
\mathbf d_k^\mathsf T\mathbf C
=\mathbf d_k^\mathsf T\mathbf q_k,
\qquad
\mathbf d_k\in\{\mathbf n,\mathbf u,\mathbf v\}.
\tag{39}
\]

把所有带权行堆叠，得到

\[
\mathbf A(\mathbf x_9)\mathbf C
\simeq\mathbf b(\mathbf x_9).
\tag{40}
\]

因此角点的最小二乘解为

\[
\mathbf C^*(\mathbf x_9)
=\arg\min_{\mathbf C}
\|\mathbf A(\mathbf x_9)\mathbf C-
\mathbf b(\mathbf x_9)\|_2^2
=\mathbf A(\mathbf x_9)^+\mathbf b(\mathbf x_9).
\tag{41}
\]

这说明计算顺序不是“先永久求一个角点，再求 \(\mathbf x_9\)”，而是：外层每提出一个新的 \(\mathbf x_9\)，内层就重新构造 \(\mathbf A,\mathbf b\) 并重新计算 \(\mathbf C^*(\mathbf x_9)\)。

### 5.2 变量投影目标函数

将式（41）代回式（40），定义约化残差

\[
\mathbf r_{\rm flat}(\mathbf x_9)
=\mathbf A(\mathbf x_9)\mathbf C^*(\mathbf x_9)
-\mathbf b(\mathbf x_9).
\tag{42}
\]

外层问题为

\[
\widehat{\mathbf x}_9
=\arg\min_{\mathbf x_9}
\frac12\|\mathbf r_{\rm flat}(\mathbf x_9)\|_2^2,
\qquad
\widehat{\mathbf C}=\mathbf C^*(\widehat{\mathbf x}_9).
\tag{43}
\]

当前代码使用 SciPy `least_squares` 的 trust-region reflective（TRF）算法，并令 `x_scale="jac"`。雅可比由中心差分计算。中心差分每扰动一次 \(\mathbf x_9\)，都会再次执行式（41），所以角点对外层状态的隐式变化已包含在数值雅可比中。

平面模型对外公开的非线性数据秩要求是 \(\operatorname{rank}(\mathbf J_{x_9})=9\)，同时式（40）的角点线性系统必须达到秩 3。虽然 \(\mathbf C\) 已从外层状态中投影消去，但它仍由同一批数据拟合，因此平面模型估计残差方差时，自由度会额外扣除这 3 个线性干扰参数。

### 5.3 多初值与几何合理性筛选

二维轮廓全部位于 \(y_{\mathcal S}=0\) 平面，传感器轴约定可能产生离散的 \(180^\circ\) 解域歧义。初次六种子求解从名义安装旋转 \(\mathbf R_{X,0}\) 的四个正规旋转假设出发：

\[
\mathbf R_{X,0},\quad
\mathbf R_{X,0}\mathbf R_x(\pi),\quad
\mathbf R_{X,0}\mathbf R_y(\pi),\quad
\mathbf R_{X,0}\mathbf R_z(\pi).
\tag{44}
\]

每个初值独立执行式（43）。当前真机实验中标定件近似水平放置，因此解出的参考法向需满足

\[
\arccos\!\left(|\widehat{\mathbf n}^{\mathsf T}\mathbf e_z^{\mathcal B}|\right)
\le20^\circ.
\tag{45}
\]

绝对值允许法向正负翻转，且不限制平面内偏航角。系统在收敛且通过式（45）的候选中选取平面残差代价最小者。该筛选是当前真机装置的已知放置先验，不是一般场景下必然成立的几何定理。

共享形貌细化完成后还会再次执行式（45）。也就是说，平面预求解负责选择解域，最终共享形貌解也必须保持在同一个物理合理域内。

端点身份用于统一 \(\mathbf u,\mathbf v\) 的顺序和符号，上一轮标定件方向用于保持滚动更新中的坐标系连续性。

## 6. 共享形貌联合求解

### 6.1 从平面解初始化 19 维状态

将通过筛选的平面解扩展为

\[
\boldsymbol\theta_0=
\begin{bmatrix}
\widehat{\mathbf x}_9^\mathsf T&
\widehat{\mathbf C}^\mathsf T&
\mathbf 0_K^\mathsf T
\end{bmatrix}^{\mathsf T}.
\tag{46}
\]

共享形貌联合目标为

\[
\widehat{\boldsymbol\theta}
=\arg\min_{\boldsymbol\theta}
\frac12
\left[
\sum_{i=1}^{M}\|\mathbf r_i(\boldsymbol\theta)\|_2^2
+\lambda\|\boldsymbol\beta\|_2^2
\right].
\tag{47}
\]

当前 \(\lambda=0.01\)。正则项表达对过大形貌幅值的弱先验，不属于传感器观测。

与平面模型不同，共享形貌模型不能直接沿用角点变量投影，因为式（31）中的 \(\xi,\eta\) 也依赖 \(\mathbf C\)，形貌高度随 \(\mathbf C\) 非线性变化。因此式（47）同时优化 \(\mathbf C\) 与 \(\boldsymbol\beta\)。

优化器仍为 TRF，但使用显式状态尺度：手眼旋转 \(10^\circ\)、手眼平移 \(0.1\ \mathrm m\)、标定件旋转 \(10^\circ\)、角点平移 \(0.1\ \mathrm m\)、形貌系数 \(0.5\ \mathrm{mm}\)。这些尺度只用于数值归一化，不改变物理残差定义。

### 6.2 数据可观性与先验支撑必须分开

令观测残差为 \(\mathbf r_d\)，正则残差为 \(\mathbf r_\lambda=\sqrt\lambda\boldsymbol\beta\)。对应雅可比为

\[
\mathbf J_d=\frac{\partial\mathbf r_d}{\partial\boldsymbol\theta},
\qquad
\mathbf J_a=
\frac{\partial}{\partial\boldsymbol\theta}
\begin{bmatrix}\mathbf r_d\\\mathbf r_\lambda\end{bmatrix}.
\tag{48}
\]

若用 \(\mathbf J_a\) 判断秩，正则项可能把没有数据支撑的形貌方向“托成满秩”。因此当前实现规定：

- 收敛判定、公开的 rank/condition 和最弱方向使用 \(\mathbf J_d\)；
- 后验协方差和 NBV 当前信息矩阵允许使用 \(\mathbf J_a\)，因为它们描述的是数据与既定先验共同形成的局部后验；
- 三阶共享模型只有在 \(\operatorname{rank}(\mathbf J_d)=19\) 时才被视为数据可观。

这就是 `data-only rank/condition` 与 `prior-augmented rank/condition` 两套诊断的区别。

### 6.3 数值尺度、协方差与手眼边缘信息

设物理状态尺度矩阵为

\[
\mathbf S=\operatorname{diag}(s_1,\ldots,s_{12+K}),
\qquad
\widetilde{\boldsymbol\theta}=\mathbf S^{-1}\boldsymbol\theta.
\tag{49}
\]

对无量纲状态的雅可比为

\[
\widetilde{\mathbf J}=\mathbf J\mathbf S.
\tag{50}
\]

以数据残差估计残差方差

\[
\widehat\sigma^2
=\frac{\mathbf r_d^\mathsf T\mathbf r_d}
{\max(n_d-n_\theta,1)},
\tag{51}
\]

再由增广雅可比形成局部后验协方差

\[
\boldsymbol\Sigma_\theta
=\mathbf S
\left[
\widehat\sigma^2
(\widetilde{\mathbf J}_a^\mathsf T
\widetilde{\mathbf J}_a+\epsilon\mathbf I)^{+}
\right]
\mathbf S.
\tag{52}
\]

式（51）不把正则项当作新的有噪测量；式（52）则保留先验对后验曲率的作用。

### 6.4 六种子初解的自助重采样稳定性

真机没有手眼真值，因此初解不能通过“真值误差”准入。系统在每个固定物理种子内部有放回抽取帧，重新执行聚合与联合求解。第 \(b\) 次重采样解相对完整批次解的变化为

\[
\delta R_b=
\operatorname{angle}
(\widehat{\mathbf R}_X^\mathsf T\widehat{\mathbf R}_{X,b}),
\qquad
\delta t_b=
\|\widehat{\mathbf t}_{X,b}-\widehat{\mathbf t}_X\|_2.
\tag{53}
\]

当前执行 4 次试验，并检查收敛比例以及 \(P_{95}(\delta R_b)\)、\(P_{95}(\delta t_b)\)。如果稳定性通过，则将手眼协方差的对角元至少扩张到

\[
\sigma_R^2=\left(\frac{P_{95}(\delta R)}{1.96}\right)^2,
\qquad
\sigma_t^2=\left(\frac{P_{95}(\delta t)}{1.96}\right)^2.
\tag{54}
\]

该操作不会改变初解均值，只防止首轮 NBV 把六种子解误认为过度精确。

## 7. 无需手眼初解的六种子自动采集

### 7.1 初始参考观察

六种子采集开始前尚无可靠的 \(\widehat{\mathbf X}\) 和 \(\widehat{\mathcal P}\)，因此初始准入只使用传感器轮廓、机器人关节余量和局部逆运动学，不使用标定结果。

对两个实测断点定义

\[
x_{\rm mid}=\frac{e_{u,x}+e_{v,x}}{2},
\qquad
z_{\rm mid}=\frac{e_{u,z}+e_{v,z}}{2},
\qquad
\ell=\|\mathbf e_u-\mathbf e_v\|_2.
\tag{55}
\]

当前真机初始工作包络要求：双边在安全域内、\(|x_{\rm mid}|\le10\ \mathrm{mm}\)、\(80\le z_{\rm mid}\le450\ \mathrm{mm}\)、\(70\le\ell\le90\ \mathrm{mm}\)、\(|e_{v,z}-e_{u,z}|\ge15\ \mathrm{mm}\)、最小归一化关节余量不低于 5%，并且局部 \(\pm X/\pm Y\) 四个方向中至少三个方向通过 IK。它是当前设备的充分操作范围，不是求解公式的必要条件。

### 7.2 局部法兰星形旋转

参考位姿记为 \((\mathbf R_F^0,\mathbf t_F^0)\)。每个微旋转直接在法兰局部坐标系中施加：

\[
\mathbf R_F^{+}
=\mathbf R_F
\operatorname{Exp}(\Delta\boldsymbol\phi_F^\wedge).
\tag{56}
\]

式（56）不需要手眼外参或标定件位姿。当前目标顺序为

\[
\text{reference},\quad
R_y(+10^\circ),\quad
R_y(-10^\circ),\quad
R_x(+10^\circ),\quad
R_x(-10^\circ),\quad
R_{xy}(+10^\circ,+10^\circ),
\tag{57}
\]

其中每条分支由 \(1^\circ\) 微步开始；获得足够安全反馈后可提升到 \(2^\circ\)。若完整目标不可达，可在累计旋转不低于 \(5^\circ\) 时保存安全部分位姿，或改用不同符号及对角组合的候选分支。

### 7.3 双特征局部平移伺服

旋转会使激光弦在标定件上平移或伸缩。只控制 \(x_{\rm mid}\) 无法防止激光线离开目标角或切换到其他边，因此当前真机控制特征为

\[
\mathbf s=
\begin{bmatrix}x_{\rm mid}&\ell\end{bmatrix}^{\mathsf T},
\qquad
\mathbf s^*=\begin{bmatrix}0&0.080\end{bmatrix}^{\mathsf T}\ \mathrm m.
\tag{58}
\]

实际控制采用死区：\(x_{\rm mid}\) 在居中容差内时误差置零，\(\ell\in[0.070,0.090]\ \mathrm m\) 时长度误差置零；一旦越过长度死区，长度误差改为 \(\ell-0.080\ \mathrm m\)，使控制量返回带宽内部而不是停在边界。

在参考位姿沿局部法兰 \(x_F,y_F,z_F\) 各执行一次 \(1\ \mathrm{mm}\) 探测。设三次实测局部位移和特征变化组成

\[
\Delta\mathbf Q=
\begin{bmatrix}
\Delta\mathbf q_1&\Delta\mathbf q_2&\Delta\mathbf q_3
\end{bmatrix}\in\mathbb R^{3\times3},
\quad
\Delta\mathbf S=
\begin{bmatrix}
\Delta\mathbf s_1&\Delta\mathbf s_2&\Delta\mathbf s_3
\end{bmatrix}\in\mathbb R^{2\times3}.
\tag{59}
\]

局部特征雅可比初始化为

\[
\mathbf J_s=\Delta\mathbf S\,\Delta\mathbf Q^+
\in\mathbb R^{2\times3},
\qquad
\Delta\mathbf s\approx\mathbf J_s\Delta\mathbf q_F.
\tag{60}
\]

给定死区后的特征误差 \(\mathbf e_s\)，阻尼最小二乘修正为

\[
\Delta\mathbf q_F
=-\gamma\mathbf J_s^\mathsf T
(\mathbf J_s\mathbf J_s^\mathsf T+\mu^2\mathbf I_2)^{-1}
\mathbf e_s.
\tag{61}
\]

修正量再经过单轴幅值和三维范数限幅。局部位移转换到基坐标系后更新法兰平移：

\[
\mathbf t_F^{+}=\mathbf t_F+\mathbf R_F\Delta\mathbf q_F.
\tag{62}
\]

### 7.4 Broyden 在线更新

每次有效平移后，使用机器人实际到达位姿计算 \(\Delta\mathbf q_F\)，而不是使用命令值。良 Broyden 更新为

\[
\mathbf J_s^{+}
=\mathbf J_s+
\frac{(\Delta\mathbf s-\mathbf J_s\Delta\mathbf q_F)
\Delta\mathbf q_F^\mathsf T}
{\Delta\mathbf q_F^\mathsf T\Delta\mathbf q_F}.
\tag{63}
\]

只有当位移超过分辨率、割线模型误差比例正常，且更新后的 \(\mathbf J_s^+\) 满足秩 2、最小奇异值和条件数门限时，更新才被接受。连续拒绝更新会触发局部重新探测。

### 7.5 旋转扰动前馈

设一次微旋转幅值为 \(\Delta\theta\)，该步同时发生的实测局部平移为 \(\Delta\mathbf q_F\)，特征总变化为 \(\Delta\mathbf s\)。扣除平移解释的部分，旋转引起的特征变化率估计为

\[
\mathbf g_{\rm meas}
=\frac{\Delta\mathbf s-\mathbf J_s\Delta\mathbf q_F}{\Delta\theta}.
\tag{64}
\]

采用指数平滑

\[
\mathbf g^{+}=(1-\eta)\mathbf g+\eta\mathbf g_{\rm meas}.
\tag{65}
\]

下一次旋转前预测

\[
\mathbf s_{\rm pred}=\mathbf s+\mathbf g\Delta\theta,
\tag{66}
\]

并将 \(\mathbf s_{\rm pred}-\mathbf s^*\) 代入式（61），把预测平移与旋转合并到同一个法兰目标中。前馈减少“先旋转、再多次纠偏”的往返动作；实测反馈仍负责最终验证。

### 7.6 每个微运动都是事务

在任何旋转、探测或伺服动作后，只有同时满足以下条件才更新跟踪状态和在线雅可比：

- 两个断点仍属于锁定的物理边；
- 断点位于测量硬域内并满足安全检查；
- 断点弦长位于 \([50,120]\ \mathrm{mm}\) 的物理边身份硬保护范围；
- 机器人确实到达并稳定；
- 断点关联没有跳变。

若失败，则数据、\(\mathbf J_s\) 和已完成旋转量均不提交；机器人回到上一已验证位姿并减小角步长。必要时回到参考位姿、执行局部重捕获、保存满足最小旋转量的部分分支，或放弃当前分支选择备用分支。

### 7.7 可选动态预检

`auto/always/off` 动态预检只是在正式种子采集前试走局部 \(\pm X/\pm Y\) 的小角度方向，并据实测安全性调整分支顺序。无论是否启用预检，正式微运动仍执行第 7.6 节的实时检查和回退。因此预检是可选工程保护，不是双边模型、联合求解或 NBV 的数学组成部分。

### 7.8 种子旋转多样性

设六个聚合法兰旋转为 \(\overline{\mathbf R}_i\)。系统检查最小两两旋转距离，并计算

\[
\mathbf G_R
=\sum_{1\le i<j\le6}
(\overline{\mathbf R}_i-\overline{\mathbf R}_j)^\mathsf T
(\overline{\mathbf R}_i-\overline{\mathbf R}_j).
\tag{67}
\]

\(\mathbf G_R\) 的三个特征值用于描述旋转激励在三维方向上的覆盖。它是种子质量诊断，不直接替代式（47）的数据雅可比秩检查。

## 8. NBV 候选构造与未来观测预测

### 8.1 在已建立的局部参考系中选取双边交点

得到 \(\widehat{\mathbf C},\widehat{\mathbf u},\widehat{\mathbf v},\widehat{\mathbf n}\) 后，在两条有限边的安全内部采样

\[
\mathbf q_u=\widehat{\mathbf C}+a\widehat{\mathbf u},
\qquad
\mathbf q_v=\widehat{\mathbf C}+b\widehat{\mathbf v},
\tag{68}
\]

其中

\[
m_e\le a\le L_u-m_e,
\qquad
m_e\le b\le L_v-m_e.
\tag{69}
\]

当前边缘安全距离 \(m_e=40\ \mathrm{mm}\)，并在当前参考激光落点附近 \(50\ \mathrm{mm}\) 的局部窗口采样，而不是遍历整个工作空间。

### 8.2 由交点对构造名义传感器位姿

定义目标激光弦方向和板内法向

\[
\mathbf l=\frac{\mathbf q_v-\mathbf q_u}
{\|\mathbf q_v-\mathbf q_u\|},
\qquad
\mathbf m=\frac{\widehat{\mathbf n}\times\mathbf l}
{\|\widehat{\mathbf n}\times\mathbf l\|}.
\tag{70}
\]

给定入射参数 \(\alpha\) 和分支符号 \(\kappa\in\{-1,+1\}\)，传感器测量轴取为

\[
{}^{\mathcal B}\mathbf z_S
=-left(cos\alpha\,\widehat{\mathbf n}
+\kappa\sin\alpha\,\mathbf m\right).
\tag{71}
\]

把当前参考传感器的 \(x_S,y_S\) 轴投影到与 \(z_S\) 正交的平面，并绕 \(z_S\) 旋转滚转角 \(\psi\)，得到完整的 \({}^{\mathcal B}\mathbf R_{S,c}^{\rm nom}\)。给定工作距离 \(d\)，传感器原点为

\[
{}^{\mathcal B}\mathbf t_{S,c}^{\rm nom}
=\frac{\mathbf q_u+\mathbf q_v}{2}
-d\,{}^{\mathcal B}\mathbf z_S.
\tag{72}
\]

当前真机枚举 \(\alpha\in\{25^\circ,31^\circ,38^\circ\}\)、\(\psi\in\{-15^\circ,0,15^\circ\}\)、\(d\in\{0.24,0.28,0.33\}\ \mathrm m\) 以及两个分支符号。

### 8.3 固定法兰命令原则

候选构造时，用当前手眼估计把名义传感器位姿转换为机器人法兰命令：

\[
{}^{\mathcal B}\mathbf T_{\mathcal F,c}^{\rm cmd}
=({}^{\mathcal B}\mathbf T_{\mathcal S,c}^{\rm nom})
\widehat{\mathbf X}^{-1}.
\tag{73}
\]

式（73）生成后，在该候选的所有不确定性样本中保持不变。对第 \(s\) 个手眼样本，真实可能到达的传感器位姿为

\[
{}^{\mathcal B}\mathbf T_{\mathcal S,c}^{(s)}
=({}^{\mathcal B}\mathbf T_{\mathcal F,c}^{\rm cmd})
\mathbf X^{(s)}.
\tag{74}
\]

这不等于后续永远使用六种子手眼初解。每次 NBV 成功提交后都会更新 \(\widehat{\mathbf X}\)，下一轮重新生成式（68）—（73）的整批候选。固定只发生在“同一候选的一次评分内部”，目的是模拟机器人实际只能收到一个确定法兰指令；若对每个不确定性样本重新反算一个完美补偿法兰位姿，就等于假设机器人预先知道该样本的真实手眼关系。

### 8.4 平面模型的有限边正向预测

二维激光平面在传感器坐标系中为 \(y_S=0\)。其在基坐标系中的法向为

\[
\mathbf n_L={}^\mathcal B\mathbf R_S\mathbf e_y.
\tag{75}
\]

平面上一点 \(\mathbf q\) 满足

\[
\mathbf n_L^\mathsf T
(\mathbf q-{}^\mathcal B\mathbf t_S)=0.
\tag{76}
\]

将 \(\mathcal E_u\) 上的 \(\mathbf q=\mathbf C+a\mathbf u\) 代入，得到

\[
a=
\frac{\mathbf n_L^\mathsf T
({}^{\mathcal B}\mathbf t_S-\mathbf C)}
{\mathbf n_L^\mathsf T\mathbf u}.
\tag{77}
\]

同理

\[
b=
\frac{\mathbf n_L^\mathsf T
({}^{\mathcal B}\mathbf t_S-\mathbf C)}
{\mathbf n_L^\mathsf T\mathbf v}.
\tag{78}
\]

若分母接近零，激光平面与对应边近似平行；若 \(a,b\) 超出式（69）的可信有限边内部，则候选不会形成目标双边交点。通过检查后，把两个基坐标系交点变换回 \(\mathcal S\)，并检查安全梯形测量域和弦长范围。

### 8.5 共享形貌的正向预测

共享形貌表面点可写为

\[
\mathbf q(\xi,\eta)
=\mathbf C+L_u\xi\mathbf u+L_v\eta\mathbf v
+h_{\boldsymbol\beta}(\xi,\eta)\mathbf n.
\tag{79}
\]

代入激光平面方程得到

\[
f(\xi,\eta)=
\mathbf n_L^\mathsf T
[\mathbf q(\xi,\eta)-{}^{\mathcal B}\mathbf t_S]=0.
\tag{80}
\]

系统先分别在 \(\eta=0\) 和 \(\xi=0\) 的两条物理边上，用 Newton 迭代求端点根；再在两端点之间对一系列 \(\xi\) 求解对应 \(\eta\)，得到弯曲表面与激光平面的预测交线。最终预测点仍需满足有限边、安全测量域和轮廓长度检查。

式（77）—（80）不是与信息评分平行的独立模块，而是整个“观测可行 NBV”的基础正向模型：可行性传播需要它判断未来能否看到目标双边，信息评分需要它生成候选的虚拟测量。

## 9. 联合不确定性下的观测可行性

### 9.1 主方向扰动样本

设当前联合估计和协方差为 \((\widehat{\boldsymbol\theta},\boldsymbol\Sigma_\theta)\)。先按式（49）的状态尺度无量纲化协方差

\[
\widetilde{\boldsymbol\Sigma}
=\mathbf S^{-1}\boldsymbol\Sigma_\theta\mathbf S^{-1}.
\tag{81}
\]

特征分解为

\[
\widetilde{\boldsymbol\Sigma}
=\mathbf V\operatorname{diag}(\lambda_1,\ldots,\lambda_D)
\mathbf V^\mathsf T,
\qquad \lambda_1\ge\cdots\ge\lambda_D,
\tag{82}
\]

其中 \(D=12+K=19\)。当前最多保留方差最大的 \(K_\sigma=12\) 个方向，构造

\[
\boldsymbol\theta_0=\widehat{\boldsymbol\theta},
\qquad
\boldsymbol\theta_{k,\pm}
=\widehat{\boldsymbol\theta}
\pm\sqrt D\,\mathbf S\mathbf v_k\sqrt{\lambda_k},
\quad k=1,\ldots,K_\sigma.
\tag{83}
\]

因此共享形貌模式当前共评估 \(1+2\times12=25\) 个样本。该样本集是截断主方向的确定性扰动集合，不是带标准无迹变换权重的完整概率积分。

### 9.2 有效比例

对同一个固定法兰命令，逐个代入式（74），再用式（77）—（80）预测观测。令

\[
\chi_{c,s}=
\begin{cases}
1,&\text{两条目标边均相交、端点在域内且质量检查通过},\\
0,&\text{其他情况}.
\end{cases}
\tag{84}
\]

定义经验有效比例

\[
p_{\rm valid}(c)
=\frac{1}{N_\sigma}\sum_{s=1}^{N_\sigma}\chi_{c,s}.
\tag{85}
\]

当前仅保留 \(p_{\rm valid}\ge0.8\) 的候选。由于式（85）使用等权计数，严格表述应为“样本有效比例”或“鲁棒有效率”，而不应把它解释成精确的统计概率。

## 10. 面向手眼参数的信息评分

### 10.1 为什么不能直接使用完整 Hessian

共享形貌联合状态包含手眼参数和标定件干扰参数。按

\[
\boldsymbol\theta=
\begin{bmatrix}
\boldsymbol\theta_X\\
\boldsymbol\theta_N
\end{bmatrix},
\quad
\boldsymbol\theta_X=
\begin{bmatrix}\boldsymbol\phi_X\\\mathbf t_X\end{bmatrix}\in\mathbb R^6,
\tag{86}
\]

划分无量纲法方程矩阵

\[
\mathbf H=\widetilde{\mathbf J}^\mathsf T\widetilde{\mathbf J}
=
\begin{bmatrix}
\mathbf H_{XX}&\mathbf H_{XN}\\
\mathbf H_{NX}&\mathbf H_{NN}
\end{bmatrix}.
\tag{87}
\]

其中 \(\boldsymbol\theta_N\) 包含标定件旋转、角点和形貌系数。完整 \(\det\mathbf H\) 变大可能只表示形貌或标定件位姿更确定，并不一定表示手眼外参更准确。

### 10.2 Schur 补边缘化

消去干扰状态后的有效手眼信息为

\[
\mathbf I_X
=\mathbf H_{XX}
-\mathbf H_{XN}
(\mathbf H_{NN}+\epsilon\mathbf I)^+
\mathbf H_{NX}.
\tag{88}
\]

式（88）保留“在允许标定件位姿和形貌重新调整后，观测真正留给手眼参数的信息”。当前共享形貌评分的基准 \(\mathbf H\) 包含一次既定形貌正则先验；增加一个虚拟候选时只加入该候选的观测信息，不会重复叠加正则项。

### 10.3 虚拟候选信息

在当前均值 \(\widehat{\boldsymbol\theta}\) 下，先用第 8 节预测一个固定虚拟物理观测 \(\widetilde{\mathbf z}_c\)。对候选残差做数值线性化时，\(\widetilde{\mathbf z}_c\) 保持固定：

\[
\mathbf J_c
=\left.
\frac{\partial\mathbf r
(\boldsymbol\theta;{}^{\mathcal B}\mathbf T_{\mathcal F,c}^{\rm cmd},
\widetilde{\mathbf z}_c)}
{\partial\boldsymbol\theta}
\right|_{\widehat{\boldsymbol\theta}}.
\tag{89}
\]

若在每次参数扰动时同时把虚拟测量改成该参数下的完美预测，残差会被人为保持接近零，候选信息将被错误抵消。固定虚拟测量是局部实验设计线性化的必要条件。

候选加入后的联合信息矩阵为

\[
\mathbf H_c^+=\mathbf H+\widetilde{\mathbf J}_c^\mathsf T
\widetilde{\mathbf J}_c.
\tag{90}
\]

18 帧静止采样用于降低随机噪声，但当前评分把候选视为一个新的物理观测，因此式（90）只加入一次，不乘以 18。

由式（88）分别得到当前和增广后的手眼信息 \(\mathbf I_X\)、\(\mathbf I_{X,c}^+\)。D-opt 信息增益为

\[
G_c=\frac12
\left[
\log\det(\mathbf I_{X,c}^++\epsilon\mathbf I_6)
-\log\det(\mathbf I_X+\epsilon\mathbf I_6)
\right].
\tag{91}
\]

最小特征值 \(\lambda_{\min}(\mathbf I_{X,c}^+)\) 只作为增益相同情况下的次级排序量。

### 10.4 运动代价与候选级联筛选

对通过式（85）的候选，先求数值 IK，并检查目标侧别、名义 ROI 余量、最大单关节步长、总关节距离以及与已提交 NBV 的关节距离。定义

\[
d_q(c)=\|\boldsymbol q_c-\boldsymbol q_{\rm now}\|_2,
\qquad
U_c=\frac{G_c}{1+\lambda_q d_q(c)}.
\tag{92}
\]

候选按 \(U_c\)、\(G_c\)、最小特征值依次降序排列。这里 \(p_{\rm valid}\) 是门限，不与 \(G_c\) 相乘。

排序靠前的候选再依次经过 MoveIt 目标状态碰撞检查和路径规划。估计标定件以带厚度和安全余量的碰撞盒加入规划场景。规划成功后，真机运动桥还会检查机器人静止、UF/UT 设置、控制器 CURPOS 与 URDF 正运动学一致性以及轨迹安全，再通过 `PC_TRACK_ALL` 执行目标。

## 11. NBV 实测、事务式滚动更新与回退

### 11.1 NBV 批次处理

机器人到达并停稳后，系统采集 18 个同步帧并按式（21）—（23）剔除异常帧。当前至少需要 9 个拟合帧和 3 个留出帧。所有入选帧必须位于传感器硬测量域；跨出规划安全域但仍在硬域内的帧会记录警告。

内点帧被分为：

- 拟合集：聚合为一个新的物理 NBV 观测并参与求解；
- 留出集：聚合为独立几何诊断观测，不进入求解和信息矩阵。

### 11.2 从上一次已接受状态局部更新

设成功提交 \(k\) 个 NBV 后的数据集为 \(\mathcal D_k\)，当前状态为 \(\widehat{\boldsymbol\theta}_k\)。新拟合观测为 \(\mathcal Z_{k+1}\)，试求解为

\[
\boldsymbol\theta_{k+1}^{\rm trial}
=\arg\min_{\boldsymbol\theta}
J(\boldsymbol\theta;\mathcal D_k\cup\mathcal Z_{k+1}),
\quad
\boldsymbol\theta^{(0)}=\widehat{\boldsymbol\theta}_k.
\tag{93}
\]

即每次都使用全部已提交的旧物理观测和当前新观测重新求解，而不是只用新观测递推一个线性公式。初始六种子的离散多初值搜索只执行一次；滚动更新从上次已接受解开始，避免后期在不同离散解域之间跳转。

### 11.3 事务式提交

只有下列条件全部满足时，才执行

\[
\mathcal D_{k+1}\leftarrow\mathcal D_k\cup\mathcal Z_{k+1},
\qquad
\widehat{\boldsymbol\theta}_{k+1}
\leftarrow\boldsymbol\theta_{k+1}^{\rm trial}.
\tag{94}
\]

条件包括：试求解收敛、数据可观性通过、手眼旋转跳变量、手眼平移跳变量和标定件旋转跳变量均不超过阶段门限。若任一条件失败，则 \(\mathcal D_k\) 和 \(\widehat{\boldsymbol\theta}_k\) 完全不变，当前候选被拒绝，机器人回到上一安全位姿后选择其他候选。

留出集分数仅用于诊断当前结果对未参与拟合帧的几何解释能力，不参与结果选择、候选评分或停止控制。

### 11.4 更新后的重新规划

每次式（94）成功后，系统同时：

1. 更新手眼、标定件参考系、共享形貌和协方差；
2. 更新 MoveIt 中标定件碰撞盒；
3. 以新的 \(\widehat{\boldsymbol\theta}_{k+1}\) 重新生成候选；
4. 重新执行第 8—10 节的预测、有效性传播和信息评分。

因此后续候选使用最新手眼结果；不存在始终固定使用六种子初解的问题。

## 12. 自适应停止与最终输出

首轮存在可行候选时，把最佳信息增益记为参考值 \(G_{\rm ref}\)。第 \(k\) 轮的相对增益为

\[
\rho_k=\frac{G_k^{\rm best}}{G_{\rm ref}}.
\tag{95}
\]

当前停止条件为：

\[
N_{\rm NBV}\ge7,
\qquad
\operatorname{rank}(\mathbf I_X)=6,
\qquad
\rho_k<0.08\ \text{连续出现 2 轮}.
\tag{96}
\]

另外保留 26 个总物理位姿的紧急保护，即六种子后最多执行 20 个 NBV。控制台可选的 NBV 数量是用户预算；设为 0 时由式（96）自适应停止。

当前最终结果是“最后一次成功提交的联合估计”。代码没有维护按真值误差、留出分数或历史最小残差选择的历史最佳结果。因此论文方法中不能声称系统会自动返回历史最优 NBV 结果。

## 13. 完整算法

**输入：** 原始二维轮廓流、机器人关节流、标定件边长 \((L_u,L_v)\)、粗略安装名义值 \(\mathbf X_0\)、传感器测量域和机器人模型。

**输出：** 手眼外参 \(\widehat{\mathbf X}\)、标定件参考系 \((\widehat{\mathbf C},\widehat{\mathbf R}_P)\)、共享形貌 \(\widehat{\boldsymbol\beta}\) 及局部协方差。

1. 度量化原始轮廓；将每帧轮廓与最近的静止机器人位姿同步。
2. 操作者把目标弦段对齐 ROI；检测表面线段及两个断点，稳定后锁定物理边身份。
3. 在参考位姿执行三轴平移探测，由式（59）—（60）初始化 \(\mathbf J_s\)。
4. 按式（57）执行局部星形微旋转；每步用式（61）—（66）维持 \(x_{\rm mid}\) 和 \(\ell\)，并执行事务式实测验证与回退。
5. 在每个已验证物理位姿采集静止批次；按式（21）—（25）剔除异常帧并聚合，直到得到六个通过旋转多样性检查的种子。
6. 对式（44）的四个初值分别求解式（43）；用式（45）筛选并选取最低代价合理平面解。
7. 由式（46）初始化并求解共享形貌目标式（47）；检查 data-only rank/condition。
8. 执行式（53）—（54）的帧内自助重采样稳定性检查并修正协方差。
9. 由式（68）—（73）生成局部候选。
10. 对每个候选保持法兰命令固定，使用式（74）—（85）计算联合状态扰动下的观测有效比例；删除低于门限者。
11. 用式（86）—（92）计算边缘化手眼信息增益和运动感知效用，并进行 IK、碰撞与路径级联筛选。
12. 执行最高效用可行候选；停稳后采集拟合帧和留出帧。
13. 用式（93）试求解。若通过事务门限，则按式（94）提交；否则回退且不改变当前结果。
14. 若式（96）成立则输出最后一次成功提交结果；否则返回步骤 9。

## 14. 方法组成与工程实现边界

### 14.1 进入标定数学模型的内容

- 同步机器人法兰位姿；
- 原始轮廓中实测的表面点和有物理边身份的双断点；
- 12-DOF 手眼—标定件联合几何；
- 所有视角共享的固定面外形貌；
- 数据雅可比、协方差、Schur 边缘化手眼信息；
- 基于有限边正向模型的候选观测预测；
- 基于实测新观测的联合滚动重估计。

### 14.2 保证真机可执行性的工程模块

- RViz 紫色模板与初始工作包络；
- CSRT 局部关联和丢失重捕获；
- 可选动态预检；
- 双特征 Broyden 伺服、旋转前馈和微步回退；
- MoveIt 碰撞场景、IK 和路径规划；
- FANUC 运动桥的软件撤防、UF/UT 与 CURPOS/FK 一致性检查；
- 全过程 rosbag 与状态日志。

这些模块会影响成功率、时间和安全性，但不会额外增加式（37）中的标定残差。

### 14.3 当前方法的明确适用边界

1. 当前只使用机器人停稳后的同步轮廓，不估计运动中连续时间轨迹或时间偏移。
2. 共享形貌描述的是相对一个参考面的固定、平滑面外高度；它不描述每次观测变化的弹性形变、目标移动或边界身份变化。
3. 常数和一阶形貌已为消除显式 gauge 而删除，但高阶形貌是否得到数据充分支撑仍必须由 data-only rank/condition 和独立验证判断，不能只看正则化后的满秩。
4. 当前边长 \(L_u,L_v\) 视为已知，且两条目标边在参考平面内保持直线、相邻和有固定身份。
5. 初始多解筛选使用标定件近似水平的真机先验。若目标平均平面不水平，需要修改该先验；若物体严重翘曲到平面预求解无法进入正确解域，当前流程会在共享形貌优化之前失败。
6. 真机没有外参真值。求解器内部残差、bootstrap 稳定性和留出几何分数只能评价一致性；绝对精度需由精密球等独立实验评价。

## 15. 原理与当前代码对应关系

| 方法环节 | 当前代码入口 |
|---|---|
| Gocator 毫米转米及坐标轴统一 | [profile_metric_adapter_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/profile_metric_adapter_node.py:103) |
| 轮廓—机器人静止同步与 FK | [measurement_sync_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/measurement_sync_node.py:144) |
| ROI 对齐、CSRT 跟踪与有界重捕获 | [breakpoint_pipeline.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/roi_tracking/breakpoint_pipeline.py:277) |
| 从期望中点向两侧生长检测表面边界 | [segment_detector.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/roi_tracking/segment_detector.py:222) |
| 固定度量轮廓栅格 | [rasterizer.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/roi_tracking/rasterizer.py:45) |
| CSRT 矩形跟踪器 | [opencv_tracker.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/roi_tracking/opencv_tracker.py:80) |
| ROS 断点检测、导向、先验和控制接口 | [profile_endpoint_detector_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_endpoint_detector_node.py:895) |
| 帧级端点 MAD 内点筛选 | [multiframe.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/multiframe.py:35) |
| 静止批次聚合与 SO(3) 均值 | [dataset_io.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/dataset_io.py:52) |
| 初始工作包络和可选动态预检 | [initial_pose.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/initial_pose.py:58) |
| 星形旋转及备用分支 | [rotation_scheduler.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/rotation_scheduler.py:18) |
| 双特征 DLS 与 Broyden 更新 | [translation_servo.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/translation_servo.py:17) |
| 真机六种子状态机、前馈、回退与批次保存 | [seed_collection_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:1872) |
| 种子旋转激励诊断 | [seed_observability.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/seed_observability.py:10) |
| 平面角点内层线性系统 | [corner_projection.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/corner_projection.py:11) |
| 平面变量投影残差和数值雅可比 | [residual.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/residual.py:13) |
| 共享形貌基函数与联合残差 | [shared_surface.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/shared_surface.py:16) |
| 多初值平面预求解与共享形貌联合优化 | [twelve_dof_v2.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/solvers/twelve_dof_v2.py:194) |
| data-only/prior-augmented 可观性与协方差 | [twelve_dof_v2.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/solvers/twelve_dof_v2.py:534) |
| 六种子 bootstrap 稳定性与协方差扩张 | [initial_validation.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/initial_validation.py:90) |
| 局部有限边 NBV 候选生成 | [candidate_generator.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/candidate_generator.py:80) |
| 平面/共享形貌未来轮廓正向预测 | [profile_predictor.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/profile_predictor.py:109) |
| 联合协方差主方向样本与有效比例 | [validity.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/validity.py:26) |
| Schur 手眼信息与候选信息增益 | [information.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/information.py:40) |
| 虚拟观测评分 | [scoring.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/scoring.py:19) |
| 数据所有权、事务式滚动更新 | [pipeline.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/pipeline.py:260) |
| 真机候选级联、MoveIt、采集、提交和回退 | [active_calibration_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_node.py:1398) |
| 自适应停止 | [stopping.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/stopping.py:19) |
| FANUC 真机运动安全桥 | [motion_bridge_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/motion_bridge_node.py:332) |
| 当前通用标定参数 | [calibration.yaml](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:230) |
| 当前真机覆盖参数 | [real_calibration.yaml](/workspace/ros2_ws/src/fanuc_gocator_bridge/config/real_calibration.yaml:1) |

## 16. 符号速查表

| 符号 | 维度 | 含义 |
|---|---:|---|
| \(\mathbf X={}^\mathcal F\mathbf T_\mathcal S\) | \(4\times4\) | 待标定手眼外参 |
| \(\mathbf T_i={}^\mathcal B\mathbf T_{\mathcal F,i}\) | \(4\times4\) | 第 \(i\) 个法兰位姿 |
| \(\mathbf C\) | 3 | 标定件所选物理角点 |
| \(\mathbf u,\mathbf v,\mathbf n\) | 3 | 标定件两条边方向和参考法向 |
| \(L_u,L_v\) | 标量 | 两条有限边的已知长度 |
| \({}^\mathcal S\mathbf p_{ifj}\) | 3 | 传感器坐标系中的表面轮廓点 |
| \({}^\mathcal S\mathbf e_{u,if},{}^\mathcal S\mathbf e_{v,if}\) | 3 | 两条物理边的实测断点 |
| \(\mathbf x_9\) | 9 | 手眼旋转、手眼平移、标定件旋转 |
| \(\mathbf x_{12}\) | 12 | \(\mathbf x_9\) 与角点 \(\mathbf C\) |
| \(\boldsymbol\beta\) | \(K\) | 所有视角共享的形貌系数 |
| \(\boldsymbol\theta\) | \(12+K\) | 共享形貌联合状态；当前为 19 维 |
| \(h_{\boldsymbol\beta}\) | 标量 | 相对参考面的固定面外高度 |
| \(\mathbf J_d,\mathbf J_a\) | 可变 | 数据雅可比、数据加先验雅可比 |
| \(\boldsymbol\Sigma_\theta\) | \((12+K)^2\) | 联合状态局部协方差 |
| \(p_{\rm valid}\) | 标量 | 候选扰动样本的有效比例 |
| \(\mathbf I_X\) | \(6\times6\) | 边缘化后的手眼有效信息矩阵 |
| \(G_c\) | 标量 | 候选 \(c\) 的 D-opt 手眼信息增益 |
| \(U_c\) | 标量 | 加入关节运动代价后的候选效用 |
| \(\boldsymbol q_c,\boldsymbol q_{\rm now}\) | 6 | 候选与当前机器人关节向量 |
| \(x_{\rm mid},z_{\rm mid},\ell\) | 标量 | 断点中点坐标与断点间距 |
| \(\mathbf J_s\) | \(2\times3\) | 局部法兰平移到双特征变化的在线雅可比 |
