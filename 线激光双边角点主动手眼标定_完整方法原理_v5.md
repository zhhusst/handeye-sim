# 基于双边角点约束与信息增益的线激光自动手眼标定方法
## 完整原理、数学边界、实现流程与验证方案（核查完善版 v5）

---


## 版本说明与核查结论

本版本以 v4 的第3—14节为主体，保留其核心方法路线，并完成以下修正与补全。

### 已修正的关键问题

1. **消除 Phase 0a 与 Phase 0b 的逻辑矛盾**  
   粗手眼初值不再用于种子阶段的几何预测。Phase 0b 全程只使用法兰编码器位姿和真实双边轮廓反馈。粗手眼仅作为12-DOF-V2优化初值；进入正式主动阶段后，候选法兰位姿使用滚动更新后的当前手眼估计反求。

2. **统一种子阶段和主动阶段的测量域定义**  
   不再混用矩形 ROI 与梯形测量域。两个阶段均在Gocator实际输出的 $X_S-Z_S$ 坐标中使用人工配置的外层有效域和内层安全域。

3. **修正变量投影的加权形式**  
   角点解析求解和约化目标函数均显式保留权重矩阵 $W$。

4. **修正初始化输出的下标冲突**  
   粗初值记为 ${}^F\hat T_{S,\mathrm{init}}$，种子数据联合初始化结果记为 ${}^F\hat T_{S,k_0}$，避免二者均使用下标0而被误认为同一结果。

5. **明确旋转多样性矩阵只是一种启发式指标**  
   $G_R$ 只能反映法兰朝向分布，不能代替完整12-DOF-V2约化Jacobian的秩、奇异值和条件数检查。

6. **修正联合不确定性阶段的机器人约束位置**  
   同一候选的法兰指令在全部状态样本中固定，因此IK、关节限位和法兰路径碰撞通常只需对候选检查一次；状态样本内部主要重复判断双边求交和传感器安全域。

7. **增加混合单位参数的尺度归一化**  
   手眼旋转、平移和平板旋转具有不同单位。协方差、条件数、D-opt和E-opt均必须在统一的无量纲缩放状态下计算，否则数值结果依赖单位选择。

8. **修正最大采集次数停止条件**  
   达到安全上限时的停止条件应为 $N_k\ge N_{\max}$。

9. **补充离散边身份与平板坐标系规范化**  
   每次滚动更新后必须保持 $u,v,n$ 的正交、右手性以及与历史 $e_1/e_2$ 标签的一致性，避免边交换和轴符号翻转。

10. **补全文档其余部分**  
    新增数学成立条件、退化与失败模式、伪代码、软件模块、仿真与实物验证、消融实验、评价指标、参数配置、论文贡献、局限性和实现检查表。

### 当前方法的统一主线

$$
\boxed{
\text{无手眼种子采集}
\rightarrow
\text{12-DOF-V2联合估计}
\rightarrow
\text{局部固定双边建模}
\rightarrow
\text{参数化候选与虚拟测量}
\rightarrow
\text{联合不确定性筛选}
\rightarrow
\text{信息增益NBV}
\rightarrow
\text{执行、更新与停止}
}
$$

以下内容中，“严格成立”与“工程近似”将明确区分。未经过实验验证的阈值和性能目标均作为待标定参数，而不作为理论保证。

---

# 1. 方法目标

本方法面向安装在线激光传感器的眼在手机器人系统，目标是自动估计线激光传感器相对于机器人法兰的手眼关系：

$$
{}^{F}T_{S}
=
\begin{bmatrix}
R_{he} & t_{he}\\
0 & 1
\end{bmatrix}.
$$

其中：

- $B$：机器人基坐标系；
- $F$：机器人法兰坐标系；
- $S$：线激光传感器输出轮廓所使用的坐标系；
- $R_{he},t_{he}$：待估计手眼旋转和平移。

系统同时联合估计标定平板局部角点几何：

$$
R_{pl}=[u,v,n],\qquad C,
$$

其中：

- $C$：被持续观测的同一物理角点；
- $u,v$：从 $C$ 出发的两条固定相邻边方向；
- $n=u\times v$：平板法向；
- $L,W$：平板物理尺寸，用于给出边段的物理上限，但主动规划不需要恢复并使用整个矩形。

方法始终要求正式采集帧包含：

$$
\boxed{
\text{平板轮廓点}
+
E_u\text{上的固定身份断点 }e_1
+
E_v\text{上的固定身份断点 }e_2
}
$$

主动采集的目标不是在任意机器人位姿中寻找最大信息，而是：

$$
\boxed{
\text{先构造能够可靠观测固定双边的候选}
\rightarrow
\text{再选择手眼信息增益最大的候选}
}
$$

---

# 2. 方法总体结构

方法由一个粗初始化步骤和两个自动化阶段组成。

## 2.1 Phase 0a：手眼粗初值

仿真中由URDF真值施加可控扰动产生；实物中由CAD、安装尺寸或人工测量提供。其作用仅是给12-DOF-V2非线性优化提供合理起点。

## 2.2 阶段A：自动种子位姿采集

在手眼关系尚不可靠时，系统采用：

$$
\boxed{
\text{预设法兰局部轴旋转}
+
\text{无标定双边特征平移伺服}
}
$$

生成多个旋转多样、双边稳定的种子位姿。种子阶段不进行NBV，不使用手眼进行运动预测。

## 2.3 Phase 0c：12-DOF-V2联合初始化

利用全部种子帧联合估计：

$$
{}^F T_S,\qquad R_{pl}=[u,v,n],\qquad C.
$$

角点 $C$ 通过变量投影消除，外层优化为9维。

## 2.4 阶段B：信息驱动的主动采集

在获得可用当前估计后，循环执行：

1. 建立同一角点的局部固定双边模型；
2. 采样候选参数 $\eta=(a,b,\alpha,\psi,h,s)$；
3. 直接生成名义传感器位姿、法兰指令和虚拟双边轮廓；
4. 在联合状态不确定性下计算 $P_{\mathrm{valid}}$；
5. 对鲁棒有效候选计算手眼有效信息增益；
6. 选择并执行NBV；
7. 验证真实双边，滚动更新12-DOF-V2；
8. 根据可观测性、协方差和剩余增益自适应停止。

## 2.5 两个阶段的职责边界

| 阶段 | 是否依赖手眼 | 运动目标 | 有效性依据 |
|---|---:|---|---|
| 种子阶段 | 否 | 制造基础旋转多样性 | 实际轮廓双边反馈 |
| 主动阶段 | 是，使用当前估计 | 最大化手眼信息增益 | 名义几何 + 联合不确定性 |
| 最终求解 | 使用全部有效数据 | 获得最终手眼结果 | 与在线阶段相同的12-DOF-V2模型 |

完整数据流为：

```text
粗手眼初值
    ↓
人工给出第一个双边稳定位姿
    ↓
无手眼小步旋转—平移伺服采集种子
    ↓
12-DOF-V2联合初始化
    ↓
局部固定双边与人工安全梯形
    ↓
参数化候选生成
    ↓
联合不确定性有效性筛选
    ↓
手眼信息增益评分
    ↓
执行、真实验证、滚动更新
    ↓
自适应停止与最终求解
```

---

# 3. 系统输入、观测与坐标关系

## 3.1 已知输入

系统已知：

1. 每个采集时刻的法兰绝对位姿：

$$
{}^{B}T_{F,i}
=
\begin{bmatrix}
R_i&t_i\\
0&1
\end{bmatrix};
$$

2. 每个位姿下的线激光轮廓点：

$$
\mathcal P_i^S=\{p_{ij}^S\}_{j=1}^{N_i};
$$

3. 两个固定身份物理断点：

$$
p_{e1,i}^S,\qquad p_{e2,i}^S;
$$

4. 平板尺寸或局部可信边段上界：

$$
L,\qquad W;
$$

5. 粗手眼初值：

$$
{}^{F}\hat T_{S,0}.
$$

## 3.2 点从传感器系到基坐标系

任意传感器点满足：

$$
p_{ij}^B
=
R_i\left(R_{he}p_{ij}^S+t_{he}\right)+t_i.
$$

记：

$$
R_{BS,i}=R_iR_{he},
\qquad
t_{BS,i}=t_i+R_it_{he},
$$

则：

$$
p_{ij}^B=R_{BS,i}p_{ij}^S+t_{BS,i}.
$$

所有平面约束、边约束和角点约束均基于该绝对坐标关系。


## 3.3 变换方向与记号约定

本文统一规定：

$$
{}^AT_B
$$

表示“将 $B$ 坐标系中的点变换到 $A$ 坐标系”。因此：

$$
p^A={}^AT_Bp^B.
$$

手眼关系为：

$$
X={}^FT_S,
$$

法兰和传感器的基坐标位姿满足：

$$
{}^BT_S={}^BT_F\,{}^FT_S.
$$

目标传感器位姿反求法兰位姿时：

$$
{}^BT_F={}^BT_S({}^FT_S)^{-1}.
$$

实现中必须对所有TF、URDF、机器人控制器位姿和点云坐标统一执行这一约定。

## 3.4 方法成立的基础假设

当前理论依赖以下假设：

1. Gocator能够直接输出统一传感器坐标系中的公制轮廓点：
   $$
   p^S=[x,0,z]^T.
   $$
2. 标定物局部区域可视为刚性平面，两个目标边在该平面内相交于同一角点。
3. $e_1$ 始终对应 $u$ 边，$e_2$ 始终对应 $v$ 边。
4. 机器人返回的 ${}^BT_F$ 具有足够重复精度，并与轮廓采集时间同步。
5. 正式数据帧均包含有效平面点和两个固定身份断点。
6. 主动阶段开始前，12-DOF-V2已达到可用于局部预测的精度，但不要求已经是最终精度。
7. 人工安全梯形与实际轮廓点使用同一个传感器数值坐标系。

这些假设中，1—5属于方法输入条件；6—7需要通过初始化验收和工程配置保证。


---

# 4. Phase 0a：手眼名义值获取（粗初值）

Phase 0a 的任务不是标定，而是为后续优化提供一个**有合理误差的名义初值**，模拟实际工作中工人用卷尺和直角尺（或看 CAD 图）对传感器安装的粗略测量。

## 4.1 名义值的生成方式

在仿真环境中，系统直接读取 URDF 中定义的手眼真值 $T_{FS}^*$，然后施加一组可控的随机扰动：

$$
{}^F\hat T_{S,\mathrm{init}}
=
\operatorname{Perturb}(T_{FS}^*,\ \delta_R,\ \delta_t).
$$

其中：

- $T_{FS}^*$ 是从 URDF 文件 `urdf/calib_robot.urdf` 中提取的手眼变换真值；
- $\delta_R$ 是旋转扰动（默认：绕随机轴旋转 $3^\circ$）；
- $\delta_t$ 是平移扰动（默认：沿随机方向偏移 $10\ \text{mm}$）。

扰动幅度通过配置文件
`ros2_ws/src/handeye_sim_bridge/config/calibration.yaml` 控制：

```yaml
handeye_init_rotation_error_deg: 3.0       # 旋转扰动 (°)
handeye_init_translation_error_mm: 10.0    # 平移扰动 (mm)
```

## 4.2 名义值的使用

为避免与种子联合初始化结果混淆，将粗初值统一记为：

$$
{}^F\hat T_{S,\mathrm{init}}.
$$

它只作为：

- 12-DOF-V2非线性优化的起始点；
- 仿真中设置可控初始误差的输入。

它**不用于Phase 0b的运动预测**。Phase 0b全程只依赖法兰编码器位姿和实际双边轮廓反馈。

进入正式主动阶段后，候选生成使用的是种子联合初始化并滚动更新后的当前估计：

$$
{}^F\hat T_{S,k},
$$

而不是始终使用原始粗初值。

## 4.3 设计意图

### 为什么要扰动？

如果直接使用 URDF 真值作为初值，12-DOF-V2 在一帧数据都不需要的情况下就能得到"完美"结果，这掩盖了算法的真实收敛能力和鲁棒性。加扰动的目的是：

1. **模拟实际工况**：真实机器人上没有 TF 可以直接读取完美手眼，工人只能用尺量——这个 $\delta$ 模拟的就是那"一尺之差"。
2. **测试优化器的收敛范围**：检查12-DOF-V2能否从设定的 $3^\circ/10\text{mm}$ 偏差稳定收敛。最终精度由数据质量、机器人运动、断点检测和模型误差共同决定，不能仅由初始扰动幅度保证。
3. **使评价更有意义**：只有从非完美的初值出发，标定误差才是真实的收敛误差。

### 为什么不直接用 TF 初值？

在真实机器人上，操作员首先提供一个粗略安装变换，并只手动找到第一个双边稳定位姿。之后的其余种子由Phase 0b自动采集。仿真中的Phase 0a用URDF真值加扰动代替“人工输入粗手眼”这一环节，而不是替代种子采集。

> 在实物部署时，Phase 0a 将被替换为操作员手动输入的手眼估计值（例如通过传感器外壳上的安装尺寸计算）。

---

# 5. Phase 0b：基于预设多轴旋转和无标定双边特征伺服的自动种子采集

## 5.1 为什么需要种子阶段

在没有稳定手眼和平板估计时，系统还无法可靠预测任意候选位姿下的未来轮廓，因此不能立即运行完整 NBV。

种子阶段的任务不是追求最优信息增益，而是：

1. 始终保持两个断点可见；
2. 生成多个不同旋转方向；
3. 避免出现"姿态 A、姿态 A、姿态 B、姿态 B"这类旋转退化；
4. 为 12-DOF-V2 提供稳定启动数据。

## 5.2 核心设计原则

Phase 0b **不应该依赖手眼初值，也不需要虚拟锚点**。

如果运动预测依赖粗手眼，就会出现逻辑上的循环：

> 需要种子数据才能得到可靠手眼，但又需要可靠手眼才能自动采集种子数据。

因此，Phase 0b 的核心设计是：

$$
\boxed{
\text{预设法兰旋转激励}
+
\text{无标定的轮廓特征平移伺服}
}
$$

它与人工示教的过程完全一致：

$$
\text{小步旋转}
\rightarrow
\text{观察两个断点}
\rightarrow
\text{平移修正}
\rightarrow
\text{再小步旋转}
\rightarrow
\text{再平移修正}.
$$

三个关键优势：

1. **不依赖待标定的手眼关系** — 全程不使用 ${}^F T_S$；
2. **完全复现已验证有效的人工操作经验**；
3. **只需要学习局部输入—输出关系，不需要恢复完整三维几何**。

## 5.3 初始人工输入

第一版系统要求操作者只完成一次操作：

> 将机械臂移动到一个能够稳定看到同一平板角点两条相邻边的起始位姿。

系统保存：

- 当前法兰位姿 ${}^B T_F$；
- 完整线激光轮廓点；
- $e_1,e_2$ 两个断点；
- 当前双边反馈状态及人工安全测量域余量（见 §5.4）；
- 当前有效位姿作为回退状态。

### 5.3.1 为什么“当前双边可见”不足以验收初始位姿

单帧双边可见只说明当前状态 $q_0$ 属于可测集合
$\mathcal V$，并不说明它的局部邻域也属于 $\mathcal V$。自动种子
采集还要求沿法兰局部 $\pm X,\pm Y$ 旋转后存在可达的平移修正，因此初始
位姿必须位于可测集合内部，而不能贴着它的边界：

$$
\exists\,\mathcal N(q_0),\qquad
\mathcal N(q_0)\cap\mathcal Q_{\mathrm{robot}}
\subseteq \mathcal V_{\mathrm{bilateral,safe}}.
$$

这也解释了为什么两个“当前轮廓都安全”的初始位姿可能有完全不同的结果：
一个位姿周围有四向运动余量，另一个位姿只在单向狭窄分支上可见。

### 5.3.2 两层初始位姿准入判据

初始位姿不依赖未知手眼，而采用“静态保守包络 + 实测动态预检”两层准入。

第一层只使用当前轮廓、关节编码器和机器人运动学。当前仿真装置的保守充分
条件为：

$$
\begin{aligned}
|x_m|&\le 30 \mathrm{mm},&
300 \mathrm{mm}\le z_m&\le550 \mathrm{mm},\\
d_{\mathrm{domain}}&\ge20 \mathrm{mm},&
50 \mathrm{mm}\le L_p&\le250 \mathrm{mm},\\
\left|z_{e2}-z_{e1}\right|&\ge15 \mathrm{mm},&
\rho_{\mathrm{joint}}&\ge5\%.
\end{aligned}
$$

其中 $d_{\mathrm{domain}}$ 是两个端点到种子安全梯形边界的最小距离，
$\rho_{\mathrm{joint}}$ 是六个关节到上下限的最小归一化余量。固定
$e_1/e_2$ 的物理身份是为了让不同帧始终约束同一条物理边，但
$z_{e2}-z_{e1}$ 的符号会随合法观察姿态改变，不能用于判定观察侧是否合法。
这里只要求其绝对值足够大，以排除两端深度几乎相同的退化轮廓。此外，以当前关节解为初值，对局部
$\pm X,\pm Y$ 各 $2^\circ$ 做四次数值 IK；至少三个方向须有解，且任一
关节变化不超过 $20^\circ$。四个带符号方向中任意三个必然同时覆盖局部
$X$ 与 $Y$ 两个轴。随后仍由实测动态预检决定最终准入，因此不必在运动前
强制四个方向全部通过数值IK。

第二层不是继续依赖模型预测，而是依次实际尝试四个 $2^\circ$ 小扰动：

$$
\delta R\in
\{R_x(-2^\circ),R_x(+2^\circ),R_y(-2^\circ),R_y(+2^\circ)\}.
$$

每次扰动都运行同一无标定平移伺服，要求
$|x_m|\le3 \mathrm{mm}$、$d_{\mathrm{domain}}\ge15 \mathrm{mm}$，然后返回
参考位姿。至少三个方向通过，并且通过集合必须同时覆盖 $X$ 与 $Y$
两个轴；一旦已有三个方向通过且覆盖两个轴，可以提前结束第4个扰动。只有两层
均通过后才保存参考种子并开始正式星形采集。

第一层阈值是针对当前仿真传感器安全域和标定板布置给出的**保守充分范围**，
不是任意装置的普适必要条件；更换量程、板尺寸或安装方向时，应使用同样的
变量重新辨识阈值。第二层的实测结果负责捕获静态阈值无法描述的局部遮挡、
非线性伺服和可达分支问题。

## 5.4 "当前双边视觉特征"的定义

原文中"当前双边视觉特征"说得过于抽象——它并不是一个额外测量，本质上只是从两个断点中计算出的控制量。

设两个断点在传感器坐标系中为：

$$
p_{e1}^S=[x_1,0,z_1]^T,
\qquad
p_{e2}^S=[x_2,0,z_2]^T.
$$

最核心的视觉特征是两个断点的中点：

$$
x_m=\frac{x_1+x_2}{2},
\qquad
z_m=\frac{z_1+z_2}{2}.
$$

还可以计算轮廓长度：

$$
L_p=\|p_{e1}^S-p_{e2}^S\|.
$$

对目前的控制方法来说，真正用于闭环控制的只有：

$$
\boxed{x_m}
$$

其目标值为：

$$
x_m^\star=0.
$$

其他量主要用于安全判断：

- $\text{valid}_{e1}$：第一个断点是否存在；
- $\text{valid}_{e2}$：第二个断点是否存在；
- $x_1,x_2,z_1,z_2$：两个断点是否还在量程内；
- $L_p$：轮廓是否过短或异常；
- 端点到 安全测量域边界的距离：是否即将丢失。

因此，文档中的"当前双边视觉特征"应明确为：

```
当前双边反馈状态：
  x_mid        两个断点的中点 X 坐标，作为平移伺服误差
  endpoint_1/2 两个断点坐标
  valid_e1/e2  双边可见状态
  domain_margin 两个端点距离人工安全测量域边界的最小余量
  profile_length 轮廓长度，作为质量检查
```

## 5.5 种子旋转计划（外层运动计划）

设起始法兰旋转为 $R_0$，采用**星形路径**——每次都从同一个已知参考姿态出发，而不是连续累积复杂旋转。

默认生成约 6 个目标朝向：

$$
\begin{aligned}
R_1 &= R_0,\\
R_2 &= R_0 R_x(+\theta),\\
R_3 &= R_0 R_x(-\theta),\\
R_4 &= R_0 R_y(+\theta),\\
R_5 &= R_0 R_y(-\theta),\\
R_6 &= R_0 R_x(+\theta) R_y(+\theta).
\end{aligned}
$$

其中：

- $R_x$：绕当前法兰局部 $X$ 轴旋转；
- $R_y$：绕当前法兰局部 $Y$ 轴旋转；
- 初始角度建议从 $\theta=10^\circ\sim15^\circ$ 开始；
- 每一步旋转步长 $\Delta\theta_{\mathrm{step}}\approx 2^\circ\sim3^\circ$；
- $R_6$ 通过两个单轴阶段实现：先绕 $X$ 蠕动到 $+\theta$，双边稳定后再绕新的局部 $Y$ 轴蠕动到 $+\theta$。

采用星形路径的优点是：

- 每次都从同一个已知参考姿态 $R_0$ 出发；
- 旋转目标易于定义；
- 不会因连续累计旋转造成越来越复杂的姿态；
- 失败时可以退回已知安全的 $R_0$；
- 采集的朝向分布容易检查。

返回 $R_0$ 的中间过程不作为新种子保存。

若默认五条分支中某一符号方向失败，不再重复死磕同一分支，而按
$R_x^+R_y^-$、$R_x^-R_y^+$、$R_x^-R_y^-$ 及交换旋转顺序的组合
加入候补分支。达到六个满足安全、居中和旋转多样性要求的种子后立即停止。
候补的目的不是降低验收标准，而是在同一局部安全邻域中换一个激励方向。

## 5.6 无标定平移伺服（内层执行控制）

### 5.6.1 为什么不使用虚拟锚点

原方案中使用虚拟锚点预测旋转后的法兰平移：

$$
P_a^B = {}^B T_F \; {}^F \hat T_S \; p_a^S,
\qquad
t_{BS}' = P_a^B - R_{BS}' p_a^S.
$$

这个计算显式依赖 ${}^F \hat T_S$。在初始手眼平移和旋转均不准确时，预测出的基坐标锚点可能存在明显误差。特别是法兰旋转时，由于传感器相对法兰存在未知杆臂，传感器不仅发生旋转，还会发生无法准确预测的平移。

人工操作没有试图计算这种平移，而是：

1. 先转一点；
2. 直接观察传感器中的断点变化；
3. 再根据实际变化移动法兰。

这比基于错误模型预测更可靠。

### 5.6.2 一维平移伺服（第一版）

对于当前种子过渡，选定一个法兰局部旋转轴 $a_r\in\{e_x,e_y\}$。

每次只旋转一个小角度（右乘，表示绕当前法兰局部坐标轴旋转）：

$$
R_{BF}^+ = R_{BF} \exp([a_r]_\times \Delta\theta).
$$

完成这一小步旋转后：

1. 读取线激光轮廓；
2. 检测两个断点；
3. 计算 $x_m = (x_1+x_2)/2$；
4. **固定法兰旋转**；
5. 只调整法兰平移，使 $x_m \to 0$。

控制完成后，再执行下一次小步旋转。

**关键问题：不使用手眼，怎么知道法兰应该往哪边平移？**

从法兰局部三个平移方向中选择一个方向 $d_F\in\{e_x,e_y,e_z\}$。令沿该方向的平移量为 $q$，局部关系为：

$$
\Delta x_m \approx g \cdot \Delta q,
$$

其中 $g = \partial x_m/\partial q$ 是未知的局部灵敏度。

通过一次很小的试探平移 $\Delta q_{\mathrm{probe}}$，测量 $\Delta x_m = x_m^{\mathrm{after}} - x_m^{\mathrm{before}}$，得到：

$$
\hat g = \frac{\Delta x_m}{\Delta q_{\mathrm{probe}}}.
$$

之后计算修正：

$$
\Delta q = -\lambda \frac{x_m}{\hat g},
$$

并进行限幅 $|\Delta q| \le q_{\max}$。

如果 $x_m>0$ 且 $\hat g>0$，控制器就沿负方向移动；如果实际效果相反，新的测量会自动修正 $\hat g$ 的符号。

### 5.6.3 自动选择平移轴

在初始有效状态下，分别沿法兰局部 $X,Y,Z$ 方向做极小试探：

$$
\hat g_x = \frac{\Delta x_m}{\Delta q_x},
\quad
\hat g_y = \frac{\Delta x_m}{\Delta q_y},
\quad
\hat g_z = \frac{\Delta x_m}{\Delta q_z}.
$$

选择灵敏度最高的轴：

$$
j^\star = \arg\max_j |\hat g_j|.
$$

每次试探必须从同一有效位姿出发，试探后先返回原位，并在整个试探过程中检查双边和安全域余量。若某轴试探导致余量快速下降，即使 $|\hat g_j|$ 较大也应排除。

随后在当前一段蠕动过程中，主要沿该轴做一维伺服。相比直接同时控制三个平移自由度，这种方法：

- 更容易实现；
- 更容易判断运动方向；
- 更不容易产生无关平移；
- 与人工实际操作最接近。

### 5.6.4 在线更新平移灵敏度

随着法兰朝向变化，$g = \partial x_m/\partial q$ 也会发生变化，不能始终使用最初的值。

每次实际执行平移后，都可以用实测值更新：

$$
g_{\mathrm{measured}} = \frac{x_{m,k+1} - x_{m,k}}{q_{k+1} - q_k},
\qquad
\hat g_{k+1} = (1-\eta)\hat g_k + \eta\, g_{\mathrm{measured}},
$$

其中 $0<\eta\le 1$。

也可以使用 Broyden 形式更新三维 Jacobian：

$$
J_{x,k+1} = J_{x,k} + \frac{(\Delta x_m - J_{x,k}\Delta t_F)\Delta t_F^T}{\Delta t_F^T \Delta t_F}.
$$

这种在线更新直接利用"刚才移动了多少、断点中点变化了多少"，不需要知道传感器相对于法兰的位置。

### 5.6.5 三维平移伺服（后续扩展）

如果后续发现单一轴无法在所有姿态下稳定控制，可以估计完整 Jacobian：

$$
J_x = \begin{bmatrix}
\frac{\partial x_m}{\partial t_x^F} &
\frac{\partial x_m}{\partial t_y^F} &
\frac{\partial x_m}{\partial t_z^F}
\end{bmatrix}.
$$

局部关系为 $\Delta x_m \approx J_x \Delta t_F$，取最小范数修正：

$$
\Delta t_F = -\lambda \frac{J_x^T}{J_x J_x^T + \mu} x_m.
$$

法兰局部平移转换为基坐标平移：$\Delta t_B = R_{BF} \Delta t_F$，因此：

$$
t_{BF}^+ = t_{BF} + R_{BF} \Delta t_F.
$$

整个过程中仍然不需要 ${}^F T_S$。不过第一版建议先实现一维版本（§5.6.2），因为控制目标只有一个标量 $x_m$，直接开放三个平移自由度会存在多解，容易产生不必要的漂移。

### 5.6.6 仅让 $x_m\to0$ 是否足够

严格来说，$x_m=0$ 不能数学上保证两个断点始终存在。例如两个端点可能同时沿 $z_S$ 方向接近测量边界，而中点横坐标仍然为零。

因此第一版采用：

- **主控制目标**：
  $$
  x_m\to0.
  $$
- **硬安全条件**：
  $$
  p_{e1}^S,p_{e2}^S\in\mathcal V_{\mathrm{seed,safe}}^S,
  $$
  并要求：
  $$
  L_{\min}<L_p<L_{\max}.
  $$

$\mathcal V_{\mathrm{seed,safe}}^S$ 与第7节的人工安全梯形采用同一表示形式，可以根据种子阶段需要设置得更保守。

定义点到安全域边界的有符号最小余量：

$$
d_{\mathcal V}(p^S)
=
\min
\left\{
z-z_n,\;
z_f-z,\;
x-x_l(z),\;
x_r(z)-x
\right\}.
$$

双边最小余量为：

$$
m_{\mathrm{domain}}
=
\min
\left\{
d_{\mathcal V}(p_{e1}^S),
d_{\mathcal V}(p_{e2}^S)
\right\}.
$$

只有：

$$
m_{\mathrm{domain}}>m_{\mathrm{safe}}
$$

才允许继续执行下一小步旋转。控制结构为：

$$
\boxed{
x_m=0\text{作为伺服目标}
+
\text{双边存在和安全域余量作为硬约束}
}
$$

如果实际测试中出现明显的 $z$ 方向漂移，再把 $z_m - z_m^{\mathrm{ref}}$ 加入第二个控制特征，形成二维特征伺服：

$$
s = \begin{bmatrix} x_m \\ z_m - z_m^{\mathrm{ref}} \end{bmatrix}.
$$

第一版没有必要立即增加复杂度。

## 5.7 端点身份必须连续跟踪

小步运动还有一个重要优势：可以通过时间连续性保持 $e_1,e_2$ 的身份。

假设上一帧端点为 $p_{e1,k}, p_{e2,k}$，当前检测到两个无标签端点 $q_1,q_2$。

比较两种匹配代价：

$$
D_1 = \|q_1 - p_{e1,k}\|^2 + \|q_2 - p_{e2,k}\|^2,
\qquad
D_2 = \|q_2 - p_{e1,k}\|^2 + \|q_1 - p_{e2,k}\|^2.
$$

取代价较小的匹配。如果 $|D_1 - D_2|$ 过小，说明身份判断不可靠，此帧不保存。

这一步非常重要，因为 12-DOF-V2 要求：
- $e_1$ 始终对应 $u$ 边；
- $e_2$ 始终对应 $v$ 边。

标签一旦交换，会直接污染角点约束。

## 5.8 蠕动停止条件

不再用肉眼观察法兰坐标轴角度，而是直接使用机器人返回的法兰旋转矩阵。

设本次种子过渡开始时的旋转为 $R_{\mathrm{start}}$，当前旋转为 $R_{\mathrm{current}}$。相对旋转：

$$
\Delta R = R_{\mathrm{start}}^T R_{\mathrm{current}},
\qquad
\theta_{\mathrm{rel}} = \cos^{-1}\left(\frac{\operatorname{tr}(\Delta R)-1}{2}\right).
$$

当满足：

- $\theta_{\mathrm{rel}} \ge \theta_{\mathrm{target}}$；
- $|x_m| < \varepsilon_x$；
- $m_{\mathrm{domain}}>m_{\mathrm{safe}}$；
- 连续 $N$ 帧双边稳定；

时，停止本次蠕动并保存种子位姿。

也可以直接累计旋转角度（因为每次旋转指令本身就是已知的）：

$$
\theta_{\mathrm{acc}} = \sum_k |\Delta\theta_k|.
$$

机器人法兰旋转由编码器直接给出，这里不需要手眼关系。

## 5.9 自动种子状态机

```
MANUAL_INITIAL_POSE
  人工找到第一个双边可见位姿
  ↓
STATIC_INITIAL_ENVELOPE
  检查 x_mid、z_mid、安全域余量、轮廓长度、端点绝对深度差、
  关节限位余量和四方向局部 IK
  ↓
MEASURED_LOCAL_PREFLIGHT
  实际执行局部 ±X/±Y 各 2°并返回参考位姿
  至少 3/4 方向安全，且同时覆盖 X/Y
  ↓
SAVE_REFERENCE
  保存 T_BF、两个断点、x_mid 和 安全域余量
  ↓
SELECT_ROTATION_TARGET
  选择局部 X/Y 轴和目标角度
  ↓
MICRO_ROTATE
  绕指定法兰轴旋转 Δθ
  ↓
READ_PROFILE
  检测两个断点并保持 e1/e2 身份
  │
  ├─ 两个断点均存在
  │   ↓
  │   TRANSLATION_SERVO
  │     固定旋转
  │     调整法兰局部平移，使 x_mid → 0
  │
  ├─ 只剩一个断点
  │   ↓
  │   回退上一有效位姿，旋转步长减半
  │
  └─ 两个断点均丢失
      ↓
      立即回退上一有效位姿，放弃盲目搜索
  ↓
CHECK_FEATURE_SAFETY
  双边存在、安全域余量足够、轮廓有效
  │
  ├─ 目标角度未达到 → MICRO_ROTATE
  └─ 目标角度已达到
      ↓
      STABILITY_CHECK
      ↓
      SAVE_SEED
      ↓
      RETURN_REFERENCE
      ↓
      SELECT_NEXT_AXIS
```

## 5.10 失败回退

系统始终保存：

```
last_valid_pose
last_valid_profile
last_valid_endpoints
last_valid_x_mid
last_valid_domain_margin
```

如果只剩一个断点或两个断点全部丢失：

1. 回退到上一个有效位姿；
2. 将旋转步长减半；
3. 重新进行平移试探；
4. 再进行局部平移搜索；
5. 多次失败则放弃该目标旋转。

## 5.11 种子位姿验收

只有满足以下条件，当前状态才作为正式种子位姿保存：

1. $e_1,e_2$ 均被检测到；
2. 两个端点属于同一角点的指定两条相邻边；
3. 轮廓点数和线段长度足够；
4. 两个端点远离传感器 安全测量域边界；
5. 当前旋转已达到目标朝向；
6. 连续若干帧测量稳定；
7. 新姿态提高已有姿态集合的旋转多样性。

可构造旋转多样性矩阵：

$$
G_R = \sum_{i<j} (R_i - R_j)^T (R_i - R_j).
$$

当 $\lambda_{\min}(G_R)$ 过小时，说明法兰朝向分布缺少某些旋转方向。

但是，$G_R$ 只是一种运动多样性启发式指标，不能证明完整12-DOF-V2可观测。最终验收必须使用约化Jacobian的：

- 数值秩；
- 奇异值谱；
- 条件数；
- 最弱参数方向。

默认目标为6个有效种子位姿，但“6”不是无条件充分保证。若约化Jacobian仍然病态，则继续补采非平行旋转轴，而不是增加相同方向的重复帧。

## 5.12 更新后的完整 Phase 0b 流程

```
1. 人工将机器人移动到一个双边稳定可见位姿
2. 按 §5.3.2 检查静态保守包络；不满足时给出具体变量和调整方向
3. 实际执行局部 $\pm X,\pm Y$ 各 $2^\circ$ 的动态预检；每次返回参考位姿
4. 仅当至少3/4方向安全且覆盖两个轴时，保存：
   - 当前 T_BF
   - 完整轮廓
   - e1/e2 端点
   - x_mid
   - 安全测量域余量
   - last_valid_pose
5. 设定种子旋转计划：R0, Rx+, Rx-, Ry+, Ry-, Rx+Ry+，并准备异号和换序候补分支
6. 对每个目标姿态：
   a. 选择当前法兰局部旋转轴
   b. 旋转 2°~3°
   c. 读取两个端点
   d. 固定旋转
   e. 通过小幅试探确定平移修正方向
   f. 平移使 x_mid 趋近 0
   g. 检查两个端点和 安全测量域余量
   h. 安全则继续下一次小步旋转
   i. 失败则回退并减小步长
   j. 累积角度达到目标且连续稳定后保存种子
7. 采集约 6 个旋转多样的有效种子
8. 使用Phase 0a提供的粗手眼作为优化初值，运行12-DOF-V2
9. 检查约化 Jacobian：
   - 数值秩
   - 最小奇异值
   - 条件数
   - 平移—角点弱方向
10. 若仍然病态：
   - 根据弱方向补充新的旋转轴种子
   - 而不是简单增加相同方向的位姿
11. 初始化通过后，进入基于平板几何和信息增益的正式 NBV 阶段
```

## 5.13 方法定位总结

Phase 0b **不是**：

> 粗手眼 → 预测虚拟锚点 → 计算下一法兰位姿

而是：

$$
\boxed{
\text{法兰局部轴小步旋转}
\rightarrow
\text{直接观察轮廓变化}
\rightarrow
\text{无标定局部平移伺服}
\rightarrow
\text{双边稳定}
}
$$

最核心的控制原则是：

$$
\begin{aligned}
&\text{旋转轴和旋转角度由种子计划给定；}\\
&\text{平移方向由实际轮廓反馈在线确定；}\\
&\text{中点 X 坐标用于闭环控制；}\\
&\text{双边存在和 安全域余量用于安全约束；}\\
&\text{全程不使用手眼关系。}
\end{aligned}
$$

---

# 6. Phase 0c：12-DOF-V2联合初始化

## 6.1 状态变量

完整状态为：

$$
\Theta=
\{
R_{he},t_{he},R_{pl},C
\},
$$

共 12 个自由度：

- 手眼旋转 $R_{he}$：3 DoF；
- 手眼平移 $t_{he}$：3 DoF；
- 平板姿态 $R_{pl}$：3 DoF；
- 角点位置 $C$：3 DoF。

令：

$$
R_{pl}=[u,v,n].
$$

## 6.2 几何残差

对于第 $i$ 帧中的任意平面点 $p_{ij}^B$：

$$
r_{p,ij}
=
n^T(p_{ij}^B-C).
$$

对于位于 $u$ 边上的端点 $p_{e1,i}^B$：

$$
r_{e1,i}
=
v^T(p_{e1,i}^B-C).
$$

对于位于 $v$ 边上的端点 $p_{e2,i}^B$：

$$
r_{e2,i}
=
u^T(p_{e2,i}^B-C).
$$

两个端点还必须位于平板平面：

$$
r_{e1,n,i}
=
n^T(p_{e1,i}^B-C),
$$

$$
r_{e2,n,i}
=
n^T(p_{e2,i}^B-C).
$$

因此，每一帧同时提供：

- 密集平面约束；
- $e_1$ 边的面内约束；
- $e_2$ 边的面内约束；
- 两个端点的平面约束；
- 所有帧共享同一个角点 $C$。

## 6.3 密集点权重

同一帧大量轮廓点高度相关，不能把所有点视为完全独立信息。

设置：

$$
w_{\mathrm{plane}}
=
\frac{N_{\mathrm{pose}}}{N_{\mathrm{plane\ points}}},
$$

使所有平面点块的总权重与位姿数量处于同一量级，避免平面点数量完全压制两条边端点约束。

后续实现中更推荐每帧等数量采样，并采用按帧归一权重。例如第 $i$ 帧有 $N_i$ 个平面点时，可令单点权重：

$$
w_{p,ij}=\frac{w_{\mathrm{frame}}}{N_i},
$$

从而每一帧的平面点块总权重均为 $w_{\mathrm{frame}}$。端点约束再根据实际检测方差设置独立权重。

## 6.4 角点变量投影

给定：

$$
x_9=
[\omega_{he},t_{he},\omega_{pl}]
$$

后，完整加权残差可以写为：

$$
r(x_9,C)
=
W^{1/2}
\left(
A(x_9)C-b(x_9)
\right).
$$

角点通过加权线性最小二乘解析求解：

$$
C^\star(x_9)
=
\arg\min_C
\left\|
W^{1/2}
\left(
A(x_9)C-b(x_9)
\right)
\right\|^2.
$$

当 $A^TWA$ 满秩时：

$$
C^\star
=
(A^TWA)^{-1}A^TWb.
$$

实现中应使用QR、SVD或带秩判定的最小二乘，而不是显式求逆。

约化非线性问题为：

$$
\min_{x_9}
\left\|
W^{1/2}
\left(
A(x_9)C^\star(x_9)-b(x_9)
\right)
\right\|^2.
$$

如果：

$$
\operatorname{rank}(A)<3,
$$

则当前数据不足以稳定确定共享角点，不能接受该初始化。

因此：

- 最终模型仍是完整12自由度；
- 非线性优化维度由12维降低为9维；
- 每次改变手眼或平板姿态时，角点都会重新计算；
- $C$不是初始化后永久固定的常量。

## 6.5 初始化输出

设正式主动阶段的起始索引为 $k_0$。12-DOF-V2输出：

$$
\hat X_{k_0}={}^F\hat T_{S,k_0},
$$

$$
\hat R_{pl,k_0}
=
[\hat u_{k_0},\hat v_{k_0},\hat n_{k_0}],
$$

$$
\hat C_{k_0}.
$$

同时应计算：

- 约化9维 Jacobian；
- 奇异值；
- 数值秩；
- 条件数；
- 近似协方差；
- 最弱参数方向。

只有当 Jacobian 具有足够秩且条件数可接受时，才能进入正式 NBV 阶段。


## 6.6 平板坐标系的正交化与离散身份固定

数值优化后，应将 $R_{pl}$ 投影到最近的旋转矩阵：

$$
R_{pl}=U\operatorname{diag}(1,1,\det(UV^T))V^T,
$$

其中：

$$
\tilde R_{pl}=U\Sigma V^T.
$$

同时必须处理离散对称性：

- $u,v$ 不得交换；
- $u$ 的方向必须与历史 $e_1$ 边一致；
- $v$ 的方向必须与历史 $e_2$ 边一致；
- $n=u\times v$；
- $\det R_{pl}=+1$。

可通过与上一轮 $R_{pl,k-1}$ 的最大列向量一致性，选择唯一的符号与排列。若无法可靠确定，则该轮更新应判为失败，而不是允许边身份静默翻转。


---

# 7. 局部双边角点模型与人工安全测量域

## 7.1 为什么只建立局部角模型

12-DOF-V2规定所有正式测量始终使用：

- 同一个平板面；
- 同一个物理角点 $C$；
- 从该角点出发的固定边 $E_u,E_v$。

因此主动阶段真正需要维护的不是完整矩形，而是：

$$
\boxed{
C+E_u+E_v
}
$$

另外两条远端边既不参与残差，也不应成为自动采集目标。

## 7.2 平板局部坐标系

利用当前估计：

$$
C_k,\qquad
R_{pl,k}=[u_k,v_k,n_k],
$$

建立平板局部坐标系 $P$：

$$
{}^BT_{P,k}
=
\begin{bmatrix}
[u_k,v_k,n_k]&C_k\\
0&1
\end{bmatrix}.
$$

在 $P$ 系中：

$$
C^P=0,\qquad
u^P=e_x,\qquad
v^P=e_y,\qquad
n^P=e_z.
$$

定义两条可信边段：

$$
E_u^P
=
\left\{
[a,0,0]^T
\mid
a_{\min}\le a\le a_{\max}
\right\},
$$

$$
E_v^P
=
\left\{
[0,b,0]^T
\mid
b_{\min}\le b\le b_{\max}
\right\}.
$$

平板尺寸只提供：

$$
a_{\max}\le L,\qquad b_{\max}\le W.
$$

## 7.3 人工配置Gocator测量域

理想激光平面在传感器系中写为：

$$
y_S=0.
$$

在Gocator已经直接输出公制轮廓点 $[x,0,z]^T$ 的前提下，主动规划不需要重新标定内部相机、激光器和外壳之间的精确光学关系。只需要在实际轮廓所使用的同一 $X_S-Z_S$ 数值坐标中，人工配置经验测量域。

这里配置的是**规划安全域**，不是对真实光学可见域的精确恢复。其正确性依赖工人或调试程序通过实际轮廓数据进行保守设定。

定义外层区域：

$$
\mathcal V_{\mathrm{hard}}^S
=
\left\{
(x,0,z)^T
\mid
z_n\le z\le z_f,\ 
x_l(z)\le x\le x_r(z)
\right\},
$$

其中：

$$
\tau(z)=\frac{z-z_n}{z_f-z_n},
$$

$$
x_l(z)
=(1-\tau)x_{l,n}+\tau x_{l,f},
$$

$$
x_r(z)
=(1-\tau)x_{r,n}+\tau x_{r,f}.
$$

再向内收缩得到：

$$
\mathcal V_{\mathrm{safe}}^S
\subset
\mathcal V_{\mathrm{hard}}^S.
$$

两者含义为：

- $\mathcal V_{\mathrm{hard}}^S$：经验上仍可能得到轮廓的范围；
- $\mathcal V_{\mathrm{safe}}^S$：候选规划必须满足的保守范围。

人工梯形不要求关于 $x_S=0$ 对称。只要真实轮廓坐标、预测轮廓坐标和人工梯形使用同一个 $S$ 系，就不需要知道坐标原点在传感器外壳中的物理位置。

## 7.4 模块输出

$$
\mathcal M_k
=
\left\{
{}^BT_{P,k},
E_u^P,
E_v^P,
\mathcal V_{\mathrm{hard}}^S,
\mathcal V_{\mathrm{safe}}^S
\right\}.
$$

---

# 8. 名义双边候选、法兰指令与虚拟测量生成

## 8.1 候选参数

定义：

$$
\eta=(a,b,\alpha,\psi,h,s),
\qquad
s\in\{-1,+1\}.
$$

其中：

- $a$：目标断点 $A$ 在 $E_u$ 上距角点的距离；
- $b$：目标断点 $B$ 在 $E_v$ 上距角点的距离；
- $\alpha$：激光平面法向与平板法向之间的夹角，即两平面的二面角；
- $\psi$：传感器坐标轴在激光平面内的旋转；
- $h$：目标轮廓中点在传感器 $z_S$ 方向的深度；
- $s$：绕目标交线倾斜的离散分支。

参数范围满足：

$$
a_{\min}\le a\le a_{\max},
\qquad
b_{\min}\le b\le b_{\max}.
$$

候选唯一性来自下述确定性构造规则，而不是一条交线天然唯一确定传感器位姿。

## 8.2 由 $a,b$ 确定目标断点和交线

在 $P$ 系中：

$$
A^P=
[a,0,0]^T,
\qquad
B^P=
[0,b,0]^T.
$$

定义：

$$
\rho=\|B^P-A^P\|=\sqrt{a^2+b^2},
$$

$$
d_\ell^P
=
\frac{B^P-A^P}{\rho}.
$$

因此：

- $a,b$ 决定两个断点在固定边上的位置；
- $a/b$ 决定交线在平板内的方向；
- $\rho$ 决定名义轮廓长度。

## 8.3 由 $\alpha,s$ 确定激光平面

令：

$$
n^P=e_z,
\qquad
q^P=n^P\times d_\ell^P.
$$

构造激光平面法向：

$$
m_L^P
=
\cos\alpha\,n^P
+
s\sin\alpha\,q^P.
$$

由于：

$$
(m_L^P)^Td_\ell^P=0,
$$

经过 $A^P,B^P$ 且法向为 $m_L^P$ 的平面包含目标交线。

应避免：

$$
\alpha\approx0,
$$

因为此时激光平面与平板接近重合，交线退化。

## 8.4 由 $\psi$ 确定传感器姿态

规定传感器激光平面为：

$$
y_S=0,
$$

令：

$$
y_S^P=m_L^P.
$$

先建立：

$$
x_{S0}^P=d_\ell^P,
\qquad
z_{S0}^P=x_{S0}^P\times y_S^P.
$$

再绕 $y_S$ 轴旋转 $\psi$：

$$
x_S^P
=
\cos\psi\,x_{S0}^P
+
\sin\psi\,z_{S0}^P,
$$

$$
z_S^P
=
-\sin\psi\,x_{S0}^P
+
\cos\psi\,z_{S0}^P.
$$

因此：

$$
R_{PS}=[x_S^P,y_S^P,z_S^P].
$$

实现中应再次进行数值正交化，并检查：

$$
R_{PS}^TR_{PS}=I,\qquad \det R_{PS}=+1.
$$

## 8.5 由 $h$ 确定传感器原点

目标线段中点为：

$$
M^P=\frac{A^P+B^P}{2}.
$$

规定：

$$
M^S=[0,0,h]^T.
$$

由：

$$
M^P=R_{PS}M^S+t_{PS},
$$

得到：

$$
t_{PS}=M^P-hz_S^P.
$$

于是：

$$
{}^PT_S(\eta)
=
\begin{bmatrix}
R_{PS}&t_{PS}\\
0&1
\end{bmatrix}.
$$

## 8.6 名义端点与虚拟轮廓

由：

$$
p^S=R_{PS}^T(p^P-t_{PS}),
$$

直接得到：

$$
A^S(\eta)
=
\begin{bmatrix}
-\frac{\rho}{2}\cos\psi\\
0\\
h+\frac{\rho}{2}\sin\psi
\end{bmatrix},
$$

$$
B^S(\eta)
=
\begin{bmatrix}
+\frac{\rho}{2}\cos\psi\\
0\\
h-\frac{\rho}{2}\sin\psi
\end{bmatrix}.
$$

在两个端点之间采样：

$$
p_j^S(\eta)
=
(1-\lambda_j)A^S(\eta)
+
\lambda_jB^S(\eta),
\qquad
0\le\lambda_j\le1,
$$

得到：

$$
\mathcal P_c^S(\eta)
=
\{p_j^S(\eta)\}_{j=1}^{N_c}.
$$

虚拟轮廓的采样数量和权重必须与真实数据处理保持一致。若真实帧采用固定数量下采样和按帧归一权重，则虚拟帧也必须采用相同规则。

因此模块8已经能够直接生成完整虚拟轮廓，不需要再设独立的名义未来轮廓预测模块。

## 8.7 目标传感器位姿与法兰指令

由当前平板位姿：

$$
{}^BT_{P,k},
$$

得到目标传感器基坐标位姿：

$$
{}^BT_{S,c}(\eta)
=
{}^BT_{P,k}\,{}^PT_S(\eta).
$$

利用当前手眼估计反求名义法兰指令：

$$
\boxed{
{}^BT_{F,c}(\eta)
=
{}^BT_{P,k}
\,{}^PT_S(\eta)
\left({}^F\hat T_{S,k}\right)^{-1}
}
$$

完整映射为：

$$
\eta
\rightarrow
{}^PT_S
\rightarrow
{}^BT_{S,c}
\rightarrow
{}^BT_{F,c}.
$$

## 8.8 名义可行性检查

由于 $a,b$ 的采样区间已经保证目标断点位于固定边段，名义几何检查可直接基于模块8输出完成。

### 安全梯形

要求：

$$
A^S(\eta)\in\mathcal V_{\mathrm{safe}}^S,
\qquad
B^S(\eta)\in\mathcal V_{\mathrm{safe}}^S.
$$

### 激光面退化

要求：

$$
\alpha\ge\alpha_{\min},
$$

并避免激光平面与平板近重合。

### 轮廓质量

要求：

- $\rho$ 不过短；
- 预测采样点数足够；
- 两个端点与安全梯形边界具有足够余量；
- $h$ 位于稳定工作深度范围。

### 机器人可执行性

要求：

- 法兰位姿存在IK解；
- 关节不越限；
- 远离奇异位形；
- 运动路径无碰撞；
- 运动增量不过大。

## 8.9 模块输出

名义候选写为：

$$
c(\eta)
=
\left\{
\eta,
{}^BT_{F,c}(\eta),
{}^BT_{S,c}(\eta),
\mathcal P_c^S(\eta),
A^S(\eta),
B^S(\eta),
m_{\mathrm{safe}}(\eta)
\right\}.
$$

虚拟测量为：

$$
\boxed{
z_c(\eta)
=
\left\{
{}^BT_{F,c}(\eta),
\mathcal P_c^S(\eta),
p_{e1,c}^S(\eta),
p_{e2,c}^S(\eta)
\right\}
}
$$

其中：

$$
p_{e1,c}^S=A^S,\qquad
p_{e2,c}^S=B^S.
$$

## 8.10 为什么不设置独立的名义正向预测模块

模块8构造：

$$
{}^BT_{F,c}
=
{}^BT_{P,k}
{}^PT_S
({}^F\hat T_{S,k})^{-1}.
$$

若使用同一名义手眼重新计算：

$$
{}^B\hat T_{S,c}
=
{}^BT_{F,c}
{}^F\hat T_{S,k},
$$

则：

$$
{}^B\hat T_{S,c}
=
{}^BT_{P,k}
{}^PT_S.
$$

它严格等于模块8中的目标传感器位姿。

再由该激光平面与同一 $E_u,E_v$ 求交，必然得到：

$$
a_c=a,\qquad b_c=b,
$$

以及：

$$
p_{e1,c}^S=A^S,\qquad
p_{e2,c}^S=B^S.
$$

因此，在同一名义状态下重新正向求交只是一致性单元测试，不构成新的算法模块。该正向计算仅保留在下一节的联合不确定性传播内部。


## 8.11 六个参数分别改变什么

在当前“轮廓中点位于 $[0,0,h]^T$”的确定性构造下：

- $a,b$ 改变平板上两个物理断点及轮廓长度；
- $\alpha,s$ 改变传感器和法兰相对于平板的三维姿态；
- $\psi,h$ 直接改变端点在传感器 $X_S-Z_S$ 平面内的位置。

需要注意：

$$
A^S(\eta),B^S(\eta)
$$

的解析式中不显含 $\alpha,s$。这不是说 $\alpha,s$ 没有信息作用，而是因为构造规则主动保持了同一目标轮廓布局。它们仍会改变：

$$
{}^BT_{S,c},\qquad {}^BT_{F,c},
$$

从而改变机器人运动以及手眼残差对参数的Jacobian，因此信息增益仍可能随 $\alpha,s$ 显著变化。


---

# 9. 联合不确定性下的鲁棒有效性判断

## 9.1 为什么名义候选还不够

模块8使用当前名义状态：

$$
\hat x_{9,k},
\qquad
\hat C_k
$$

生成固定法兰指令。真实手眼、平板方向和角点仍存在误差，因此执行后可能出现：

- 激光面不再截到某条固定边；
- 交点越出可信边段；
- 端点离开人工安全梯形；
- 端点安全余量不足；
- 求交数值退化。

需要估计：

$$
P_{\mathrm{valid}}(\eta)
=
P\left(
\text{固定法兰指令执行后仍获得指定双边安全观测}
\right).
$$

## 9.2 9维约化状态不确定性

当前约化Jacobian为：

$$
J_9
=
\frac{\partial r(x_9,D_k)}{\partial x_9}.
$$

由于旋转和平移单位不同，先定义无量纲缩放状态：

$$
\tilde x_9=S_x^{-1}x_9,
$$

其中 $S_x$ 给出各参数的参考尺度。对应缩放Jacobian为：

$$
\tilde J_9=J_9S_x.
$$

构造：

$$
\tilde H_9=\tilde J_9^TW\tilde J_9,
$$

$$
\Sigma_{\tilde x9}
\approx
\hat\sigma_r^2
(\tilde H_9+\lambda I)^{-1}.
$$

再映射回原状态：

$$
\Sigma_{x9}
=
S_x\Sigma_{\tilde x9}S_x^T.
$$

这是局部高斯和高斯—牛顿近似。若使用强鲁棒核、残差明显非高斯或系统误差占主导，应通过bootstrap、Monte Carlo或经验放大系数校准协方差。

从协方差生成Sigma点或联合样本：

$$
x_9^{(j)}.
$$

每个样本包含：

$$
X^{(j)},
\qquad
R_{pl}^{(j)}=[u^{(j)},v^{(j)},n^{(j)}].
$$

角点不应独立随意采样，而应使用全部已有数据重新变量投影：

$$
C^{(j)}
=
C^\star(x_9^{(j)},D_k).
$$

## 9.3 固定同一个法兰指令

候选参数 $\eta$ 只在名义状态下生成一次：

$$
{}^BT_{F,c}(\eta).
$$

所有样本均执行同一机器人命令。第 $j$ 个状态下的传感器位姿为：

$$
{}^BT_{S,c}^{(j)}
=
{}^BT_{F,c}(\eta)X^{(j)}.
$$

禁止对每个样本重新反求一个新的法兰位姿，否则评价的不是同一个真实动作。

## 9.4 内部正向观测函数

定义：

$$
\mathcal F
\left(
{}^BT_F,
X,R_{pl},C
\right)
$$

为固定法兰指令和给定状态下的正向观测预测函数。

设：

$$
{}^BT_S
=
{}^BT_FX
=
\begin{bmatrix}
R_{BS}&t_{BS}\\
0&1
\end{bmatrix}.
$$

激光平面在基坐标系中的法向为：

$$
m_B=R_{BS}e_y.
$$

激光平面方程：

$$
m_B^T(p-t_{BS})=0.
$$

与固定 $u$ 边：

$$
p=C+a_cu
$$

求交得到：

$$
a_c
=
\frac{m_B^T(t_{BS}-C)}
{m_B^Tu}.
$$

与固定 $v$ 边：

$$
p=C+b_cv
$$

求交得到：

$$
b_c
=
\frac{m_B^T(t_{BS}-C)}
{m_B^Tv}.
$$

若分母接近零，则激光平面与对应边近似平行，样本无效。

物理端点为：

$$
p_{e1}^B=C+a_cu,
\qquad
p_{e2}^B=C+b_cv.
$$

变换到该样本传感器系：

$$
p_{e1}^S=({}^BT_S)^{-1}p_{e1}^B,
$$

$$
p_{e2}^S=({}^BT_S)^{-1}p_{e2}^B.
$$

该函数不是独立的名义模块，而是模块9对每个不确定性样本调用的内部函数。

## 9.5 样本有效条件

第 $j$ 个样本有效，当且仅当：

$$
a_{\min}\le a_c^{(j)}\le a_{\max},
$$

$$
b_{\min}\le b_c^{(j)}\le b_{\max},
$$

$$
p_{e1,c}^{S,(j)}
\in
\mathcal V_{\mathrm{safe}}^S,
$$

$$
p_{e2,c}^{S,(j)}
\in
\mathcal V_{\mathrm{safe}}^S,
$$

并且：

- 求交不存在近零分母；
- 轮廓长度和点数满足要求；
- 安全余量达到阈值。

IK、关节限位和法兰路径碰撞由固定的 ${}^BT_{F,c}$ 决定，通常在模块8或候选外层只检查一次，不需要对每个状态样本重复检查。只有当碰撞模型显式依赖不确定的传感器外形或平板位姿时，才需要在样本内部重复评价。

## 9.6 有效概率和风险分类

有效概率近似为：

$$
P_{\mathrm{valid}}(\eta)
\approx
\sum_jw_j
\mathbf1
\left[
\operatorname{Valid}
\left(
{}^BT_{F,c}(\eta),x_9^{(j)}
\right)
\right].
$$

等权采样时为有效样本比例。

同时记录互斥或可重叠的失败概率：

$$
P_{\mathrm{fail}}^{u},
\quad
P_{\mathrm{fail}}^{v},
\quad
P_{\mathrm{fail}}^{\mathrm{trapezoid}},
\quad
P_{\mathrm{fail}}^{\mathrm{degenerate}}.
$$

机器人不可执行候选在进入联合状态采样前已经被删除，因此不再作为状态样本失败概率。

只保留：

$$
P_{\mathrm{valid}}(\eta)\ge p_{\min}
$$

的候选：

$$
\mathcal C_k^{\mathrm{safe}}
=
\left\{
\eta\mid
P_{\mathrm{valid}}(\eta)\ge p_{\min}
\right\}.
$$

## 9.7 早期保守、后期扩展

标定早期协方差较大，应采用：

- 较小的 $\alpha,\psi$变化；
- 较居中的 $a,b,h$；
- 更高的安全余量；
- 更严格的 $p_{\min}$。

随着估计收敛，可以逐步扩大候选范围，以获取更强旋转和平移激励。


## 9.8 鲁棒有效性不是碰撞概率

当前 $P_{\mathrm{valid}}$ 主要描述标定状态不确定性导致的双边几何风险：

$$
X,\ R_{pl},\ C
\quad\Rightarrow\quad
e_1,e_2\text{是否仍在可信边和安全域内}.
$$

它不自动涵盖：

- 机器人控制误差；
- 动力学跟踪误差；
- 反射和曝光造成的断点检测失败；
- 平板非平面误差；
- 环境障碍物的不确定性。

这些因素可以通过：

1. 收缩安全梯形；
2. 放大 $\Sigma_{x9}$；
3. 增加经验检测成功率模型；
4. 单独建立执行风险项；

逐步加入，但不得把当前几何概率解释为所有物理失败的总概率。


---

# 10. 基于12-DOF-V2的信息增益与NBV选择

## 10.1 NBV的作用

NBV不负责保证双边可见。模块8和模块9已经完成：

- 名义双边候选构造；
- 联合不确定性鲁棒筛选。

NBV只解决：

$$
\boxed{
\text{在所有可靠候选中，哪个完整法兰位姿最有助于估计手眼参数}
}
$$

## 10.2 当前手眼有效信息

当前全部数据为：

$$
D_k.
$$

为避免旋转和平移单位不同导致信息矩阵失真，采用与第9.2节相同的无量纲状态：

$$
\tilde x_9=S_x^{-1}x_9.
$$

计算缩放约化Jacobian：

$$
\tilde J_k
=
\frac{\partial r(\tilde x_9,D_k)}
{\partial\tilde x_9},
$$

$$
\tilde H_k=\tilde J_k^TW\tilde J_k.
$$

下文为简化仍记为 $J_k,H_k$，但所有秩、条件数、特征值、Schur补、D-opt和E-opt均应基于同一无量纲尺度。

将：

$$
x_9=
\begin{bmatrix}
x_X\\
x_P
\end{bmatrix},
$$

其中：

- $x_X\in\mathbb R^6$：手眼参数；
- $x_P\in\mathbb R^3$：平板姿态参数。

分块：

$$
H_k=
\begin{bmatrix}
H_{XX}&H_{XP}\\
H_{PX}&H_{PP}
\end{bmatrix}.
$$

边缘化平板姿态，得到手眼有效信息：

$$
H_{X,k}^{\mathrm{eff}}
=
H_{XX}
-
H_{XP}H_{PP}^{\dagger}H_{PX}.
$$

## 10.3 候选虚拟测量必须固定

模块8已经产生：

$$
z_c(\eta)
=
\left\{
{}^BT_{F,c},
\mathcal P_c^S,
p_{e1,c}^S,
p_{e2,c}^S
\right\}.
$$

构造：

$$
D_k^c=D_k\cup\{z_c(\eta)\}.
$$

计算候选Jacobian时，虚拟测量 $z_c(\eta)$ 必须保持固定。不能在每次数值扰动 $x_9$ 时重新生成轮廓，否则状态变化和观测变化会同时发生，可能人为抵消残差变化并错误低估信息。

需要区分：

- 模块9：为了评价执行风险，轮廓随不确定性状态样本重新预测；
- 模块10：为了计算Fisher信息，候选虚拟观测固定，只扰动待估状态。

## 10.4 变量投影下重新计算完整Jacobian

由于角点变量投影依赖全部数据，不能简单写为：

$$
H_{\mathrm{new}}
=
H_{\mathrm{old}}+J_c^TJ_c.
$$

正确做法为：

$$
J_k^c(\eta)
=
\frac{\partial r(x_9,D_k^c)}{\partial x_9}.
$$

每次扰动 $x_9$ 时，使用固定增广数据集 $D_k^c$ 重新求解：

$$
C^\star(x_9,D_k^c).
$$

然后：

$$
H_k^c(\eta)
=
(J_k^c)^TWJ_k^c.
$$

再通过Schur补得到：

$$
H_{X,k}^{\mathrm{eff}}(\eta).
$$

## 10.5 信息评分

在统一无量纲尺度和相同正则化下，D-opt增益为：

$$
\Delta I_X(\eta)
=
\frac12
\left[
\log\det
\left(
H_{X,k}^{\mathrm{eff}}(\eta)+\epsilon I
\right)
-
\log\det
\left(
H_{X,k}^{\mathrm{eff}}+\epsilon I
\right)
\right].
$$

其中 $\epsilon$ 仅用于数值稳定。当前信息矩阵和候选增广信息矩阵必须使用相同的：

- 状态尺度；
- 残差权重；
- 鲁棒核线性化规则；
- 正则化。

E-opt指标：

$$
\lambda_{\min}
\left(
H_{X,k}^{\mathrm{eff}}(\eta)
\right)
$$

用于保护最弱信息方向。

在尚未满秩时，评分优先级为：

1. 提高数值秩；
2. 提高最小非零特征值；
3. 满秩后主要使用D-opt；
4. 使用E-opt避免只增强已有强方向。

## 10.6 候选参数与信息增益的关系

由于：

$$
\eta
\rightarrow
{}^BT_{F,c}(\eta),
$$

并且：

$$
\eta
\rightarrow
z_c(\eta),
$$

因此：

$$
\boxed{
\Delta I_X
=
G(a,b,\alpha,\psi,h,s)
}
$$

该关系一般没有简单闭式表达，但可以通过候选网格、随机采样或无梯度优化数值建立。

可绘制：

$$
G(a,b),
\qquad
G(\alpha,\psi),
\qquad
G(\alpha,h)
$$

等信息增益地形，用于研究候选参数对手眼可观测性的影响。

## 10.7 NBV选择

在鲁棒有效候选集合中选择：

$$
\eta^\star
=
\arg\max_{\eta\in\mathcal C_k^{\mathrm{safe}}}
S(\eta),
$$

其中综合评分可以写为：

$$
S(\eta)
=
\Delta I_X(\eta)
-\lambda_m C_{\mathrm{motion}}(\eta)
-\lambda_r C_{\mathrm{risk}}(\eta).
$$

若安全约束均作为硬约束，则可直接：

$$
\eta^\star
=
\arg\max_{\eta\in\mathcal C_k^{\mathrm{safe}}}
\Delta I_X(\eta).
$$


## 10.8 名义信息增益与期望信息增益

第一版使用模块8生成的名义虚拟测量计算：

$$
\Delta I_X(\eta).
$$

这是一个局部名义近似，计算量较低。它假设候选实际测量接近当前名义预测。

更严格的扩展是对联合状态样本计算期望信息：

$$
\mathbb E[\Delta I_X(\eta)]
\approx
\sum_jw_j
\Delta I_X^{(j)}(\eta).
$$

但该方法需要为每个候选、每个状态样本重新构造固定虚拟测量并计算完整增广Jacobian，计算量显著增加。因此建议：

- 第一版：$P_{\mathrm{valid}}$ 做鲁棒筛选，名义 $\Delta I_X$ 做排序；
- 后续：只对前若干候选计算期望信息增益。


---

# 11. 候选筛选与执行顺序

为降低计算量，推荐采用由便宜到昂贵的级联筛选：

```text
1. 采样 η=(a,b,α,ψ,h,s)
2. 解析生成 A^S、B^S、虚拟轮廓、T_PS、T_BS和T_BF
3. 检查：
   - a,b可信边段
   - α退化
   - 人工安全梯形
   - 轮廓长度与安全余量
4. 检查固定法兰候选：
   - IK
   - 关节限位
   - 奇异性
   - 与当前位置的运动增量
5. 对剩余候选计算联合不确定性 P_valid
6. 删除 P_valid<p_min 的候选
7. 对鲁棒候选计算完整12-DOF-V2信息增益
8. 仅对信息评分前K名进行详细连续路径碰撞检查
9. 按信息、运动代价和风险选择最佳候选
10. 执行、到位确认、采集真实轮廓
```

不存在独立的“名义正向预测阶段”。名义虚拟轮廓由模块8直接生成；正向求交函数只在联合不确定性样本中调用。

若某一轮没有任何候选通过筛选，依次采取：

1. 降低候选姿态变化幅度；
2. 将 $a,b,h$ 向安全域中心收缩；
3. 回到最近安全位姿；
4. 检查当前协方差是否过于乐观或模型是否更新失败；
5. 必要时重新进入局部种子补采，而不是降低双边硬约束。

---

# 12. 执行验证与滚动更新

执行最佳候选后，真实系统必须验证：

- 两个断点均被检测；
- 仍对应同一固定 $E_u,E_v$；
- 两个端点位于外层有效域；
- 轮廓长度和点数正常；
- 残差不异常；
- 法兰实际到位。

若通过：

$$
D_{k+1}=D_k\cup z_{\mathrm{real}}.
$$

随后重新运行12-DOF-V2：

$$
({}^F\hat T_{S,k+1},
R_{pl,k+1},
C_{k+1})
=
\operatorname{SolveV2}(D_{k+1}).
$$

并更新：

$$
J_{9,k+1},
\qquad
\Sigma_{x9,k+1},
\qquad
H_{X,k+1}^{\mathrm{eff}}.
$$

若执行失败：

1. 不将该帧加入正式数据；
2. 回退到上一安全位姿；
3. 将候选或附近区域加入短期黑名单；
4. 缩小候选范围或提高 $p_{\min}$；
5. 重新选择。

---

# 13. 自适应停止

停止条件不应只依赖固定姿态数量。建议同时检查：

## 13.1 最小样本数

$$
N_k\ge N_{\min}.
$$

## 13.2 手眼有效信息满秩

$$
\operatorname{rank}
\left(
H_{X,k}^{\mathrm{eff}}
\right)=6.
$$

## 13.3 最弱方向足够强

$$
\lambda_{\min}
\left(
H_{X,k}^{\mathrm{eff}}
\right)
\ge\lambda_{\mathrm{target}}.
$$

## 13.4 最大剩余增益足够小

$$
\max_{\eta\in\mathcal C_k^{\mathrm{safe}}}
\Delta I_X(\eta)
<
\varepsilon_I
$$

连续若干轮成立。

## 13.5 协方差达到目标

手眼旋转和平移标准差低于预设阈值。

## 13.6 最大采集次数

当：

$$
N_k\ge N_{\max}
$$

时无条件停止，作为安全上限。达到该条件并不代表精度目标已经满足，最终报告中必须区分“信息收敛停止”和“达到最大次数停止”。

---

# 14. 最终标定

停止后使用全部有效帧进行最终12-DOF-V2求解，输出：

$$
{}^F\hat T_S,
\qquad
R_{pl},
\qquad
C.
$$

同时输出：

- 残差统计；
- 参数协方差；
- 数值秩与条件数；
- 每帧双边一致性；
- 可重复性和外部验证误差。

正式最终结果不应使用与在线阶段不同的几何模型，否则在线选姿评分和最终估计目标不一致。

最终结果至少应通过两类独立验证：

1. **重投影/几何一致性验证**：未参与求解的验证位姿中，平面残差和双边残差保持较小；
2. **外部几何验证**：使用已知平面、精密靶标、机器人触碰点或独立测量装置评价手眼变换的实际误差。

仅报告训练残差不能证明手眼标定准确。

---

# 15. 数学一致性核查

## 15.1 12-DOF计数是否正确

完整状态为：

$$
R_{he}(3)+t_{he}(3)+R_{pl}(3)+C(3)=12.
$$

变量投影只是在每次外层迭代中解析消除 $C$，并没有将角点从物理模型中删除。因此“12-DOF模型、9维非线性优化”这一表述是正确的。

## 15.2 边约束是否足以表示两条直边

对 $u$ 边：

$$
v^T(p-C)=0,\qquad n^T(p-C)=0.
$$

两个独立平面约束的交集是一条经过 $C$、方向平行 $u$ 的直线。

对 $v$ 边：

$$
u^T(p-C)=0,\qquad n^T(p-C)=0.
$$

其交集是一条经过 $C$、方向平行 $v$ 的直线。

因此当前端点残差与几何定义一致。

## 15.3 候选参数是否唯一确定传感器相对位姿

仅有 $A,B$ 不能唯一确定传感器位姿。当前方法额外规定：

1. $\alpha,s$ 确定包含 $AB$ 的激光平面；
2. $\psi$ 确定传感器在激光平面内的旋转；
3. 轮廓中点满足：
   $$
   M^S=[0,0,h]^T.
   $$

在这些确定性规则下：

$$
\eta
\mapsto
{}^PT_S(\eta)
$$

为确定映射。该唯一性来自人为构造规则，而不是物理交线本身。

## 15.4 为什么模块8能直接生成虚拟轮廓

平板和激光平面相交于直线 $AB$。在理想平面标定物假设下，两个断点之间的所有平面轮廓点均位于：

$$
p_j^S=(1-\lambda_j)A^S+\lambda_jB^S.
$$

因此模块8可以直接生成理想虚拟轮廓。实际轮廓的缺失、反射和噪声不属于理想几何模型，应在执行风险或实验噪声模型中处理。

## 15.5 为什么模块9必须固定法兰指令

实际机器人只能执行一个确定命令。若对每个状态样本重新反求法兰位姿，则每个样本评价的是不同动作，得到的 $P_{\mathrm{valid}}$ 不再表示当前候选的真实成功概率。因此：

$$
{}^BT_{F,c}
$$

必须在全部不确定性样本中保持不变。

## 15.6 为什么信息Jacobian中虚拟测量必须固定

Fisher信息描述“固定观测对未知状态的敏感程度”。若状态扰动时虚拟观测也随之重新生成，则会让观测和模型同步移动，部分残差变化被抵消。因此计算：

$$
\frac{\partial r(x_9;z_c)}{\partial x_9}
$$

时，$z_c$ 必须固定。

---

# 16. 可观测性、退化与失败模式

## 16.1 两个有效朝向造成的平移—角点弱方向

若种子位姿实际上只有两组有效旋转：

$$
R_1\approx R_2=R_A,\qquad
R_3\approx R_4=R_B,
$$

则相对旋转只有一根固定轴。沿该轴的某些手眼平移变化可能被共享角点 $C$ 吸收，产生严格或近似gauge。

因此必须使用至少三组真正不同、相对旋转轴不平行的朝向，并通过完整Jacobian验证。

## 16.2 激光平面与平板近重合

当：

$$
\alpha\to0,
$$

激光平面和平板接近同一平面，唯一交线退化。此类候选必须删除。

## 16.3 激光平面与目标边近平行

当：

$$
|m_B^Tu|\to0
\quad\text{或}\quad
|m_B^Tv|\to0,
$$

求交参数对微小误差极其敏感。除设置分母阈值外，还应将：

$$
\min(|m_B^Tu|,|m_B^Tv|)
$$

作为连续安全余量。

## 16.4 边身份交换

若 $e_1/e_2$ 标签交换，优化器仍可能降低部分平面残差，但角点和边方向会被错误解释。必须通过时间连续性、边方向连续性和更新后的残差共同检测。

## 16.5 人工安全域设置错误

- 过大：名义候选通过，但实际容易丢失端点；
- 过小：候选数量不足，姿态多样性受限；
- 坐标系不一致：所有安全判断失效。

因此安全域需要通过真实轮廓数据调试，并保存配置版本。

## 16.6 协方差过度乐观

若残差模型忽略系统误差，$\Sigma_{x9}$ 可能明显偏小，使 $P_{\mathrm{valid}}$ 虚高。应通过Monte Carlo覆盖率或重复实验检查：

$$
\frac{\text{真实误差落入预测置信区间的次数}}
{\text{实验次数}}.
$$

## 16.7 无候选可用

无候选可能意味着：

- 当前估计偏差过大；
- 安全域过窄；
- 机器人可达空间不足；
- 候选采样范围不合理；
- 平板角点接近工作空间边界。

系统应回退和重新局部补采，不能直接放松固定双边要求。

---

# 17. 完整算法伪代码

## 17.1 自动种子采集

```text
Input:
    first dual-edge visible pose T_BF_ref
    rotation plan R_targets
    safe trapezoid V_seed_safe

Seeds ← {measure(T_BF_ref)}
last_valid_pose ← T_BF_ref

for each target orientation R_target:
    return safely to reference branch start

    while relative rotation has not reached target:
        rotate flange by a small local-axis step
        profile ← read_profile()
        endpoints ← detect_and_track_e1_e2(profile)

        if endpoints are missing:
            rollback(last_valid_pose)
            reduce rotation step
            retry or abandon this branch
            continue

        hold flange orientation fixed

        if translation sensitivity is unavailable or stale:
            estimate local sensitivity by safe probe motions

        while |x_mid| > epsilon_x:
            translate flange using measured local sensitivity
            read profile and update endpoint identities
            if dual-edge or safe-domain constraint fails:
                rollback(last_valid_pose)
                reduce translation step
                break

        if stable for N frames:
            last_valid_pose ← current pose

    if target orientation, dual-edge stability and diversity checks pass:
        save seed

run 12-DOF-V2 with all seeds
if reduced Jacobian is ill-conditioned:
    add a new nonparallel rotation branch
```

## 17.2 主动NBV循环

```text
Input:
    current dataset D_k
    current estimate x9_hat, C_hat
    covariance Sigma_x9
    local trusted edges and safe trapezoid

while stop condition is false:
    candidate_pool ← sample eta=(a,b,alpha,psi,h,s)

    nominal_candidates ← {}
    for eta in candidate_pool:
        generate T_PS, T_BS, T_BF, A_S, B_S, virtual profile
        reject nominal geometric or safe-domain failures
        reject IK, limit, singularity and coarse motion failures
        keep the candidate

    robust_candidates ← {}
    for candidate in nominal_candidates:
        keep T_BF fixed
        generate joint state samples from Sigma_x9
        for each sample:
            recompute C by variable projection from D_k
            forward-predict intersections with E_u and E_v
            check trusted segments and safe trapezoid
        compute P_valid
        keep candidate if P_valid >= p_min

    for candidate in robust_candidates:
        freeze its virtual measurement z_c
        D_aug ← D_k union {z_c}
        recompute full variable-projected Jacobian
        compute hand-eye effective information gain

    perform detailed path collision checking for top K
    execute the best feasible candidate
    validate the real dual-edge measurement

    if real validation passes:
        trial-update D_k and rerun 12-DOF-V2
        commit only if numerical and residual checks pass
    else:
        rollback and blacklist the failed neighborhood
```

---

# 18. 软件模块与接口建议

```text
phase0a/
    coarse_handeye_provider.py
    urdf_truth_perturbation.py

seed_collection/
    rotation_plan.py
    endpoint_detector.py
    endpoint_identity_tracker.py
    local_sensitivity_probe.py
    translation_servo.py
    seed_safety.py
    rollback_manager.py
    seed_observability.py

calibration_v2/
    state_parameterization.py
    residual_builder.py
    point_weighting.py
    corner_variable_projection.py
    solver.py
    reduced_jacobian.py
    covariance.py
    plane_frame_canonicalization.py

local_corner_model/
    trusted_edges.py
    trapezoid_domain.py
    local_frame.py

candidate_generation/
    eta_sampler.py
    candidate_geometry.py
    virtual_profile.py
    nominal_filter.py

robust_validity/
    sigma_points.py
    forward_observation.py
    sample_validity.py
    validity_probability.py

nbv/
    augmented_dataset.py
    augmented_jacobian.py
    handeye_schur_information.py
    information_gain.py
    candidate_ranker.py
    stopping_rule.py

execution/
    ik_filter.py
    singularity_filter.py
    collision_checker.py
    motion_executor.py
    real_measurement_validator.py
    transaction_update.py
```

## 18.1 关键接口

### 候选生成

```python
candidate = build_candidate(
    eta,
    T_BP=current_plane_pose,
    X_FS=current_handeye,
    safe_domain=configured_trapezoid,
)
```

输出至少包括：

```text
T_PS
T_BS_target
T_BF_command
endpoint_e1_S
endpoint_e2_S
virtual_profile_S
nominal_margin
```

### 联合有效性

```python
result = evaluate_robust_validity(
    T_BF_command=candidate.T_BF_command,
    state_samples=sigma_points,
    dataset=current_dataset,
    trusted_edges=local_edges,
    safe_domain=configured_trapezoid,
)
```

### 信息评分

```python
gain = evaluate_information_gain(
    dataset=current_dataset,
    fixed_virtual_measurement=candidate.virtual_measurement,
    current_state=current_state,
)
```

---

# 19. 仿真验证方案

## 19.1 几何单元测试

必须分别验证：

1. $\eta\to{}^PT_S$ 后：
   $$
   A^S,B^S
   $$
   与解析公式一致；
2. 名义法兰指令重新正向恢复时：
   $$
   a_c=a,\qquad b_c=b;
   $$
3. 状态样本改变而法兰固定时，端点按预期漂移；
4. $C^\star$ 的解析解与直接12维优化结果一致；
5. 数值Jacobian与自动微分或中心差分一致。

## 19.2 可观测性实验

设计：

- 两个有效朝向；
- 三个非平行旋转轴；
- 多个重复朝向；
- 不同端点位置范围。

比较：

- 约化Jacobian秩；
- 奇异值谱；
- 条件数；
- 手眼平移漂移；
- 角点漂移。

## 19.3 初值收敛域

扫描：

$$
\delta_R\in\{1^\circ,3^\circ,5^\circ,8^\circ\},
$$

$$
\delta_t\in\{2,5,10,20,30\}\text{ mm}.
$$

统计收敛成功率和最终误差，而不是只报告单一初值。

## 19.4 联合不确定性校准

对每个候选：

1. 使用预测 $\Sigma_{x9}$ 得到 $P_{\mathrm{valid}}$；
2. 从真实误差分布或Monte Carlo真值重复执行；
3. 比较预测概率与经验成功率；
4. 绘制可靠性图。

## 19.5 信息增益有效性

验证预测增益是否与实际后验改善一致：

$$
\Delta I_X^{\mathrm{predicted}}
\quad\text{对比}\quad
\Delta \operatorname{Cov}_X^{\mathrm{actual}}.
$$

计算二者的秩相关系数，并比较NBV与随机候选的收敛速度。

---

# 20. 实物实验方案

## 20.1 重复标定

在相同安装条件下独立执行多次完整自动标定，报告：

- 手眼旋转重复性；
- 手眼平移重复性；
- 自动成功率；
- 平均采集帧数；
- 平均运行时间；
- 回退次数。

## 20.2 外部验证

可选方案：

1. 机器人触碰已知平面或角点；
2. 使用精密标定块；
3. 使用独立测量臂或激光跟踪仪；
4. 在未参与标定的机器人位姿中检查平面和双边一致性；
5. 比较标定后多位姿重建点在基坐标系中的重合误差。

## 20.3 鲁棒性因素

分别改变：

- 初始手眼误差；
- 平板放置姿态；
- 角点在工作空间中的位置；
- 材质和反射；
- 安全梯形大小；
- 机器人起始姿态；
- 轮廓噪声和断点检测误差。

---

# 21. 消融实验

至少包含：

## 21.1 单边约束与双边约束

比较：

- 6-DoF手眼误差；
- 最小特征值；
- 达到目标精度所需帧数；
- 失败率。

## 21.2 无标定种子伺服

比较：

- 手动种子；
- 固定预设位姿；
- 粗手眼虚拟锚点；
- 无标定轮廓反馈伺服。

## 21.3 随机位姿与信息增益NBV

在相同安全候选集合中比较：

- 随机选择；
- 最大姿态距离；
- D-opt；
- D-opt + E-opt弱方向保护。

## 21.4 无不确定性筛选与联合筛选

比较：

- 仅名义安全域；
- 仅手眼不确定性；
- 手眼与平板联合不确定性；
- 联合不确定性 + 变量投影角点。

## 21.5 固定帧数与自适应停止

比较最终误差、帧数和运行时间。

---

# 22. 评价指标

## 22.1 手眼误差

若仿真有真值：

$$
e_R
=
\cos^{-1}
\left(
\frac{\operatorname{tr}
(R_{\mathrm{est}}^TR_{\mathrm{gt}})-1}{2}
\right),
$$

$$
e_t
=
\|t_{\mathrm{est}}-t_{\mathrm{gt}}\|.
$$

## 22.2 平板与角点误差

$$
e_n
=
\cos^{-1}
(n_{\mathrm{est}}^Tn_{\mathrm{gt}}),
$$

$$
e_C=\|C_{\mathrm{est}}-C_{\mathrm{gt}}\|.
$$

## 22.3 双边执行指标

- 双边成功率；
- 边身份错误率；
- 端点安全域最小余量；
- 回退率；
- 无候选率。

## 22.4 信息效率

- 达到目标协方差所需帧数；
- 达到目标外部误差所需帧数；
- 单帧平均信息增益；
- 预测增益与实际后验改善的相关性。

## 22.5 计算性能

- 候选生成时间；
- 联合不确定性评价时间；
- 单候选信息评分时间；
- 每轮总规划时间；
- 机器人执行时间。

---

# 23. 参数配置与调试顺序

## 23.1 必须人工配置

- 粗手眼初值；
- 第一个双边稳定位姿；
- $a_{\min},a_{\max},b_{\min},b_{\max}$；
- 外层有效梯形；
- 内层安全梯形；
- 机器人碰撞模型和关节限位。

## 23.2 需要实验整定

- 种子旋转角和步长；
- 平移试探量；
- 伺服增益、阻尼和限幅；
- 稳定帧数；
- 协方差放大系数；
- $p_{\min}$；
- D-opt正则化；
- 运动代价权重；
- 停止阈值。

## 23.3 推荐调试顺序

```text
1. 验证坐标系和点变换
2. 验证双边端点检测与身份连续性
3. 验证种子小步旋转和回退
4. 验证一维无标定平移伺服
5. 验证12-DOF-V2离线求解
6. 验证局部角点模型和人工梯形
7. 验证eta候选构造
8. 验证固定法兰下的状态样本正向求交
9. 校准P_valid
10. 验证信息增益
11. 接入机器人连续执行和自适应停止
```

---

# 24. 论文叙事与方法贡献

整篇论文应围绕一个统一问题展开：

> 如何仅使用简单平板角点，在始终保持同一双边可见的前提下，以较少机器人位姿完成线激光手眼自动标定。

可组织为三项贡献。

## 贡献1：双边角点高信息密度联合标定

利用：

$$
\text{平面点}+u\text{边断点}+v\text{边断点}
$$

构建12-DOF-V2联合模型，并通过变量投影降低非线性维度。

## 贡献2：不依赖手眼的自动种子采集

通过：

$$
\text{预设局部轴旋转}
+
\text{实际双边轮廓平移伺服}
$$

解决“可靠手眼尚不存在时如何自动获得可观测种子”的启动问题。

## 贡献3：双边可见约束下的信息驱动主动采集

使用：

$$
\eta=(a,b,\alpha,\psi,h,s)
$$

直接构造名义双边候选；通过联合不确定性保证执行可靠性；再依据手眼有效信息增益选择NBV并自适应停止。

需要避免将以下工程实现单独拔高为理论贡献：

- 人工梯形本身；
- 普通IK和碰撞检查；
- 状态机和回退机制；
- 数值优化器选择。

这些内容是保证系统闭环可运行的必要组成。

---

# 25. 局限性与适用范围

当前方法的局限性包括：

1. 需要操作者提供第一个双边可见位姿；
2. 依赖两个端点稳定检测和身份连续跟踪；
3. 假设局部平板和两条边具有足够几何精度；
4. 人工安全梯形不能自动反映所有材质、曝光和遮挡变化；
5. 当前 $P_{\mathrm{valid}}$ 主要覆盖标定状态不确定性，不等于全部执行失败概率；
6. 名义信息增益是局部线性化结果；
7. 候选参数空间较大，需要合理采样和级联筛选；
8. 机器人运动学误差若不可忽略，应扩展状态模型；
9. 对严重反光、边缘圆角和轮廓缺失，需要额外的检测质量模型。

因此，本方法的理论结论应限定为：

$$
\boxed{
\text{在刚性局部平板、固定双边身份、可靠轮廓坐标和局部高斯近似下，
实现受双边可见约束的主动手眼标定}
}
$$

---

# 26. 实现前最终检查表

## 坐标与模型

- [ ] ${}^AT_B$ 的方向在所有模块中一致；
- [ ] $S$ 系与Gocator输出坐标一致；
- [ ] $e_1/e_2$ 固定对应 $u/v$ 边；
- [ ] $R_{pl}$ 每轮正交化并保持右手性；
- [ ] 粗手眼和种子初始化结果使用不同变量名。

## 种子阶段

- [ ] 不调用手眼预测运动；
- [ ] 每次只旋转小步；
- [ ] 固定旋转后再平移；
- [ ] 试探运动可安全返回；
- [ ] 双边丢失立即回退；
- [ ] $G_R$ 仅作启发式，最终检查完整Jacobian。

## 主动阶段

- [ ] 候选直接生成虚拟轮廓；
- [ ] 安全域与轮廓点处于同一传感器坐标系；
- [ ] 状态样本中法兰指令固定；
- [ ] 每个状态样本重新变量投影角点；
- [ ] IK和法兰路径检查不在每个状态样本中重复；
- [ ] 信息Jacobian中虚拟测量固定；
- [ ] 信息矩阵使用统一无量纲尺度。

## 更新与停止

- [ ] 新帧采用事务性试更新；
- [ ] 参数跳变和离散轴翻转被检测；
- [ ] $N_k\ge N_{\max}$ 时停止；
- [ ] 最终结果使用独立数据或外部装置验证；
- [ ] 区分信息收敛停止和安全上限停止。

---

# 27. 最终总结

本方法的完整闭环为：

$$
\begin{aligned}
&\text{人工给出第一个双边稳定位姿}\\
&\rightarrow
\text{无手眼小步旋转—平移伺服采集种子}\\
&\rightarrow
\text{12-DOF-V2联合初始化}\\
&\rightarrow
\text{固定角点与双边可信区间}\\
&\rightarrow
\text{参数化名义候选与虚拟测量}\\
&\rightarrow
\text{固定法兰指令下的联合不确定性筛选}\\
&\rightarrow
\text{固定虚拟观测下的信息增益NBV}\\
&\rightarrow
\text{执行、真实验证、事务性更新}\\
&\rightarrow
\text{自适应停止和最终外部验证}.
\end{aligned}
$$

当前版本在数学结构上已经闭合：

- 种子阶段不存在手眼依赖循环；
- 主动候选和12-DOF-V2观测结构一致；
- 名义候选生成与不确定性正向传播职责清晰；
- 变量投影在估计、协方差和信息评分中保持一致；
- 可观测性、安全性和信息性分别由不同指标评价。

剩余工作主要是通过仿真和实物实验确定参数、校准不确定性，并验证预测信息增益能否稳定转化为实际标定精度和采集效率。