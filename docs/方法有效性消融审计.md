# 主动手眼标定方法有效性消融审计

生成时间：2026-07-30 12:01:02 UTC

## 结论

当前方法确实存在机制叠加过多的问题。本报告只把具有消融证据的机制列为`KEEP`；无法由现有数据证明的机制统一列为`UNPROVEN`，不能继续作为既定事实。

| 机制 | 判定 | 证据 |
|---|---:|---|
| raw-profile gap segmentation | KEEP | controlled disconnected-clutter ablation |
| half-sample endpoint extension | KEEP | noise-free discretization-bias ablation |
| four-pass robust TLS | SIMPLIFY | current one-pass versus four-pass outlier ablation |
| multi-frame seed averaging | KEEP | single-frame versus 15-frame saved-data replay |
| exactly 18 seed frames | UNPROVEN | only 1/3/6/9/12/15-frame replay is available; more independent batches are needed |
| edge-alignment residual | KEEP | residual-term removal replay |
| endpoint-plane residual | KEEP | residual-term removal replay |
| seed bootstrap admission | KEEP | historical truth-separated replay; threshold still needs tuning |
| held-out plateau and historical-best rollback | REMOVE | correlation of truth-independent score with simulation truth |
| covariance-only precision stopping | REMOVE | nominal 95% bound coverage over historical iterations |
| minimal relative-information-gain stopping | KEEP | 5%/three-cycle Gazebo regression stopped at 8 NBVs in about 89 seconds |
| active NBV stage | KEEP | paired initial/final error over historical runs |
| six individual initial-envelope thresholds | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| dynamic initial-pose preflight | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| translation recenter servo | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| adaptive signed/reordered seed fallback branches | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| partial seed acceptance | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| bootstrap covariance inflation | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| candidate grid density and eight-candidate scoring budget | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| unscented candidate-validity probability | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| motion-cost utility | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| minimum committed-pose separation | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| rolling update magnitude guards | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |
| transactional rollback | UNPROVEN | requires controlled Gazebo A/B runs with identical initial poses |

## 复杂度基线

- 配置叶参数：131 个
- 其中种子阶段：38 个，NBV阶段：28 个，断点检测：16 个
- 算法核心：37 个 Python 文件，3881 行
- ROS 节点：12 个 Python 文件，5545 行
- `seed_collection_node.py`：1705 行
- `active_calibration_sim_node.py`：1489 行

## 关键数值

- 断点 `current`：接受率 100.0%，P95误差 0.1845812684143685 mm
- 断点 `no_half_pitch_extension`：接受率 100.0%，P95误差 0.21794185826539336 mm
- 断点 `four_robust_fit_passes`：接受率 100.0%，P95误差 0.1845812684143685 mm
- 断点 `no_gap_segmentation`：接受率 0.0%，P95误差 None mm

### 种子批量

- 1帧：收敛率 100.0%，旋转中位数 1.8784006939117521°，平移中位数 19.965958194025497 mm
- 3帧：收敛率 100.0%，旋转中位数 0.8727928359732345°，平移中位数 6.210539390450629 mm
- 6帧：收敛率 100.0%，旋转中位数 0.6360998096047135°，平移中位数 3.886051925851222 mm
- 9帧：收敛率 100.0%，旋转中位数 0.5154438127102616°，平移中位数 5.571691663015131 mm
- 12帧：收敛率 100.0%，旋转中位数 0.622742221184759°，平移中位数 4.540159588027099 mm
- 15帧：收敛率 100.0%，旋转中位数 0.365307018217337°，平移中位数 3.9273081361223077 mm

### 历史运行

- NBV改善旋转的运行比例：100.0%
- NBV改善平移的运行比例：100.0%
- 留出分数/旋转真值误差相关系数中位数：-0.5394290728644971
- 留出分数/平移真值误差相关系数中位数：-0.12829655733102427
- 协方差名义95%旋转覆盖率：68.0%
- 协方差名义95%平移覆盖率：54.9%
- 历史回滚比最后一次解旋转更差：2/7 次
- 历史回滚比最后一次解平移更差：2/7 次

### 三类V2残差

- `current_1_0.25_0.25`：收敛率 100.0%，旋转中位数 0.2929212138312224°，平移中位数 2.795867950440965 mm
- `equal_1_1_1`：收敛率 100.0%，旋转中位数 0.358005783782254°，平移中位数 4.484915087704161 mm
- `no_profile_plane`：收敛率 100.0%，旋转中位数 0.5095812910085576°，平移中位数 3.969213141387556 mm
- `no_edge_alignment`：收敛率 0.0%，旋转中位数 None°，平移中位数 None mm
- `no_endpoint_plane`：收敛率 100.0%，旋转中位数 0.4427165499018536°，平移中位数 3.9078525430501108 mm

## 下一阶段的最小方法候选

1. 原始轮廓鲁棒断点检测与固定物理边身份；
2. 六个具有两轴旋转激励的物理种子，每姿态采用消融得到的最小帧数；
3. 12-DOF-V2联合初解与一个稳定性门控；
4. 解析几何、ROI、IK和MoveIt硬可行性筛选；
5. 单一信息增益目标加运动代价；
6. 采用最小NBV数、信息增益饱和和总位姿保护停止；
7. 在留出分数被重新证明前，取消其停止和历史回滚决策权。

## 解释边界

- `KEEP`表示在本次数据域内具有可重复证据，不表示已经完成实机证明。
- `REMOVE`表示当前实现的决策权没有得到证据支持；相关日志指标仍可保留。
- `UNPROVEN`必须通过相同初始位姿、相同噪声随机种子的Gazebo A/B测试。
- 历史运行跨越了若干代码和噪声配置版本，因此历史百分比只能用于发现问题，不能替代冻结版本后的正式统计。
- 完整原始数值见同名JSON文件。
