# 基于双边角点约束与信息增益的线激光自动手眼标定方法
## 完整原理与系统流程（当前统一方案 v4）

---

# 1. 方法目标

本方法面向安装在线激光传感器的眼在手机器人系统，目标是自动估计线激光传感器相对于机器人法兰的手眼关系：

$$
{}^{F}T_{S}
=
\begin{bmatrix}
R_{he} & t_{he}\\
0 & 1
\end{bmatrix}.
$$

其中：

- $B$：机器人基坐标系；
- $F$：机器人法兰坐标系；
- $S$：线激光传感器输出轮廓所使用的坐标系；
- $R_{he},t_{he}$：待估计手眼旋转和平移。

系统同时联合估计标定平板局部角点几何：

$$
R_{pl}=[u,v,n],\qquad C,
$$

其中：

- $C$：被持续观测的同一物理角点；
- $u,v$：从 $C$ 出发的两条固定相邻边方向；
- $n=u\times v$：平板法向；
- $L,W$：平板物理尺寸，用于给出边段的物理上限，但主动规划不需要恢复并使用整个矩形。

方法始终要求正式采集帧包含：

$$
\boxed{
\text{平板轮廓点}
+
E_u\text{上的固定身份断点 }e_1
+
E_v\text{上的固定身份断点 }e_2
}
$$

主动采集的目标不是在任意机器人位姿中寻找最大信息，而是：

$$
\boxed{
\text{先构造能够可靠观测固定双边的候选}
\rightarrow
\text{再选择手眼信息增益最大的候选}
}
$$

---

# 2. 方法总体结构

方法由两个自动化阶段组成。

## 2.1 阶段A：自动种子位姿采集

## 2.2 阶段B：信息驱动的主动采集

---

# 3. 系统输入、观测与坐标关系

## 3.1 已知输入

系统已知：

1. 每个采集时刻的法兰绝对位姿：

$$
{}^{B}T_{F,i}
=
\begin{bmatrix}
R_i&t_i\\
0&1
\end{bmatrix};
$$

2. 每个位姿下的线激光轮廓点：

$$
\mathcal P_i^S=\{p_{ij}^S\}_{j=1}^{N_i};
$$

3. 两个固定身份物理断点：

$$
p_{e1,i}^S,\qquad p_{e2,i}^S;
$$

4. 平板尺寸或局部可信边段上界：

$$
L,\qquad W;
$$

5. 粗手眼初值：

$$
{}^{F}\hat T_{S,0}.
$$

## 3.2 点从传感器系到基坐标系

任意传感器点满足：

$$
p_{ij}^B
=
R_i\left(R_{he}p_{ij}^S+t_{he}\right)+t_i.
$$

记：

$$
R_{BS,i}=R_iR_{he},
\qquad
t_{BS,i}=t_i+R_it_{he},
$$

则：

$$
p_{ij}^B=R_{BS,i}p_{ij}^S+t_{BS,i}.
$$

所有平面约束、边约束和角点约束均基于该绝对坐标关系。

---

# 4. Phase 0a：手眼名义值获取（粗初值）

Phase 0a 的任务不是标定，而是为后续优化提供一个**有合理误差的名义初值**，模拟实际工作中工人用卷尺和直角尺（或看 CAD 图）对传感器安装的粗略测量。

## 4.1 名义值的生成方式

在仿真环境中，系统直接读取 URDF 中定义的手眼真值 $T_{FS}^*$，然后施加一组可控的随机扰动：

$$
{}^F\hat T_{S,0}
=
\text{perturb}(T_{FS}^*,\ \delta_R,\ \delta_t)
$$

其中：

- $T_{FS}^*$ 是从 URDF 文件 `urdf/calib_robot.urdf` 中提取的手眼变换真值；
- $\delta_R$ 是旋转扰动（默认：绕随机轴旋转 $3^\circ$）；
- $\delta_t$ 是平移扰动（默认：沿随机方向偏移 $10\ \text{mm}$）。

扰动幅度通过配置文件
`ros2_ws/src/handeye_sim_bridge/config/calibration.yaml` 控制：

```yaml
handeye_init_rotation_error_deg: 3.0       # 旋转扰动 (°)
handeye_init_translation_error_mm: 10.0    # 平移扰动 (mm)
```

## 4.2 名义值的使用

该名义值 ${}^F\hat T_{S,0}$ 仅作为：

- 12-DOF-V2 非线性优化的起始点；
- Phase 0b 种子运动中的几何预测模型；
- 候选生成中的名义轮廓预测。

## 4.3 设计意图

### 为什么要扰动？

如果直接使用 URDF 真值作为初值，12-DOF-V2 在一帧数据都不需要的情况下就能得到"完美"结果，这掩盖了算法的真实收敛能力和鲁棒性。加扰动的目的是：

1. **模拟实际工况**：真实机器人上没有 TF 可以直接读取完美手眼，工人只能用尺量——这个 $\delta$ 模拟的就是那"一尺之差"。
2. **测试优化器的鲁棒性**：一个好的初值应该使 12-DOF-V2 从 $3^\circ/10\text{mm}$ 的偏差收敛到亚毫米/百分度的精度。
3. **使评价更有意义**：只有从非完美的初值出发，标定误差才是真实的收敛误差。

### 为什么不直接用 TF 初值？

在真实机器人上，本系统的推荐作业流程是操作员通过手动示教几个粗略位姿（Phase 0b 种子采集）后，直接从种子数据启动 12-DOF-V2。仿真中的 Phase 0a 只是替代了这个"人工输入初值"的环节，用 URDF 真值加扰动来产生结果相当的名义误差。

> 在实物部署时，Phase 0a 将被替换为操作员手动输入的手眼估计值（例如通过传感器外壳上的安装尺寸计算）。

---

# 5. Phase 0b：基于预设多轴旋转和无标定双边特征伺服的自动种子采集

## 5.1 为什么需要种子阶段

在没有稳定手眼和平板估计时，系统还无法可靠预测任意候选位姿下的未来轮廓，因此不能立即运行完整 NBV。

种子阶段的任务不是追求最优信息增益，而是：

1. 始终保持两个断点可见；
2. 生成多个不同旋转方向；
3. 避免出现"姿态 A、姿态 A、姿态 B、姿态 B"这类旋转退化；
4. 为 12-DOF-V2 提供稳定启动数据。

## 5.2 核心设计原则

Phase 0b **不应该依赖手眼初值，也不需要虚拟锚点**。

如果运动预测依赖粗手眼，就会出现逻辑上的循环：

> 需要种子数据才能得到可靠手眼，但又需要可靠手眼才能自动采集种子数据。

因此，Phase 0b 的核心设计是：

$$
\boxed{
\text{预设法兰旋转激励}
+
\text{无标定的轮廓特征平移伺服}
}
$$

它与人工示教的过程完全一致：

$$
\text{小步旋转}
\rightarrow
\text{观察两个断点}
\rightarrow
\text{平移修正}
\rightarrow
\text{再小步旋转}
\rightarrow
\text{再平移修正}.
$$

三个关键优势：

1. **不依赖待标定的手眼关系** — 全程不使用 ${}^F T_S$；
2. **完全复现已验证有效的人工操作经验**；
3. **只需要学习局部输入—输出关系，不需要恢复完整三维几何**。

## 5.3 初始人工输入

第一版系统要求操作者只完成一次操作：

> 将机械臂移动到一个能够稳定看到同一平板角点两条相邻边的起始位姿。

系统保存：

- 当前法兰位姿 ${}^B T_F$；
- 完整线激光轮廓点；
- $e_1,e_2$ 两个断点；
- 当前双边反馈状态（见 §5.4）；
- 当前有效位姿作为回退状态。

## 5.4 "当前双边视觉特征"的定义

原文中"当前双边视觉特征"说得过于抽象——它并不是一个额外测量，本质上只是从两个断点中计算出的控制量。

设两个断点在传感器坐标系中为：

$$
p_{e1}^S=[x_1,0,z_1]^T,
\qquad
p_{e2}^S=[x_2,0,z_2]^T.
$$

最核心的视觉特征是两个断点的中点：

$$
x_m=\frac{x_1+x_2}{2},
\qquad
z_m=\frac{z_1+z_2}{2}.
$$

还可以计算轮廓长度：

$$
L_p=\|p_{e1}^S-p_{e2}^S\|.
$$

对目前的控制方法来说，真正用于闭环控制的只有：

$$
\boxed{x_m}
$$

其目标值为：

$$
x_m^\star=0.
$$

其他量主要用于安全判断：

- $\text{valid}_{e1}$：第一个断点是否存在；
- $\text{valid}_{e2}$：第二个断点是否存在；
- $x_1,x_2,z_1,z_2$：两个断点是否还在量程内；
- $L_p$：轮廓是否过短或异常；
- 端点到 ROI 边界的距离：是否即将丢失。

因此，文档中的"当前双边视觉特征"应明确为：

```
当前双边反馈状态：
  x_mid        两个断点的中点 X 坐标，作为平移伺服误差
  endpoint_1/2 两个断点坐标
  valid_e1/e2  双边可见状态
  roi_margin   两个端点距离传感器量程边界的最小余量
  profile_length 轮廓长度，作为质量检查
```

## 5.5 种子旋转计划（外层运动计划）

设起始法兰旋转为 $R_0$，采用**星形路径**——每次都从同一个已知参考姿态出发，而不是连续累积复杂旋转。

默认生成约 6 个目标朝向：

$$
\begin{aligned}
R_1 &= R_0,\\
R_2 &= R_0 R_x(+\theta),\\
R_3 &= R_0 R_x(-\theta),\\
R_4 &= R_0 R_y(+\theta),\\
R_5 &= R_0 R_y(-\theta),\\
R_6 &= R_0 R_x(+\theta) R_y(+\theta).
\end{aligned}
$$

其中：

- $R_x$：绕当前法兰局部 $X$ 轴旋转；
- $R_y$：绕当前法兰局部 $Y$ 轴旋转；
- 初始角度建议从 $\theta=10^\circ\sim15^\circ$ 开始；
- 每一步旋转步长 $\Delta\theta_{\rm step}\approx 2^\circ\sim3^\circ$；
- $R_6$ 通过两个单轴阶段实现：先绕 $X$ 蠕动到 $+\theta$，双边稳定后再绕新的局部 $Y$ 轴蠕动到 $+\theta$。

采用星形路径的优点是：

- 每次都从同一个已知参考姿态 $R_0$ 出发；
- 旋转目标易于定义；
- 不会因连续累计旋转造成越来越复杂的姿态；
- 失败时可以退回已知安全的 $R_0$；
- 采集的朝向分布容易检查。

返回 $R_0$ 的中间过程不作为新种子保存。

## 5.6 无标定平移伺服（内层执行控制）

### 5.6.1 为什么不使用虚拟锚点

原方案中使用虚拟锚点预测旋转后的法兰平移：

$$
P_a^B = {}^B T_F \; {}^F \hat T_S \; p_a^S,
\qquad
t_{BS}' = P_a^B - R_{BS}' p_a^S.
$$

这个计算显式依赖 ${}^F \hat T_S$。在初始手眼平移和旋转均不准确时，预测出的基坐标锚点可能存在明显误差。特别是法兰旋转时，由于传感器相对法兰存在未知杆臂，传感器不仅发生旋转，还会发生无法准确预测的平移。

人工操作没有试图计算这种平移，而是：

1. 先转一点；
2. 直接观察传感器中的断点变化；
3. 再根据实际变化移动法兰。

这比基于错误模型预测更可靠。

### 5.6.2 一维平移伺服（第一版）

对于当前种子过渡，选定一个法兰局部旋转轴 $a_r\in\{e_x,e_y\}$。

每次只旋转一个小角度（右乘，表示绕当前法兰局部坐标轴旋转）：

$$
R_{BF}^+ = R_{BF} \exp([a_r]_\times \Delta\theta).
$$

完成这一小步旋转后：

1. 读取线激光轮廓；
2. 检测两个断点；
3. 计算 $x_m = (x_1+x_2)/2$；
4. **固定法兰旋转**；
5. 只调整法兰平移，使 $x_m \to 0$。

控制完成后，再执行下一次小步旋转。

**关键问题：不使用手眼，怎么知道法兰应该往哪边平移？**

从法兰局部三个平移方向中选择一个方向 $d_F\in\{e_x,e_y,e_z\}$。令沿该方向的平移量为 $q$，局部关系为：

$$
\Delta x_m \approx g \cdot \Delta q,
$$

其中 $g = \partial x_m/\partial q$ 是未知的局部灵敏度。

通过一次很小的试探平移 $\Delta q_{\rm probe}$，测量 $\Delta x_m = x_m^{\rm after} - x_m^{\rm before}$，得到：

$$
\hat g = \frac{\Delta x_m}{\Delta q_{\rm probe}}.
$$

之后计算修正：

$$
\Delta q = -\lambda \frac{x_m}{\hat g},
$$

并进行限幅 $|\Delta q| \le q_{\max}$。

如果 $x_m>0$ 且 $\hat g>0$，控制器就沿负方向移动；如果实际效果相反，新的测量会自动修正 $\hat g$ 的符号。

### 5.6.3 自动选择平移轴

在初始有效状态下，分别沿法兰局部 $X,Y,Z$ 方向做极小试探：

$$
\hat g_x = \frac{\Delta x_m}{\Delta q_x},
\quad
\hat g_y = \frac{\Delta x_m}{\Delta q_y},
\quad
\hat g_z = \frac{\Delta x_m}{\Delta q_z}.
$$

选择灵敏度最高的轴：

$$
j^\star = \arg\max_j |\hat g_j|.
$$

随后在当前一段蠕动过程中，主要沿该轴做一维伺服。相比直接同时控制三个平移自由度，这种方法：

- 更容易实现；
- 更容易判断运动方向；
- 更不容易产生无关平移；
- 与人工实际操作最接近。

### 5.6.4 在线更新平移灵敏度

随着法兰朝向变化，$g = \partial x_m/\partial q$ 也会发生变化，不能始终使用最初的值。

每次实际执行平移后，都可以用实测值更新：

$$
g_{\rm measured} = \frac{x_{m,k+1} - x_{m,k}}{q_{k+1} - q_k},
\qquad
\hat g_{k+1} = (1-\eta)\hat g_k + \eta\, g_{\rm measured},
$$

其中 $0<\eta\le 1$。

也可以使用 Broyden 形式更新三维 Jacobian：

$$
J_{x,k+1} = J_{x,k} + \frac{(\Delta x_m - J_{x,k}\Delta t_F)\Delta t_F^T}{\Delta t_F^T \Delta t_F}.
$$

这种在线更新直接利用"刚才移动了多少、断点中点变化了多少"，不需要知道传感器相对于法兰的位置。

### 5.6.5 三维平移伺服（后续扩展）

如果后续发现单一轴无法在所有姿态下稳定控制，可以估计完整 Jacobian：

$$
J_x = \begin{bmatrix}
\frac{\partial x_m}{\partial t_x^F} &
\frac{\partial x_m}{\partial t_y^F} &
\frac{\partial x_m}{\partial t_z^F}
\end{bmatrix}.
$$

局部关系为 $\Delta x_m \approx J_x \Delta t_F$，取最小范数修正：

$$
\Delta t_F = -\lambda \frac{J_x^T}{J_x J_x^T + \mu} x_m.
$$

法兰局部平移转换为基坐标平移：$\Delta t_B = R_{BF} \Delta t_F$，因此：

$$
t_{BF}^+ = t_{BF} + R_{BF} \Delta t_F.
$$

整个过程中仍然不需要 ${}^F T_S$。不过第一版建议先实现一维版本（§5.6.2），因为控制目标只有一个标量 $x_m$，直接开放三个平移自由度会存在多解，容易产生不必要的漂移。

### 5.6.6 仅让 $x_m\to 0$ 是否足够

严格来说，$x_m=0$ 不能数学上保证两个断点始终存在——例如两个断点可能同时向传感器 $z$ 量程外移动，但中点的 $x$ 仍然为零。

不过人工经验表明，在当前机械结构和小步旋转范围内，控制 $x_m$ 是维持双边可见的主要因素。因此第一版采用：

- **主控制目标**：$x_m \to 0$。
- **硬安全条件**：两个断点必须同时满足：

$$
x_{\min} + m_x < x_j < x_{\max} - m_x,
\qquad
z_{\min} + m_z < z_j < z_{\max} - m_z,
\quad j=1,2.
$$

同时要求 $L_{\min} < L_p < L_{\max}$。

定义最小 ROI 余量：

$$
m_{\rm ROI} = \min_{j=1,2}
\left\{
x_j - x_{\min},\;
x_{\max} - x_j,\;
z_j - z_{\min},\;
z_{\max} - z_j
\right\}.
$$

只有 $m_{\rm ROI} > m_{\rm safe}$ 才允许继续执行下一小步旋转。控制结构为：

$$
\boxed{
x_m=0 \text{ 作为伺服目标}
+
\text{双边存在和 ROI 余量作为硬约束}
}
$$

如果实际测试中出现明显的 $z$ 方向漂移，再把 $z_m - z_m^{\rm ref}$ 加入第二个控制特征，形成二维特征伺服：

$$
s = \begin{bmatrix} x_m \\ z_m - z_m^{\rm ref} \end{bmatrix}.
$$

第一版没有必要立即增加复杂度。

## 5.7 端点身份必须连续跟踪

小步运动还有一个重要优势：可以通过时间连续性保持 $e_1,e_2$ 的身份。

假设上一帧端点为 $p_{e1,k}, p_{e2,k}$，当前检测到两个无标签端点 $q_1,q_2$。

比较两种匹配代价：

$$
D_1 = \|q_1 - p_{e1,k}\|^2 + \|q_2 - p_{e2,k}\|^2,
\qquad
D_2 = \|q_2 - p_{e1,k}\|^2 + \|q_1 - p_{e2,k}\|^2.
$$

取代价较小的匹配。如果 $|D_1 - D_2|$ 过小，说明身份判断不可靠，此帧不保存。

这一步非常重要，因为 12-DOF-V2 要求：
- $e_1$ 始终对应 $u$ 边；
- $e_2$ 始终对应 $v$ 边。

标签一旦交换，会直接污染角点约束。

## 5.8 蠕动停止条件

不再用肉眼观察法兰坐标轴角度，而是直接使用机器人返回的法兰旋转矩阵。

设本次种子过渡开始时的旋转为 $R_{\rm start}$，当前旋转为 $R_{\rm current}$。相对旋转：

$$
\Delta R = R_{\rm start}^T R_{\rm current},
\qquad
\theta_{\rm rel} = \cos^{-1}\left(\frac{\operatorname{tr}(\Delta R)-1}{2}\right).
$$

当满足：

- $\theta_{\rm rel} \ge \theta_{\rm target}$；
- $|x_m| < \varepsilon_x$；
- $m_{\rm ROI} > m_{\rm safe}$；
- 连续 $N$ 帧双边稳定；

时，停止本次蠕动并保存种子位姿。

也可以直接累计旋转角度（因为每次旋转指令本身就是已知的）：

$$
\theta_{\rm acc} = \sum_k |\Delta\theta_k|.
$$

机器人法兰旋转由编码器直接给出，这里不需要手眼关系。

## 5.9 自动种子状态机

```
MANUAL_INITIAL_POSE
  人工找到第一个双边可见位姿
  ↓
SAVE_REFERENCE
  保存 T_BF、两个断点、x_mid 和 ROI 余量
  ↓
SELECT_ROTATION_TARGET
  选择局部 X/Y 轴和目标角度
  ↓
MICRO_ROTATE
  绕指定法兰轴旋转 Δθ
  ↓
READ_PROFILE
  检测两个断点并保持 e1/e2 身份
  │
  ├─ 两个断点均存在
  │   ↓
  │   TRANSLATION_SERVO
  │     固定旋转
  │     调整法兰局部平移，使 x_mid → 0
  │
  ├─ 只剩一个断点
  │   ↓
  │   回退上一有效位姿，旋转步长减半
  │
  └─ 两个断点均丢失
      ↓
      立即回退上一有效位姿，放弃盲目搜索
  ↓
CHECK_FEATURE_SAFETY
  双边存在、ROI 余量足够、轮廓有效
  │
  ├─ 目标角度未达到 → MICRO_ROTATE
  └─ 目标角度已达到
      ↓
      STABILITY_CHECK
      ↓
      SAVE_SEED
      ↓
      RETURN_REFERENCE
      ↓
      SELECT_NEXT_AXIS
```

## 5.10 失败回退

系统始终保存：

```
last_valid_pose
last_valid_profile
last_valid_endpoints
last_valid_x_mid
last_valid_roi_margin
```

如果只剩一个断点或两个断点全部丢失：

1. 回退到上一个有效位姿；
2. 将旋转步长减半；
3. 重新进行平移试探；
4. 再进行局部平移搜索；
5. 多次失败则放弃该目标旋转。

## 5.11 种子位姿验收

只有满足以下条件，当前状态才作为正式种子位姿保存：

1. $e_1,e_2$ 均被检测到；
2. 两个端点属于同一角点的指定两条相邻边；
3. 轮廓点数和线段长度足够；
4. 两个端点远离传感器 ROI 边界；
5. 当前旋转已达到目标朝向；
6. 连续若干帧测量稳定；
7. 新姿态提高已有姿态集合的旋转多样性。

可构造旋转多样性矩阵：

$$
G_R = \sum_{i<j} (R_i - R_j)^T (R_i - R_j).
$$

当 $\lambda_{\min}(G_R)$ 过小时，说明仍存在旋转弱方向，需要继续采集不同旋转轴的位姿。

默认目标为 6 个有效种子位姿，但"6"不是无条件充分保证。若约化 Jacobian 仍然病态，则继续补采。

## 5.12 更新后的完整 Phase 0b 流程

```
1. 人工将机器人移动到一个双边稳定可见位姿
2. 保存：
   - 当前 T_BF
   - 完整轮廓
   - e1/e2 端点
   - x_mid
   - ROI 安全余量
   - last_valid_pose
3. 设定种子旋转计划：R0, Rx+, Rx-, Ry+, Ry-, Rx+Ry+
4. 对每个目标姿态：
   a. 选择当前法兰局部旋转轴
   b. 旋转 2°~3°
   c. 读取两个端点
   d. 固定旋转
   e. 通过小幅试探确定平移修正方向
   f. 平移使 x_mid 趋近 0
   g. 检查两个端点和 ROI 安全余量
   h. 安全则继续下一次小步旋转
   i. 失败则回退并减小步长
   j. 累积角度达到目标且连续稳定后保存种子
5. 采集约 6 个旋转多样的有效种子
6. 使用 TF 粗手眼作为优化初值，运行 12-DOF-V2
7. 检查约化 Jacobian：
   - 数值秩
   - 最小奇异值
   - 条件数
   - 平移—角点弱方向
8. 若仍然病态：
   - 根据弱方向补充新的旋转轴种子
   - 而不是简单增加相同方向的位姿
9. 初始化通过后，进入基于平板几何和信息增益的正式 NBV 阶段
```

## 5.13 方法定位总结

Phase 0b **不是**：

> 粗手眼 → 预测虚拟锚点 → 计算下一法兰位姿

而是：

$$
\boxed{
\text{法兰局部轴小步旋转}
\rightarrow
\text{直接观察轮廓变化}
\rightarrow
\text{无标定局部平移伺服}
\rightarrow
\text{双边稳定}
}
$$

最核心的控制原则是：

$$
\boxed{
\begin{aligned}
&\text{旋转轴和旋转角度由种子计划给定；}\\
&\text{平移方向由实际轮廓反馈在线确定；}\\
&\text{中点 X 坐标用于闭环控制；}\\
&\text{双边存在和 ROI 余量用于安全约束；}\\
&\text{全程不使用手眼关系。}
\end{aligned}
}
$$

---

# 6. Phase 0c：12-DOF-V2联合初始化

## 6.1 状态变量

完整状态为：

$$
\Theta=
\{
R_{he},t_{he},R_{pl},C
\},
$$

共 12 个自由度：

- 手眼旋转 $R_{he}$：3 DoF；
- 手眼平移 $t_{he}$：3 DoF；
- 平板姿态 $R_{pl}$：3 DoF；
- 角点位置 $C$：3 DoF。

令：

$$
R_{pl}=[u,v,n].
$$

## 6.2 几何残差

对于第 $i$ 帧中的任意平面点 $p_{ij}^B$：

$$
r_{p,ij}
=
n^T(p_{ij}^B-C).
$$

对于位于 $u$ 边上的端点 $p_{e1,i}^B$：

$$
r_{e1,i}
=
v^T(p_{e1,i}^B-C).
$$

对于位于 $v$ 边上的端点 $p_{e2,i}^B$：

$$
r_{e2,i}
=
u^T(p_{e2,i}^B-C).
$$

两个端点还必须位于平板平面：

$$
r_{e1,n,i}
=
n^T(p_{e1,i}^B-C),
$$

$$
r_{e2,n,i}
=
n^T(p_{e2,i}^B-C).
$$

因此，每一帧同时提供：

- 密集平面约束；
- $e_1$ 边的面内约束；
- $e_2$ 边的面内约束；
- 两个端点的平面约束；
- 所有帧共享同一个角点 $C$。

## 6.3 密集点权重

同一帧大量轮廓点高度相关，不能把所有点视为完全独立信息。

设置：

$$
w_{\rm plane}
=
\frac{N_{\rm pose}}{N_{\rm plane\ points}},
$$

使所有平面点块的总权重与位姿数量处于同一量级，避免平面点数量完全压制两条边端点约束。

后续实现中更推荐每帧等数量采样，并采用按帧归一权重。

## 6.4 角点变量投影

给定：

$$
x_9=
[\omega_{he},t_{he},\omega_{pl}]
$$

后，所有残差对 $C$ 都是线性的，可写为：

$$
A(x_9)C\approx b(x_9).
$$

角点通过线性最小二乘解析求解：

$$
C^\star(x_9)
=
\arg\min_C
\|A(x_9)C-b(x_9)\|^2.
$$

非线性优化只需要求解：

$$
\min_{x_9}
\left\|
A(x_9)C^\star(x_9)-b(x_9)
\right\|^2.
$$

因此：

- 最终模型仍是完整12自由度；
- 非线性优化维度由12维降低为9维；
- 每次改变手眼或平板姿态时，角点都会重新计算；
- $C$不是初始化后永久固定的常量。

## 6.5 初始化输出

12-DOF-V2输出：

$$
\hat X_0={}^F\hat T_{S,0},
$$

$$
\hat R_{pl,0}
=
[\hat u_0,\hat v_0,\hat n_0],
$$

$$
\hat C_0.
$$

同时应计算：

- 约化9维 Jacobian；
- 奇异值；
- 数值秩；
- 条件数；
- 近似协方差；
- 最弱参数方向。

只有当 Jacobian 具有足够秩且条件数可接受时，才能进入正式 NBV 阶段。

---

# 7. 局部双边角点模型与人工安全测量域

## 7.1 为什么只建立局部角模型

12-DOF-V2规定所有正式测量始终使用：

- 同一个平板面；
- 同一个物理角点 $C$；
- 从该角点出发的固定边 $E_u,E_v$。

因此主动阶段真正需要维护的不是完整矩形，而是：

$$
\boxed{
C+E_u+E_v
}
$$

另外两条远端边既不参与残差，也不应成为自动采集目标。

## 7.2 平板局部坐标系

利用当前估计：

$$
C_k,\qquad
R_{pl,k}=[u_k,v_k,n_k],
$$

建立平板局部坐标系 $P$：

$$
{}^BT_{P,k}
=
\begin{bmatrix}
[u_k,v_k,n_k]&C_k\\
0&1
\end{bmatrix}.
$$

在 $P$ 系中：

$$
C^P=0,\qquad
u^P=e_x,\qquad
v^P=e_y,\qquad
n^P=e_z.
$$

定义两条可信边段：

$$
E_u^P
=
\left\{
[a,0,0]^T
\mid
a_{\min}\le a\le a_{\max}
\right\},
$$

$$
E_v^P
=
\left\{
[0,b,0]^T
\mid
b_{\min}\le b\le b_{\max}
\right\}.
$$

平板尺寸只提供：

$$
a_{\max}\le L,\qquad b_{\max}\le W.
$$

## 7.3 人工配置Gocator测量域

理想激光平面在传感器系中写为：

$$
y_S=0.
$$

主动规划不需要恢复Gocator内部相机、激光器和外壳之间的精确光学关系。只需要在Gocator实际输出轮廓所使用的同一 $X_S-Z_S$ 数值坐标中，人工配置经验测量域。

定义外层区域：

$$
\mathcal V_{\rm hard}^S
=
\left\{
(x,0,z)^T
\mid
z_n\le z\le z_f,\ 
x_l(z)\le x\le x_r(z)
\right\},
$$

其中：

$$
\tau(z)=\frac{z-z_n}{z_f-z_n},
$$

$$
x_l(z)
=(1-\tau)x_{l,n}+\tau x_{l,f},
$$

$$
x_r(z)
=(1-\tau)x_{r,n}+\tau x_{r,f}.
$$

再向内收缩得到：

$$
\mathcal V_{\rm safe}^S
\subset
\mathcal V_{\rm hard}^S.
$$

两者含义为：

- $\mathcal V_{\rm hard}^S$：经验上仍可能得到轮廓的范围；
- $\mathcal V_{\rm safe}^S$：候选规划必须满足的保守范围。

人工梯形不要求关于 $x_S=0$ 对称。只要真实轮廓坐标、预测轮廓坐标和人工梯形使用同一个 $S$ 系，就不需要知道坐标原点在传感器外壳中的物理位置。

## 7.4 模块输出

$$
\mathcal M_k
=
\left\{
{}^BT_{P,k},
E_u^P,
E_v^P,
\mathcal V_{\rm hard}^S,
\mathcal V_{\rm safe}^S
\right\}.
$$

---

# 8. 名义双边候选、法兰指令与虚拟测量生成

## 8.1 候选参数

定义：

$$
\eta=(a,b,\alpha,\psi,h,s),
\qquad
s\in\{-1,+1\}.
$$

其中：

- $a$：目标断点 $A$ 在 $E_u$ 上距角点的距离；
- $b$：目标断点 $B$ 在 $E_v$ 上距角点的距离；
- $\alpha$：激光平面法向与平板法向之间的夹角，即两平面的二面角；
- $\psi$：传感器坐标轴在激光平面内的旋转；
- $h$：目标轮廓中点在传感器 $z_S$ 方向的深度；
- $s$：绕目标交线倾斜的离散分支。

参数范围满足：

$$
a_{\min}\le a\le a_{\max},
\qquad
b_{\min}\le b\le b_{\max}.
$$

候选唯一性来自下述确定性构造规则，而不是一条交线天然唯一确定传感器位姿。

## 8.2 由 $a,b$ 确定目标断点和交线

在 $P$ 系中：

$$
A^P=
[a,0,0]^T,
\qquad
B^P=
[0,b,0]^T.
$$

定义：

$$
\rho=\|B^P-A^P\|=\sqrt{a^2+b^2},
$$

$$
d_\ell^P
=
\frac{B^P-A^P}{\rho}.
$$

因此：

- $a,b$ 决定两个断点在固定边上的位置；
- $a/b$ 决定交线在平板内的方向；
- $\rho$ 决定名义轮廓长度。

## 8.3 由 $\alpha,s$ 确定激光平面

令：

$$
n^P=e_z,
\qquad
q^P=n^P\times d_\ell^P.
$$

构造激光平面法向：

$$
m_L^P
=
\cos\alpha\,n^P
+
s\sin\alpha\,q^P.
$$

由于：

$$
(m_L^P)^Td_\ell^P=0,
$$

经过 $A^P,B^P$ 且法向为 $m_L^P$ 的平面包含目标交线。

应避免：

$$
\alpha\approx0,
$$

因为此时激光平面与平板接近重合，交线退化。

## 8.4 由 $\psi$ 确定传感器姿态

规定传感器激光平面为：

$$
y_S=0,
$$

令：

$$
y_S^P=m_L^P.
$$

先建立：

$$
x_{S0}^P=d_\ell^P,
\qquad
z_{S0}^P=x_{S0}^P\times y_S^P.
$$

再绕 $y_S$ 轴旋转 $\psi$：

$$
x_S^P
=
\cos\psi\,x_{S0}^P
+
\sin\psi\,z_{S0}^P,
$$

$$
z_S^P
=
-\sin\psi\,x_{S0}^P
+
\cos\psi\,z_{S0}^P.
$$

因此：

$$
R_{PS}=[x_S^P,y_S^P,z_S^P].
$$

## 8.5 由 $h$ 确定传感器原点

目标线段中点为：

$$
M^P=\frac{A^P+B^P}{2}.
$$

规定：

$$
M^S=[0,0,h]^T.
$$

由：

$$
M^P=R_{PS}M^S+t_{PS},
$$

得到：

$$
t_{PS}=M^P-hz_S^P.
$$

于是：

$$
{}^PT_S(\eta)
=
\begin{bmatrix}
R_{PS}&t_{PS}\\
0&1
\end{bmatrix}.
$$

## 8.6 名义端点与虚拟轮廓

由：

$$
p^S=R_{PS}^T(p^P-t_{PS}),
$$

直接得到：

$$
A^S(\eta)
=
\begin{bmatrix}
-\frac{\rho}{2}\cos\psi\\
0\\
h+\frac{\rho}{2}\sin\psi
\end{bmatrix},
$$

$$
B^S(\eta)
=
\begin{bmatrix}
+\frac{\rho}{2}\cos\psi\\
0\\
h-\frac{\rho}{2}\sin\psi
\end{bmatrix}.
$$

在两个端点之间采样：

$$
p_j^S(\eta)
=
(1-\lambda_j)A^S(\eta)
+
\lambda_jB^S(\eta),
\qquad
0\le\lambda_j\le1,
$$

得到：

$$
\mathcal P_c^S(\eta)
=
\{p_j^S(\eta)\}_{j=1}^{N_c}.
$$

因此模块8已经能够直接生成完整虚拟轮廓，不需要再设独立的名义未来轮廓预测模块。

## 8.7 目标传感器位姿与法兰指令

由当前平板位姿：

$$
{}^BT_{P,k},
$$

得到目标传感器基坐标位姿：

$$
{}^BT_{S,c}(\eta)
=
{}^BT_{P,k}\,{}^PT_S(\eta).
$$

利用当前手眼估计反求名义法兰指令：

$$
\boxed{
{}^BT_{F,c}(\eta)
=
{}^BT_{P,k}
\,{}^PT_S(\eta)
\left({}^F\hat T_{S,k}\right)^{-1}
}
$$

完整映射为：

$$
\eta
\rightarrow
{}^PT_S
\rightarrow
{}^BT_{S,c}
\rightarrow
{}^BT_{F,c}.
$$

## 8.8 名义可行性检查

由于 $a,b$ 的采样区间已经保证目标断点位于固定边段，名义几何检查可直接基于模块8输出完成。

### 安全梯形

要求：

$$
A^S(\eta)\in\mathcal V_{\rm safe}^S,
\qquad
B^S(\eta)\in\mathcal V_{\rm safe}^S.
$$

### 激光面退化

要求：

$$
\alpha\ge\alpha_{\min},
$$

并避免激光平面与平板近重合。

### 轮廓质量

要求：

- $\rho$ 不过短；
- 预测采样点数足够；
- 两个端点与安全梯形边界具有足够余量；
- $h$ 位于稳定工作深度范围。

### 机器人可执行性

要求：

- 法兰位姿存在IK解；
- 关节不越限；
- 远离奇异位形；
- 运动路径无碰撞；
- 运动增量不过大。

## 8.9 模块输出

名义候选写为：

$$
c(\eta)
=
\left\{
\eta,
{}^BT_{F,c}(\eta),
{}^BT_{S,c}(\eta),
\mathcal P_c^S(\eta),
A^S(\eta),
B^S(\eta),
m_{\rm safe}(\eta)
\right\}.
$$

虚拟测量为：

$$
\boxed{
z_c(\eta)
=
\left\{
{}^BT_{F,c}(\eta),
\mathcal P_c^S(\eta),
p_{e1,c}^S(\eta),
p_{e2,c}^S(\eta)
\right\}
}
$$

其中：

$$
p_{e1,c}^S=A^S,\qquad
p_{e2,c}^S=B^S.
$$

## 8.10 为什么不设置独立的名义正向预测模块

模块8构造：

$$
{}^BT_{F,c}
=
{}^BT_{P,k}
{}^PT_S
({}^F\hat T_{S,k})^{-1}.
$$

若使用同一名义手眼重新计算：

$$
{}^B\hat T_{S,c}
=
{}^BT_{F,c}
{}^F\hat T_{S,k},
$$

则：

$$
{}^B\hat T_{S,c}
=
{}^BT_{P,k}
{}^PT_S.
$$

它严格等于模块8中的目标传感器位姿。

再由该激光平面与同一 $E_u,E_v$ 求交，必然得到：

$$
a_c=a,\qquad b_c=b,
$$

以及：

$$
p_{e1,c}^S=A^S,\qquad
p_{e2,c}^S=B^S.
$$

因此，在同一名义状态下重新正向求交只是一致性单元测试，不构成新的算法模块。该正向计算仅保留在下一节的联合不确定性传播内部。

---

# 9. 联合不确定性下的鲁棒有效性判断

## 9.1 为什么名义候选还不够

模块8使用当前名义状态：

$$
\hat x_{9,k},
\qquad
\hat C_k
$$

生成固定法兰指令。真实手眼、平板方向和角点仍存在误差，因此执行后可能出现：

- 激光面不再截到某条固定边；
- 交点越出可信边段；
- 端点离开人工安全梯形；
- 端点安全余量不足；
- 求交数值退化。

需要估计：

$$
P_{\rm valid}(\eta)
=
P\left(
\text{固定法兰指令执行后仍获得指定双边安全观测}
\right).
$$

## 9.2 9维约化状态不确定性

当前约化Jacobian为：

$$
J_9
=
\frac{\partial r(x_9,D_k)}{\partial x_9}.
$$

构造：

$$
H_9=J_9^TWJ_9,
$$

$$
\Sigma_{x9}
\approx
\hat\sigma_r^2
(H_9+\lambda I)^{-1}.
$$

从协方差生成Sigma点或联合样本：

$$
x_9^{(j)}.
$$

每个样本包含：

$$
X^{(j)},
\qquad
R_{pl}^{(j)}=[u^{(j)},v^{(j)},n^{(j)}].
$$

角点不应独立随意采样，而应使用全部已有数据重新变量投影：

$$
C^{(j)}
=
C^\star(x_9^{(j)},D_k).
$$

## 9.3 固定同一个法兰指令

候选参数 $\eta$ 只在名义状态下生成一次：

$$
{}^BT_{F,c}(\eta).
$$

所有样本均执行同一机器人命令。第 $j$ 个状态下的传感器位姿为：

$$
{}^BT_{S,c}^{(j)}
=
{}^BT_{F,c}(\eta)X^{(j)}.
$$

禁止对每个样本重新反求一个新的法兰位姿，否则评价的不是同一个真实动作。

## 9.4 内部正向观测函数

定义：

$$
\mathcal F
\left(
{}^BT_F,
X,R_{pl},C
\right)
$$

为固定法兰指令和给定状态下的正向观测预测函数。

设：

$$
{}^BT_S
=
{}^BT_FX
=
\begin{bmatrix}
R_{BS}&t_{BS}\\
0&1
\end{bmatrix}.
$$

激光平面在基坐标系中的法向为：

$$
m_B=R_{BS}e_y.
$$

激光平面方程：

$$
m_B^T(p-t_{BS})=0.
$$

与固定 $u$ 边：

$$
p=C+a_cu
$$

求交得到：

$$
a_c
=
\frac{m_B^T(t_{BS}-C)}
{m_B^Tu}.
$$

与固定 $v$ 边：

$$
p=C+b_cv
$$

求交得到：

$$
b_c
=
\frac{m_B^T(t_{BS}-C)}
{m_B^Tv}.
$$

若分母接近零，则激光平面与对应边近似平行，样本无效。

物理端点为：

$$
p_{e1}^B=C+a_cu,
\qquad
p_{e2}^B=C+b_cv.
$$

变换到该样本传感器系：

$$
p_{e1}^S=({}^BT_S)^{-1}p_{e1}^B,
$$

$$
p_{e2}^S=({}^BT_S)^{-1}p_{e2}^B.
$$

该函数不是独立的名义模块，而是模块9对每个不确定性样本调用的内部函数。

## 9.5 样本有效条件

第 $j$ 个样本有效，当且仅当：

$$
a_{\min}\le a_c^{(j)}\le a_{\max},
$$

$$
b_{\min}\le b_c^{(j)}\le b_{\max},
$$

$$
p_{e1,c}^{S,(j)}
\in
\mathcal V_{\rm safe}^S,
$$

$$
p_{e2,c}^{S,(j)}
\in
\mathcal V_{\rm safe}^S,
$$

并且：

- 求交不存在近零分母；
- 轮廓长度和点数满足要求；
- 安全余量达到阈值；
- 机器人静态和路径约束通过。

## 9.6 有效概率和风险分类

有效概率近似为：

$$
P_{\rm valid}(\eta)
\approx
\sum_jw_j
\mathbf1
\left[
\operatorname{Valid}
\left(
{}^BT_{F,c}(\eta),x_9^{(j)}
\right)
\right].
$$

等权采样时为有效样本比例。

同时记录：

$$
P_{u\text{-edge}},
\quad
P_{v\text{-edge}},
\quad
P_{\rm trapezoid},
\quad
P_{\rm degenerate},
\quad
P_{\rm robot}.
$$

只保留：

$$
P_{\rm valid}(\eta)\ge p_{\min}
$$

的候选：

$$
\mathcal C_k^{\rm safe}
=
\left\{
\eta\mid
P_{\rm valid}(\eta)\ge p_{\min}
\right\}.
$$

## 9.7 早期保守、后期扩展

标定早期协方差较大，应采用：

- 较小的 $\alpha,\psi$变化；
- 较居中的 $a,b,h$；
- 更高的安全余量；
- 更严格的 $p_{\min}$。

随着估计收敛，可以逐步扩大候选范围，以获取更强旋转和平移激励。

---

# 10. 基于12-DOF-V2的信息增益与NBV选择

## 10.1 NBV的作用

NBV不负责保证双边可见。模块8和模块9已经完成：

- 名义双边候选构造；
- 联合不确定性鲁棒筛选。

NBV只解决：

$$
\boxed{
\text{在所有可靠候选中，哪个完整法兰位姿最有助于估计手眼参数}
}
$$

## 10.2 当前手眼有效信息

当前全部数据为：

$$
D_k.
$$

计算约化Jacobian：

$$
J_k
=
\frac{\partial r(x_9,D_k)}{\partial x_9},
$$

$$
H_k=J_k^TWJ_k.
$$

将：

$$
x_9=
\begin{bmatrix}
x_X\\
x_P
\end{bmatrix},
$$

其中：

- $x_X\in\mathbb R^6$：手眼参数；
- $x_P\in\mathbb R^3$：平板姿态参数。

分块：

$$
H_k=
\begin{bmatrix}
H_{XX}&H_{XP}\\
H_{PX}&H_{PP}
\end{bmatrix}.
$$

边缘化平板姿态，得到手眼有效信息：

$$
H_{X,k}^{\rm eff}
=
H_{XX}
-
H_{XP}H_{PP}^{\dagger}H_{PX}.
$$

## 10.3 候选虚拟测量必须固定

模块8已经产生：

$$
z_c(\eta)
=
\left\{
{}^BT_{F,c},
\mathcal P_c^S,
p_{e1,c}^S,
p_{e2,c}^S
\right\}.
$$

构造：

$$
D_k^c=D_k\cup\{z_c(\eta)\}.
$$

计算候选Jacobian时，虚拟测量 $z_c(\eta)$ 必须保持固定。不能在每次数值扰动 $x_9$ 时重新生成轮廓，否则状态变化和观测变化会同时发生，可能人为抵消残差变化并错误低估信息。

需要区分：

- 模块9：为了评价执行风险，轮廓随不确定性状态样本重新预测；
- 模块10：为了计算Fisher信息，候选虚拟观测固定，只扰动待估状态。

## 10.4 变量投影下重新计算完整Jacobian

由于角点变量投影依赖全部数据，不能简单写为：

$$
H_{\rm new}
=
H_{\rm old}+J_c^TJ_c.
$$

正确做法为：

$$
J_k^c(\eta)
=
\frac{\partial r(x_9,D_k^c)}{\partial x_9}.
$$

每次扰动 $x_9$ 时，使用固定增广数据集 $D_k^c$ 重新求解：

$$
C^\star(x_9,D_k^c).
$$

然后：

$$
H_k^c(\eta)
=
(J_k^c)^TWJ_k^c.
$$

再通过Schur补得到：

$$
H_{X,k}^{\rm eff}(\eta).
$$

## 10.5 信息评分

D-opt增益：

$$
\Delta I_X(\eta)
=
\frac12
\left[
\log\det
\left(
H_{X,k}^{\rm eff}(\eta)+\epsilon I
\right)
-
\log\det
\left(
H_{X,k}^{\rm eff}+\epsilon I
\right)
\right].
$$

E-opt指标：

$$
\lambda_{\min}
\left(
H_{X,k}^{\rm eff}(\eta)
\right)
$$

用于保护最弱信息方向。

在尚未满秩时，评分优先级为：

1. 提高数值秩；
2. 提高最小非零特征值；
3. 满秩后主要使用D-opt；
4. 使用E-opt避免只增强已有强方向。

## 10.6 候选参数与信息增益的关系

由于：

$$
\eta
\rightarrow
{}^BT_{F,c}(\eta),
$$

并且：

$$
\eta
\rightarrow
z_c(\eta),
$$

因此：

$$
\boxed{
\Delta I_X
=
G(a,b,\alpha,\psi,h,s)
}
$$

该关系一般没有简单闭式表达，但可以通过候选网格、随机采样或无梯度优化数值建立。

可绘制：

$$
G(a,b),
\qquad
G(\alpha,\psi),
\qquad
G(\alpha,h)
$$

等信息增益地形，用于研究候选参数对手眼可观测性的影响。

## 10.7 NBV选择

在鲁棒有效候选集合中选择：

$$
\eta^\star
=
\arg\max_{\eta\in\mathcal C_k^{\rm safe}}
S(\eta),
$$

其中综合评分可以写为：

$$
S(\eta)
=
\Delta I_X(\eta)
-\lambda_m C_{\rm motion}(\eta)
-\lambda_r C_{\rm risk}(\eta).
$$

若安全约束均作为硬约束，则可直接：

$$
\eta^\star
=
\arg\max_{\eta\in\mathcal C_k^{\rm safe}}
\Delta I_X(\eta).
$$

---

# 11. 候选筛选与执行顺序

为降低计算量，推荐：

```text
1. 采样 η=(a,b,α,ψ,h,s)
2. 模块8直接计算 A^S、B^S、虚拟轮廓和法兰指令
3. 检查边段范围、α退化和人工安全梯形
4. 快速检查IK、关节限位和奇异性
5. 模块9进行联合不确定性有效概率评估
6. 对 P_valid 达标候选计算模块10信息增益
7. 对前K名进行详细碰撞与路径检查
8. 选择最佳可执行候选
9. 执行并采集真实轮廓
```

需要特别强调：不存在独立的“名义正向预测阶段”。名义虚拟轮廓由模块8直接生成；正向求交函数仅在模块9的不确定性样本中调用。

---

# 12. 执行验证与滚动更新

执行最佳候选后，真实系统必须验证：

- 两个断点均被检测；
- 仍对应同一固定 $E_u,E_v$；
- 两个端点位于外层有效域；
- 轮廓长度和点数正常；
- 残差不异常；
- 法兰实际到位。

若通过：

$$
D_{k+1}=D_k\cup z_{\rm real}.
$$

随后重新运行12-DOF-V2：

$$
({}^F\hat T_{S,k+1},
R_{pl,k+1},
C_{k+1})
=
\operatorname{SolveV2}(D_{k+1}).
$$

并更新：

$$
J_{9,k+1},
\qquad
\Sigma_{x9,k+1},
\qquad
H_{X,k+1}^{\rm eff}.
$$

若执行失败：

1. 不将该帧加入正式数据；
2. 回退到上一安全位姿；
3. 将候选或附近区域加入短期黑名单；
4. 缩小候选范围或提高 $p_{\min}$；
5. 重新选择。

---

# 13. 自适应停止

停止条件不应只依赖固定姿态数量。建议同时检查：

## 13.1 最小样本数

$$
N_k\ge N_{\min}.
$$

## 13.2 手眼有效信息满秩

$$
\operatorname{rank}
\left(
H_{X,k}^{\rm eff}
\right)=6.
$$

## 13.3 最弱方向足够强

$$
\lambda_{\min}
\left(
H_{X,k}^{\rm eff}
\right)
\ge\lambda_{\rm target}.
$$

## 13.4 最大剩余增益足够小

$$
\max_{\eta\in\mathcal C_k^{\rm safe}}
\Delta I_X(\eta)
<
\varepsilon_I
$$

连续若干轮成立。

## 13.5 协方差达到目标

手眼旋转和平移标准差低于预设阈值。

## 13.6 最大采集次数

$$
N_k\le N_{\max}
$$

作为安全上限。

---

# 14. 最终标定

停止后使用全部有效帧进行最终12-DOF-V2求解，输出：

$$
{}^F\hat T_S,
\qquad
R_{pl},
\qquad
C.
$$

同时输出：

- 残差统计；
- 参数协方差；
- 数值秩与条件数；
- 每帧双边一致性；
- 可重复性和外部验证误差。

正式最终结果不应使用与在线阶段不同的几何模型，否则在线选姿评分和最终估计目标不一致。

---

