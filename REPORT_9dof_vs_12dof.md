# 9-DOF vs 12-DOF 标定方法对比报告

**测试日期:** 2026-07-20  
**测试数据:** manual_v4.json (6 个手动采集位姿, e1/e2 来自 /gocator/endpoints 几何求交)

---

## 1. 方法原理

### 1.1 9-DOF (平面 + 边 pairwise LM)

**参数:** θ = [ω_he(3), t_he(3), ω_pl(3)]，共 9 个

**残差模型:**

| 约束类型 | 残差公式 | 含义 |
|---------|---------|------|
| 平面 | n_B · p_base (centered) | 所有扫描点在同一平面上 |
| 边 e1 共线 | cross(p_e1_{k+1} − p_e1_k, u_B) | **相邻位姿**的 e1 点共线于 u_B |
| 边 e2 共线 | cross(p_e2_{k+1} − p_e2_k, v_B) | **相邻位姿**的 e2 点共线于 v_B |

**关键特征:**
- 边约束是 **pairwise**（相邻位姿之间），不引用全局参考点
- 没有角点坐标 C 这个参数
- 权重: w_plane=0.1, w_edge=1.0

```
数据流:
  [R_i, t_i] → R_BS = R_i·R_he → 投影 e1/e2/scan_pts 到 base 系
                                           ↓
  平面点 → n_B·p 残差
  e1点   → cross(p_{k+1}−p_k, u_B)  pairwise
  e2点   → cross(p_{k+1}−p_k, v_B)  pairwise
```

### 1.2 12-DOF (C-anchored cross-product LM)

**参数:** θ = [ω_he(3), t_he(3), ω_pl(3), C(3)]，共 12 个

**残差模型:**

| 约束类型 | 残差公式 | 含义 |
|---------|---------|------|
| 平面 | n_B · (p_base − C) | 所有扫描点到 C 的距离沿法向为零 |
| 边 e1 共线 | cross(p_e1 − C, u_B) | 每个 e1 点都跟 **同一个 C** 共线于 u_B |
| 边 e2 共线 | cross(p_e2 − C, v_B) | 每个 e2 点都跟 **同一个 C** 共线于 v_B |

**关键特征:**
- 所有残差都锚定在全局角点 C 上
- C 是自由参数，参与 LM 迭代
- C 和 t_he 在 Jacobian 中高度耦合

```
数据流:
  [R_i, t_i] → R_BS = R_i·R_he → 投影 e1/e2/scan_pts 到 base 系
                                           ↓
  平面点 → n_B·(p − C) 残差          ← 引用 C
  e1点   → cross(p − C, u_B)  每帧   ← 引用 C
  e2点   → cross(p − C, v_B)  每帧   ← 引用 C
```

---

## 2. 测试结果

**测试数据:** 6 位姿, e1=6, e2=6, 总扫描点 367  
**手眼初值扰动:** R 5°, t 12mm  
**硬件:** 单线程 CPU, numpy LM

| 指标 | 9-DOF | 12-DOF | 9-DOF 优势 |
|------|-------|--------|-----------|
| **R_err** | 0.0828° | 0.1368° | **1.65×** |
| **t_err** | 0.38 mm | 0.70 mm | **1.84×** |
| cost | 2.17e-07 | 8.75e-07 | 4.0× |
| cond(J) | 90 | 330 | 3.7× |
| σ² (残差方差) | 1.12e-09 | 4.48e-09 | 4.0× |
| σ(t_he) 估计 | [0.10,0.12,0.06] mm | [0.23,0.17,0.10] mm | ≈2× |
| σ(w_he) 估计 | [0.01,0.01,0.02]° | [0.02,0.02,0.03]° | ≈1.5× |
| 残差/参数 | 44.1 | 33.6 | 31% 更多 |
| 参数数量 | 9 | 12 | — |

---

## 3. 问题分析

### 3.1 C ↔ t_he 强耦合（核心问题）

12-DOF 的 Jacobian 协方差矩阵中，C 和 t_he 的相关系数:

```
ρ(Cx, tx) = +0.922    ← 接近完全共线
ρ(Cy, ty) = +0.864
ρ(Cx, ty) = +0.764    ← 交叉耦合
ρ(Cy, tx) = +0.763
ρ(Cz, tz) = -0.803
```

**所有 9 个相关系数绝对值 > 0.3，6 个 > 0.75。**

物理原因: 传感器平移 Δt 和角点平移 −R_i·ΔC 对投影点的位置贡献几乎不可区分。平面约束能部分断开 z 方向（ρ=0.80 < 0.92），但 x/y 方向几乎线性相关。LM 在这 3 个方向上没有足够的梯度信息来区分两个参数，导致:
- 协方差膨胀 → σ(t_he) 翻倍
- 条件数恶化 → 收敛更慢
- 残差方差增大 → 模型拟合更差

### 3.2 为什么多帧也没完全断开这个耦合

每帧的 `cross(p_e1_i − C, u_B) = 0` 约束的几何含义是"e1 到 C 的向量平行于 u_B"。这个约束对 C 沿 u_B 方向的位移完全不可观——C 沿 u_B 滑动不会改变 e1 残差。同理 C 沿 v_B 方向对 e2 残差不敏感。只有平面约束的 `n·(p−C)` 提供了 C 沿法向的约束。

所以 C 只有 **1 个有效约束方向（法向）**，另外 2 个方向（面内）与 t_he 的 3 个方向混合成一个大的耦合空间。6~9 个位姿换不同朝向能在一定程度上打破耦合，但耦合源自几何本质，无法彻底消除。

### 3.3 位姿数敏感度

旧数据 9 个位姿 → 12-DOF t_err=0.09mm（达标）  
新数据 6 个位姿 → 12-DOF t_err=0.70mm（超标）

位姿多 = 残差/参数比大 → 耦合方向有更多独立观测量来约束 → 协方差下降。但下降速度慢于 9-DOF（9-DOF 每加一个位姿直接增加 3 个边残差对，12-DOF 增加的残差还要跟 C 分账）。

---

## 4. 结论

| | 9-DOF | 12-DOF |
|---|-------|--------|
| 精度 | ✅ R<0.1°, t≈0.4mm | ⚠ R≈0.14°, t≈0.7mm |
| 鲁棒性 | ✅ 不受 C 耦合影响 | ❌ C↔t_he 耦合放大误差 |
| 参数效率 | ✅ 44 残差/参数 | ⚠ 34 残差/参数 |
| 可扩展性 | ⚠ 无法显式输出 C | ✅ 输出 C（对后续有用） |

**9-DOF 在所有指标上优于 12-DOF。** 差距根源是 C-anchored 模型引入的 C↔t_he 参数耦合，而非数据质量或实现 bug。
