# 线激光双边角点主动手眼标定：完整方法原理 V6

> 本文档以 2026-07-29 的仓库代码为唯一实现基线。
> 当前范围仅为 Gazebo + MoveIt + RViz 仿真；真实机器人、真实 Gocator、现场同步与外部精度验证尚未完成。  
> V6 的目的不是描述“理想中准备实现的方法”，而是给出当前代码实际执行算法的完整数学解释。

---

# 0. 阅读路线与代码索引

建议按以下顺序学习：

1. 第1～4章：先统一坐标系、观测对象和总体流程；
2. 第5～7章：理解为什么六种子阶段不使用待标定手眼；
3. 第8～11章：掌握12-DOF-V2变量投影求解；
4. 第12～16章：理解候选构造、鲁棒概率和信息增益；
5. 第17～19章：理解 MoveIt 执行、同步批处理和停止；
6. 第20～23章：理解仿真结果、限制和真实现场迁移。

主要实现位置：

| 内容 | 实现文件 |
|---|---|
| 数据模型、ROI | `calibration_pipeline/models.py` |
| SO(3)/SE(3)工具 | `calibration_pipeline/geometry.py` |
| 初始位姿准入 | `seed_collection/initial_pose.py` |
| 双边特征 | `seed_collection/features.py` |
| 原始轮廓断点检测 | `calibration_pipeline/perception/endpoint_detector.py` |
| 断点检测ROS适配 | `profile_endpoint_detector_node.py` |
| 无标定平移伺服 | `seed_collection/translation_servo.py` |
| 星形旋转与候补分支 | `seed_collection/rotation_scheduler.py` |
| 种子定点多帧稳健筛选 | `seed_collection/multiframe.py` |
| 六种子ROS状态机 | `seed_collection_node.py` |
| 分组种子数据与聚合 | `calibration_pipeline/dataset_io.py` |
| 初解bootstrap门控 | `calibration_pipeline/initial_validation.py` |
| 留出帧几何验证 | `calibration_pipeline/validation.py` |
| 角点变量投影 | `v2_backend/corner_projection.py` |
| 约化残差与Jacobian | `v2_backend/residual.py` |
| 协方差、可观测性、信息矩阵 | `v2_backend/information.py` |
| 12-DOF-V2求解器 | `solvers/twelve_dof_v2.py` |
| NBV候选生成 | `nbv/candidate_generator.py` |
| 未来双边轮廓预测 | `nbv/profile_predictor.py` |
| Sigma点有效概率 | `nbv/validity.py` |
| 信息增益评分 | `nbv/scoring.py` |
| 停止条件 | `nbv/stopping.py` |
| 事务化数据更新 | `pipeline.py` |
| MoveIt执行与回退 | `active_calibration_sim_node.py` |
| 参数 | `config/calibration.yaml` |

## 0.1 相对V5最重要的落地修正

V6纳入了当前代码已经验证的修正：

1. 初始深度差使用 $|z(e_v)-z(e_u)|$，不再错误限制正负顺序；
2. 静态局部IK从强制4/4改为至少3/4，最终仍由实测动态预检准入；
3. 已提交候选和近重复关节位姿不会再次执行；
4. 轮廓、物理断点和编码器法兰位姿使用同一时间戳严格配对；
5. 一个NBV的多帧各自绑定同步法兰位姿，经筛选后聚合成一个物理观测并事务提交；
6. NBV虚拟信息按一个物理位姿计算，不再由同姿态重复帧数夸大几何激励；
7. YAML中的最低有效概率真正接入候选评分；
8. 残差方差自由度同时计入9维非线性状态和3维投影角点；
9. 仿真端只向算法发布带可复现噪声的原始轮廓，真值断点隔离到仿真专用话题；
10. 六种子改为每个物理姿态多帧、断点MAD整帧筛选和schema 3分组聚合；
11. NBV开始前增加真值无关的初解bootstrap稳定性门控和协方差下界；
12. 明确区分信息协方差下降与单次真值误差的非单调波动。
13. 同一静止NBV的多帧先做断点MAD整帧筛选，再分成“拟合帧”和“留出验证帧”；
14. 一个物理NBV只向求解和信息矩阵增加一个聚合观测，不再把15帧误当成15个独立几何位姿；
15. 自适应停止同时使用相对信息增益、留出几何验证平台期和协方差目标；
16. 最终结果从历史快照中选择，留出分数统计等价时优先更新、信息更充分的快照；
17. 控制台不再限制最多6个NBV；0表示自适应停止，内部仍保留20个NBV紧急保护；
18. 动态预检结果用于重排种子方向；局部回滚失败时增加参考位姿二级恢复。
19. 新增仿真/真实Gocator共用的纯轮廓断点检测器、鲁棒直线拟合和跨帧物理身份跟踪；
20. 断点检测结果、原始轮廓和同步法兰位姿按完全相同的消息时间戳三路配对。

---

# 1. 问题定义

## 1.1 要估计什么

机器人法兰上刚性安装线激光传感器。目标是估计传感器坐标系 $S$ 到机器人法兰
坐标系 $F$ 的刚体变换：

$$
X \triangleq {}^F T_S
=
\begin{bmatrix}
R_X & t_X\\
0 & 1
\end{bmatrix}
\in SE(3).
$$

其中：

- $R_X={}^F R_S\in SO(3)$；
- $t_X={}^F t_S\in\mathbb R^3$；
- $X$ 将传感器坐标变换到法兰坐标。

机器人在第 $i$ 个观测时刻给出：

$$
T_i\triangleq{}^B T_{F_i}
=
\begin{bmatrix}
R_i&t_i\\
0&1
\end{bmatrix}.
$$

于是传感器在基坐标系 $B$ 下的位姿为：

$$
{}^B T_{S_i}=T_iX,
$$

即：

$$
R_{S_i}=R_iR_X,
\qquad
t_{S_i}=t_i+R_it_X.
$$

传感器中一点 $p^S$ 变换到基坐标系：

$$
q^B
=R_iR_Xp^S+t_i+R_it_X.
$$

这个式子是后续所有平面残差、断点残差和候选预测的共同基础。

## 1.2 标定板模型

标定板被建模为一个有限矩形平面。指定相邻两条物理边的公共角点为：

$$
C\in\mathbb R^3.
$$

板坐标方向为：

$$
R_P=[u\;v\;n]\in SO(3),
$$

其中：

- $u$：第一条目标边的方向；
- $v$：第二条目标边的方向；
- $n=u\times v$：平板法向；
- $L_u,L_v$：两条边长度。

有限矩形为：

$$
\mathcal P=
\left\{
C+a u+b v
\mid
0\le a\le L_u,\;0\le b\le L_v
\right\}.
$$

两条指定相邻边分别为：

$$
E_u=\{C+a u\mid0\le a\le L_u\},
$$

$$
E_v=\{C+b v\mid0\le b\le L_v\}.
$$

代码中的 `endpoint_u` 是激光平面与 $E_u$ 的物理交点，`endpoint_v` 是与
$E_v$ 的物理交点。下文也记作 $e_u,e_v$。

## 1.3 “固定物理身份”不等于“固定深度顺序”

固定 $e_u/e_v$ 的意义是：

- 所有帧中的 $e_u$ 始终约束物理边 $E_u$；
- 所有帧中的 $e_v$ 始终约束物理边 $E_v$；
- 不允许在求解过程中交换两条边。

但它不意味着：

$$
z(e_v)>z(e_u).
$$

合法观察姿态可能满足任意一种深度顺序。初始位姿只要求：

$$
|z(e_v)-z(e_u)|\ge \Delta z_{\min}.
$$

当前仿真取：

$$
\Delta z_{\min}=15 \mathrm{mm}.
$$

绝对值的作用是排除两端深度几乎相同的退化或近退化轮廓，符号本身不参与合法性判断。

---

# 2. 传感器观测模型

## 2.1 单帧数据

第 $i$ 帧有效观测为：

$$
D_i=
\left(
T_i,\;
\mathcal Q_i^S,\;
p_{u,i}^S,\;
p_{v,i}^S
\right),
$$

其中：

- $T_i={}^BT_{F_i}$：与该轮廓同一触发时刻的法兰位姿；
- $\mathcal Q_i^S=\{p_{ij}^S\}_{j=1}^{N_i}$：完整线激光轮廓点；
- $p_{u,i}^S$：物理边 $E_u$ 的断点；
- $p_{v,i}^S$：物理边 $E_v$ 的断点。

当前 Gocator 模型的测量平面是传感器 $X_S-Z_S$ 平面，因此理想点满足：

$$
p^S=[x,0,z]^T.
$$

## 2.2 双边约束为什么比普通平面约束更强

仅使用轮廓点共面条件：

$$
n^T(q_{ij}^B-C)=0
$$

只能说明变换后的点位于平板平面内。平面内沿 $u,v$ 的运动不容易被区分。

双边断点额外提供：

$$
q_{u,i}^B\in E_u,
\qquad
q_{v,i}^B\in E_v.
$$

因此：

$$
v^T(q_{u,i}^B-C)=0,
\qquad
n^T(q_{u,i}^B-C)=0,
$$

$$
u^T(q_{v,i}^B-C)=0,
\qquad
n^T(q_{v,i}^B-C)=0.
$$

两条有固定物理身份的相邻边把平面内自由度也约束起来，是本方法能够同时估计手眼、
平板方向和角点的关键。

当前仿真发布器不再向标定链发布断点，只发布 `/gocator/profile`。独立节点
`profile_endpoint_detector` 对有序 $X_S-Z_S$ 轮廓做间断分段、MAD迭代TLS直线拟合、
半采样间距边界外推和质量门控，再发布 `/calibration/endpoints`。几何真值只发布到
`/simulation/ground_truth/endpoints`，该话题没有标定订阅者。

`EndpointTracker` 对无标签的两个候选断点做跨帧最小位移匹配，并在直接匹配与交换
匹配代价过于接近时拒绝该帧。检测器启动时用
`endpoint_detection.initial_first_label` 建立一次采样顺序到物理边的绑定，随后不再按
每帧 $x$ 大小排序。当前仿真参考位姿中第一个采样方向端点属于 `e2`；真实装置必须在
安装验证时确定一次该配置。

---

# 3. 传感器硬有效域与规划安全域

## 3.1 非对称梯形

代码不再假设左右对称的理想扇形，而是直接在 $X_S-Z_S$ 平面定义标定后的梯形：

$$
\mathcal V=
\left\{
(x,z)\mid
z_n\le z\le z_f,\;
x_l(z)\le x\le x_r(z)
\right\}.
$$

左右边界线性插值：

$$
\tau(z)=\frac{z-z_n}{z_f-z_n},
$$

$$
x_l(z)=(1-\tau)x_{l,n}+\tau x_{l,f},
$$

$$
x_r(z)=(1-\tau)x_{r,n}+\tau x_{r,f}.
$$

点 $p=[x,0,z]^T$ 到域边界的有符号最小余量为：

$$
m_{\mathcal V}(p)
=
\min
\left\{
z-z_n,\;
z_f-z,\;
x-x_l(z),\;
x_r(z)-x
\right\}.
$$

当且仅当：

$$
m_{\mathcal V}(p)\ge0
$$

时点在该梯形内。

## 3.2 两层测量域

系统维护两个域：

1. **hard domain**：传感器真正允许接受数据的外层硬有效域；
2. **safe domain**：规划与种子操作使用的内层安全域。

对一对断点定义：

$$
m_{\mathrm{pair}}
=
\min\{m_{\mathcal V}(p_u^S),m_{\mathcal V}(p_v^S)\}.
$$

候选规划必须位于 safe domain；实际执行后若越过 safe domain 但仍在 hard domain，
当前代码会警告并接受。只有越过 hard domain 才丢弃数据并回退。

当前仿真参数见 `calibration.yaml`：

```yaml
sensor:
  hard_trapezoid: [-0.292, 0.82, -0.021, -0.22, -0.019, 0.22]
  safe_trapezoid: [0.27, 0.78, -0.115, -0.19, 0.095, 0.19]
```

这六个数依次对应：

$$
[z_n,z_f,x_{l,n},x_{l,f},x_{r,n},x_{r,f}].
$$

它们是当前仿真FOV标定结果，不是实物通用常数。

---

# 4. 完整算法分阶段结构

当前代码的完整流程为：

$$
\text{Phase 0a粗手眼}
\rightarrow
\text{Phase 0b六种子}
\rightarrow
\text{12-DOF-V2联合初始化}
\rightarrow
\text{主动NBV循环}
\rightarrow
\text{最终结果}.
$$

各阶段职责严格分开：

## 4.1 Phase 0a

提供一个足以让非线性求解收敛的粗手眼初值：

$$
\bar X={}^F\bar T_S.
$$

在当前仿真中，代码用真值人为加入固定的 $3^\circ/10 \mathrm{mm}$ 扰动来生成
$\bar X$。真实现场必须由CAD、安装尺寸或粗测量提供，不能使用未知真值。

## 4.2 Phase 0b

在尚无可靠手眼和平板估计时，仅使用：

- Gocator双边轮廓；
- 机器人编码器；
- 局部法兰运动学；
- 实际运动后的安全反馈；

取得6个旋转多样的种子。该阶段不使用 $\bar X$ 来预测板上三维目标。

## 4.3 联合初始化

用六种子联合估计：

$$
X,\quad R_P,\quad C.
$$

初始化通过秩和条件数检查后才能进入NBV。

## 4.4 主动NBV

利用当前估计：

- 构造保证名义双边相邻边相交的候选；
- 传播 $X,R_P,C$ 的联合不确定性；
- 过滤低有效概率候选；
- 用手眼有效信息增益排序；
- 经IK、碰撞与MoveIt整路径检查后执行；
- 采集同步多帧，事务化滚动更新。

---

# 5. Phase 0b：初始位姿的可操作定义

## 5.1 双边反馈特征

对传感器系断点：

$$
p_u^S=[x_u,0,z_u]^T,
\qquad
p_v^S=[x_v,0,z_v]^T,
$$

定义中点：

$$
p_m^S=\frac12(p_u^S+p_v^S),
$$

$$
x_m=\frac{x_u+x_v}{2},
\qquad
z_m=\frac{z_u+z_v}{2}.
$$

轮廓端点距离：

$$
L_p=\|p_v^S-p_u^S\|_2.
$$

双边安全余量：

$$
m_{\mathrm{pair}}
=
\min\{m_{\mathrm{safe}}(p_u^S),m_{\mathrm{safe}}(p_v^S)\}.
$$

这些量全部在传感器坐标系中直接计算，不需要手眼。

## 5.2 静态保守工作包络

当前仿真初始位姿要求：

$$
|x_m|\le30 \mathrm{mm},
$$

$$
300 \mathrm{mm}\le z_m\le550 \mathrm{mm},
$$

$$
m_{\mathrm{pair}}\ge20 \mathrm{mm},
$$

$$
50 \mathrm{mm}\le L_p\le250 \mathrm{mm},
$$

$$
|z_v-z_u|\ge15 \mathrm{mm}.
$$

机器人最小归一化关节余量定义为：

$$
\rho_q
=
\min_j
\frac{
\min(q_j-q_{j,\min},q_{j,\max}-q_j)
}{
q_{j,\max}-q_{j,\min}
},
$$

要求：

$$
\rho_q\ge0.05.
$$

此外，从当前法兰位姿分别构造局部：

$$
R_F\exp(\pm2^\circ[e_x]_\times),
\qquad
R_F\exp(\pm2^\circ[e_y]_\times).
$$

对四个目标做数值IK，解必须满足最大单关节变化不超过 $20^\circ$。静态层要求
至少3/4方向有解。四个带符号方向中任意三个必然同时覆盖 $X,Y$ 两个局部轴。

这些条件只是当前装置的保守充分包络。真实现场不要求肉眼看见空气中的激光平面，
操作者以二维轮廓、双边断点和上述终端数值为准。

## 5.3 动态实测预检

静态通过后，系统实际执行：

$$
\{-X,+X,-Y,+Y\}\times2^\circ.
$$

每个方向都：

1. 从参考位姿开始；
2. 做局部法兰微旋转；
3. 必要时运行无标定平移伺服使 $x_m$ 回到预检工作包络；
4. 检查实际双边测量；
5. 返回参考位姿。

扰动后要求：

$$
|x_m|\le30 \mathrm{mm},
\qquad
m_{\mathrm{pair}}\ge15 \mathrm{mm}.
$$

这里的30 mm只用于判断该带符号方向是否仍可工作；真正保存种子使用10 mm中心容差。
把二者混成同一个很小阈值会把“方向安全性预检”错误变成一次完整种子采集，显著缩窄
允许的初始位姿范围。至少2/4方向通过，且通过方向必须覆盖 $X,Y$ 两轴。代码会完成
四方向预检并记录各方向余量，随后优先尝试实测安全方向；只有一个安全符号的局部轴仍
可用，不再强制要求某一轴正负两向都安全。动态预检才是最终准入条件。

---

# 6. 无手眼平移伺服

## 6.1 为什么需要平移修正

传感器相对法兰存在未知杆臂。法兰局部旋转时，传感器既旋转也平移。没有可靠手眼时，
无法直接计算保持轮廓居中的三维补偿，因此系统从实际输入—输出响应中学习。

## 6.2 局部试探

对法兰局部平移轴 $j\in\{x,y,z\}$，执行一个小试探：

$$
t_F^+
=
t_F+R_F e_j\Delta q_{\mathrm{probe}}.
$$

当前仿真：

$$
\Delta q_{\mathrm{probe}}=1 \mathrm{mm}.
$$

测得：

$$
g_j
\approx
\frac{x_m^+-x_m}{\Delta q_{\mathrm{probe}}}.
$$

不安全或IK失败的方向记为不可用。选择：

$$
j^\star=\arg\max_j|g_j|.
$$

这使系统优先使用对横向居中最敏感的法兰局部平移轴。

## 6.3 闭环修正

选定轴后，局部近似：

$$
\Delta x_m\approx g\Delta q.
$$

控制律：

$$
\Delta q
=
\operatorname{clip}
\left(
-\lambda\frac{x_m}{\hat g},
-q_{\max},
q_{\max}
\right).
$$

当前实现：

$$
\lambda=0.8,
\qquad
q_{\max}=8 \mathrm{mm}.
$$

每次执行后用实测响应更新灵敏度：

$$
g_{\mathrm{measured}}
=
\frac{x_{m,k+1}-x_{m,k}}{\Delta q_k},
$$

$$
\hat g_{k+1}
=(1-\eta)\hat g_k+\eta g_{\mathrm{measured}},
$$

当前：

$$
\eta=0.5.
$$

若缓存的灵敏度失效、离开安全域或在8次修正内不收敛，系统回到最近有效位姿并重新
试探，而不是继续沿错误方向运动。缓存标志必须保留到居中完成或局部重新试探完成；
若第一步后就清除，后续安全恢复分支将永远无法触发。

一次仿真帧、端点或真实通信帧可能短暂漏检。每次运动稳定后，系统最多等待1 s取得新的
同步双边帧；只有持续缺测才进入回退。因此Bernoulli漏检不会因刚好落在状态机检查时刻
而被放大成整个目标失败。

---

# 7. 六种子星形运动与失败恢复

## 7.1 局部旋转

法兰局部旋转使用右乘：

$$
R_F^+
=
R_F\exp(\delta\theta[e_j]_\times).
$$

当前默认：

- 目标角度：$5^\circ$；
- 初始小步：$2.5^\circ$；
- 失败后步长减半；
- 最小步长：$0.25^\circ$；
- 单目标最多失败2次。

## 7.2 默认六种子

参考种子加五个目标：

$$
\mathcal R_{\mathrm{seed}}
=
\{R_0,R_x^+,R_x^-,R_y^-,R_y^+,R_x^+R_y^+\}.
$$

动态预检完成后，代码先按实测安全方向生成5°单轴目标，再生成2.5°小幅单轴、完整组合
和小幅组合候补。完整组合不能仅因某个单轴5°失败就删除：另一轴旋转可能补偿FOV位置，
并且组合激励对初解很重要。若局部回滚没有恢复双边，状态机还会返回人工确认过的参考
位姿，跳过该分支后继续，而不是让单个失败分支终止整批采集。

若默认分支失败，还会尝试异号和换序组合：

$$
R_x^+R_y^-,
R_x^-R_y^+,
R_x^-R_y^-,
R_y^+R_x^+,
R_y^+R_x^-,
R_y^-R_x^+,
R_y^-R_x^-.
$$

得到6个合格种子后立即停止，不会无条件执行完全部候补。

## 7.3 部分姿态

若目标 $5^\circ$ 无法完全到达，但最近有效姿态相对参考至少达到：

$$
\theta_{\mathrm{partial}}\ge2.5^\circ,
$$

并满足：

$$
|x_m|\le10 \mathrm{mm},
\qquad
m_{\mathrm{pair}}\ge2 \mathrm{mm},
$$

则允许保存为 `_partial` 种子。这样避免因窄FOV丢掉已经提供有效旋转激励的安全姿态。

## 7.4 旋转多样性

对种子法兰旋转 $R_i$，定义最小两两旋转距离：

$$
d_{\min}
=
\min_{i<j}
\left\|
\log(R_i^TR_j)
\right\|_2.
$$

当前要求：

$$
d_{\min}\ge2^\circ.
$$

同时报告启发式Gram矩阵：

$$
G_R
=
\sum_{i<j}(R_i-R_j)^T(R_i-R_j).
$$

其特征值用于描述旋转方向覆盖，但当前种子保存只硬性使用最小两两间隔。完整可观测性
最终由12-DOF-V2约化Jacobian判断。

## 7.5 人工六种子模式

交互控制台也支持操作者人工采集6帧。当前人工捕获接口检查：

- 同步轮廓、断点和法兰位姿存在；
- 双边位于safe domain；
- 机器人已经稳定；
- 新法兰旋转与已有种子至少相隔 $2^\circ$。

人工模式不执行自动初始包络和动态预检，因为运动选择责任由操作者承担；最终仍必须由
联合初始化的秩、条件数和收敛性验收。人工采集“数量达到6”本身不证明数据可观测。

---

# 8. 12-DOF-V2状态参数化

## 8.1 为什么叫12-DOF

联合几何未知量为：

- 手眼旋转：3；
- 手眼平移：3；
- 平板方向：3；
- 公共角点：3。

合计：

$$
3+3+3+3=12.
$$

但角点 $C$ 在固定旋转状态下是线性的，可以解析消元。因此数值优化只保留9维
非线性状态：

$$
x_9=
\begin{bmatrix}
\omega_X\\
t_X\\
\omega_P
\end{bmatrix}
\in\mathbb R^9,
$$

$$
R_X=\exp([\omega_X]_\times),
\qquad
R_P=\exp([\omega_P]_\times).
$$

所以该方法是：

> 12个几何自由度，9维非线性优化，3维角点变量投影。

## 8.2 SO(3)指数映射

令 $\theta=\|\omega\|$，$\hat a=\omega/\theta$。代码使用Rodrigues公式：

$$
\exp([\omega]_\times)
=
I+\sin\theta[\hat a]_\times
+(1-\cos\theta)[\hat a]_\times^2.
$$

当 $\theta$ 很小时使用：

$$
\exp([\omega]_\times)\approx I+[\omega]_\times.
$$

优化状态是旋转的全局指数坐标，不是每轮局部增量状态。

---

# 9. 12-DOF-V2残差完整推导

## 9.1 点变换

给定 $x_9$，第 $i$ 帧传感器点 $p^S$ 在基坐标系中为：

$$
q_i(p^S;x_9)
=
R_iR_Xp^S+t_i+R_it_X.
$$

简记：

$$
R_{S_i}=R_iR_X,
\qquad
t_{S_i}=t_i+R_it_X,
$$

$$
q_i=R_{S_i}p^S+t_{S_i}.
$$

## 9.2 稠密平面点残差

每个轮廓点应位于板平面：

$$
n^T(q_{ij}-C)=0.
$$

代码采用等价的反号形式：

$$
r_{p,ij}
=
\sqrt{\frac{w_p}{N_i}}
n^T(C-q_{ij}).
$$

每帧乘：

$$
\sqrt{\frac{1}{N_i}}
$$

是为了防止点数多的轮廓仅因采样更密就压倒断点约束。于是每一帧全部平面点的总权重
近似保持在 $w_p$ 的量级。

## 9.3 $E_u$断点残差

$e_u$ 位于通过 $C$、方向为 $u$ 的边：

$$
q_{u,i}=C+a_i u.
$$

因此它既在板平面上，也没有 $v$ 分量：

$$
v^T(q_{u,i}-C)=0,
$$

$$
n^T(q_{u,i}-C)=0.
$$

代码残差：

$$
r_{u,e}
=
\sqrt{w_e}\,v^T(C-q_{u,i}),
$$

$$
r_{u,n}
=
\sqrt{w_n}\,n^T(C-q_{u,i}).
$$

## 9.4 $E_v$断点残差

同理：

$$
q_{v,i}=C+b_i v,
$$

所以：

$$
u^T(q_{v,i}-C)=0,
\qquad
n^T(q_{v,i}-C)=0.
$$

代码残差：

$$
r_{v,e}
=
\sqrt{w_e}\,u^T(C-q_{v,i}),
$$

$$
r_{v,n}
=
\sqrt{w_n}\,n^T(C-q_{v,i}).
$$

当前配置：

$$
w_p=w_e=w_n=1.
$$

这些权重已经通过平方根乘进残差，因此后续最小二乘直接计算 $\|r\|^2$，不再另乘
一个 $W$ 矩阵。

---

# 10. 角点变量投影

## 10.1 构造线性系统

固定 $x_9$ 后，$R_X,t_X,R_P$ 已知，所有约束对角点 $C$ 都是线性的。

每条残差可以写成：

$$
a_k^TC-b_k.
$$

例如平面点：

$$
a_k^T=\sqrt{\frac{w_p}{N_i}}n^T,
$$

$$
b_k=\sqrt{\frac{w_p}{N_i}}n^Tq_{ij}.
$$

$e_u$边约束对应 $a_k^T=\sqrt{w_e}v^T$，
$e_v$边约束对应 $a_k^T=\sqrt{w_e}u^T$，两个断点平面约束对应
$a_k^T=\sqrt{w_n}n^T$。

堆叠后：

$$
A(x_9)C\approx b(x_9).
$$

## 10.2 解析消元

给定 $x_9$，最优角点为：

$$
C^\star(x_9)
=
\arg\min_C\|A(x_9)C-b(x_9)\|_2^2.
$$

用最小二乘伪逆：

$$
C^\star(x_9)=A(x_9)^+b(x_9).
$$

要求：

$$
\operatorname{rank}(A)=3.
$$

约化残差：

$$
r(x_9)
=
A(x_9)C^\star(x_9)-b(x_9).
$$

最终优化：

$$
\hat x_9
=
\arg\min_{x_9}\frac12\|r(x_9)\|_2^2.
$$

代码使用SciPy trust-region reflective最小二乘：

- `method="trf"`；
- `x_scale="jac"`；
- 最大函数求值3000；
- `ftol=xtol=gtol=10^{-11}`。

## 10.3 变量投影为什么比直接优化12维更合适

每次给定旋转和手眼平移后，$C$ 都被求到条件最优值，因此：

- 数值优化维度从12降为9；
- 不需要为角点选择独立步长；
- NBV计算状态扰动时，角点会随全部已有数据重新投影；
- 不会错误地把角点当作与平板方向无关的独立随机量。

---

# 11. 初始化、平板坐标规范化与收敛

## 11.1 平板方向初值

先用粗手眼 $(\bar R_X,\bar t_X)$ 将所有 $e_u,e_v$ 变换到基坐标：

$$
\bar q_{u,i}=R_i\bar R_Xp_{u,i}^S+t_i+R_i\bar t_X,
$$

$$
\bar q_{v,i}=R_i\bar R_Xp_{v,i}^S+t_i+R_i\bar t_X.
$$

对 $\{\bar q_{u,i}\}$ 去中心后做SVD，主右奇异向量作为 $u_0$。对
$\{\bar q_{v,i}\}$ 同样得到 $v_{\mathrm{raw}}$，再做Gram-Schmidt：

$$
v_0'
=
v_{\mathrm{raw}}-u_0(u_0^Tv_{\mathrm{raw}}),
$$

$$
v_0=\frac{v_0'}{\|v_0'\|},
$$

$$
n_0=\frac{u_0\times v_0}{\|u_0\times v_0\|},
$$

最后重新令：

$$
v_0=n_0\times u_0.
$$

得到：

$$
R_{P,0}=[u_0\;v_0\;n_0].
$$

## 11.2 初始状态

$$
x_{9,0}
=
\begin{bmatrix}
\log(\bar R_X)\\
\bar t_X\\
\log(R_{P,0})
\end{bmatrix}.
$$

滚动NBV更新时，不重新从粗手眼开始，而以上一轮：

$$
\hat R_X,\hat t_X,\hat R_P
$$

作为下一轮初值。

## 11.3 平板坐标的符号规范化

矩形平面在残差平方意义下存在轴符号歧义。代码只考虑保持右手性的四种符号：

$$
R_P'
=
R_P\operatorname{diag}(s_u,s_v,s_us_v),
\qquad
s_u,s_v\in\{-1,+1\}.
$$

禁止交换 $u,v$，因为交换会破坏固定的物理边身份。

四个候选按以下证据选择：

- 与上一轮平板方向接近；
- $e_u-C$ 更倾向沿 $+u$；
- $e_v-C$ 更倾向沿 $+v$。

第一轮没有历史参考时，优先使用断点方向证据。规范化后重新解析求解角点。

## 11.4 收敛判定

初始化或滚动结果必须同时满足：

1. 非线性优化器成功；
2. 约化Jacobian秩为9；
3. 条件数不超过配置上限，当前为 $10^{12}$；
4. 角点投影系统秩为3。

六个种子是当前流程的最小业务要求；底层求解器数学接口允许至少4个双边姿态，但不表示
任意4帧都可观测。

---

# 12. 无量纲Jacobian、可观测性与协方差

## 12.1 数值Jacobian

约化Jacobian：

$$
J_9
=
\frac{\partial r}{\partial x_9}.
$$

代码使用中心差分，原始状态每一维步长：

$$
h=10^{-6},
$$

$$
J_{:,k}
\approx
\frac{r(x_9+he_k)-r(x_9-he_k)}{2h}.
$$

每次计算 $r$ 时都会重新求 $C^\star(x_9)$，因此这是变量投影后的完整约化Jacobian。

## 12.2 状态缩放

旋转和平移单位不同，定义：

$$
x_9=S\tilde x_9,
\qquad
\tilde x_9=S^{-1}x_9.
$$

当前尺度：

$$
S=
\operatorname{diag}
\left(
10^\circ,10^\circ,10^\circ,
0.1,0.1,0.1,
10^\circ,10^\circ,10^\circ
\right).
$$

这里角度换算为弧度，平移单位为米。对无量纲状态的Jacobian：

$$
\tilde J=J_9S.
$$

使用 $\tilde J$ 可避免“把米改成毫米就改变条件数”的单位依赖。

## 12.3 秩和条件数

SVD：

$$
\tilde J=U\Sigma V^T.
$$

数值秩由奇异值相对机器精度阈值判断。条件数：

$$
\kappa
=
\frac{\sigma_{\max}}{\sigma_{\min}}.
$$

最弱参数方向为：

$$
v_{\mathrm{weak}}=V_{:,-1}.
$$

它是无量纲9维状态中最不容易被当前数据辨识的组合方向。

## 12.4 残差方差

设残差维数为 $m$。虽然数值优化状态只有9维，但角点3维也由同一批残差拟合，
因此总拟合参数数为12。当前修正后的自由度为：

$$
\nu=\max(m-12,1).
$$

残差方差估计：

$$
\hat\sigma_r^2
=
\max
\left(
\frac{r^Tr}{\nu},
10^{-12}
\right).
$$

## 12.5 协方差

无量纲协方差：

$$
\Sigma_{\tilde x}
=
\hat\sigma_r^2
\left(
\tilde J^T\tilde J+\lambda I
\right)^+,
$$

当前：

$$
\lambda=10^{-9}.
$$

映射回物理状态：

$$
\Sigma_{x9}
=
S\Sigma_{\tilde x}S^T.
$$

报告的旋转和平移1σ上界分别取：

$$
\sigma_R^{\max}
=
\max_{k=1,2,3}
\sqrt{[\Sigma_{x9}]_{kk}},
$$

$$
\sigma_t^{\max}
=
\max_{k=4,5,6}
\sqrt{[\Sigma_{x9}]_{kk}}.
$$

旋转报告时从弧度转换为度。

## 12.6 重要限制

该协方差是局部高斯—高斯牛顿近似。若存在：

- 未建模机器人位姿误差；
- 轮廓与编码器不同步；
- 平板翘曲；
- 断点系统偏差；
- 非高斯离群点；

则它可能严重乐观。预测标准差达到目标不等于真实外部误差必然达到目标。

---

# 13. 只针对手眼的有效信息矩阵

约化9维状态仍包含平板方向3维。NBV的目标是提高手眼6维信息，而不是只提高整个
9维系统的某个混合方向。

无量纲Gauss-Newton信息矩阵：

$$
H_9=\tilde J^T\tilde J.
$$

分块：

$$
H_9=
\begin{bmatrix}
H_{XX}&H_{XP}\\
H_{PX}&H_{PP}
\end{bmatrix},
$$

其中 $X$ 对应手眼6维，$P$ 对应平板方向3维。

消去平板方向后的手眼有效信息：

$$
H_X^{\mathrm{eff}}
=
H_{XX}
-H_{XP}
\left(H_{PP}+\lambda_PI\right)^+
H_{PX}.
$$

当前：

$$
\lambda_P=10^{-10}.
$$

代码最后对结果做对称化：

$$
H_X^{\mathrm{eff}}
\leftarrow
\frac12
\left(
H_X^{\mathrm{eff}}+H_X^{\mathrm{eff}}\,T}
\right).
$$

注意：角点 $C$ 已在残差层通过变量投影消除；这里的Schur补只消除剩余的平板方向。

---

# 14. NBV候选的闭式构造

## 14.1 板上两个目标交点

从指定相邻边内部选：

$$
Q_u=C+a u,
\qquad
Q_v=C+b v.
$$

当前：

$$
a\in\operatorname{linspace}(0.04,L_u-0.04,4),
$$

$$
b\in\operatorname{linspace}(0.04,L_v-0.04,4).
$$

定义目标轮廓方向：

$$
d=\frac{Q_v-Q_u}{\|Q_v-Q_u\|}.
$$

板面内且垂直于轮廓的方向：

$$
m=\frac{n\times d}{\|n\times d\|}.
$$

## 14.2 激光平面法向

候选倾角 $\alpha$ 和分支 $s\in\{-1,+1\}$ 定义激光平面法向：

$$
\ell
=
\cos\alpha\,n+s\sin\alpha\,m.
$$

当前：

$$
\alpha\in\{55^\circ,65^\circ,75^\circ\}.
$$

传感器 $Y_S$ 轴就是激光平面法向：

$$
y_S=\ell.
$$

## 14.3 传感器平面内姿态

零滚转基：

$$
x_0=d,
$$

$$
z_0=\frac{x_0\times \ell}{\|x_0\times\ell\|}.
$$

再用 $\psi$ 绕激光平面法向旋转：

$$
x_S=\cos\psi\,x_0+\sin\psi\,z_0,
$$

$$
z_S=-\sin\psi\,x_0+\cos\psi\,z_0.
$$

当前：

$$
\psi\in\{-15^\circ,0^\circ,15^\circ\}.
$$

代码随后重新正交化，并令：

$$
R_{BS}^c=[x_S\;y_S\;z_S].
$$

## 14.4 工作距离

目标交点中点：

$$
Q_m=\frac12(Q_u+Q_v).
$$

给定工作距离 $d_w$，传感器原点：

$$
t_{BS}^c=Q_m-d_w z_S.
$$

当前：

$$
d_w\in\{0.33,0.40,0.50\} \mathrm{m}.
$$

名义传感器候选：

$$
{}^BT_S^c=
\begin{bmatrix}
R_{BS}^c&t_{BS}^c\\
0&1
\end{bmatrix}.
$$

根据当前手眼估计生成固定法兰指令：

$$
{}^BT_F^c
=
{}^BT_S^c
\left({}^F\hat T_S\right)^{-1}.
$$

这个法兰指令一旦生成，在该候选的不确定性传播和信息评分中保持固定。

## 14.5 候选规模

配置笛卡尔积的理论规模为：

$$
4\times4\times3\times3\times3\times2=864.
$$

无效几何、ROI越界、错误板侧、IK和运动增量过滤后，代码从剩余确定性列表中均匀抽取
最多8个做昂贵的信息评分，而不是只截取列表开头。候选覆盖仍遍及整个确定性列表，
但评分计算量近似减半。

---

# 15. 固定法兰指令下的未来双边预测

## 15.1 为什么必须固定法兰

评价候选时，真实执行量是：

$$
{}^BT_F^c.
$$

状态样本改变时，不能重新生成一个新的法兰指令，否则相当于假设机器人能预先知道真实
手眼并自动补偿，信息量会被错误计算。

对状态样本 $x_9^{(s)}$，传感器实际预测位姿为：

$$
{}^BT_S^{(s)}
=
{}^BT_F^c{}^FT_S^{(s)}.
$$

## 15.2 激光平面与两条目标边求交

设样本传感器位姿为 $R,t$，激光平面法向：

$$
\ell=Re_y.
$$

激光平面：

$$
\ell^T(q-t)=0.
$$

对 $E_u:C+\lambda_u u$：

$$
\ell^T(C+\lambda_u u-t)=0,
$$

所以：

$$
\lambda_u
=
\frac{\ell^T(t-C)}{\ell^Tu}.
$$

对 $E_v:C+\lambda_v v$：

$$
\lambda_v
=
\frac{\ell^T(t-C)}{\ell^Tv}.
$$

若：

$$
\min(|\ell^Tu|,|\ell^Tv|)<10^{-4},
$$

则激光平面与可信边近似平行，候选退化。

## 15.3 有限边段检查

当前预测器要求交点离边端至少：

$$
\delta_e=0.02 \mathrm{m}.
$$

即：

$$
\delta_e\le\lambda_u\le L_u-\delta_e,
$$

$$
\delta_e\le\lambda_v\le L_v-\delta_e.
$$

之后将交点变到传感器系：

$$
p_u^S=R^T(C+\lambda_u u-t),
$$

$$
p_v^S=R^T(C+\lambda_v v-t).
$$

再检查：

- 两端都位于safe trapezoid；
- 轮廓长度在 $0.02\sim0.8 \mathrm{m}$；
- 交点不是近退化。

虚拟轮廓由两断点间40个等距点构成。

---

# 16. 联合不确定性与候选有效概率

## 16.1 9维Sigma点

当前估计：

$$
x_9\sim\mathcal N(\hat x_9,\Sigma_{x9}).
$$

代码对对称化协方差做特征分解：

$$
\Sigma=V\Lambda V^T,
$$

$$
L=V\sqrt{\max(\Lambda,0)}.
$$

对 $n=9$ 维状态生成：

$$
\chi_0=\hat x_9,
$$

$$
\chi_k^\pm
=
\hat x_9\pm\sqrt{n}\,L_{:,k},
\qquad k=1,\ldots,n.
$$

总计：

$$
2n+1=19
$$

个样本。

当前实现不是带Unscented权重的均值传播，而是对19个样本做等权有效/无效计数。

## 16.2 每个样本重新投影角点

对每个 $\chi_s$：

1. 用全部已有真实数据求：
   $$
   C_s=C^\star(\chi_s,D_k);
   $$
2. 构造：
   $$
   X_s,\quad R_{P,s},\quad C_s;
   $$
3. 保持法兰指令 ${}^BT_F^c$ 不变；
4. 预测两条固定物理边上的交点和ROI余量。

若角点投影秩不足3，该样本记为退化失败。

## 16.3 有效概率

令样本有效指示函数：

$$
I_s=
\begin{cases}
1,&\text{双边、有限边段、ROI、长度和退化检查均通过},\\
0,&\text{否则}.
\end{cases}
$$

则：

$$
P_{\mathrm{valid}}
=
\frac1{19}\sum_{s=1}^{19}I_s.
$$

当前阈值已由配置真正接入评分：

$$
P_{\mathrm{valid}}\ge0.8.
$$

代码同时统计：

- 目标边交点失败概率；
- ROI失败概率；
- 近平行退化概率；
- 其他质量失败概率。

---

# 17. 手眼信息增益评分

## 17.1 当前信息

对全部已有真实数据 $D_k$，在当前解 $\hat x_9$ 处计算变量投影Jacobian：

$$
\tilde J_k.
$$

得到当前手眼有效信息：

$$
H_{X,k}^{\mathrm{eff}}.
$$

## 17.2 固定虚拟观测

候选生成时，根据名义板模型构造一帧固定代表性虚拟观测：

$$
\tilde D_c=
\left(
{}^BT_F^c,\;
\tilde{\mathcal Q}_c^S,\;
\tilde p_{u,c}^S,\;
\tilde p_{v,c}^S
\right).
$$

一个物理NBV实际计划采集 $M=15$ 帧，但它们用于降低同一静止位姿的随机测量方差，
不产生新的姿态变化。候选评分因此只加入一个代表性虚拟物理观测：

$$
\tilde{\mathcal B}_c=\{\tilde D_c\}.
$$

这避免把同一法兰指令和同一组名义坐标重复15次而夸大几何信息。计算增广残差时：

- ${}^BT_F^c$ 固定；
- 虚拟观测坐标固定；
- 状态扰动只改变模型预测；
- 每次扰动都对 $D_k\cup\tilde{\mathcal B}_c$ 重新变量投影角点。

于是得到：

$$
\tilde J_{k+c},
\qquad
H_{X,k+c}^{\mathrm{eff}}.
$$

如果状态扰动时重新生成虚拟观测，观测会跟模型一起移动，从而错误抵消部分敏感度；
当前代码明确避免了这一问题。

## 17.3 D-optimal增益

加入正则：

$$
\epsilon=10^{-9}.
$$

候选信息增益：

$$
\Delta I_X(c)
=
\frac12
\left[
\log\det(H_{X,k+c}^{\mathrm{eff}}+\epsilon I)
-
\log\det(H_{X,k}^{\mathrm{eff}}+\epsilon I)
\right].
$$

同时记录增广手眼信息的最小特征值：

$$
\lambda_{\min}
\left(
H_{X,k+c}^{\mathrm{eff}}
\right).
$$

候选先按 $P_{\mathrm{valid}}$ 做硬过滤。当前实现不会再把概率乘进信息增益，因此含义是：

> 先保证候选足够可靠，再在可靠候选中比较信息量。

## 17.4 运动感知效用

对候选IK解 $q_c$ 和当前关节 $q_k$，定义：

$$
d_q=\|q_c-q_k\|_2.
$$

最终用于规划顺序的效用：

$$
U(c)
=
\frac{\Delta I_X(c)}
{1+w_m d_q}.
$$

当前：

$$
w_m=1.
$$

排序键依次为：

1. $U(c)$；
2. $\Delta I_X(c)$；
3. $\lambda_{\min}(H_{X,k+c}^{\mathrm{eff}})$。

---

# 18. 候选级联、MoveIt与安全执行

## 18.1 规划前级联

候选依次检查：

1. 传感器位于当前估计平板的允许侧：
   $$
   n^T(t_{BS}^c-C)\ge0.15 \mathrm{m};
   $$
2. 名义safe ROI余量至少10 mm；
3. 数值IK存在；
4. 最大单关节变化：
   $$
   \|q_c-q_k\|_\infty\le70^\circ;
   $$
5. 总关节距离：
   $$
   \|q_c-q_k\|_2\le1.6 \mathrm{rad};
   $$
6. 与已提交NBV关节位姿距离至少0.10 rad；
7. Sigma点有效概率至少0.8；
8. 信息增益评分。

已失败或已执行候选进入黑名单，不再重复评分。

## 18.2 MoveIt检查

对排序后的候选：

1. 调用 `/check_state_validity` 检查目标状态碰撞；
2. 调用 `/plan_kinematic_path` 检查整条路径；
3. 使用OMPL `RRTConnect`；
4. 3次规划尝试；
5. 最长规划时间5 s；
6. 速度和加速度缩放均为0.2；
7. 目标关节容差 $10^{-3} \mathrm{rad}$。

目标无碰撞不等于整条路径无碰撞，所以两层检查缺一不可。

## 18.3 执行后真实观测

MoveIt轨迹成功后，等待稳定时间。当前全局配置：

$$
t_{\mathrm{settle}}=0.2 \mathrm{s}.
$$

随后收集配置的 $M$ 个同步帧。每帧都必须处于hard domain。平均断点如果越过safe domain但仍在
hard domain，系统警告后接受；这是“规划保守、执行按真实硬边界验收”的两层策略。
测量超时自动取

$$
t_{\mathrm{timeout}}
=
\max\left(t_{\mathrm{configured}},\frac{M}{f_{\min}}+1\ \mathrm{s}\right),
$$

当前 $f_{\min}=2$ Hz，避免增大批量后仍被固定5 s超时误判为漏检。

---

# 19. 同帧同步与多帧事务

## 19.1 为什么同步是精度核心

每个残差都假设：

$$
T_i
\leftrightarrow
\mathcal Q_i^S,p_{u,i}^S,p_{v,i}^S
$$

来自同一物理时刻。若轮廓来自时刻 $t_a$，法兰位姿来自 $t_b$，即使机器人只发生
很小的收敛运动，也会造成不能由固定手眼解释的系统误差。

## 19.2 当前仿真消息协议

无同步扰动时，仿真场景发布端从同一份关节编码器快照生成原始轮廓和
$R_{BF},t_{BF}$。启用第23.2节同步模型后，轮廓由当前物理位姿生成，但
`/calibration/flange_pose` 携带历史缓冲区中 $t-\tau$ 时刻的编码器位姿。这种故意
制造的内容偏差用于模拟触发延迟；它不是DDS到达顺序误差。

三路消息必须具有完全相同的ROS时间戳：

1. `/gocator/profile`：仿真或真实Gocator的原始公制轮廓；
2. `/calibration/endpoints`：检测器从该轮廓得到的两个物理断点；
3. `/calibration/flange_pose`：与该轮廓关联的编码器法兰位姿。

种子节点和主动节点分别维护三份待配对字典，只在时间戳完全一致后生成完整观测。

## 19.3 一个NBV内的多帧如何加入求解

错误方式是：

1. 拼接 $M$ 帧轮廓；
2. 平均断点；
3. 只绑定最后一帧法兰位姿。

这会假设 $M$ 帧具有完全相同的机器人位姿。

当前正确方式先保留同步关系：

$$
\mathcal B_c
=
\left\{
\left(
T_c^{(m)},
\mathcal Q_c^{S,(m)},
p_{u,c}^{S,(m)},
p_{v,c}^{S,(m)}
\right)
\right\}_{m=1}^{M}.
$$

再对断点做MAD整帧筛选，并划分不相交的拟合集与留出集。拟合集在SO(3)切空间和欧氏
空间稳健聚合为一个观测 $(\bar T_c,\bar{\mathcal Q}_c,\bar p_{u,c},\bar p_{v,c})$；
留出集独立聚合，仅做第21.3节验证。这样既没有把不同法兰位姿错误绑定到最后一帧，也
没有把同一物理位姿伪装成 $M$ 个独立几何激励。业务计数为：

$$
N_{\mathrm{NBV}}\leftarrow N_{\mathrm{NBV}}+1,
$$

而不是增加5个机器人位姿。

真实现场应使用硬件触发号、共同时间戳或经过验证的时间同步机制复现这一关系。

---

# 20. 事务化滚动更新与回退

## 20.1 试更新

设当前数据为 $D_k$，新NBV批次为 $\mathcal B_c$。先在临时数据上求解：

$$
\hat\Theta_{\mathrm{trial}}
=
\operatorname{Solve}(D_k\cup\mathcal B_c),
$$

其中：

$$
\Theta=(X,R_P,C).
$$

只有试解通过才提交：

$$
D_{k+1}=D_k\cup\mathcal B_c.
$$

## 20.2 试解验收

当前要求：

- 求解器收敛；
- 约化Jacobian秩为9；
- 条件数合格；
- 一般滚动更新相对上一轮手眼旋转跳变不超过 $5^\circ$；
- 一般滚动更新手眼平移跳变不超过50 mm；
- 平板旋转跳变不超过 $10^\circ$。

这些跳变阈值是故障保护，不是期望标定误差。

第一次NBV具有特殊意义：它是六种子粗解之后的第一批独立观测，可能承担较大的纠偏。
若仍机械使用 $5^\circ$ 门限，会把合理纠偏误判为离群事务。代码因此使用分阶段门限：

$$
\theta_{\max}^{(k)}
=
\begin{cases}
\max(5^\circ,\theta_{\mathrm{Phase0a}}),&k=0,\\
5^\circ,&k\ge1,
\end{cases}
$$

$$
d_{\max}^{(k)}
=
\begin{cases}
\max(50\ \mathrm{mm},d_{\mathrm{Phase0a}}),&k=0,\\
50\ \mathrm{mm},&k\ge1.
\end{cases}
$$

其中 $\theta_{\mathrm{Phase0a}}$ 和 $d_{\mathrm{Phase0a}}$ 分别来自配置中的
`handeye_init_rotation_error_deg` 和 `handeye_init_translation_error_mm`。也可用
`nbv.initial_maximum_update_*` 显式覆盖自动值。第一次成功提交之后立即恢复较紧门限，
所以该机制只允许“有先验边界的首次纠偏”，不会取消事务保护。

## 20.3 回退

若出现：

- 执行后任一帧越过hard domain；
- 测量超时；
- 试解不可观测；
- 事务更新跳变过大；

则：

1. 不修改正式数据集；
2. 拉黑当前候选；
3. 返回执行前关节位姿；
4. 等待新鲜双边数据；
5. 验证回退位姿恢复安全；
6. 重新评分或尝试下一候选。

当前同一NBV步骤最多允许3次候选执行失败。若已经成功提交过至少一个NBV而后续没有
可行候选，系统以“提前结束”保存当前结果；若一个NBV都没有成功，则标记失败。

---

# 21. 自适应停止与历史最佳保留

## 21.1 安全上限

物理位姿总数：

$$
N_{\mathrm{physical}}
=
N_{\mathrm{seed}}+N_{\mathrm{NBV}}.
$$

若：

$$
N_{\mathrm{physical}}\ge26,
$$

无条件停止，即6个种子之后最多再执行20个NBV。这只是异常情况下的紧急保护，不代表
精度目标已经达到。

交互控制台输入0表示完全交给自适应停止；也可输入1～20作为本次实验的可选预算，不再
存在固定“最多6个NBV”的限制。

## 21.2 信息收敛停止

除最大次数外，当前代码先要求下列可观测性条件成立：

1. 至少3个NBV：
   $$
   N_{\mathrm{NBV}}\ge3;
   $$
2. 手眼有效信息满秩：
   $$
   \operatorname{rank}(H_X^{\mathrm{eff}})=6;
   $$
3. 最小有效特征值：
   $$
   \lambda_{\min}(H_X^{\mathrm{eff}})\ge10^{-6};
   $$
然后采用三个并列的停止分支，任一分支满足即可停止：

1. 协方差目标分支：所有手眼旋转轴和平移轴1σ分别不超过
   $0.025^\circ$和$0.05$ mm，停止原因为
   `hand-eye uncertainty target reached`；
2. 信息饱和分支：最佳剩余信息增益连续3轮满足
   $$
   \max_c\Delta I_X(c)<10^{-3}
   \quad\text{或}\quad
   \frac{\max_c\Delta I_{X,k}(c)}
   {\max_c\Delta I_{X,1}(c)}<0.03;
   $$
   停止原因为 `information gain saturated`。
3. 留出验证平台分支：新结果连续3个物理NBV不能把留出帧几何分数改善至少1%，停止原因为
   `held-out validation score plateaued`。

## 21.3 同步批次、留出验证与历史快照

一个静止NBV采集15个严格时间戳配对帧。先对
$(e_u^x,e_u^z,e_v^x,e_v^z)$做联合MAD筛选，再把内点划分为：

$$
\mathcal B_k^{\rm fit}\cap\mathcal B_k^{\rm val}=\varnothing.
$$

拟合帧的法兰旋转在SO(3)切空间取均值、平移和断点取均值、轮廓拼接后均匀降采样，形成
一个物理位姿代表；留出帧独立聚合成一个验证代表。后者从不进入求解和信息矩阵。

对历史结果 $\hat\Theta_j=(\hat X_j,\hat\Pi_j,\hat C_j)$，固定其全部参数，在当前共同留出
集合上计算加权几何残差：

$$
s_j
=
\operatorname{median}_{k}
\sqrt{\frac{1}{m_k}\left\|
A_k(\hat X_j,\hat\Pi_j)\hat C_j-b_k
\right\|_2^2}.
$$

结束时选择留出分数最低的历史区域。考虑少量高噪声留出位姿的统计波动，分数不超过
$1.10\min_j s_j$的快照视为统计等价，并在其中选择最新快照；只有超过10%的实质退化才
回滚到更早结果。整个选择不使用Gazebo真值。

---

# 22. 为什么NBV后的真值误差不单调

NBV优化的是：

$$
\mathbb E[\text{信息量}]
$$

或局部协方差，而不是当前仿真真值误差：

$$
\|\hat X_k-X_{\mathrm{gt}}\|.
$$

加入一批含噪观测后，估计值会受本次噪声具体实现影响。因此可能出现：

- 信息矩阵增强；
- 预测协方差减小；
- 但某一轮旋转或平移真值误差略微增加。

这不是逻辑矛盾。合理的统计评价包括：

- 多次独立重复的均值和分位数；
- 达到目标所需NBV数；
- 协方差覆盖率；
- NBV与随机候选的对照；
- 外部独立几何验证。

但如果实际误差长期远大于预测标准差，则不是普通随机波动，应检查：

1. 轮廓、断点、编码器是否同帧；
2. 机器人位姿是否存在未建模误差；
3. 平板是否刚性平面；
4. 断点是否有方向性偏差；
5. 残差权重与噪声单位是否一致。

---

# 23. 当前仿真、已验证结果与边界

## 23.1 名义仿真真值

当前仿真专用手眼真值：

$$
t_X^{\mathrm{gt}}
=
[-0.011579,-0.004621,0.359284]^T \mathrm{m}.
$$

旋转由：

$$
\operatorname{RPY}_{xyz}
=
[0.485145,0.160648,-1.509479] \mathrm{rad}
$$

生成。

平板：

$$
C=[0.7,0,0.25]^T \mathrm{m},
$$

$$
R_P=I,
\qquad
L_u=0.4 \mathrm{m},
\qquad
L_v=0.5 \mathrm{m}.
$$

这些真值只允许用于：

- 生成仿真测量；
- 构造Phase 0a的已知扰动；
- 打印仿真误差。

它们不进入种子运动、12-DOF-V2残差、候选评分或停止条件。

## 23.2 可配置综合噪声模型

物理与原始轮廓噪声在 `scene_publisher_node.py` 的测量生成端注入，求解器、种子采集和NBV模块
不会收到“这是噪声”的标志。因此它们必须依靠现有的安全域、双边验证、多帧事务和
鲁棒求解链路处理扰动。注入顺序为：

$$
\text{名义机器人位姿}
\rightarrow
\text{真实机器人扰动}
\rightarrow
\text{激光与非理想板面相交}
\rightarrow
\text{原始轮廓噪声、离群与漏检}
\rightarrow
\text{独立断点检测与身份跟踪}
\rightarrow
\text{绑定延迟编码器位姿}.
$$

### 23.2.1 轮廓采样和断点提取

对传感器系二维轮廓点 $q_j^S=[x_j,0,z_j]^T$：

$$
\tilde q_j^S
=
q_j^S+
\begin{bmatrix}
\epsilon_{x,j}\\0\\\epsilon_{z,j}
\end{bmatrix},
\qquad
\epsilon_{x,j},\epsilon_{z,j}
\overset{\mathrm{iid}}{\sim}
\mathcal N(0,\sigma_q^2).
$$

当前

$$
\sigma_q=0.055\ \mathrm{mm}.
$$

场景端不再独立采样 $\sigma_e$。断点误差由固定约0.25 mm的轮廓采样间距、
$\sigma_q$、离群、漏点、板面形状及检测算法共同产生。检测器先按相邻点距离切分连续
段，再用MAD迭代TLS拟合主线，把拟合线两端沿方向各外推半个中位采样间距。参考位姿
100帧纯算法检查得到断点误差中位数约0.079 mm、P95约0.133 mm；实际ROS运行时检测器
报告的单帧估计标准差约0.108 mm。`simulation_noise`下旧断点噪声字段仅为兼容保留，
当前均为0且场景发布器不读取它们生成测量。

### 23.2.2 机器人位姿误差

编码器给出的名义法兰位姿为 $T_{BF}$。用于生成真实测量的隐藏物理位姿为

$$
T_{BF}^{*}
=
\begin{bmatrix}
R_{BF}\operatorname{Exp}([\delta\theta]_\times)&
t_{BF}+\delta t\\
0&1
\end{bmatrix},
$$

其中

$$
\delta t\sim\mathcal N(0,\sigma_t^2I_3),
\qquad
\delta\theta\sim\mathcal N(0,\sigma_R^2I_3).
$$

轮廓由 $T_{BF}^{*}X_{\mathrm{GT}}$ 生成，但消息携带的仍是未经该扰动的编码器
$T_{BF}$。所以这是真正的不可观测模型误差，而不是同时扰动测量和机器人位姿后相互
抵消。默认

$$
\sigma_t=0.030\ \mathrm{mm},
\qquad
\sigma_R=0.003^\circ.
$$

当前实现按帧独立采样，近似重复性和编码器残差；尚未建立齿隙、热漂移和负载相关误差。

### 23.2.3 平板平面度

平面度不能用“每帧随机旋转整块平板”表示，因为真实平板缺陷固定在空间中。代码在板面
归一化坐标 $(\xi,\eta)$ 上生成一次固定的平滑高度场：

$$
h(\xi,\eta)
=
\frac{\sigma_f}{s_h}
\sum_{r=1}^{6}c_r\phi_r(\xi,\eta),
\qquad
c_r\sim\mathcal N(0,1),
$$

其中 $\phi_r$ 由一阶、二阶正弦项和双线性项组成，$s_h$ 在板面 $41\times41$ 网格上
将均方根归一化到1。因此 $\sigma_f$ 就是板面高度场的目标RMS，默认

$$
\sigma_f=0.030\ \mathrm{mm}.
$$

理想交点 $q$ 沿方向

$$
d=n_B-(\ell^Tn_B)\ell
$$

修正为

$$
q^*
=
q+
\frac{h(\xi,\eta)}{n_B^Td}d,
$$

其中 $\ell$ 是激光平面法向。该构造同时满足

$$
\ell^T(q^*-q)=0,
\qquad
n_B^T(q^*-q)=h(\xi,\eta),
$$

即修正点仍在当前激光平面内，同时相对名义板面具有指定高度。它是小变形一阶近似，
而不是对曲面与激光平面重新做非线性求交。

### 23.2.4 同步延迟和抖动

每帧采样

$$
\tau
=
\max\left(
0,\,
\bar\tau+\epsilon_\tau
\right),
\qquad
\epsilon_\tau\sim\mathcal N(0,\sigma_\tau^2).
$$

测量由当前真实位姿生成，而发布的法兰位姿从关节历史缓冲区选择不晚于
$t-\tau$ 的最新快照。默认

$$
\bar\tau=0,
\qquad
\sigma_\tau=1\ \mathrm{ms}.
$$

轮廓、检测断点和法兰位姿使用完全相同的ROS时间戳，所以采集端可以稳定配对；配对后的内容中
保留了待检验的时序误差。机器人静止时该项几乎无影响，运动或未完全稳定时影响增大。

### 23.2.5 离群点

每个轮廓点独立服从Bernoulli变量：

$$
o_j\sim\operatorname{Bernoulli}(p_o).
$$

若 $o_j=1$，则在高斯采样误差之外再加
$\mathcal N(0,\sigma_o^2I_2)$ 大扰动。当前值为：

$$
p_o=0.002,\quad \sigma_o=0.5\ \mathrm{mm}.
$$

### 23.2.6 漏检

场景端区分两层Bernoulli漏检：

1. 点漏检：每个轮廓点以 $p_d$ 被删除；
2. 整帧漏检：以 $p_f$ 清空原始点云。

默认

$$
p_d=0.01,
\qquad
p_f=0.001.
$$

断点漏检不再独立注入；当剩余点数、线性残差、长度、候选唯一性或身份匹配不满足门限
时，检测器自然发布空断点点云。

### 23.2.7 可复现性、关闭方法与生效检查

所有随机项共享 `random_seed: 20260728`，而板面度系数只在场景节点启动时生成一次。
相同配置、相同运动序列和相同发布调用序列可得到相同随机序列；设为 `-1` 才使用
不可复现随机种子。

全部参数在 `config/calibration.yaml` 的 `simulation_noise` 下修改。参数单位固定为
米、度、秒和 $[0,1]$ 概率。修改后必须重启终端1中的仿真。终端2进行环境检查时会通过
`/scene_publisher/noise_status` 打印实际加载值。要建立完全理想数据基线，应把全部
标准差、延迟均值和概率设为0；仅修改随机种子不会关闭噪声。

## 23.3 已完成的名义仿真

下表是引入23.2节综合噪声之前、仅含0.055 mm轮廓高斯噪声的历史基线，用于检查算法
链路和本次噪声扩展是否造成代码回归；不能冒充综合扰动下的鲁棒性统计。

严格同帧同步后，一次6种子+10 NBV结果：

| 阶段 | 旋转真值误差 | 平移真值误差 |
|---|---:|---:|
| 六种子初始化 | 0.0340° | 0.2997 mm |
| NBV 1 | 0.0065° | 0.0362 mm |
| NBV 3 | 0.0033° | 0.0034 mm |
| NBV 10 | 0.0004° | 0.0031 mm |

负深度顺序初始位姿：

$$
\Delta z=z(e_v)-z(e_u)=-96.903 \mathrm{mm}
$$

也已完成真实Gazebo自动运动：

- 动态预检3/4通过；
- 采满6个种子；
- NBV 1：0.0065° / 0.0417 mm；
- NBV 5：0.0019° / 0.0048 mm。

这些结果证明当前仿真代码链路可运行，不构成真实设备精度承诺。

## 23.4 历史直接断点接口下的综合噪声端到端验证

> 本节记录改造前的回归结果。该版本由仿真发布器直接生成断点，现已被第23.9节的
> 原始轮廓检测链路取代，不应再作为当前消息链路的实验结果引用。

2026-07-28 使用第23.2节全部默认扰动、固定随机种子 `20260728`，从默认观察位姿执行
一次完整 Gazebo + MoveIt 运行：

- 动态预检：3/4方向通过；
- 6个自动种子：73.7 s；
- 种子标签：`reference, rx_positive, rx_negative, ry_positive,
  ry_negative_partial, rx_ry_positive`；
- 最小两两旋转间隔：3.428°；
- 旋转激励Gram特征值：$7.4465\times10^{-2},
  1.4756\times10^{-1},2.2180\times10^{-1}$；
- 主动标定：5个NBV全部完成。

真值误差和内部预测标准差如下：

| 阶段 | 旋转真值误差 | 平移真值误差 | 最大旋转预测1σ | 最大平移预测1σ |
|---|---:|---:|---:|---:|
| 六种子初始化 | 0.3121° | 1.3949 mm | 0.0783° | 0.7661 mm |
| NBV 1 | 0.1518° | 0.9228 mm | 0.0417° | 0.3171 mm |
| NBV 2 | 0.1145° | 0.2679 mm | 0.0262° | 0.1053 mm |
| NBV 3 | 0.0616° | 0.3755 mm | 0.0164° | 0.0877 mm |
| NBV 4 | 0.0083° | 0.0421 mm | 0.0126° | 0.0456 mm |
| NBV 5 | 0.0084° | 0.0480 mm | 0.0119° | 0.0329 mm |

第4个NBV首次同时达到 $0.05^\circ/0.1$ mm目标。第5个NBV的预测不确定性继续下降，
但平移真值误差由0.0421 mm小幅上升到0.0480 mm；这再次说明第22章的非单调性不是
异常。结果保存在
`data/calibration_runs/20260728_123647/calibration_result.json`。

这只是固定种子的一次端到端回归，不等于鲁棒性统计结论。正式比较应对每个噪声等级
使用多个随机种子和多个初始位姿，报告成功率、中位数、95%分位数和总耗时。

## 23.5 历史直接断点接口下的高噪声失效归因

2026-07-28 将断点提取、机器人位姿和平面度同时提高约一个数量级后，六种子初解变为
$7.0499^\circ/21.9962$ mm。用同一份数据从真值、默认粗值、$15^\circ/50$ mm等不同
初值重算，均收敛到同一结果。这说明主要原因是观测噪声和模型失配改变了最优点，而不是
非线性优化陷入了由Phase-0a粗值引起的局部极小。

对六个相同几何种子做20次单因素Monte Carlo消融，得到：

| 扰动设置 | 旋转误差中位数 / 95% | 平移误差中位数 / 95% |
|---|---:|---:|
| 默认综合噪声 | 0.249° / 0.541° | 1.782 / 4.101 mm |
| 仅断点标准差改为0.8 mm | 1.861° / 2.924° | 15.414 / 31.144 mm |
| 仅机器人平移改为0.5 mm | 2.159° / 3.482° | 14.573 / 25.536 mm |
| 仅机器人旋转改为0.03° | 1.205° / 2.418° | 8.335 / 15.130 mm |
| 仅平面度改为0.5 mm RMS | 0.253° / 0.530° | 1.913 / 4.207 mm |
| 所有高噪声同时启用 | 3.166° / 5.308° | 19.984 / 44.656 mm |

因此这次流程失败的主导参数依次是：

1. `endpoint_gaussian_std_m=0.0008`；
2. `robot_translation_std_m=0.0005`；
3. `robot_rotation_std_deg=0.03`。

`board_flatness_rms_m=0.0005`会形成后期系统误差地板，但不是六种子初解崩坏的主因；
本次很低的离群概率下，仅把离群幅度从0.5改到0.8 mm也不是主因。Phase-0a的
$15^\circ/50$ mm会要求更宽的第一次NBV纠偏门限，但本身不改变该数据的最终最优解。

修复分阶段门限后，复用同一失败种子，首个NBV把误差从
$7.0499^\circ/21.9962$ mm降到 $0.7090^\circ/2.9572$ mm，10个NBV均完成，最终为
$0.1881^\circ/1.0201$ mm。这证明执行闭环已恢复，但也证明当前高噪声组合不属于
$0.05^\circ/0.1$ mm目标的已验证工作区。

进一步把每个NBV的同步帧数从5提高到20，同一坏种子的5个NBV结果改善为
$0.0968^\circ/0.1724$ mm；主动阶段约101 s，加上原75 s种子阶段约176 s。这个结果
验证了随机项的多帧抑制作用，也显示仅增加帧数尚不能消除固定平面度和单帧种子造成的
误差地板。

随机噪声可以通过同一物理位姿的独立同步多帧降低，近似按 $1/\sqrt M$ 衰减；固定
平面度、机器人系统偏差和断点方向性偏差不会按此规律消失。若断点每坐标高斯标准差
已经达到0.8 mm，仅靠6个单帧种子和有限NBV，不能把0.1 mm当作必达指标。当前论文
方法的改进边界明确为：

- 对六个种子和每个NBV都增加定点同步帧数；
- 对断点四坐标进行整帧稳健筛选，避免混用来自不同帧的两个断点；
- 在NBV开始前检验六种子初解的重采样稳定性；
- 逐项扫描噪声等级，报告成功率和95%分位数，而不是只看一次固定随机种子。

机器人位姿误差变量模型和平板非平面模型不加入当前12-DOF-V2待估状态。二者只作为
仿真适用边界的外部压力项，不扩展论文核心待估变量。当前配置的0.5 mm机器人平移和
0.3 mm板面度属于组合压力测试；它们用于暴露适用边界，不表示本方法已经专门解决这两
类系统误差。

## 23.6 六种子多帧稳健初解

六种子初解决定第一次NBV的线性化点、手眼协方差和候选排序，因此它不是普通的前置
步骤。若初解受到单帧断点误差支配，后续NBV可能围绕错误模型评分，甚至无法获得第一帧
有效纠偏观测。

### 23.6.1 每个物理种子采集一批同步帧

当前每个种子姿态保持静止，目标接收 $M_s=18$ 个通过时间匹配、稳定性检查和双边
检查的观测，最多检查30帧，批次超时6 s。第 $m$ 帧的断点四维向量写为：

$$
y_m=
\begin{bmatrix}
x_{u,m}&z_{u,m}&x_{v,m}&z_{v,m}
\end{bmatrix}^T.
$$

分别计算各坐标的中位数和MAD尺度：

$$
\tilde y_j=\operatorname{median}_m y_{m,j},
$$

$$
s_j=1.4826\operatorname{median}_m|y_{m,j}-\tilde y_j|.
$$

整帧保留条件为：

$$
\max_j
\frac{|y_{m,j}-\tilde y_j|}
{\max(s_j,\epsilon)}
\le 3.5.
$$

这里必须整帧筛选，不能分别挑选两个端点，否则会人为组合出从未同时出现过的测量。
18帧中至少保留14帧才接受该物理种子。

### 23.6.2 一个物理种子只贡献一个几何姿态

数据文件升级为schema 3：保存6个物理种子组，以及每组全部保留的同步原始帧。求解前
每组聚合成一个代表观测：

$$
\bar t=\frac1M\sum_{m=1}^{M}t_m,
$$

$$
\bar R=R_0\operatorname{Exp}
\left(
\frac1M\sum_{m=1}^{M}
\operatorname{Log}(R_0^TR_m)
\right),
$$

$$
\bar p_u=\frac1M\sum_{m=1}^{M}p_{u,m},
\qquad
\bar p_v=\frac1M\sum_{m=1}^{M}p_{v,m}.
$$

轮廓点集合并后均匀降采样到不超过200点。这样随机断点误差近似下降为
$\sigma_e/\sqrt M$，同时求解器仍只看到6个独立物理姿态，不会把同一姿态的18帧错误
计成20倍姿态激励。

### 23.6.3 真值无关的初解稳定性门控

在每个物理种子组内有放回重采样，再聚合为6个观测并重新运行12-DOF-V2。设完整批次
解为 $\hat X$，第 $b$ 个重采样解为 $\hat X_b$，定义：

$$
d_R^{(b)}
=
\left\|
\operatorname{Log}
\left(
\hat R_X^T\hat R_{X,b}
\right)
\right\|,
$$

$$
d_t^{(b)}
=
\|\hat t_{X,b}-\hat t_X\|.
$$

当前在线快速检查使用4次重采样，要求至少80%收敛，并满足：

$$
Q_{0.95}(d_R)\le1.0^\circ,
\qquad
Q_{0.95}(d_t)\le10\ \mathrm{mm}.
$$

该检查不读取仿真真值。若不通过，系统在生成NBV之前停止，明确提示重采种子，而不是
让不稳定初解污染主动规划。

初解的局部Hessian协方差可能低估0.8 mm断点噪声。第一次NBV评分前，再用稳定性分位数
构造保守标准差下界：

$$
\sigma_{R,\min}=\frac{Q_{0.95}(d_R)}{1.96},
\qquad
\sigma_{t,\min}=\frac{Q_{0.95}(d_t)}{1.96}.
$$

手眼协方差对应对角元不得小于这些下界的平方，使19个Sigma点覆盖重采样实际波动范围。

### 23.6.4 0.8 mm断点专项实测

为遵守当前研究边界，断点噪声设置为每坐标
`endpoint_gaussian_std_m=0.0008`，机器人位姿扰动和平板面度保持已验证背景水平。同一份
6姿态、每姿态20帧的数据得到：

| 六种子处理方式 | 旋转误差 | 平移误差 |
|---|---:|---:|
| 每姿态随机取1帧，20次试验中位数 | 2.3177° | 18.7056 mm |
| 每姿态随机取1帧，20次试验95%分位 | 3.6401° | 40.1862 mm |
| 当前20帧MAD筛选与聚合 | 0.6168° | 1.3805 mm |

当前初解的4次bootstrap波动95%分位为
$0.5102^\circ/6.0155$ mm，稳定性门控通过。随后5个NBV全部完成，第一次NBV即纠偏到
$0.0936^\circ/0.6929$ mm，5个NBV后的误差为
$0.0982^\circ/0.3343$ mm。该结果说明初解和执行鲁棒性已经显著改善，但在每坐标
$1\sigma=0.8$ mm断点噪声下，尚不能宣称稳定达到
$0.05^\circ/0.1$ mm目标。

## 23.7 200 mm粗平移初值下的耗时归因与快速配置

`handeye_init_translation_error_mm`只构造Phase-0a名义手眼平移及第一次NBV纠偏门限。
六种子运动完全不使用待标定手眼，因此把它从50 mm改为200 mm不会直接拖慢种子采集。

2026-07-28的慢运行中，真正的两个原因是：

1. 该初始观察位姿在3 mm严格居中条件下触发大量伺服、越界回退和旋转步长减半，
   六种子耗时159 s；
2. 交互输入选择了20个NBV。内部20物理姿态保护最终在14个NBV停止；滚动数据越来越多，
   单轮8维候选前的原16候选评分由约9 s增长到约71 s，主动阶段耗时约670 s。

200 mm粗初值本身仍被12-DOF-V2正确收敛；慢运行的六种子初解为
$0.4694^\circ/2.9199$ mm。

当前快速配置做了以下修改：

- 正式种子的 $|x_m|$ 容差由3 mm放宽为10 mm，仍同时要求双边位于安全域；
- 中间微旋转若 $|x_m|\le25$ mm且安全余量至少15 mm，则直接继续旋转，只在目标角度
  或包络变紧时居中；
- 最大伺服平移由8 mm提高到12 mm，仿真轨迹/稳定等待改为0.45 s/0.15 s；
- 评分候选由16降为8，每个NBV同步帧由20降为15；
- 控制台默认0，即自适应停止，不再施加6个NBV上限；
- 总物理姿态紧急保护为26，即最多20个NBV；
- 修正第21.2节的并列停止逻辑，并增加留出验证平台期和历史快照保留；
- 同一NBV的15帧聚合为一个物理观测，其中3帧独立留出，不再让滚动求解规模按15倍增长。

此外，0.8 mm断点残差不能再与0.055 mm轮廓残差等权。以轮廓权重为1，当前相对白化
权重为：

$$
w_e=w_n=
\left(\frac{\sigma_p}{\sigma_e}\right)^2
=
\left(\frac{0.055}{0.8}\right)^2
=0.00473\approx0.005.
$$

同一份慢运行种子离线重算时，等权初解为
$0.4694^\circ/2.9199$ mm，使用0.005相对权重后为
$0.1151^\circ/1.2178$ mm。该权重来自已配置噪声比，不使用仿真真值调参。

从默认观察位姿重新执行完整流程，保持200 mm粗平移初值和每坐标0.8 mm断点噪声：

| 阶段 | 耗时 |
|---|---:|
| 动态预检与6个20帧种子（历史配置） | 78.1 s |
| 初始化与5个15帧NBV | 82.3 s |
| 自动阶段合计 | 160.4 s |

初解为$0.1372^\circ/1.0866$ mm，5个NBV后为
$0.0109^\circ/0.1101$ mm。自动阶段进入三分钟，但单次平移真值误差仍比0.1 mm高
0.0101 mm。第6个NBV复核使主动阶段增至105.6 s，且由于噪声非单调性并未稳定改善
真值误差，因此默认仍采用5个NBV。

## 23.8 2026-07-29直接断点接口组合压力历史记录

最终代码在下列组合压力配置上完成了一次端到端仿真：

$$
\sigma_{\rm endpoint}=0.8\ {\rm mm},\qquad
\sigma_{\rm robot,t}=0.5\ {\rm mm},\qquad
\sigma_{\rm flatness}=0.3\ {\rm mm},
$$

并使用 $15^\circ/200$ mm 的Phase-0a粗手眼偏差。运行目录为
`data/calibration_runs/20260729_034816`。

| 阶段 | 结果 |
|---|---:|
| 动态预检与六种子 | 99.1 s |
| 种子最小Gram特征值 | $3.2596\times10^{-2}$ |
| 初解bootstrap P95 | 0.6960° / 3.2274 mm，通过 |
| 六种子真值误差（只作仿真评价） | 0.7938° / 4.7976 mm |
| 初始化、5个NBV与自适应停止 | 63.4 s |
| 自动阶段合计 | 162.5 s |
| 最终真值误差（只作仿真评价） | 0.1724° / 1.3696 mm |
| 停止原因 | 留出验证分数连续3轮平台 |

同一参考附近还验证了一个74.9 s的小角度六种子方案，但其bootstrap P95达到
8.0334°/51.8953 mm，已被门控拒绝。这说明不能用相近姿态的数量换取真正的旋转激励。
最终策略因此保留“5°强激励优先、2.5°安全后备”，而不是保留更快但错误的版本。

由本次实跑可得：

1. 自动化与三分钟软目标可以同时达到，本次为162.5 s；
2. 在上述组合压力下，不能同时承诺0.05°/0.1 mm；
3. 这不是继续增加NBV上限就能保证解决的问题：0.8 mm断点即使按约12个拟合内点帧
   独立平均，单物理位姿的端点随机标准差仍约为
   $0.8/\sqrt{12}=0.231$ mm；0.3 mm固定板面度不随同位姿多帧平均消失；
4. 当前论文核心仍是自动、高效标定。机器人误差和平板非平面保持为外部仿真压力项，
   不把它们扩展成新的待估模型；
5. 因而正确实验方式是分别报告常规物理误差配置和组合最坏压力配置的成功率、耗时与
   误差分布，不能把一次压力运行写成普适精度承诺。

## 23.9 2026-07-29原始轮廓断点检测闭环

当前实现完成了下列接口隔离：

$$
\texttt{/gocator/profile}
\rightarrow
\texttt{profile\_endpoint\_detector}
\rightarrow
\texttt{/calibration/endpoints}.
$$

`scene_publisher`只在`/simulation/ground_truth/endpoints`发布真值，端到端运行期间该
话题订阅数为0。原始轮廓、检测断点和`/calibration/flange_pose`具有相同时间戳，种子
与主动节点只消费三路匹配结果。

在当前组合压力配置

$$
\sigma_q=0.055\ {\rm mm},\quad
\sigma_{\rm robot,t}=0.5\ {\rm mm},\quad
\sigma_{\rm flatness}=0.3\ {\rm mm}
$$

下，从仓库参考观察位姿完成一次Gazebo+MoveIt闭环，运行目录为
`data/calibration_runs/endpoint_integration_nB07rg`：

| 阶段 | 结果 |
|---|---:|
| 检测器接受率 | 参考静止阶段100% |
| 轮廓支撑点 | 约446/447 |
| 直线拟合RMS | 约0.056 mm |
| 单帧端点估计σ | 约0.108 mm |
| 动态预检与6个自动种子 | 约56 s |
| 六种子同步观测 | 104帧 |
| 六种子初解 | 1.0700° / 3.8680 mm |
| bootstrap P95 | 1.0080° / 6.7512 mm，通过 |
| 5个NBV | 约50 s |
| 最终真值误差 | 0.1027° / 0.5980 mm |
| 自动阶段合计 | 约106 s |

旧权重$w_e=w_n=0.005$来自0.8 mm直接断点高斯模型。切换到检测器后继续使用旧权重会
把关键双边约束过度降权，使同一六种子数据收敛到22.61°/64.83 mm错误解。依据参考
位姿检测器报告的约0.108 mm单帧端点标准差重新白化：

$$
w_e=w_n
\approx
\left(\frac{0.055}{0.108}\right)^2
\approx0.26,
$$

代码取0.25。调整后初解和NBV闭环恢复。该次结果证明新接口能够完整运行，也再次说明
0.5 mm机器人逐帧误差和0.3 mm固定板面度压力下不能承诺0.05°/0.1 mm。

---

# 24. 当前参数总表

## 24.1 六种子

| 参数 | 当前值 |
|---|---:|
| 种子数 | 6 |
| 目标旋转 | 5° |
| 初始微旋转步长 | 2.5° |
| 最小微旋转步长 | 0.25° |
| 最小部分姿态角 | 2.5° |
| 平移试探 | 1 mm |
| 居中容差 | 10 mm |
| 中间微旋转继续包络 | $|x_m|\le25$ mm，安全余量$\ge15$ mm |
| 最大单次伺服平移 | 12 mm |
| 最大伺服迭代 | 8 |
| 运动后缺测重试窗口 | 1 s |
| 每目标最大失败次数 | 2 |
| 最小种子旋转间隔 | 2° |
| 动态预检角 | 2° |
| 动态预检最大 $|x_m|$ | 30 mm |
| 动态预检最小通过数 | 2/4，且覆盖X/Y两轴 |
| 每个物理种子目标同步帧数 | 18 |
| 最少MAD内点数 | 14 |
| 最大候选帧数 | 30 |
| 批次超时 | 6 s |
| 断点MAD倍数 | 3.5 |
| 初解bootstrap次数 | 4 |
| bootstrap收敛比例 | $\ge80\%$ |
| 初解稳定性95%门限 | 1° / 10 mm |
| 仿真轨迹时间 / 稳定等待 | 0.45 s / 0.15 s |

## 24.2 求解器

| 参数 | 当前值 |
|---|---:|
| 平面权重 $w_p$ | 1 |
| 边约束权重 $w_e$ | 0.25 |
| 断点平面权重 $w_n$ | 0.25 |
| 最大函数求值 | 3000 |
| 三个优化容差 | $10^{-11}$ |
| 手眼旋转尺度 | 10° |
| 手眼平移尺度 | 0.1 m |
| 平板旋转尺度 | 10° |
| 最大条件数 | $10^{12}$ |

## 24.3 NBV

| 参数 | 当前值 |
|---|---:|
| $a,b$采样数 | 各4 |
| 候选边端预留 | 40 mm |
| $\alpha$ | 55°、65°、75° |
| $\psi$ | -15°、0°、15° |
| 工作距离 | 0.33、0.40、0.50 m |
| 最小名义ROI余量 | 10 mm |
| 最小传感器板侧间距 | 150 mm |
| 最低有效概率 | 0.8 |
| 最大评分候选数 | 8 |
| 最大单关节变化 | 70° |
| 最大关节L2距离 | 1.6 rad |
| 已观测位姿最小间距 | 0.10 rad |
| 每NBV同步帧数 | 15 |
| 留出验证帧数 | 3 |
| 最少拟合内点帧数 | 9 |
| 交互NBV预算 | 0=自适应；可选1～20 |
| 物理姿态紧急保护 | 26（6种子+至多20 NBV） |
| 相对信息增益阈值 | 首轮的3% |
| 留出验证平台期 | 连续3轮，最小改善1% |
| 历史等价带 | 最低留出分数的10% |
| 最低预期有效帧率 | 2 Hz |
| 一般手眼旋转/平移更新门限 | 5° / 50 mm |
| 首次NBV更新门限 | 自动不小于Phase-0a不确定度 |
| 平板旋转更新门限 | 10° |

## 24.4 仿真噪声

| 参数 | 已验证基线 | 当前压力配置 |
|---|---:|---:|
| 随机种子 | 20260728 | 20260728 |
| 轮廓高斯标准差 | 0.055 mm | 0.055 mm |
| 直接断点高斯标准差 | 禁用 | 禁用 |
| 隐藏机器人平移标准差 | 0.030 mm | 0.500 mm |
| 隐藏机器人旋转标准差 | 0.003° | 0.003° |
| 固定板面度RMS | 0.030 mm | 0.300 mm |
| 同步延迟均值 | 0 ms | 0 ms |
| 同步抖动标准差 | 1 ms | 1 ms |
| 轮廓点离群概率/幅度 | 0.2% / 0.5 mm | 0.2% / 0.5 mm |
| 轮廓点漏检概率 | 1% | 1% |
| 整帧漏检概率 | 0.1% | 0.1% |
| 参考位姿检测端点估计σ | 约0.108 mm | 约0.108 mm |

---

# 25. 操作者实际使用流程

## 25.1 终端1

```bash
cd /workspace
./scripts/start_simulation.sh --web
```

浏览器：

```text
http://localhost:6080/vnc.html?autoconnect=1&resize=scale
```

## 25.2 终端2

```bash
cd /workspace
./scripts/start_calibration.sh
```

## 25.3 初始位姿怎么调

真实现场看不到空气中的激光平面并不影响操作。按终端反馈：

1. 先确保两个物理断点都存在；
2. 先让断点进入safe domain；
3. 让 $z_m$ 接近400～450 mm；
4. 让 $x_m$ 接近0；
5. 保证安全余量至少20 mm；
6. 保证 $|\Delta z|\ge15$ mm，正负均可；
7. 避开关节限位和腕部翻转；
8. 静态通过后让系统自己执行动态预检。

一次只做小幅调整，然后刷新观察数值变化方向，比试图肉眼想象不可见激光平面更可靠。

---

# 26. 从仿真迁移到真实现场必须补齐的工作

当前代码可作为真实系统算法骨架，但还不能直接声明现场精度。至少需要：

1. 用真实Gocator标定hard/safe trapezoid；
2. 验证断点检测能保持 $E_u/E_v$ 物理身份；
3. 建立硬件触发或PTP级时间同步；
4. 标定机器人基坐标和板固定方式；
5. 测量机器人重复性与绝对位姿误差；
6. 测量断点和轮廓噪声的异方差；
7. 根据噪声方差重新设置 $w_p,w_e,w_n$；
8. 加入鲁棒损失或离群帧检测；
9. 用独立外部几何而非内部残差验收手眼；
10. 做多初始位姿、多噪声等级、多次重复统计；
11. 校准预测协方差的实际覆盖率；
12. 重新辨识初始工作包络和停止阈值。

尤其应避免把当前仿真的：

$$
0.05^\circ/0.1 \mathrm{mm}
$$

当作未经现场实验即可保证的真实指标。

---

# 27. 一页式数学总结

完整数据：

$$
D_k=
\left\{
\left(
{}^BT_{F_i},
\mathcal Q_i^S,
p_{u,i}^S,
p_{v,i}^S
\right)
\right\}_{i=1}^{N}.
$$

状态：

$$
x_9=[\omega_X^T,t_X^T,\omega_P^T]^T.
$$

点变换：

$$
q_i=R_iR_Xp^S+t_i+R_it_X.
$$

残差：

$$
r_p=\sqrt{w_p/N_i}\,n^T(C-q),
$$

$$
r_u=
\begin{bmatrix}
\sqrt{w_e}\,v^T(C-q_u)\\
\sqrt{w_n}\,n^T(C-q_u)
\end{bmatrix},
$$

$$
r_v=
\begin{bmatrix}
\sqrt{w_e}\,u^T(C-q_v)\\
\sqrt{w_n}\,n^T(C-q_v)
\end{bmatrix}.
$$

角点投影：

$$
C^\star(x_9)=\arg\min_C\|A(x_9)C-b(x_9)\|^2.
$$

非线性求解：

$$
\hat x_9=\arg\min_{x_9}\frac12
\|A(x_9)C^\star(x_9)-b(x_9)\|^2.
$$

无量纲Jacobian：

$$
\tilde J=J_9S.
$$

手眼有效信息：

$$
H_X^{\mathrm{eff}}
=
H_{XX}-H_{XP}(H_{PP}+\lambda I)^+H_{PX}.
$$

候选固定法兰：

$$
{}^BT_F^c
=
{}^BT_S^c({}^F\hat T_S)^{-1}.
$$

样本预测：

$$
{}^BT_S^{(s)}
=
{}^BT_F^c{}^FT_S^{(s)}.
$$

有效概率：

$$
P_{\mathrm{valid}}
=
\frac1{19}\sum_s I_s.
$$

信息增益：

$$
\Delta I_X
=
\frac12
\log
\frac{
\det(H_{X,k+c}^{\mathrm{eff}}+\epsilon I)
}{
\det(H_{X,k}^{\mathrm{eff}}+\epsilon I)
}.
$$

运动感知效用：

$$
U(c)=
\frac{\Delta I_X(c)}
{1+w_m\|q_c-q_k\|_2}.
$$

最后形成闭环：

$$
\boxed{
\text{双边安全种子}
\rightarrow
\text{变量投影联合初始化}
\rightarrow
\text{固定法兰鲁棒候选}
\rightarrow
\text{手眼信息增益}
\rightarrow
\text{MoveIt执行}
\rightarrow
\text{同步多帧事务更新}
\rightarrow
\text{自适应停止}
}
$$
