# 基于双边角点约束与信息增益的线激光自动手眼标定方法
## 完整原理与系统流程（当前统一方案 v1）

---

## 1. 方法目标

本方法面向安装在线激光传感器的眼在手机器人系统，目标是在已知矩形平板长宽的条件下，自动完成线激光传感器相对于机器人法兰的手眼标定。

系统需要估计：

$$
{}^{F}T_{S}
=
\begin{bmatrix}
R_{he} & t_{he}\\
0 & 1
\end{bmatrix},
$$

其中：

- $F$：机器人法兰坐标系；
- $S$：线激光传感器坐标系；
- $R_{he},t_{he}$：待求手眼旋转和平移。

与此同时，系统还联合估计标定平板在机器人基坐标系中的几何状态：

$$
R_{pl}=[u,\ v,\ n],\qquad C,
$$

其中：

- $u,v$：从同一物理角点出发的两条相邻平板边方向；
- $n=u\times v$：平板法向；
- $C$：两条相邻边的共享角点；
- $L,W$：用户预先给定的平板长、宽。

整个方法围绕一个核心原则展开：

$$
\boxed{
\text{始终获得同时包含两条相邻边断点的有效轮廓}
+
\text{从有效候选中选择信息量最大的下一位姿}
}
$$

因此，方法不是“先随意移动，再判断有没有数据”，而是：

1. 先构造双边可见的候选测量；
2. 再评价候选能提供多少手眼标定信息；
3. 执行最有价值且足够安全的候选；
4. 使用新数据更新手眼、平板姿态和角点；
5. 循环直到信息增益趋于饱和。

---

## 2. 方法总体结构

方法分为两个自动化阶段。

### 阶段 A：自动种子位姿采集

目标是获得一组旋转方向充分多样、且每帧同时包含两条边断点的初始测量，使 12-DOF-V2 能够稳定启动。

核心动作是：

$$
\boxed{
\text{预设旋转激励}
+
\text{固定旋转下的平移补偿}
}
$$

旋转负责增加标定可观测性，平移负责把两个断点重新调整回传感器有效区域。

### 阶段 B：信息驱动的 NBV 主动采集

当 12-DOF-V2 已经得到可用的手眼、平板和角点估计后，利用已知平板尺寸恢复有限平板范围，在指定两条相邻边上生成目标交点，构造双边可见候选激光线，并根据 12-DOF-V2 的信息增益选择下一位姿。

总体流程为：

```text
TF粗手眼初值
      ↓
人工提供第一个双边可见起始位姿
      ↓
自动生成旋转多样的种子位姿
      ↓
旋转后通过平移补偿恢复双边可见
      ↓
获得约6个有效种子位姿
      ↓
12-DOF-V2联合估计手眼、平板姿态和角点
      ↓
利用角点、平板方向和已知长宽恢复有限平板
      ↓
生成双边可见候选激光线
      ↓
不确定性传播与有效性筛选
      ↓
12-DOF-V2信息增益评分
      ↓
执行最佳候选并采集
      ↓
重新求解12-DOF-V2
      ↓
循环，直至满足停止条件
```

---

# 3. 系统输入、观测与坐标关系

## 3.1 已知输入

系统已知：

1. 每个采集时刻的法兰绝对位姿：

$$
{}^{B}T_{F,i}
=
\begin{bmatrix}
R_i&t_i\\
0&1
\end{bmatrix};
$$

2. 每个位姿下的线激光轮廓点：

$$
\mathcal P_i^S=\{p_{ij}^S\}_{j=1}^{N_i};
$$

3. 两个物理断点：

$$
p_{e1,i}^S,\qquad p_{e2,i}^S;
$$

4. 平板尺寸：

$$
L,\qquad W;
$$

5. 一个通过 TF 或人工测量获得的粗手眼初值：

$$
{}^{F}\hat T_{S,0}.
$$

## 3.2 点从传感器系到基坐标系

任一传感器点变换到机器人基坐标系为：

$$
p_{ij}^B
=
R_i\left(R_{he}p_{ij}^S+t_{he}\right)+t_i.
$$

记：

$$
R_{BS,i}=R_iR_{he},
\qquad
t_{BS,i}=t_i+R_it_{he},
$$

则：

$$
p_{ij}^B=R_{BS,i}p_{ij}^S+t_{BS,i}.
$$

所有平面点、两条边端点和角点约束均建立在该绝对几何关系上。

---

# 4. Phase 0a：手眼名义值获取（粗初值）

Phase 0a 的任务不是标定，而是为后续优化提供一个**有合理误差的名义初值**，模拟实际工作中工人用卷尺和直角尺（或看 CAD 图）对传感器安装的粗略测量。

## 4.1 名义值的生成方式

在仿真环境中，系统直接读取 URDF 中定义的手眼真值 $T_{FS}^*$，然后施加一组可控的随机扰动：

$$
{}^F\hat T_{S,0}
=
\text{perturb}(T_{FS}^*,\ \delta_R,\ \delta_t)
$$

其中：

- $T_{FS}^*$ 是从 URDF 文件 `urdf/calib_robot.urdf` 中提取的手眼变换真值；
- $\delta_R$ 是旋转扰动（默认：绕随机轴旋转 $3^\circ$）；
- $\delta_t$ 是平移扰动（默认：沿随机方向偏移 $10\ \text{mm}$）。

扰动幅度通过配置文件
`ros2_ws/src/handeye_sim_bridge/config/calibration.yaml` 控制：

```yaml
handeye_init_rotation_error_deg: 3.0       # 旋转扰动 (°)
handeye_init_translation_error_mm: 10.0    # 平移扰动 (mm)
```

## 4.2 名义值的使用

该名义值 ${}^F\hat T_{S,0}$ 仅作为：

- 12-DOF-V2 非线性优化的起始点；
- Phase 0b 种子运动中的几何预测模型；
- 候选生成中的名义轮廓预测。

## 4.3 设计意图

### 为什么要扰动？

如果直接使用 URDF 真值作为初值，12-DOF-V2 在一帧数据都不需要的情况下就能得到"完美"结果，这掩盖了算法的真实收敛能力和鲁棒性。加扰动的目的是：

1. **模拟实际工况**：真实机器人上没有 TF 可以直接读取完美手眼，工人只能用尺量——这个 $\delta$ 模拟的就是那"一尺之差"。
2. **测试优化器的鲁棒性**：一个好的初值应该使 12-DOF-V2 从 $3^\circ/10\text{mm}$ 的偏差收敛到亚毫米/百分度的精度。
3. **使评价更有意义**：只有从非完美的初值出发，标定误差才是真实的收敛误差。

### 为什么不直接用 TF 初值？

在真实机器人上，本系统的推荐作业流程是操作员通过手动示教几个粗略位姿（Phase 0b 种子采集）后，直接从种子数据启动 12-DOF-V2。仿真中的 Phase 0a 只是替代了这个"人工输入初值"的环节，用 URDF 真值加扰动来产生结果相当的名义误差。

> 在实物部署时，Phase 0a 将被替换为操作员手动输入的手眼估计值（例如通过传感器外壳上的安装尺寸计算）。

---

# 5. Phase 0b：基于预设多轴旋转和无标定双边特征伺服的自动种子采集

## 5.1 为什么需要种子阶段

在没有稳定手眼和平板估计时，系统还无法可靠预测任意候选位姿下的未来轮廓，因此不能立即运行完整 NBV。

种子阶段的任务不是追求最优信息增益，而是：

1. 始终保持两个断点可见；
2. 生成多个不同旋转方向；
3. 避免出现"姿态 A、姿态 A、姿态 B、姿态 B"这类旋转退化；
4. 为 12-DOF-V2 提供稳定启动数据。

## 5.2 核心设计原则

Phase 0b **不应该依赖手眼初值，也不需要虚拟锚点**。

如果运动预测依赖粗手眼，就会出现逻辑上的循环：

> 需要种子数据才能得到可靠手眼，但又需要可靠手眼才能自动采集种子数据。

因此，Phase 0b 的核心设计是：

$$
\boxed{
\text{预设法兰旋转激励}
+
\text{无标定的轮廓特征平移伺服}
}
$$

它与人工示教的过程完全一致：

$$
\text{小步旋转}
\rightarrow
\text{观察两个断点}
\rightarrow
\text{平移修正}
\rightarrow
\text{再小步旋转}
\rightarrow
\text{再平移修正}.
$$

三个关键优势：

1. **不依赖待标定的手眼关系** — 全程不使用 ${}^F T_S$；
2. **完全复现已验证有效的人工操作经验**；
3. **只需要学习局部输入—输出关系，不需要恢复完整三维几何**。

## 5.3 初始人工输入

第一版系统要求操作者只完成一次操作：

> 将机械臂移动到一个能够稳定看到同一平板角点两条相邻边的起始位姿。

系统保存：

- 当前法兰位姿 ${}^B T_F$；
- 完整线激光轮廓点；
- $e_1,e_2$ 两个断点；
- 当前双边反馈状态（见 §5.4）；
- 当前有效位姿作为回退状态。

## 5.4 "当前双边视觉特征"的定义

原文中"当前双边视觉特征"说得过于抽象——它并不是一个额外测量，本质上只是从两个断点中计算出的控制量。

设两个断点在传感器坐标系中为：

$$
p_{e1}^S=[x_1,0,z_1]^T,
\qquad
p_{e2}^S=[x_2,0,z_2]^T.
$$

最核心的视觉特征是两个断点的中点：

$$
x_m=\frac{x_1+x_2}{2},
\qquad
z_m=\frac{z_1+z_2}{2}.
$$

还可以计算轮廓长度：

$$
L_p=\|p_{e1}^S-p_{e2}^S\|.
$$

对目前的控制方法来说，真正用于闭环控制的只有：

$$
\boxed{x_m}
$$

其目标值为：

$$
x_m^\star=0.
$$

其他量主要用于安全判断：

- $\text{valid}_{e1}$：第一个断点是否存在；
- $\text{valid}_{e2}$：第二个断点是否存在；
- $x_1,x_2,z_1,z_2$：两个断点是否还在量程内；
- $L_p$：轮廓是否过短或异常；
- 端点到 ROI 边界的距离：是否即将丢失。

因此，文档中的"当前双边视觉特征"应明确为：

```
当前双边反馈状态：
  x_mid        两个断点的中点 X 坐标，作为平移伺服误差
  endpoint_1/2 两个断点坐标
  valid_e1/e2  双边可见状态
  roi_margin   两个端点距离传感器量程边界的最小余量
  profile_length 轮廓长度，作为质量检查
```

## 5.5 种子旋转计划（外层运动计划）

设起始法兰旋转为 $R_0$，采用**星形路径**——每次都从同一个已知参考姿态出发，而不是连续累积复杂旋转。

默认生成约 6 个目标朝向：

$$
\begin{aligned}
R_1 &= R_0,\\
R_2 &= R_0 R_x(+\theta),\\
R_3 &= R_0 R_x(-\theta),\\
R_4 &= R_0 R_y(+\theta),\\
R_5 &= R_0 R_y(-\theta),\\
R_6 &= R_0 R_x(+\theta) R_y(+\theta).
\end{aligned}
$$

其中：

- $R_x$：绕当前法兰局部 $X$ 轴旋转；
- $R_y$：绕当前法兰局部 $Y$ 轴旋转；
- 初始角度建议从 $\theta=10^\circ\sim15^\circ$ 开始；
- 每一步旋转步长 $\Delta\theta_{\rm step}\approx 2^\circ\sim3^\circ$；
- $R_6$ 通过两个单轴阶段实现：先绕 $X$ 蠕动到 $+\theta$，双边稳定后再绕新的局部 $Y$ 轴蠕动到 $+\theta$。

采用星形路径的优点是：

- 每次都从同一个已知参考姿态 $R_0$ 出发；
- 旋转目标易于定义；
- 不会因连续累计旋转造成越来越复杂的姿态；
- 失败时可以退回已知安全的 $R_0$；
- 采集的朝向分布容易检查。

返回 $R_0$ 的中间过程不作为新种子保存。

## 5.6 无标定平移伺服（内层执行控制）

### 5.6.1 为什么不使用虚拟锚点

原方案中使用虚拟锚点预测旋转后的法兰平移：

$$
P_a^B = {}^B T_F \; {}^F \hat T_S \; p_a^S,
\qquad
t_{BS}' = P_a^B - R_{BS}' p_a^S.
$$

这个计算显式依赖 ${}^F \hat T_S$。在初始手眼平移和旋转均不准确时，预测出的基坐标锚点可能存在明显误差。特别是法兰旋转时，由于传感器相对法兰存在未知杆臂，传感器不仅发生旋转，还会发生无法准确预测的平移。

人工操作没有试图计算这种平移，而是：

1. 先转一点；
2. 直接观察传感器中的断点变化；
3. 再根据实际变化移动法兰。

这比基于错误模型预测更可靠。

### 5.6.2 一维平移伺服（第一版）

对于当前种子过渡，选定一个法兰局部旋转轴 $a_r\in\{e_x,e_y\}$。

每次只旋转一个小角度（右乘，表示绕当前法兰局部坐标轴旋转）：

$$
R_{BF}^+ = R_{BF} \exp([a_r]_\times \Delta\theta).
$$

完成这一小步旋转后：

1. 读取线激光轮廓；
2. 检测两个断点；
3. 计算 $x_m = (x_1+x_2)/2$；
4. **固定法兰旋转**；
5. 只调整法兰平移，使 $x_m \to 0$。

控制完成后，再执行下一次小步旋转。

**关键问题：不使用手眼，怎么知道法兰应该往哪边平移？**

从法兰局部三个平移方向中选择一个方向 $d_F\in\{e_x,e_y,e_z\}$。令沿该方向的平移量为 $q$，局部关系为：

$$
\Delta x_m \approx g \cdot \Delta q,
$$

其中 $g = \partial x_m/\partial q$ 是未知的局部灵敏度。

通过一次很小的试探平移 $\Delta q_{\rm probe}$，测量 $\Delta x_m = x_m^{\rm after} - x_m^{\rm before}$，得到：

$$
\hat g = \frac{\Delta x_m}{\Delta q_{\rm probe}}.
$$

之后计算修正：

$$
\Delta q = -\lambda \frac{x_m}{\hat g},
$$

并进行限幅 $|\Delta q| \le q_{\max}$。

如果 $x_m>0$ 且 $\hat g>0$，控制器就沿负方向移动；如果实际效果相反，新的测量会自动修正 $\hat g$ 的符号。

### 5.6.3 自动选择平移轴

在初始有效状态下，分别沿法兰局部 $X,Y,Z$ 方向做极小试探：

$$
\hat g_x = \frac{\Delta x_m}{\Delta q_x},
\quad
\hat g_y = \frac{\Delta x_m}{\Delta q_y},
\quad
\hat g_z = \frac{\Delta x_m}{\Delta q_z}.
$$

选择灵敏度最高的轴：

$$
j^\star = \arg\max_j |\hat g_j|.
$$

随后在当前一段蠕动过程中，主要沿该轴做一维伺服。相比直接同时控制三个平移自由度，这种方法：

- 更容易实现；
- 更容易判断运动方向；
- 更不容易产生无关平移；
- 与人工实际操作最接近。

### 5.6.4 在线更新平移灵敏度

随着法兰朝向变化，$g = \partial x_m/\partial q$ 也会发生变化，不能始终使用最初的值。

每次实际执行平移后，都可以用实测值更新：

$$
g_{\rm measured} = \frac{x_{m,k+1} - x_{m,k}}{q_{k+1} - q_k},
\qquad
\hat g_{k+1} = (1-\eta)\hat g_k + \eta\, g_{\rm measured},
$$

其中 $0<\eta\le 1$。

也可以使用 Broyden 形式更新三维 Jacobian：

$$
J_{x,k+1} = J_{x,k} + \frac{(\Delta x_m - J_{x,k}\Delta t_F)\Delta t_F^T}{\Delta t_F^T \Delta t_F}.
$$

这种在线更新直接利用"刚才移动了多少、断点中点变化了多少"，不需要知道传感器相对于法兰的位置。

### 5.6.5 三维平移伺服（后续扩展）

如果后续发现单一轴无法在所有姿态下稳定控制，可以估计完整 Jacobian：

$$
J_x = \begin{bmatrix}
\frac{\partial x_m}{\partial t_x^F} &
\frac{\partial x_m}{\partial t_y^F} &
\frac{\partial x_m}{\partial t_z^F}
\end{bmatrix}.
$$

局部关系为 $\Delta x_m \approx J_x \Delta t_F$，取最小范数修正：

$$
\Delta t_F = -\lambda \frac{J_x^T}{J_x J_x^T + \mu} x_m.
$$

法兰局部平移转换为基坐标平移：$\Delta t_B = R_{BF} \Delta t_F$，因此：

$$
t_{BF}^+ = t_{BF} + R_{BF} \Delta t_F.
$$

整个过程中仍然不需要 ${}^F T_S$。不过第一版建议先实现一维版本（§5.6.2），因为控制目标只有一个标量 $x_m$，直接开放三个平移自由度会存在多解，容易产生不必要的漂移。

### 5.6.6 仅让 $x_m\to 0$ 是否足够

严格来说，$x_m=0$ 不能数学上保证两个断点始终存在——例如两个断点可能同时向传感器 $z$ 量程外移动，但中点的 $x$ 仍然为零。

不过人工经验表明，在当前机械结构和小步旋转范围内，控制 $x_m$ 是维持双边可见的主要因素。因此第一版采用：

- **主控制目标**：$x_m \to 0$。
- **硬安全条件**：两个断点必须同时满足：

$$
x_{\min} + m_x < x_j < x_{\max} - m_x,
\qquad
z_{\min} + m_z < z_j < z_{\max} - m_z,
\quad j=1,2.
$$

同时要求 $L_{\min} < L_p < L_{\max}$。

定义最小 ROI 余量：

$$
m_{\rm ROI} = \min_{j=1,2}
\left\{
x_j - x_{\min},\;
x_{\max} - x_j,\;
z_j - z_{\min},\;
z_{\max} - z_j
\right\}.
$$

只有 $m_{\rm ROI} > m_{\rm safe}$ 才允许继续执行下一小步旋转。控制结构为：

$$
\boxed{
x_m=0 \text{ 作为伺服目标}
+
\text{双边存在和 ROI 余量作为硬约束}
}
$$

如果实际测试中出现明显的 $z$ 方向漂移，再把 $z_m - z_m^{\rm ref}$ 加入第二个控制特征，形成二维特征伺服：

$$
s = \begin{bmatrix} x_m \\ z_m - z_m^{\rm ref} \end{bmatrix}.
$$

第一版没有必要立即增加复杂度。

## 5.7 端点身份必须连续跟踪

小步运动还有一个重要优势：可以通过时间连续性保持 $e_1,e_2$ 的身份。

假设上一帧端点为 $p_{e1,k}, p_{e2,k}$，当前检测到两个无标签端点 $q_1,q_2$。

比较两种匹配代价：

$$
D_1 = \|q_1 - p_{e1,k}\|^2 + \|q_2 - p_{e2,k}\|^2,
\qquad
D_2 = \|q_2 - p_{e1,k}\|^2 + \|q_1 - p_{e2,k}\|^2.
$$

取代价较小的匹配。如果 $|D_1 - D_2|$ 过小，说明身份判断不可靠，此帧不保存。

这一步非常重要，因为 12-DOF-V2 要求：
- $e_1$ 始终对应 $u$ 边；
- $e_2$ 始终对应 $v$ 边。

标签一旦交换，会直接污染角点约束。

## 5.8 蠕动停止条件

不再用肉眼观察法兰坐标轴角度，而是直接使用机器人返回的法兰旋转矩阵。

设本次种子过渡开始时的旋转为 $R_{\rm start}$，当前旋转为 $R_{\rm current}$。相对旋转：

$$
\Delta R = R_{\rm start}^T R_{\rm current},
\qquad
\theta_{\rm rel} = \cos^{-1}\left(\frac{\operatorname{tr}(\Delta R)-1}{2}\right).
$$

当满足：

- $\theta_{\rm rel} \ge \theta_{\rm target}$；
- $|x_m| < \varepsilon_x$；
- $m_{\rm ROI} > m_{\rm safe}$；
- 连续 $N$ 帧双边稳定；

时，停止本次蠕动并保存种子位姿。

也可以直接累计旋转角度（因为每次旋转指令本身就是已知的）：

$$
\theta_{\rm acc} = \sum_k |\Delta\theta_k|.
$$

机器人法兰旋转由编码器直接给出，这里不需要手眼关系。

## 5.9 自动种子状态机

```
MANUAL_INITIAL_POSE
  人工找到第一个双边可见位姿
  ↓
SAVE_REFERENCE
  保存 T_BF、两个断点、x_mid 和 ROI 余量
  ↓
SELECT_ROTATION_TARGET
  选择局部 X/Y 轴和目标角度
  ↓
MICRO_ROTATE
  绕指定法兰轴旋转 Δθ
  ↓
READ_PROFILE
  检测两个断点并保持 e1/e2 身份
  │
  ├─ 两个断点均存在
  │   ↓
  │   TRANSLATION_SERVO
  │     固定旋转
  │     调整法兰局部平移，使 x_mid → 0
  │
  ├─ 只剩一个断点
  │   ↓
  │   回退上一有效位姿，旋转步长减半
  │
  └─ 两个断点均丢失
      ↓
      立即回退上一有效位姿，放弃盲目搜索
  ↓
CHECK_FEATURE_SAFETY
  双边存在、ROI 余量足够、轮廓有效
  │
  ├─ 目标角度未达到 → MICRO_ROTATE
  └─ 目标角度已达到
      ↓
      STABILITY_CHECK
      ↓
      SAVE_SEED
      ↓
      RETURN_REFERENCE
      ↓
      SELECT_NEXT_AXIS
```

## 5.10 失败回退

系统始终保存：

```
last_valid_pose
last_valid_profile
last_valid_endpoints
last_valid_x_mid
last_valid_roi_margin
```

如果只剩一个断点或两个断点全部丢失：

1. 回退到上一个有效位姿；
2. 将旋转步长减半；
3. 重新进行平移试探；
4. 再进行局部平移搜索；
5. 多次失败则放弃该目标旋转。

## 5.11 种子位姿验收

只有满足以下条件，当前状态才作为正式种子位姿保存：

1. $e_1,e_2$ 均被检测到；
2. 两个端点属于同一角点的指定两条相邻边；
3. 轮廓点数和线段长度足够；
4. 两个端点远离传感器 ROI 边界；
5. 当前旋转已达到目标朝向；
6. 连续若干帧测量稳定；
7. 新姿态提高已有姿态集合的旋转多样性。

可构造旋转多样性矩阵：

$$
G_R = \sum_{i<j} (R_i - R_j)^T (R_i - R_j).
$$

当 $\lambda_{\min}(G_R)$ 过小时，说明仍存在旋转弱方向，需要继续采集不同旋转轴的位姿。

默认目标为 6 个有效种子位姿，但"6"不是无条件充分保证。若约化 Jacobian 仍然病态，则继续补采。

## 5.12 更新后的完整 Phase 0b 流程

```
1. 人工将机器人移动到一个双边稳定可见位姿
2. 保存：
   - 当前 T_BF
   - 完整轮廓
   - e1/e2 端点
   - x_mid
   - ROI 安全余量
   - last_valid_pose
3. 设定种子旋转计划：R0, Rx+, Rx-, Ry+, Ry-, Rx+Ry+
4. 对每个目标姿态：
   a. 选择当前法兰局部旋转轴
   b. 旋转 2°~3°
   c. 读取两个端点
   d. 固定旋转
   e. 通过小幅试探确定平移修正方向
   f. 平移使 x_mid 趋近 0
   g. 检查两个端点和 ROI 安全余量
   h. 安全则继续下一次小步旋转
   i. 失败则回退并减小步长
   j. 累积角度达到目标且连续稳定后保存种子
5. 采集约 6 个旋转多样的有效种子
6. 使用 TF 粗手眼作为优化初值，运行 12-DOF-V2
7. 检查约化 Jacobian：
   - 数值秩
   - 最小奇异值
   - 条件数
   - 平移—角点弱方向
8. 若仍然病态：
   - 根据弱方向补充新的旋转轴种子
   - 而不是简单增加相同方向的位姿
9. 初始化通过后，进入基于平板几何和信息增益的正式 NBV 阶段
```

## 5.13 方法定位总结

Phase 0b **不是**：

> 粗手眼 → 预测虚拟锚点 → 计算下一法兰位姿

而是：

$$
\boxed{
\text{法兰局部轴小步旋转}
\rightarrow
\text{直接观察轮廓变化}
\rightarrow
\text{无标定局部平移伺服}
\rightarrow
\text{双边稳定}
}
$$

最核心的控制原则是：

$$
\boxed{
\begin{aligned}
&\text{旋转轴和旋转角度由种子计划给定；}\\
&\text{平移方向由实际轮廓反馈在线确定；}\\
&\text{中点 X 坐标用于闭环控制；}\\
&\text{双边存在和 ROI 余量用于安全约束；}\\
&\text{全程不使用手眼关系。}
\end{aligned}
}
$$

---

# 6. Phase 0c：12-DOF-V2联合初始化

## 6.1 状态变量

完整状态为：

$$
\Theta=
\{
R_{he},t_{he},R_{pl},C
\},
$$

共 12 个自由度：

- 手眼旋转 $R_{he}$：3 DoF；
- 手眼平移 $t_{he}$：3 DoF；
- 平板姿态 $R_{pl}$：3 DoF；
- 角点位置 $C$：3 DoF。

令：

$$
R_{pl}=[u,v,n].
$$

## 6.2 几何残差

对于第 $i$ 帧中的任意平面点 $p_{ij}^B$：

$$
r_{p,ij}
=
n^T(p_{ij}^B-C).
$$

对于位于 $u$ 边上的端点 $p_{e1,i}^B$：

$$
r_{e1,i}
=
v^T(p_{e1,i}^B-C).
$$

对于位于 $v$ 边上的端点 $p_{e2,i}^B$：

$$
r_{e2,i}
=
u^T(p_{e2,i}^B-C).
$$

两个端点还必须位于平板平面：

$$
r_{e1,n,i}
=
n^T(p_{e1,i}^B-C),
$$

$$
r_{e2,n,i}
=
n^T(p_{e2,i}^B-C).
$$

因此，每一帧同时提供：

- 密集平面约束；
- $e_1$ 边的面内约束；
- $e_2$ 边的面内约束；
- 两个端点的平面约束；
- 所有帧共享同一个角点 $C$。

## 6.3 密集点权重

同一帧大量轮廓点高度相关，不能把所有点视为完全独立信息。

设置：

$$
w_{\rm plane}
=
\frac{N_{\rm pose}}{N_{\rm plane\ points}},
$$

使所有平面点块的总权重与位姿数量处于同一量级，避免平面点数量完全压制两条边端点约束。

后续实现中更推荐每帧等数量采样，并采用按帧归一权重。

## 6.4 角点变量投影

给定：

$$
x_9=
[\omega_{he},t_{he},\omega_{pl}]
$$

后，所有残差对 $C$ 都是线性的，可写为：

$$
A(x_9)C\approx b(x_9).
$$

角点通过线性最小二乘解析求解：

$$
C^\star(x_9)
=
\arg\min_C
\|A(x_9)C-b(x_9)\|^2.
$$

非线性优化只需要求解：

$$
\min_{x_9}
\left\|
A(x_9)C^\star(x_9)-b(x_9)
\right\|^2.
$$

因此：

- 最终模型仍是完整12自由度；
- 非线性优化维度由12维降低为9维；
- 每次改变手眼或平板姿态时，角点都会重新计算；
- $C$不是初始化后永久固定的常量。

## 6.5 初始化输出

12-DOF-V2输出：

$$
\hat X_0={}^F\hat T_{S,0},
$$

$$
\hat R_{pl,0}
=
[\hat u_0,\hat v_0,\hat n_0],
$$

$$
\hat C_0.
$$

同时应计算：

- 约化9维 Jacobian；
- 奇异值；
- 数值秩；
- 条件数；
- 近似协方差；
- 最弱参数方向。

只有当 Jacobian 具有足够秩且条件数可接受时，才能进入正式 NBV 阶段。

---

# 7. 利用平板尺寸恢复有限平板

已知当前估计：

$$
C_k,\quad u_k,\quad v_k,\quad n_k
$$

以及平板尺寸：

$$
L,\quad W,
$$

可恢复四个名义角点：

$$
\begin{aligned}
C_{00}&=C_k,\\
C_{10}&=C_k+Lu_k,\\
C_{01}&=C_k+Wv_k,\\
C_{11}&=C_k+Lu_k+Wv_k.
\end{aligned}
$$

我们持续使用从 $C_k$ 出发的两条相邻边：

$$
E_u=
\{C_k+a u_k\mid0\le a\le L\},
$$

$$
E_v=
\{C_k+b v_k\mid0\le b\le W\}.
$$

这一步将“无限平面估计”转化为“有限物理平板模型”，从而使系统能够明确预测：

- 激光线会与哪两条边相交；
- 端点是否位于真实边段内部；
- 是否误切换到另外两条远端边；
- 两个端点是否仍在传感器有效量程内。

---

# 8. Phase 2：双边可见候选生成

## 8.1 候选参数化

候选不再由任意平面内中心和方向参数化，而是在指定两条边上选择两个目标交点：

$$
A=C_k+a u_k,
$$

$$
B=C_k+b v_k.
$$

其中：

$$
m_u\le a\le L-m_u,
$$

$$
m_v\le b\le W-m_v.
$$

$m_u,m_v$ 是边上安全裕量，用于避免目标交点靠近角点或边的远端。

候选参数为：

$$
c=(a,b,\alpha,\psi,h,\mathrm{sign}),
$$

其中：

- $a,b$：两条边上的目标交点位置；
- $\alpha$：激光平面与平板之间的夹角；
- $\psi$：传感器在激光平面内的旋转自由度；
- $h$：工作距离；
- `sign`：激光平面法向的离散方向分支。

目标激光线方向：

$$
d_\ell
=
\frac{B-A}{\|B-A\|}.
$$

## 8.2 构造候选激光平面

定义：

$$
q=n_k\times d_\ell.
$$

由于 $d_\ell$位于平板内，$q$也位于平板内且与 $d_\ell$正交。

候选激光平面法向可以构造为：

$$
m_L
=
\cos\alpha\,n_k+\sin\alpha\,q.
$$

其满足：

$$
m_L^Td_\ell=0,
$$

因此经过 $A,B$ 且法向为 $m_L$ 的激光平面，与平板的交线就是目标直线 $AB$。

需要避免：

$$
\alpha\approx0,
$$

因为这会使激光平面与平板接近重合，造成几何退化。

## 8.3 构造传感器位姿

若传感器激光平面在传感器坐标系中为：

$$
y_S=0,
$$

则其法向为传感器 $y$ 轴。令：

$$
y_S^B=\pm m_L.
$$

再利用 $d_\ell$、平面内转角 $\psi$ 和工作距离 $h$ 构造完整传感器旋转和平移，得到：

$$
{}^BT_{S,c}.
$$

利用当前手眼估计反求候选法兰指令：

$$
{}^BT_{F,c}
=
{}^BT_{S,c}
({}^F\hat T_{S,k})^{-1}.
$$

在后续不确定性分析中，所有状态样本都必须使用同一个：

$$
{}^BT_{F,c},
$$

因为机器人最终只会执行一个固定法兰指令。

---

# 9. 边感知的未来轮廓预测

## 9.1 正确的几何求交顺序

不能直接把激光直线与传感器 ROI 的交点当作物理平板端点。

正确顺序是：

```text
候选激光平面
   ∩
平板平面
   ↓
无限交线
   ∩
有限矩形平板
   ↓
两个物理边缘断点
   ↓
变换到传感器坐标系
   ↓
检查两个端点是否位于传感器ROI
```

## 9.2 边身份判断

在平板局部坐标中：

$$
p=C+\xi u+\eta v.
$$

四条边分别是：

$$
\eta=0,\quad
\eta=W,\quad
\xi=0,\quad
\xi=L.
$$

系统要求两个端点必须分别属于：

$$
E_u:\eta=0,
$$

$$
E_v:\xi=0.
$$

若端点落在：

$$
\eta=W
\quad\text{或}\quad
\xi=L,
$$

说明激光线已经切换到另外两条远端边，该候选应判为无效。

## 9.3 预测输出

每个候选预测应返回：

- 平面轮廓点 `samples_S`；
- $e_1$端点 `p_e1_S`；
- $e_2$端点 `p_e2_S`；
- 两个端点对应的物理边编号；
- 轮廓长度；
- 传感器 ROI 裕量；
- 边段裕量；
- 夹角和工作距离；
- 无效原因。

候选名义有效条件包括：

1. 激光平面与平板具有唯一交线；
2. 交线与有限平板有两个有效交点；
3. 两个交点分别属于 $E_u,E_v$；
4. 两个物理端点均位于传感器 ROI；
5. 轮廓长度和点数满足要求；
6. 工作距离与入射角合理；
7. 法兰位姿具有 IK 解；
8. 不违反关节限位、奇异性和碰撞约束。

---

# 10. 联合不确定性感知的有效性判断

## 10.1 为什么不能只用名义解

当前估计：

$$
\hat X_k,\quad
\hat R_{pl,k},\quad
\hat C_k
$$

都存在误差。名义预测能够截到两条边，不代表真实执行后仍能截到同两条边。

因此，需要评价：

$$
P_{\rm valid}(c)
=
P\left(
\text{候选 }c\text{ 执行后仍产生指定双边观测}
\right).
$$

## 10.2 9维约化状态不确定性

12-DOF-V2的非线性状态为：

$$
x_9=
[\omega_{he},t_{he},\omega_{pl}].
$$

由当前约化 Jacobian $J_9$ 构造近似信息矩阵：

$$
H_9=J_9^TWJ_9.
$$

协方差近似为：

$$
\Sigma_{x9}
\approx
\hat\sigma_r^2
(H_9+\lambda I)^{-1},
$$

其中：

- $\hat\sigma_r^2$：残差方差估计；
- $\lambda$：数值阻尼和误差下限。

从 $\Sigma_{x9}$ 生成 Sigma 点：

$$
x_9^{(s)}.
$$

对于每个样本，重新通过全部已有数据求解：

$$
C^{(s)}
=
C^\star(x_9^{(s)}).
$$

因此每个样本包含：

$$
X^{(s)},\quad
R_{pl}^{(s)},\quad
C^{(s)}.
$$

## 10.3 固定法兰指令进行预测

对候选 $c$，机器人指令固定为：

$$
{}^BT_{F,c}.
$$

第 $s$ 个状态样本下，实际传感器位姿为：

$$
{}^BT_{S,c}^{(s)}
=
{}^BT_{F,c}\,X^{(s)}.
$$

然后使用：

$$
R_{pl}^{(s)},C^{(s)},L,W
$$

重新构造有限平板，预测实际激光交线和两个断点。

样本有效条件为：

- 仍与指定的 $E_u,E_v$ 相交；
- 两个端点均位于 ROI；
- 轮廓长度、点数和几何质量合格；
- 没有切换到远端两条边。

有效概率为：

$$
P_{\rm valid}(c)
\approx
\frac{1}{N_s}
\sum_{s=1}^{N_s}
\mathbf 1[\operatorname{Valid}(c,x_9^{(s)})].
$$

只保留：

$$
P_{\rm valid}(c)\ge p_{\min}
$$

的候选。

---

# 11. 基于12-DOF-V2的信息增益评分

## 11.1 当前信息

当前全部测量集记为：

$$
D_k.
$$

在当前估计处，对完整变量投影残差计算约化 Jacobian：

$$
J_k
=
\frac{\partial r(x_9,D_k)}{\partial x_9}.
$$

计算：

$$
H_k=J_k^TWJ_k.
$$

将9维状态划分为：

$$
x_9=
\begin{bmatrix}
x_X\\
x_P
\end{bmatrix},
$$

其中：

- $x_X\in\mathbb R^6$：手眼参数；
- $x_P\in\mathbb R^3$：平板姿态参数。

角点 $C$ 已通过变量投影隐式消除。

信息矩阵分块为：

$$
H_k=
\begin{bmatrix}
H_{XX}&H_{XP}\\
H_{PX}&H_{PP}
\end{bmatrix}.
$$

边缘化平板姿态后，手眼有效信息为：

$$
H_{X,k}^{\rm eff}
=
H_{XX}
-
H_{XP}
H_{PP}^{\dagger}
H_{PX}.
$$

## 11.2 候选虚拟测量

候选预测得到：

$$
z_c=
\{
{}^BT_{F,c},
\mathcal P_c^S,
p_{e1,c}^S,
p_{e2,c}^S
\}.
$$

构造增广数据集：

$$
D_k^c=D_k\cup\{z_c\}.
$$

由于角点变量投影依赖全部数据，候选加入后不能简单写为：

$$
H_{\rm new}
=
H_{\rm old}+J_c^TJ_c.
$$

正确方法是重新计算：

$$
J_k^c
=
\frac{\partial r(x_9,D_k^c)}{\partial x_9},
$$

并且每次数值扰动 $x_9$ 时，都重新解析求解：

$$
C^\star(x_9,D_k^c).
$$

然后得到：

$$
H_{X,k}^{\rm eff}(c).
$$

## 11.3 D-opt信息增益

候选的主要信息评分为：

$$
\Delta I_X(c)
=
\frac12
\left[
\log\det
\left(
H_{X,k}^{\rm eff}(c)+\epsilon I
\right)
-
\log\det
\left(
H_{X,k}^{\rm eff}+\epsilon I
\right)
\right].
$$

它表示候选加入后，手眼参数总体不确定性体积的预测下降量。

在信息矩阵尚未满秩时，应优先：

1. 增加手眼有效信息矩阵的秩；
2. 提高最小非零特征值；
3. 满秩后再以 D-opt 信息增益作为主要评分。

E-opt指标：

$$
\lambda_{\min}
\left(
H_X^{\rm eff}
\right)
$$

可以作为最弱方向的安全检查，避免 D-opt 只提高已有强方向而忽略弱方向。

---

# 12. 候选筛选与执行顺序

为了降低计算开销，推荐按照由快到慢的顺序：

```text
1. 在Eu、Ev上采样A、B并生成候选
2. 名义有限平板交线检查
3. 名义传感器ROI检查
4. 快速IK和关节限位检查
5. 9维Sigma点双边有效概率检查
6. 对可行候选计算完整变量投影信息增益
7. 对排名前K的候选进行详细碰撞和路径检查
8. 执行排名最高的可执行候选
```

执行后必须重新检测真实轮廓：

- 两个断点均存在；
- 端点身份正确；
- 轮廓质量合格；
- 当前机器人位姿稳定。

只有通过真实验证的测量才加入标定数据集。

若执行失败：

1. 不加入数据集；
2. 回退到上一个有效位姿；
3. 将候选或其邻域加入黑名单；
4. 增大不确定性下限或安全裕量；
5. 从剩余候选中重新选择。

---

# 13. 每轮12-DOF-V2滚动更新

每获得一帧新的双边观测后，使用全部历史数据重新运行 12-DOF-V2：

$$
D_{k+1}=D_k\cup\{z_k\}.
$$

更新：

$$
\hat X_{k+1},
\quad
\hat R_{pl,k+1},
\quad
\hat C_{k+1},
\quad
\Sigma_{x9,k+1}.
$$

随后重新构造：

- 当前有限平板；
- 两条目标边；
- 安全边段范围；
- 候选池；
- 有效概率；
- 信息增益。

因此：

$$
C_0\rightarrow C_1\rightarrow C_2\rightarrow\cdots
$$

角点并不固定，而是随着有效数据增加不断更新。

整个系统始终使用同一个估计模型：

```text
种子阶段最终求解：12-DOF-V2
自动NBV阶段更新：12-DOF-V2
最终标定：12-DOF-V2
```

---

# 14. 自适应停止

位姿数量不应预先固定。

当满足以下条件时停止：

1. 手眼有效信息矩阵已经满秩；
2. 最优候选信息增益低于阈值：

$$
\max_c\Delta I_X(c)<I_\tau;
$$

3. 最弱方向的信息量足够：

$$
\lambda_{\min}
(H_X^{\rm eff})
>
\lambda_\tau;
$$

4. 连续若干轮信息增益均较小；
5. 当前协方差估计达到设定要求；
6. 已达到最大保护位姿数时强制停止。

建议采用：

```text
最小正式位姿数
+
连续低信息增益次数
+
最大位姿保护数
```

的组合，而不是单次低信息增益立即停止。

---

# 15. 最终标定与输出

自动采集结束后，使用全部有效帧：

$$
D_{\rm all}
=
D_{\rm seed}
\cup
D_{\rm NBV}
$$

进行最后一次12-DOF-V2联合优化。

最终输出：

$$
{}^F\hat T_S,
$$

$$
\hat R_{pl}=[\hat u,\hat v,\hat n],
$$

$$
\hat C,
$$

以及：

- 最终残差；
- Jacobian奇异值；
- 数值秩和条件数；
- 手眼有效信息矩阵；
- 参数协方差估计；
- 总采集位姿数；
- 无效候选和失败执行统计；
- 每轮信息增益曲线。

最终精度不能只通过训练残差或内部协方差评价，还应使用独立验证位姿、独立平面或外部测量进行验证。

---

# 16. 完整状态机

```text
WAIT_MANUAL_INIT
    │
    ├── 未检测到双边 → 等待人工调整
    └── 双边稳定
           ↓
SAVE_INITIAL_VALID_POSE
           ↓
GENERATE_SEED_ROTATION
           ↓
STEP_ROTATE_WITH_ANCHOR
    │
    ├── 双边存在 → TRANSLATION_SERVO
    ├── 单边丢失 → ROLLBACK_AND_REDUCE_STEP
    └── 双边丢失 → ROLLBACK_AND_REPLAN
           ↓
TRANSLATION_SERVO
    │
    ├── 双边稳定 → CHECK_SEED_DIVERSITY
    └── 调整失败 → ROLLBACK
           ↓
CHECK_SEED_DIVERSITY
    │
    ├── 合格 → SAVE_SEED
    └── 重复 → 丢弃并换旋转轴
           ↓
SEED_COUNT_AND_OBSERVABILITY
    │
    ├── 不足 → GENERATE_SEED_ROTATION
    └── 足够
           ↓
SOLVE_12DOF_V2
    │
    ├── Jacobian病态 → 补采种子
    └── 初始化有效
           ↓
BUILD_FINITE_BOARD
           ↓
GENERATE_DOUBLE_EDGE_CANDIDATES
           ↓
NOMINAL_GEOMETRY_AND_IK_FILTER
           ↓
JOINT_UNCERTAINTY_VALIDITY
           ↓
FULL_V2_INFORMATION_SCORING
           ↓
EXECUTE_BEST_CANDIDATE
    │
    ├── 真实双边失败 → ROLLBACK_AND_BLACKLIST
    └── 真实双边成功
           ↓
APPEND_MEASUREMENT
           ↓
SOLVE_12DOF_V2
           ↓
CHECK_STOP
    │
    ├── 未满足 → BUILD_FINITE_BOARD
    └── 满足
           ↓
FINAL_OPTIMIZATION_AND_REPORT
```

---

# 17. 方法中的三个核心机制

## 17.1 高信息密度观测

利用：

$$
\text{平面}
+
\text{两条正交边}
+
\text{共享角点}
$$

构造12-DOF-V2联合几何模型，使每个正式位姿都同时提供平面和双边信息。

## 17.2 双边可见候选生成

利用当前估计：

$$
C,u,v,n
$$

和已知尺寸：

$$
L,W
$$

直接在指定两条边上选择目标交点，从几何源头生成与12-DOF-V2兼容的候选观测。

## 17.3 信息驱动选姿

不再以固定旋转序列、最大运动距离或随机采样作为正式阶段的选姿依据，而是预测候选加入完整12-DOF-V2模型后，对手眼参数有效信息的提升。

---

# 18. 方法的论文逻辑

整篇方法可以围绕“高效、可靠的自动数据采集”展开。

### 贡献1：双边角点高信息密度标定模型

提出12-DOF-V2联合估计模型，将平面、两条相邻边和共享角点统一到同一优化框架，并通过变量投影解析消除角点非线性变量。

### 贡献2：双边可见的自动候选生成

利用平板尺寸、当前角点和平板姿态估计，从指定相邻边上生成目标交点，反求候选机器人位姿，并通过联合不确定性传播保证候选具有较高双边有效概率。

### 贡献3：基于12-DOF-V2信息增益的主动采集

将候选虚拟观测加入完整变量投影模型，重新计算手眼有效信息矩阵，以信息增益选择下一最佳位姿并自适应停止。

自动种子采集是上述系统的启动机制：

$$
\text{预设旋转激励}
+
\text{平移可见性伺服}
$$

使系统只需要一个人工初始双边可见位姿，后续初始测量可以自动完成。

---

# 19. 实现模块建议

```text
calibration_pipeline/
├── pipeline.py
│   ├── Phase 0a TF粗初值
│   ├── Phase 0b 自动种子采集
│   ├── Phase 0c 12-DOF-V2初始化
│   └── Phase 2 NBV主动循环
│
├── seed_collection/
│   ├── rotation_scheduler.py
│   ├── features.py
│   ├── endpoint_tracker.py
│   ├── translation_servo.py
│   ├── seed_observability.py
│   └── ROS适配层中的回退管理
│
├── solvers/
│   └── twelve_dof_v2.py
│
├── v2_backend/
│   ├── residual.py
│   ├── corner_projection.py
│   ├── jacobian.py
│   ├── covariance.py
│   └── information.py
│
├── nbv/
│   ├── candidate_generator.py
│   ├── finite_board_intersection.py
│   ├── profile_predictor.py
│   ├── validity.py
│   ├── scoring.py
│   └── stopping.py
│
└── motion/
    ├── ik_filter.py
    ├── collision_filter.py
    └── executor.py
```

---

# 20. 当前仍需通过实验确定的内容

以下问题属于实现参数和实验验证内容，目前不能直接视为已经证明：

1. 自动种子阶段最合适的旋转轴集合和角度；
2. 小步旋转的最佳步长；
3. 平移伺服交互矩阵的估计方式；
4. 需要5、6还是更多种子位姿才能稳定启动；
5. Jacobian条件数和最小奇异值的验收阈值；
6. 9维协方差是否需要Bootstrap校准；
7. Sigma点数量和有效概率阈值；
8. 边段安全裕量 $m_u,m_v$；
9. D-opt与E-opt的组合方式；
10. 信息增益停止阈值；
11. 真实Gocator环境下断点误检、漏检和反射干扰处理；
12. 候选终点有效与运动全程双边可见之间是否需要进一步统一。

第一版实验应先分别验证：

```text
自动种子采集是否稳定
→ 12-DOF-V2是否能够可靠启动
→ 双边候选预测是否准确
→ 信息增益是否与真实误差下降相关
→ 完整闭环是否优于随机和固定序列
```

---

# 21. 方法总结

整个方法可以压缩为一句话：

> 系统首先通过预设多轴旋转与平移可见性伺服，自动获得旋转多样的双边角点种子观测；随后利用12-DOF-V2联合估计手眼、平板姿态和角点，并结合已知平板尺寸恢复有限平板；在指定两条相邻边上生成双边可见候选激光线，通过联合不确定性传播筛选可靠候选，再依据候选加入完整12-DOF-V2模型后的手眼信息增益选择下一最佳位姿，循环采集并更新，直至信息趋于饱和。

其最核心的逻辑为：

$$
\boxed{
\begin{aligned}
&\text{旋转制造可观测性}\\
&\text{平移恢复双边可见性}\\
&\text{角点法提高单帧信息密度}\\
&\text{有限平板几何生成有效候选}\\
&\text{信息增益决定下一位姿}\\
&\text{滚动估计与不确定性收缩扩大自动探索范围}
\end{aligned}
}
$$
