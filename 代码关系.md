可以。现在这套系统看起来文件很多，但运行主线其实可以归纳为：

> 启动脚本负责组织流程，ROS 节点负责通信与状态机，`handeye_calibration_core` 负责真正的数学算法；仿真与真机只替换底层“传感器与机器人后端”。

## 一、整个系统的总关系

```text
                         第一终端：启动环境
                                  │
                 ┌────────────────┴────────────────┐
                 │                                 │
              仿真后端                           真机后端
        Gazebo + MoveIt + RViz           FANUC + Gocator + RViz
                 │                                 │
                 └──────────统一 ROS 接口──────────┘
                                  │
                 /joint_states
                 /gocator/profile
                 /calibration/flange_pose
                                  │
                         原始轮廓断点检测
                                  │
            /calibration/target_surface_points
            /calibration/endpoints
                                  │
                         第二终端：标定控制台
                                  │
                  ┌───────────────┴───────────────┐
                  │                               │
             自动种子采集                    主动标定 NBV
                  │                               │
              seeds.json            12-DOF-V2 + 候选预测/评分
                  │                               │
                  └───────────────┬───────────────┘
                                  │
                      calibration_result.json
```

仿真和真机在断点检测之后，使用的是同一套种子采集、求解器和 NBV 算法。

---

# 二、两个终端分别启动了什么

## 1. 第一终端：环境启动

总入口是 [start_environment.sh](/workspace/scripts/start_environment.sh:1)。

```bash
./scripts/start_environment.sh sim
./scripts/start_environment.sh real --motion-mode automatic
```

它根据参数分流：

```text
start_environment.sh
├── sim  → start_simulation.sh
└── real → start_real_environment.sh
```

### 仿真入口

[start_simulation.sh](/workspace/scripts/start_simulation.sh:1) 启动：

- Gazebo
- 机器人控制器
- `robot_state_publisher`
- MoveIt `move_group`
- 仿真轮廓生成节点
- 断点检测节点
- RViz 可视化节点
- RViz

主要配置：

- [calib_robot.urdf](/workspace/urdf/calib_robot.urdf:1)：机器人、传感器和标定场景模型
- [fanuc.srdf](/workspace/ros2_ws/src/handeye_sim_bridge/config/fanuc.srdf:1)：MoveIt 规划组
- [gz_controllers.yaml](/workspace/ros2_ws/src/handeye_sim_bridge/config/gz_controllers.yaml:1)：Gazebo 控制器
- [calibration.yaml](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:1)：完整算法参数

### 真机入口

[start_real_environment.sh](/workspace/scripts/start_real_environment.sh:1) 根据运动模式启动：

```text
disabled
└── observe_only.launch.py

plan_only / step_confirm / automatic
└── calibration_with_motion.launch.py
    ├── observe_only.launch.py
    └── fanuc_motion_bridge
```

其中：

- [observe_only.launch.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/launch/observe_only.launch.py:39)：启动真机测量链
- [calibration_with_motion.launch.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/launch/calibration_with_motion.launch.py:24)：在测量链上增加运动桥
- [real_calibration.yaml](/workspace/ros2_ws/src/fanuc_gocator_bridge/config/real_calibration.yaml:1)：真机参数覆盖

配置加载关系是：

```text
calibration.yaml       通用算法默认值
        ↓
real_calibration.yaml  真机特有参数覆盖
```

---

## 2. 第二终端：标定控制台

入口是：

```bash
./scripts/start_calibration.sh --backend real
```

[start_calibration.sh](/workspace/scripts/start_calibration.sh:1) 本身不实现算法，只负责加载 ROS 环境并启动：

- [calibration_console.py](/workspace/scripts/calibration_console.py:292)

控制台的主流程在 [run_new()](/workspace/scripts/calibration_console.py:610)：

```text
创建本次运行目录
        ↓
启动种子采集节点
        ↓
检查初始位姿
        ↓
自动或人工采集六个种子
        ↓
保存 seeds.json
        ↓
启动主动标定节点
        ↓
保存 calibration_result.json
```

种子节点启动代码位于 [_start_seed_node()](/workspace/scripts/calibration_console.py:691)，实际执行：

```bash
ros2 run handeye_sim_bridge seed_collection
```

主动标定启动代码位于 [_run_active()](/workspace/scripts/calibration_console.py:1125)，实际执行：

```bash
ros2 run handeye_sim_bridge active_calibration
```

需要注意：当前控制台在真机采集完种子后会直接返回，代码见 [calibration_console.py:674](/workspace/scripts/calibration_console.py:674)。也就是说：

- 真机自动种子运动已经接通；
- 真机 NBV 自动执行仍未在控制台正式开放；
- 仿真会继续进入主动标定。

---

# 三、仿真后端的文件关系

仿真场景的主要节点是：

- [scene_publisher_node.py](/workspace/ros2_ws/src/handeye_sim_backend/handeye_sim_backend/scene_publisher_node.py:1)

调用关系：

```text
Gazebo
  │
  └── /joint_states
           │
           ▼
scene_publisher_node.py
  ├── 根据关节角计算法兰位姿
  ├── 计算激光平面与标定件的交线
  ├── 生成原始二维轮廓
  ├── 添加仿真噪声
  ├── 发布 /gocator/profile
  └── 发布 /calibration/flange_pose
```

它只生成原始轮廓，不负责从轮廓中提取断点。

仿真噪声模型位于：

- [noise.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/simulation/noise.py:14)
- [scanline.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/simulation/scanline.py:1)
- [synthetic.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/simulation/synthetic.py:17)
- [scene_truth.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/simulation/scene_truth.py:1)

---

# 四、真机后端的文件关系

## 1. FANUC 关节状态

[joint_state_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/joint_state_node.py:25)

```text
FANUC 控制器
    │ Ethernet/IP
    ▼
fanuc_eip.py
    ▼
eip_io_thread.py
    ▼
joint_state_node.py
    ├── /fanuc/joint_states_raw
    └── /joint_states
```

相关文件：

- [fanuc_eip.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/fanuc_eip.py:122)：底层 Ethernet/IP 通信
- [eip_io_thread.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/eip_io_thread.py:15)：通信线程
- [joint_state_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/joint_state_node.py:25)：关节角读取、转换和发布

## 2. Gocator 轮廓

```text
Gocator
   ▼
gocator_profile_driver
   ▼
/gocator/profile_raw_mm
   ▼
profile_metric_adapter_node.py
   ▼
/gocator/profile
```

相关文件：

- [/workspace/ros2_ws/src/gocator_profile_driver](/workspace/ros2_ws/src/gocator_profile_driver)：Gocator 原始驱动
- [profile_metric_adapter_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/profile_metric_adapter_node.py:44)：毫米到米、坐标方向和异常量纲检查

## 3. 机器人位姿与轮廓同步

[measurement_sync_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/measurement_sync_node.py:32)

```text
/joint_states ──────┐
                    ├── measurement_sync_node
/gocator/profile ───┘             │
                                  └── /calibration/flange_pose
```

它负责为轮廓寻找对应时刻的机器人法兰位姿，并检查：

- 时间偏差；
- 机器人是否静止；
- 当前轮廓与关节状态是否能够同步。

---

# 五、断点检测与跟踪

ROS 节点入口：

- [profile_endpoint_detector_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_endpoint_detector_node.py:61)

核心算法：

- [endpoint_detector.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/perception/endpoint_detector.py:115)
- [dual_endpoint_kalman.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/perception/dual_endpoint_kalman.py:49)

关系如下：

```text
/gocator/profile
       │
       ▼
profile_endpoint_detector_node.py    ROS适配、状态机
       │
       ├── endpoint_detector.py      从原始轮廓提取候选线段和两个断点
       │
       └── dual_endpoint_kalman.py   双断点时序预测、关联和滤波
       │
       ├── /calibration/endpoints
       ├── /calibration/target_surface_points
       ├── /calibration/detection_guide
       └── /profile_endpoint_detector/diagnostics
```

两者的职责区别：

- `endpoint_detector.py`：只处理一帧轮廓的几何识别；
- `dual_endpoint_kalman.py`：利用前后帧信息跟踪两个断点；
- `profile_endpoint_detector_node.py`：决定当前是初始对齐、锁定跟踪、预测跟踪还是丢失重捕获。

种子节点还会向检测器发送：

```text
/calibration/detection_measured_prior
/calibration/detection_control
```

用来告诉检测器机器人正在运动、停止、回退或者需要重新锁定。

---

# 六、RViz 可视化

文件：

- [profile_viz_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/profile_viz_node.py:24)
- [handeye_sim_moveit.rviz](/workspace/ros2_ws/src/handeye_sim_bridge/rviz/handeye_sim_moveit.rviz:1)

它订阅：

- 原始轮廓；
- 绿色目标表面点；
- 两个断点；
- 紫色 ROI 和期望线；
- 检测诊断信息。

它只负责显示，不参与断点识别、运动控制或标定求解。因此，RViz 中颜色不对、Marker 不显示，一般先检查这个文件；检测结果本身不对，则检查断点检测节点和核心检测器。

---

# 七、自动六种子采集

ROS 状态机：

- [seed_collection_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/seed_collection_node.py:63)

它接收：

```text
/joint_states
/calibration/flange_pose
/calibration/endpoints
/calibration/target_surface_points
```

它向仿真控制器或真机运动桥发送同一种标准 action：

```text
/joint_trajectory_controller/follow_joint_trajectory
```

种子采集的内部调用关系：

```text
seed_collection_node.py
├── features.py
│   └── 将断点和表面点转换成 x_mid、z_mid、安全余量等特征
├── initial_pose.py
│   └── 判断初始位姿是否满足工作包络
├── rotation_scheduler.py
│   └── 安排局部 ±X、±Y 等星型旋转顺序
├── translation_servo.py
│   └── 断点靠近边界时执行平移居中
├── multiframe.py
│   └── 静止后采集多帧并进行鲁棒平均
└── seed_observability.py
    └── 检查六个位姿的旋转多样性和可观性
```

对应文件：

- [features.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/features.py:13)
- [initial_pose.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/initial_pose.py:17)
- [rotation_scheduler.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/rotation_scheduler.py:9)
- [translation_servo.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/translation_servo.py:8)
- [multiframe.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/multiframe.py:11)
- [seed_observability.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/seed_collection/seed_observability.py:1)

最终数据写入：

```text
/workspace/data/calibration_runs/时间戳/seeds.json
```

---

# 八、真机运动桥

文件：

- [motion_bridge_node.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/motion_bridge_node.py:28)

调用关系：

```text
seed_collection_node / active_calibration_node
           │ FollowJointTrajectory
           ▼
motion_bridge_node.py
           │
           ├── motion_safety.py
           │   ├── 关节步长检查
           │   ├── 局部路径IK
           │   ├── 关节限位检查
           │   └── CURPOS/FK一致性检查
           │
           └── reg_sender.py
                 ▼
           FANUC PC_TRACK_ALL
```

相关文件：

- [motion_safety.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/motion_safety.py:44)：运动安全验证
- [reg_sender.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/reg_sender.py:243)：当前实际使用的 `PcTrackAllStepSender`
- [fifo_ring_sender.py](/workspace/ros2_ws/src/fanuc_gocator_bridge/fanuc_gocator_bridge/fifo_ring_sender.py:58)：FIFO 支持代码，目前不是自动种子主路径

仿真中同一个 action 由 Gazebo 控制器响应，真机中则由 `motion_bridge_node.py` 响应。因此上层种子状态机不需要区分目标机器人是真是假。

---

# 九、12-DOF-V2 与共享形貌求解器

统一求解器入口：

- [twelve_dof_v2.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/solvers/twelve_dof_v2.py:74)

它根据配置决定运行：

```text
surface_model: flat
└── 普通12-DOF-V2平面模型

surface_model: shared
└── 共享形貌12-DOF-V2模型
```

当前默认配置为共享形貌，见 [calibration.yaml:292](/workspace/ros2_ws/src/handeye_sim_bridge/config/calibration.yaml:292)。

求解器内部文件关系：

```text
twelve_dof_v2.py
├── corner_projection.py
│   └── 给定外层状态时，求角点/局部变量 C
├── residual.py
│   └── 普通平面模型残差
├── shared_surface.py
│   └── 共享形貌基函数及共享形貌残差
├── plane_frame.py
│   └── 建立标定件规范局部坐标系
└── information.py
    ├── 数值雅可比
    ├── 信息矩阵
    ├── 协方差
    ├── rank/condition
    └── 消去干扰参数后的手眼信息
```

对应文件：

- [corner_projection.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/corner_projection.py:1)
- [residual.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/residual.py:1)
- [shared_surface.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/shared_surface.py:16)
- [plane_frame.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/plane_frame.py:1)
- [information.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/v2_backend/information.py:1)

求解过程由 [solve()](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/solvers/twelve_dof_v2.py:178) 分流到普通模型或共享形貌模型。

---

# 十、主动标定与 NBV

ROS 执行层：

- [active_calibration_node.py](/workspace/ros2_ws/src/handeye_sim_bridge/handeye_sim_bridge/active_calibration_node.py:106)

纯算法状态机：

- [pipeline.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/pipeline.py:27)

总体流程：

```text
seeds.json
    │
    ▼
dataset_io.py
    │
    ▼
ActiveCalibrationPipeline
    │
    ├── TwelveDofV2Solver.solve()
    │       └── 得到当前手眼、标定件和共享形貌估计
    │
    ├── candidate_generator.py
    │       └── 生成候选法兰位姿
    │
    ├── profile_predictor.py
    │       └── 预测候选位姿下的二维轮廓和两个断点
    │
    ├── validity.py
    │       └── 判断未来观测是否可能有效
    │
    ├── scoring.py
    │       └── 计算手眼信息增益、有效概率和运动代价
    │
    ├── MoveIt规划与执行
    │
    ├── 采集真实新观测
    │
    ├── 将新旧数据一起重新求解
    │
    └── stopping.py
            └── 判断继续NBV还是信息已经饱和
```

NBV 文件：

- [candidate_generator.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/candidate_generator.py:1)：候选生成
- [profile_predictor.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/profile_predictor.py:109)：未来轮廓预测
- [finite_board_intersection.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/finite_board_intersection.py:1)：激光平面与有限标定件求交
- [validity.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/validity.py:16)：观测有效性
- [scoring.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/scoring.py:19)：信息增益评分
- [stopping.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/nbv/stopping.py:11)：自适应停止

`pipeline.py` 只做算法决策，不直接控制机器人。源码中也明确规定机器人运动位于 ROS/硬件适配层之外，见 [pipeline.py:27](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/pipeline.py:27)。

---

# 十一、基础数据和工具文件

这些文件被多个模块共同调用：

- [models.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/models.py:32)：位姿、测量、候选、估计结果等数据结构
- [geometry.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/geometry.py:1)：旋转矩阵、四元数、SE(3) 等数学工具
- [dataset_io.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/dataset_io.py:16)：读取和保存种子数据
- [initial_validation.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/initial_validation.py:15)：初解稳定性检查
- [validation.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/validation.py:14)：独立留出数据验证
- [sphere_validation.py](/workspace/ros2_ws/src/handeye_calibration_core/calibration_pipeline/sphere_validation.py:19)：精密球独立精度验证
- [sphere_validation_console.py](/workspace/scripts/sphere_validation_console.py:1)：精密球实验控制台

---





