# 平板角点法手眼标定：12-DOF 完整方法

> 日期：2026-07-03
> 状态：✅ 实测验证 — R=0.0225°, t=0.09mm（9 个角点位姿，3 朝向）
> 
> 理论基础：`/home/z/research_contact_handeye/verification/Num2/PRINCIPLE.md`（§2-5：gauge 分析、信息效率、对称群证明）
> 代码：`calibrate.py`、`auto_calib_node.py`、`common/calib_solver.py`

---

## 一、理论框架

### 1.1 动机：为什么需要角点

线激光 + 单平面标定（Sharifzadeh 2020）的瓶颈：$n_B$（平板法向量）和 $R_{he}$（手眼旋转）在约束方程中以乘积形式出现——

$$n_B^T \cdot \big(R_i(R_{he} \cdot p_S + t_{he}) + t_i\big) = d_B$$

单个位姿只能给出 $n_B^T R_i R_{he}$ 这一个组合——法向量和旋转"耦合"在一起，单个位姿下无法分离。每个位姿贡献约 2 个有效独立方程（1 个来自平面平移梯度 $n_B^T R_i$，1 个来自旋转梯度 $n_B^T R_i R p_S$），需要 ~48 个位姿积累足够多样性才能把耦合撕开。

**平板角点打破这个耦合**：引入两条正交边的断点。边约束走面内方向（$v_B$, $n_{\perp 2}$），平面约束走法向（$n_B$）。三个方向线性独立（PRINCIPLE.md §2.4 证明）→ 每个角点位姿贡献 ~4 个有效独立方程。**信息密度翻倍，理论最少位姿从 5 降到 3。**

| | 纯平面（Sharifzadeh） | 角点法 |
|:--|:--|:--|
| 未知数 | 10 | 12 |
| 每 pose $t_{he}$ 梯度方向 | $\{n_B^T R_i\}$ — 1 个 | $\{v_B^T R_i, n_{\perp 2}^T R_i, n_B^T R_i\}$ — 3 个 |
| 每 pose 有效独立方程 | ≈ 2 | ≈ 4 |
| 理论最少位姿 | 5 | **3** |
| 实际推荐位姿数 | 48 | **6-9** |

**精度不是主打**——48 位姿的统计平均效应本身有精度优势。我们的 claim 是：**同等精度可以用更少的位姿达到，且位姿选择有理论指导**（Jacobian 秩分析告诉你旋转不能全共轴、平移不能全共面）。

### 1.2 数学模型

**状态向量**（12 参数）：

$$\theta = [\omega_{he}(3),\; t_{he}(3),\; \omega_{pl}(3),\; C(3)] \in \mathbb{R}^{12}$$

| 参数 | 含义 |
|:--|:--|
| $\omega_{he}$ | 手眼旋转（轴角），$R_{he} = \exp([\omega_{he}]_\times)$，传感器→法兰 |
| $t_{he}$ | 手眼平移（传感器→法兰） |
| $\omega_{pl}$ | 平板姿态（轴角），$R_{pl} = [u_B, v_B, n_B]$，$u_B$ 对齐边 1 |
| $C$ | 角点在基座标系下的位置（两正交边的交点） |

**已知量**：$R_i, t_i$（FK）、断点 $p_{S,e1}, p_{S,e2}$（Gocator 扫描线端点落在板边处）、平板尺寸 $w \times h$。

**坐标系变换**（传感器→法兰→基座标）：

$$p_B = R_i(R_{he} \cdot p_S + t_{he}) + t_i$$

**约束方程**（PRINCIPLE.md §4.3，通用记法适用于任意已知夹角 $\alpha \neq 0,\pi$）：

$$r_{e1} = v_B^T \cdot (p_{B,e1} - C) = 0 \quad\text{（边 1：断点到 $u_B$ 方向线的垂直距离为零）}$$

$$r_{e2} = n_{\perp 2}^T \cdot (p_{B,e2} - C) = 0 \quad\text{（边 2：断点到 $d_2$ 方向线的垂直距离为零）}$$

$$r_{plane} = n_B^T \cdot (p_B - C) = 0 \quad\text{（平面：点落在过 $C$ 的平板上）}$$

其中边 2 方向 $d_2 = \cos\alpha \cdot u_B + \sin\alpha \cdot v_B$，边 2 法向量 $n_{\perp 2} = n_B \times d_2 = \cos\alpha \cdot v_B - \sin\alpha \cdot u_B$。

本文实际场景为**直角矩形板**（$\alpha = \pi/2$），此时 $n_{\perp 2} = -u_B$，$d_2 = v_B$，边 2 约束退化为 $u_B^T \cdot (p_{B,e2} - C) = 0$。

### 1.3 为什么共享角点消除 Gauge（耦合机制）

**关键洞察**（PRINCIPLE.md §3.2）：两条边共享同一个 $C$，而非各有独立原点。

**若两条边有独立原点 $C_1, C_2$**：平移变换 $t_{he} \to t_{he} + \delta$ 可与 $C_1 \to C_1 + \Delta_1$、$C_2 \to C_2 + \Delta_2$ 联合补偿。令 $A_i = R_i\delta$，两边的残差变化分别为 $(A_i - \Delta_1)^T v_B = 0$ 和 $(A_i - \Delta_2)^T n_{\perp 2} = 0$。$\Delta_1$ 和 $\Delta_2$ 独立 → 存在非零的 $(\delta, \Delta_1, \Delta_2)$ 使两式同时成立 → **多维权衡空间（gauge）**。

**角点法中共享 $C$**：只有单一 $\Delta$（$C \to C + \Delta$）。两边约束同时施加在同一个 $\Delta$ 上：

$$A_i = R_i\delta - \Delta$$

边 1：$A_i^T v_B = 0 \;\Rightarrow\; A_i \perp v_B$；边 2：$A_i^T n_{\perp 2} = 0 \;\Rightarrow\; A_i \perp n_{\perp 2}$。

$v_B$ 和 $n_{\perp 2}$ 张成平板面 → $A_i \parallel n_B$，即：

$$R_i\delta - \Delta = \lambda_i n_B \quad \forall i \tag{★}$$

方程 (★) 对不同 $R_i$ 给出不同的 $\lambda_i$。仅用边缘约束时，$\delta = 0$ 且 $\Delta = \alpha n_B$（$\Delta$ 沿法向自由）是残差不变的解——**1 维残余 gauge**。

加上平面约束 $n_B^T \cdot (p_B - C) = 0$：$C \to C + \alpha n_B$ 时平面残差变化 $n_B^T \cdot \alpha n_B = \alpha \neq 0$ → 残余 gauge 被消灭。

**对称群从多维压缩到平凡。** 这就是角点法每 pose 信息效率翻倍的数学根源（PRINCIPLE.md §2.4-2.5）。

### 1.4 代码实现中的残差形式

对于直角矩形板（$\alpha = \pi/2$），代码使用等价的 cross-product 形式：

```python
# 状态: θ = [ω_he(3), t_he(3), ω_pl(3), C(3)] — 12 参数
R_he = so3_exp(ω_he)
[u_B, v_B, n_B] = so3_exp(ω_pl).T  # R_pl 的列

p_B = R_i @ (R_he @ p_S + t_he) + t_i

r_e1 = cross(p_B_e1 - C, u_B)     # 3 分量, 2 独立 (⟂ u_B)
r_e2 = cross(p_B_e2 - C, v_B)     # 3 分量, 2 独立 (⟂ v_B)
r_plane = n_B @ (p_B - C)         # 1 分量
```

$cross(p - C, u_B) = 0$ 强制 $p - C \parallel u_B$，等价于 $v_B^T(p-C) = 0$ **且** $n_B^T(p-C) = 0$（前者是边 1 约束，后者与平面约束冗余）。同理 $cross(p - C, v_B) = 0$ 等价于 $u_B^T(p-C) = 0$ 且 $n_B^T(p-C) = 0$。代码用满 3 分量是为了简洁——LM 的 pseudoinverse 自动处理冗余。

**与旧 9-DOF 的关键区别**：① 边约束过 $C$（不用差向量）；② 平面不去心（相对于 $C$）。旧 9-DOF 的 `plane_vals - mean(plane_vals)` 消除绝对参考点 → $t_{he}$ 沿 $n_B$ 存在 1-DOF gauge → Jacobian 秩 = 8 < 9。

**12-DOF Gauge 证明**（cross-product 形式）：

假设 $C \to C + \delta_C$、$t_{he} \to t_{he} + \delta_t$ 使所有残差不变。

- 边 1：$cross(\delta_C, u_B) = 0$ → $\delta_C \parallel u_B$
- 边 2：$cross(\delta_C, v_B) = 0$ → $\delta_C \parallel v_B$
- $u_B \perp v_B$ → **$\delta_C = 0$**（边约束单独锁定 $C$）

带 $\delta_C = 0$：
- 边 1：$cross(R_i\delta_t, u_B) = 0$ → $R_i\delta_t \parallel u_B$
- 边 2：$cross(R_i\delta_t, v_B) = 0$ → $R_i\delta_t \parallel v_B$
- 平面：$n_B \cdot R_i\delta_t = 0$ → $R_i\delta_t \perp n_B$

$R_i\delta_t$ 同时 ∥ $u_B$、∥ $v_B$、⟂ $n_B$ → $R_i\delta_t = 0$ → **$\delta_t = 0$**。

$\delta_\omega = 0$ 类似论证（若 $\omega_{pl}$ 改变 → $u_B, v_B, n_B$ 方向改变 → 残差非零）。

**QED：12-DOF 满秩，Jacobian 秩 = 12，无需正则化。**

### 1.5 与 PRINCIPLE.md 的对应关系

代码与 PRINCIPLE.md 理论框架完全一致，仅有实现层面的等价变换：

| PRINCIPLE.md | 代码 (`calib_solver.py`) | 说明 |
|:--|:--|:--|
| $v_B^T(p-C)=0$（边 1，标量） | `cross(p-C, u_B)`（3 分量） | 等价：$cross=0 \iff p-C \parallel u_B \iff v_B^T(p-C)=0$ 且 $n_B^T(p-C)=0$。后者与平面约束冗余 |
| $n_{\perp 2}^T(p-C)=0$（边 2） | `cross(p-C, v_B)`（3 分量） | 直角 $(\alpha=\pi/2)$ 时 $n_{\perp 2}=-u_B$，$cross(p-C, v_B)=0 \iff u_B^T(p-C)=0$ |
| $n_B^T(p_B-C)=0$（平面） | `n_B @ (p_B-C)` | 完全一致 |
| 状态 $\theta = [\omega_{he}, t_{he}, \omega_{pl}, C]$ | 同 | 完全一致 |
| Jacobian 解析推导 | 有限差分（$\epsilon=10^{-6}$） | 实现选择：解析形式仅用于信息效率分析，LM 用有限差分更简单 |

**差异**：代码的 cross-product 形式将平面约束嵌入边约束中（边断点同时满足边和平面），对 LM 无影响（pseudoinverse 自动处理冗余行）。

### 1.6 $u_B \leftrightarrow v_B$ 对称性被打破

旧 9-DOF $cross(p_B^{[k+1]} - p_B^{[k]}, u_B) = 0$ 中 $C$ 被差向量消去 → 交换 $u_B$、$v_B$ 同时重标断点所属边，残差不变 → ~90° 对称解。

12-DOF $cross(p_B - C, u_B) = 0$ 中 $C$ 是锚点。$p_{B,e1}$ 的测量值在 $u_B$ 线上（不在 $v_B$ 线上）→ 交换 $u_B \leftrightarrow v_B$ 后 $cross(p_{B,e1} - C, v_B) \neq 0$。**对称性被 $C$ 的自然物理含义打破。** 实测确认无对称解残留。

---

## 二、求解管线

### 2.1 闭式解 $R_{he}$（Xu 2021 直边法，可选）

**核心观察**：同一法兰朝向 $R_b$ 下，同一条边上的所有断点共线——**这个约束只依赖 $R_{he}$**，不依赖 $C$ 或平板位置。

两点差 $u_b = R_b R_{he} u_s + u_t$（$u_s = p_S^{[j]} - p_S^{[i]}$，$u_t = t_i^{[j]} - t_i^{[i]}$），三点的共线性：

$$\| u_b^{[ij]} \times u_b^{[ik]} \| = 0$$

关键项 $R_b R_{he}(u_s^{[ij]} \times u_s^{[ik]})$ 中，叉积在传感器 $XZ$ 平面内 → 纯沿 $y$ 方向 → **$R_{he}$ 的中间列出现**（与平面约束不同，后者 $y=0$ 消掉中间列）。利用 Kronecker 积线性化为 $A \cdot \text{vec}(R_{he}) = b$，堆叠所有朝向 × 所有边 × 所有三点组 → SVD 最小二乘 → 投影到 SO(3)。

**局限性**：从 $R=I$ 出发，阻尼 Gauss-Newton 限制步长 >30°。当真实 $R_{he}$ 距 $I$ 超过 ~30° 时（如仿真中 RPY ≈ 92.5°），闭式解几乎不动。因此 2.3 节的多重重启是必需的。

### 2.2 初始化 $t_{he}$、$\omega_{pl}$、$C$

给定候选 $R_{cf}$（来自闭式解或随机采样），估算其余 9 个参数：

1. **$t_{he}$ 线性解**：跨朝向边距离约束 $v_B^T \cdot (R_j - R_i) \cdot t_{he} = \cdots$，消去未知 $C$ → 线性最小二乘
2. **$\omega_{pl}$ 估算**：用 $(R_{cf}, t_{cf})$ 将断点变换到基座标系，同朝向内 SVD 拟合边方向 → 正交化 → $R_{pl}$
3. **$C$ 估算**：两条边线在基座标系中的最小二乘交点（含法向分量由平面约束确定）

### 2.3 12-DOF LM — 多重随机重启

闭式解不可靠时的解决方案：在 SO(3) 上均匀采样 $N=20$ 个随机初始旋转，各运行完整 `init_12dof()` + `solve_12dof_lm()`，选 cost 最低的解。

```python
theta_opt, info = solve_12dof_with_restarts(poses, meas, n_restarts=20)
# info = {'n_good': 17, 'n_tried': 20, 'best_cost': 2.57e-06}
```

实测：所有法兰旋转 >140° 时（闭式解仅移动 0.001°），17/20 重启收敛到 R=0.02°。3 个未收敛的落入对称分支（cost 略高，可通过平板尺寸 post-hoc 排除）。

**LM 参数**：$\lambda_0 = 10^{-4}$，有限差分 Jacobian（$\epsilon = 10^{-6}$），无正则化，满秩系统快速收敛。

### 2.4 完整流水线

```
采集 6-9 个位姿（≥2 朝向，角点+纯边混合）
    ↓
20 次随机重启:
  R_rand ← random SO(3) sample
  θ_init = init_12dof(poses, meas, R_cf=R_rand)
  θ_opt = solve_12dof_lm(θ_init)            ← 满秩 LM
    ↓
选 cost 最低的解
    ↓
post-hoc: 平板尺寸合规检查（断点投影是否在 [0,400mm]/[0,500mm] 内）
    ↓
R_he, t_he, ω_pl, C
```

---

## 三、采集操作指南

### 3.1 启动

```bash
cd /home/z/research_contact_handeye/verification/Sim
./start.sh --tmux
```

### 3.2 键盘控制（auto_calib_node）

| 按键 | 作用 |
|:--|:--|
| `r` | 记录当前位姿 |
| `s` | 显示位姿列表 |
| `c` | 运行 12-DOF 标定（20 重启） |
| `w` | 保存到 `~/recorded_poses.json` |
| `q` | 保存并退出 |

### 3.3 RViz 中看什么

灰色平板（400×500mm）+ 绿色 FOV 三角 + 黄色扫描线 + 红/蓝球（断点 e1/e2）。终端每帧打印 `e1=1 e2=1`（角点）或 `e1=1 e2=0`（纯边）。

### 3.4 采集方式

**最低要求**：≥6 位姿，≥2 朝向（夹角 >30°），全体位姿覆盖两边各 ≥1 次。

**推荐方式 A**（3 朝向 × 3 角点 = 9 个）：

```
用 MoveIt2 拖动法兰：

朝向 A: 摇到角点(FOV过两边) → r  #1
        沿边平移 ~3cm       → r  #2
        沿边平移 ~3cm       → r  #3

朝向 B: 换朝向(转J6 ±30°或加pitch/yaw) → 摇角点 → r  #4-6

朝向 C: 再换朝向 → 摇角点 → r  #7-9

按 w 保存 → 按 c 标定
```

**方式 B**（混合纯边位姿）：

```
朝向 A: 角点位姿 ×1              ← 锁定 C
朝向 A: 纯边位姿 ×2（平移 >10cm）← 改善条件数
朝向 B: 角点位姿 ×1
...
```

纯边位姿优势：FOV 只需切一条边，平移不受限。但**至少需要一个角点位姿锁定 $C$**——边约束 $cross(p-C, u_B)=0$ 只锁定 $C$ 在 $\perp u_B$ 方向的分量，缺少另一条边则 $C$ 沿该边方向可自由滑动。这与理论一致：3 个位姿要达到满秩必须每个都同时过两边；实际位姿数更多时只需至少一个角点位姿。

### 3.5 运行标定

```bash
PYTHONPATH=./common python3 calibrate.py ~/recorded_poses.json
```

或 Docker 终端按 `c`。

### 3.6 朝向检查

标定前终端显示朝向分组：
```
朝向组: 3
  G0: 3 poses  G1: 3 poses  G2: 3 poses
  G0↔G1: 170.4°  G0↔G2: 81.8°  G1↔G2: 140.8°
```

**确保 ≥2 组，彼此夹角 >30°。** 单朝向会失败（朝向间约束不足，$t_{he}$ 不可观测）。

---

## 四、实测验证

数据：9 个角点位姿（3 朝向，间距 82°–170°），$R_{he}$ 真值 ~92.5°（URDF: RPY=[27.8°,9.2°,-86.5°]）。

| 指标 | 12-DOF（20 重启） | 目标 |
|:--|:--|:--|
| **R 误差** | **0.0225°** | <0.1° ✅ |
| **t 误差** | **0.09mm** | <0.1mm ✅ |
| **C 收敛** | **[0.70, 0.00, 0.25]** | ✅ |
| **Jacobian 秩** | **12/12** | 满秩 ✅ |
| **收敛率** | **17/20** 重启 | ✅ |
| **cost** | **2.57×10⁻⁶** | ✅ |

---

## 五、发现的问题与解决

| # | 问题 | 现象 | 根因 | 解决 |
|:--|:--|:--|:--|:--|
| 1 | **R_gt 硬编码错误** | R 恒 ~90°，t 完美（0.09mm） | auto_calib_node 中 R_gt 为 ~1.6°；URDF 真值 ~92.5° | 从 URDF 读取 RPY 替换 |
| 2 | **边界约束扭曲 LM** | cost 0.13，t 误差 50mm | 平板尺寸铰链损失混入优化梯度 | 移除；平板尺寸仅 post-hoc |
| 3 | **t_he 初始化极差仍收敛** | t 误差 265mm → LM 得 0.09mm | 12-DOF 满秩 → 梯度覆盖全空间 | 不需额外修复 |
| 4 | **双分支仲裁降质** | 90° 旋转后 cost 恶化 | 替换解不在真正极小值 | 回退单分支 LM |
| 5 | **u_B↔v_B 对称不存** | 理论担忧，实测不存在 | $C$ 作为锚点打破对称 | 无需处理 |
| 6 | **闭式解卡在 R≈I** | 法兰旋转 >140° 时闭式解仅移 0.001° | 阻尼限制步长 >30°，距真值 92° | 多重随机重启（17/20 收敛） |

**核心原则**：纯几何 LM + post-hoc 合规检查 + 多重随机重启。物理约束不混入优化目标。

---

## 六、代码参考

| 文件 | 内容 |
|:--|:--|
| `calibrate.py` | 标定管线（加载 → 12-DOF → 真值对比） |
| `common/calib_solver.py` | `solve_he_closed_form()`, `init_12dof()`, `solve_12dof_lm()`, `solve_12dof_with_restarts()` |
| `auto_calib_node.py` | 仿真采集节点（键盘 r/s/c/w/q） |
| `verify_12dof.py` | 12-DOF 验证（含 SVD 满秩检查） |

**唯一外部输入**：平板尺寸。不需要 URDF 名义值、不需要平板位姿、不需要角点位置。

---

# 附录 B：自动化采集方案（基于 Xiao 2022 框架 + 2D 轮廓适配）

> 日期：2026-07-03
> 状态：设计中
> 参考文献：
> - Xiao et al. 2022 — "An automatic calibration algorithm for laser vision sensor in robotic autonomous welding system" (J. Intell. Manuf.)
> - Yang et al. 2022 — "A Fast Calibration of Laser Vision Robotic Welding Systems Using Automatic Path Planning" (IEEE TIM)
> - Yang et al. 2023 — "Next-Best-View Selection for Robot Eye-in-Hand Calibration" (arXiv:2303.06766)

---

## B.1 Xiao 2022 框架要点

论文将自动标定分解为三个子系统，全部无需手眼先验：

**Phase 1: 探索运动** — 从初始位姿做 2 次小幅随机移动，用 Navy 算法（Park & Martin 1994）粗略估计 $^b_M H$（标定板在基座标系中的位姿）。

### B.1.1 为什么 Navy 算法不适合线激光（以及我们的替代方案）

Navy 算法本质是解 $AX=XB$：

$$^b_M H \cdot \Delta^c_M H = \Delta^b_t H \cdot {}^b_M H$$

要求两个输入：$\Delta^b_t H$（机器人相对运动，FK 可得 ✅）和 $\Delta^c_M H$（传感器相对板的运动）。Xiao 2022 用相机拍标定板 → **PnP 解 6-DOF 位姿**拿到 $\Delta^c_M H$。线激光只有一条交线，丢失了板平面内的平移和旋转 → **无法直接解 $^c_M H$** → Navy 不可用。

**替代方案（Xu 2021 风格的两阶段闭式解）**：

利用名义手眼 $(R_{HS}^{nom}, t_{HS}^{nom})$ 将 3-4 次小幅扫描点转到基座标系 → **PCA 拟合平面** → 得到板平面粗估 $(n_B, d)$：

$$n_B = \arg\min_{||n||=1} \sum_i \sum_j \big(n^T \cdot p_{B,ij} - d\big)^2$$

其中 $p_{B,ij} = R_{BH,i} R_{HS}^{nom} p_{S,ij} + R_{BH,i} t_{HS}^{nom} + t_{BH,i}$。

**为什么粗估"够用"**：

| | Xiao 2022 的 Navy 估计 | 本方法的 PCA 估计 |
|:--|:--|:--|
| 偏置来源 | PnP 误差 | 名义手眼误差 |
| Phase 1 角色 | 粗估板位姿 | 粗估板位姿 |
| Phase 2 用法 | 约束候选筛选 | 约束候选筛选 |
| Phase 3 修正 | 联合优化修正 | 联合优化修正 |

两者在框架中的角色完全等价——都是"有偏但够用的粗估"，给 Phase 2 的约束方程提供初始几何信息。代码中对应 `init_plane_from_scans()` → 输出 `(n_B, d)` → 传入 Phase 2 的激光面-板平面相交约束。

**关于名义手眼先验**：

其他线激光手眼标定论文（Sharifzadeh 2020、Yang 2022、Xiao 2022）同样依赖某种形式的先验——相机内参对相机方法相当于我们的机械安装尺寸。提供两种方案：

| 方案 | 适用场景 | 方法 |
|:--|:--|:--|
| **A: 名义值法**（默认） | 有 CAD/卡尺粗略测量 | Phase 1 PCA 用 $(R_{HS}^{nom}, t_{HS}^{nom})$ 转点，Phase 3 LM 修正 |
| **B: 多重随机重启** | 完全无先验 | Phase 1 跳过，直接 20 次随机重启 LM 盲搜 9-DOF |

方案 A 更高效（约束方程减少无效探索），方案 B 更通用（零先验）。两者在 Phase 3 收敛结果等价——LM 的 basin of attraction 足够大。

**Phase 2: 运动约束方程** — 建立两类约束保证采集数据有效：

1. **机器人运动方程**（正文 Eq.3-5）：将相机运动需求映射为机器人关节运动

2. **运动范围约束方程**（正文 Eq.6-7）：
   $$\theta = \cos^{-1}(Z_m, -Z_c) < \theta_{max}$$
   限制相机 Z 轴与标定板 Z 轴夹角，保证激光线不出视野。用 EKF 在线修正 $^b_M R$。

**Phase 3: 变换矩阵拟合** — 手眼矩阵（Tsai + GA 精化）+ 激光平面（RANSAC 平面拟合）。

---

## B.2 线激光适配

Xiao 2022 用**相机**看标定板（2D 图像 + 特征点）。我们用**线激光**看平板（2D 轮廓 = 深度分布 + 点数 + 断点位置）。

核心映射：

|| Xiao 2022（相机） | 本方法（线激光） | 说明 |
|:--|:--|:--|
| 图像中心到板中心距离 | 激光中心深度 $Z_{center}$ | 2D 轮廓点的 Z 坐标均值 |
| 标定板图案覆盖率 | 轮廓点数 $N_{pts}$ | 激光线落在板上的有效点数 |
| 相机 Z 轴与板 Z 轴夹角 | **Z 值分布范围** $Z_{max} - Z_{min}$ | 替代斜率 k — 见下文 |
| 图像特征点提取 | 轮廓端点检测（曲率突变 → 断点） | 断点位置用于"碰壁"检测 |
| 相机外参 $^C_M H$ | 板平面参数 $(n_B, d)$ | Phase 1 PCA 估算 |

### B.2.1 核心约束：FOV 梯形 + 平板边界 — 统一处理

线激光传感器的坐标原点在激光平面（FOV 三角形内，之前 FOV 校准时标定）。2D 轮廓点的 Z 值从该原点起算。Gocator 的有效测量区域是一个**梯形**：从激光窗口沿 Z 轴 270mm 处开始有效，820mm 处截止。

**关键洞察**：激光线与平板的交线如果伸出了这个梯形，超出的部分**没有任何 2D 轮廓数据**。这和激光线碰到平板边缘的效果**物理上完全一致**——都是某侧信号丢失。

因此，不需要区分"平板边缘"和"FOV 梯形边缘"。两者统一处理：

| 信号 | 含义 | 动作 |
|:--|:--|:--|
| $N_{pts}$ 相对于锚点下降 > 50% | 激光线接近了某个边界（平板边 或 FOV 梯形边） | **激光线中心向板中心 C 移动** |
| $N_{pts} < 10$ | 激光几乎完全离开有效区域 | 紧急回退到上一有效位姿 |
| `|Z_center - Z_0| > 20mm` | 深度漂移 | Z 方向伺服回正 |

**相对量优于绝对量**：$Z_0$ 是锚点位姿下的激光中心深度——操作工初始摆放保证它在有效区域内。后续位姿的 Z 值只要不偏离锚点太远就是安全的。不需要依赖 Gocator 规格中的 270/820 绝对值（传感器原点与 Gocator 光学参考点的偏移未知）。

**$Z_{max} - Z_{min}$ 的作用保留**：这是传感器倾斜程度的直接度量，不受绝对偏移影响。倾斜越大 → 范围越大 → 可能更接近 FOV 边界 → 结合 N_pts 检查即可。

### B.2.2 适配后的运动范围约束：Z-range 检查替代 θ_max

原 B.1 中的 $\theta = \arccos(|n_{laser} \cdot n_B|) < \theta_{max}$ 过于粗糙——它不看实际深度值，只看角度。更精确的做法是：

**直接检查激光面-板交线上的预测 Z 值是否与锚点 Z_0 一致**：

$$\text{valid} \iff |Z_{pred} - Z_0| < 50 \text{ mm}$$

其中 $Z_{pred}$ 是将板平面上的候选交点投影到传感器系后得到的 Z 分量，$Z_0$ 是锚点位姿的激光中心深度。`compute_laser_plate_intersection()` 已在 `plane_calib.py` 中实现了交线计算和端点投影，可直接复用。

**关于 FIM 自动处理绕激光线旋转**：传感器绕其 X 轴（激光线方向）旋转时，2D 轮廓可能不变（局部不可观测）。这不需手动排除——NBV 的 FIM 信息增益计算会自然地给这些方向低分（协方差矩阵的零特征值 → 零增益）。Yang 2023 的框架本身处理了这个退化。

### B.2.3 "弹球反弹"机制

当激光线接近边界时（$N_{pts}$ 下降），系统应向板中心方向收缩，而非试图判断碰的是哪条边。

**统一策略：向板中心 C 移动**

| 现象 | 动作 | 原理 |
|:--|:--|:--|
| $N_{pts}$ 下降（任一原因） | 激光线中心向板中心 C 方向移动 20mm | 边界在远离 C 的方向 → 向 C 收缩肯定安全 |
| $N_{pts} < 10$ | 紧急回退到上一有效位姿 | 完全丢失 → 需要已知安全点 |
| `|Z_center - Z_0| > 20mm` | Z 方向伺服回正 | 深度漂移独立于边缘 |

**为什么不需要区分"哪条边"**：

板中心 C 是 Phase 1 PCA 的副产品（所有转点均值）。无论激光线碰到了板的哪条边、还是 FOV 梯形的哪条边，边界一定在远离 C 的方向上——因为 C 在板中心，激光线目前的中心偏离了 C 才导致碰壁。所以向 C 移动一定是安全的。

**与 NBV 候选筛选的互补关系**：NBV（Phase 2）做**事前预防**——通过 Z-range 检查过滤会导致离开有效区域的候选位姿。弹球做**实时兜底**——万一 NBV 预测有误或 IK 执行偏差导致边界接近，弹球兜底拉回 C。

---

### B.2.4 借鉴 Xiao 2022：自动数据采集闭环的关键机制

Xiao 2022 的相机系统与我们线激光系统有本质差异（2D 图像 vs 1D 轮廓），但闭环采集的**架构模式**高度可迁移。

#### (1) Eq.5 = vTCP：手眼矩阵在采集阶段自动消掉

Xiao 的 Eq.5 约束标定板原点 $P_m$ 始终投影在图像中心。给定候选朝向 $^{b_t}R_i$，平移 $^{b_t}T_i$ 被唯一确定。关键洞察：**推导中手眼矩阵 $^{t_c}H$ 被消去了**——规划运动不需要知道手眼参数。

这正是 vTCP 的数学本质。我们的 vTCP 同样让手眼矩阵在位移计算中消去：传感器到法兰的变换是手眼，法兰到基座标的变换是 FK，串联后手眼出现在两侧，相消。

→ **Phase 1 和 Phase 2 的运动规划完全不需要手眼先验。** 手眼仅在 Phase 3 被求解。

#### (2) Eq.6-7：预判约束，而非事后兜底

Xiao 不是等激光离开板再拉回——他在执行位姿**之前**就用 Eq.7 预测新位姿下的 $\theta_i$，用 Eq.6 检查，违反阈值则拒绝候选。

我们的 Phase 2 已采用这个模式：Z 偏移预检查（$|Z_{pred} - Z_0| < 50$mm）在 FIM 评估之前过滤候选。弹球反馈是兜底，不是主力。

#### (3) Eq.12：每次移动后做位置修正

Xiao 的简化图像 Jacobian 解决了一个实际问题：IK 执行偏差导致 $P_m$ 偏离图像中心。每次移动后，观察实际偏移 → 用 Jacobian 计算 $(dx, dy)$ 修正量 → 微调机器人位置。

**线激光等价方案**：每次移动后，检查 `|Z_center - Z_0|`。若超过 5mm → 沿传感器 Z 轴微调机器人 ±(Z_center - Z_0)。这比"仅触发伺服"更系统——**作为每次移动后标准步骤执行**，不是异常处理。

#### (4) EKF 增量更新：每 1 个位姿更新，非每 5 个

Xiao 用 EKF 每采集一个位姿就更新 $^{b_M}R$ 估计。这比"每 5 个位姿批量重估"更稳健——累积误差在下一帧就被纠正。

**线激光适配**：每采集一个新位姿，用新增数据增量更新 $(n_B, d, C)$ 估计。不需要跑完整的 9-DOF LM，可以用：
- 新点加权加入 PCA 拟合（指数衰减权重，最近位姿权重大）
- 或轻量 LM（仅更新板平面参数，手眼不动）→ 速度快

→ Phase 2 第 5 步从"每 5 个位姿重新估计"改为 **"每 1 个位姿增量更新板平面"**。

#### (5) 候选朝向：随置信度逐步扩展范围

Xiao 的候选生成是小步长随机（0-3° Z，±5° X/Y），因为相机 FOV 小。但启示是：**开始保守，随估计精度提高再扩展**。

线激光适配：Phase 2 初期用 ±15°~±25°，中期当板估计协方差（`parameter_covariance()` 的 det）降到阈值以下时，扩展到 ±35°~±45°。

---

## B.3 约束方程（线激光版 Yang 2022 Eq.12）

Yang 2022 的光轴约束保证相机始终看到标定板中心。线激光的类比：

**vTCP（虚拟 TCP）**：初始位姿下，激光线中心 $X=0$ 打在板上的物理点。

$$p_B^{TCP} = t_{BS,0} + R_{BS,0} \cdot [0, 0, Z_0]$$

**通俗解释**：想象你用激光笔指一面墙。

1. 初始姿势：你的手在位置 A（$t_{BS,0}$），手腕朝方向 A（$R_{BS,0}$），激光打在墙上 P 点
2. 现在你想转动手腕到方向 B（$R_{BS,new}$），**但希望激光仍然打在同一个 P 点**
3. 因为手腕转了，手的位置必须跟着调整：手的位置_新 = P - 方向_新 × 距离

**约束方程**：对任意目标传感器朝向 $R_{BS,new}$，保持激光中心打在同一点的传感器位置：

$$t_{BS,new} = p_B^{TCP} - R_{BS,new} \cdot [0, 0, Z_0]$$

对应法兰位姿（传感器 → 法兰 → 基座标）：

$$R_{BH,new} = R_{BS,new} \cdot R_{HS}^T$$

$$t_{BH,new} = t_{BS,new} - R_{BH,new} \cdot t_{HS}$$

**为什么需要这个**：Xiao 2022 框架的核心思想是"先定朝向，自动算位置"。你只选传感器朝向（保证激光面与板相交），这个方程自动算出传感器中心应该在哪——不需要手动调 XYZ。候选位姿生成只需遍历朝向，位置由方程给出。

**注意**：$Z_0$ 是锚点位姿下的激光中心深度值。vTCP 方程仅在 **Phase 1** 使用（无板平面估计时保障激光在板上）。Phase 2 有 $(n_B, d, C)$ 后，直接用板平面生成传感器位置（$t_{BS} = C + d_{offset} \cdot n_B$），不再需要 vTCP。

---

## B.4 完整自动化管线（v2：Z-range 约束 + 弹球反馈 + Sharifzadeh 风格 Phase 1）

```
┌──────────────────────────────────────────────────────────────┐
│  Phase 0: 初始化（操作工一键启动）                              │
│                                                              │
│  1. 操作工将激光放到板中心区域 → 按 'g'                         │
│  2. 系统提取锚点特征:                                          │
│     - 传感器系→基座标系: R_BS_0, t_BS_0 (FK × 名义手眼)        │
│     - 激光中心深度: Z_0 = Z_center（当前扫描点的 Z 均值）       │
│     - 锚点点数: N_anchor（当前扫描的有效点数）                  │
│     - Z 范围: Z_min, Z_max                                    │
│  3. 验证锚点位姿: N_pts > 20 → 激光在板上 → 合格               │
│     N_pts < 20 → 提示操作工调整位姿                              │
│  4. 计算 vTCP = t_BS_0 + R_BS_0 · [0, 0, Z_0]                │
│  5. 记录 pose_0                                               │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 1: 探索运动 — 粗略估计板平面（Sharifzadeh 2020 风格）    │
│                                                              │
│  目标: 从锚点出发，6 次多样化小扰动 → PCA 拟合板平面              │
│                                                              │
│  扰动序列（传感器系运动，用 vTCP 方程计算法兰位姿）：             │
│    pose_1: 传感器 +X 30mm（沿激光线平移）                      → 扩大覆盖宽度  │
│    pose_2: 传感器 -X 30mm                                    → 对称覆盖     │
│    pose_3: 传感器 +Z 20mm（靠近板）                           → 深度变化     │
│    pose_4: 绕 Y +10°（倾斜激光面）                              → 角度多样性   │
│    pose_5: 绕 Y -10°                                          → 对称角度     │
│    pose_6: 绕 Z +8°（旋转激光平面）                             → 平面内旋转   │
│                                                              │
│  每次移动后:                                                  │
│    a. vTCP 方程 → 法兰位姿 → IK → 移动                         │
│    b. 检查 Z_center: |Z_center - Z_0| < 50mm? → 有效则记录       │
│       N_pts > 0.5 × N_anchor?             → 有效则记录          │
│       任一失败 → 回退到锚点，跳过此扰动                          │
│                                                              │
│  收集完成后:                                                  │
│    → 用名义手眼将全部 7 个位姿(含 pose_0)的扫描点转到基座标系    │
│    → PCA 拟合平面 → (n_B, d)                                  │
│    → 估算板中心 C ≈ 所有转点均值                                │
│                                                              │
│  参考: Sharifzadeh 2020 用 48 位姿圆形模式做 PCA。              │
│  我们的 7 位姿更多样（6 个方向 vs 圆形）、位姿更少                │
│  → PCA 质量足够用于 Phase 2 候选筛选，Phase 3 会联合优化修正    │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 2: 约束引导的自动采集（板平面直接约束 + NBV + 增量更新）  │
│                                                              │
│  初始化:                                                      │
│    θ_current = 初始 9-DOF 参数（名义手眼 + Phase 1 的 n_B, d）  │
│    C = Phase 1 估算的板中心                                   │
│    poses_collected = [pose_0 .. pose_6]                       │
│    scans_collected = [scan_0 .. scan_6]                       │
│    cand_angles = [±15°, ±25°]          ← 初始保守范围          │
│                                                              │
│  Phase 2 有板平面估计，不需要 vTCP：直接用板平面约束生成候选位姿   │
│                                                              │
│  核心原则（来自 Xiao 2022）:                                    │
│    - 手眼矩阵在运动规划中消去，Phase 2 不需要手眼先验             │
│    - 每次移动后标准执行 Z 位置修正（Xiao Eq.12 线激光版）         │
│    - 每 1 个位姿增量更新板平面（Xiao EKF 思路）                  │
│    - 候选范围随协防差缩小逐步扩展（Xiao 保守起步思路）             │
│                                                              │
│  NBV 循环 (目标: 25 个位姿):                                   │
│  ┌─────────────────────────────────────────────────┐         │
│  │  1. 生成候选传感器朝向 (N=30):                     │         │
│  │     绕 RX/RY/RZ: cand_angles (随置信度扩展)        │         │
│  │     初期: ±15°, ±25°                              │         │
│  │     中期: det(Σ_plane) < τ → 扩展至 ±35°, ±45°    │         │
│  │     (绕 X 不会自动被排除——FIM 会自然给低分)         │         │
│  │                                                   │         │
│  │  2. 对每个候选朝向 R_BS_cand:                       │         │
│  │     a. 在板平面上方生成传感器位置:                   │         │
│  │        t_BS = C + d_offset · n_B                  │         │
│  │        (d_offset ≈ 500mm, 保证在最佳工作距离)       │         │
│  │     b. Z 预检查 (Xiao Eq.6-7 等价):                │         │
│  │        |Z_pred - Z_0| < 50mm? → 失败则跳过         │         │
│  │     c. 预测扫描点 pts_S (交线采样 50 点)            │         │
│  │     d. FIM 信息增益 = predict_info_gain(θ_current, │         │
│  │        scans, poses, R_BH_cand, t_BH_cand, pts_S)  │         │
│  │     e. 按增益排序                                   │         │
│  │                                                   │         │
│  │  3. 选增益最大的候选 → IK → MoveIt2 执行             │         │
│  │                                                   │         │
│  │  3b. Z 位置修正 (Xiao Eq.12 线激光版 — 每次移动后):  │         │
│  │       计算 Z_center = 当前扫描点 Z 均值              │         │
│  │       IF |Z_center - Z_0| > 5mm:                  │         │
│  │         沿传感器 Z 轴修正 ±(Z_center - Z_0)         │         │
│  │         → IK → 移动 → 重新扫描                       │         │
│  │                                                   │         │
│  │  4. 特征检查 + 弹球反馈:                             │         │
│  │     ✓ N_pts > 0.5×N_anchor 且 Z_center ≈ Z_0     │         │
│  │       → 记录位姿 → 继续                              │         │
│  │     ⚠ N_pts 下降但 > 10（接近某边界）                │         │
│  │       → 激光线中心向 C 移动 20mm → 记录              │         │
│  │     🔴 N_pts < 10（完全离开有效区域）                 │         │
│  │       → 回退到上一有效位姿 → 下一个候选               │         │
│  │     ⚠ |Z_center - Z_0| > 20mm (修正后仍超限)      │         │
│  │       → Z 方向伺服修正 → 记录                        │         │
│  │                                                   │         │
│  │  4b. 增量更新板平面 (Xiao EKF 等价 — 每个位姿后):      │         │
│  │       - 新扫描点加权加入累积点集                       │         │
│  │         权重 ∝ exp(-λ·(当前帧 - 旧帧)), λ≈0.3      │         │
│  │       - 加权 PCA 更新 (n_B, d, C)                   │         │
│  │       - 更新 θ_current (仅平面参数)                  │         │
│  │       - 若 det(Σ_plane) < 阈值 → 扩展 cand_angles   │         │
│  └─────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 3: 联合优化 — 平面约束手眼标定                           │
│                                                              │
│  目标函数:                                                    │
│    min Σ_i Σ_j [n_B · (R_BH_i R_HS p_S_ij + R_BH_i t_HS     │
│                       + t_BH_i) - d]²                        │
│                                                              │
│  DOF: 9 (R_HS 3, t_HS 3, n_B 2, d 1)                       │
│  约束数: ~25 位姿 × 50 点 = 1250 约束（严重过定）             │
│                                                              │
│  求解: 多重随机重启 LM (20 次)                                 │
│        - 无正则化（9-DOF 满秩，平面约束本身无 gauge）          │
│        - 选 cost 最低的解                                    │
│                                                              │
│  输出: R_HS, t_HS, n_B, d                                   │
└──────────────────────────────────────────────────────────────┘
```

### B.4.1 约束层次总结

| 层级 | 约束类型 | 作用阶段 | 机制 |
|:--|:--|:--|:--|
| **几何约束** | Z 偏移检查（$|Z_{pred} - Z_0| < 50$mm） | Phase 2 候选筛选 | 事前过滤 — 预测交线 Z 值是否接近锚点 |
| **信息约束** | FIM 信息增益排序 | Phase 2 候选排序 | 定量选优 — 选对参数估计改善最大的候选 |
| **反馈约束** | N_pts 下降检测 + Z 偏移检测 | Phase 2 执行后 | 事后兜底 — 边缘反弹、深度伺服、紧急回退 |
| **板平面约束** | n_B · (p_B) - d = 0 | Phase 3 联合优化 | 最终求解 — 全部位姿联合优化手眼参数 |

### B.4.2 与旧版的关键差异

| | 旧版 B.4 (v1) | 新版 B.4 (v2) |
|:--|:--|:--|
| Phase 1 位姿数 | 4 (含锚点) | **7** (6 方向多样化扰动 + 锚点) |
| Phase 1 风格 | 简单平移 | **Sharifzadeh 2020 风格**：覆盖 X/Z/RY/RZ 多方向 |
| Phase 1 位置保障 | 无明确机制 | **vTCP 方程**（"定朝向 → 自动算位置"，Phase 1 必须） |
| Phase 2 位置生成 | vTCP 方程 | **板平面直接约束**（$C + d\_offset \cdot n_B$，有板估计后不需要 vTCP） |
| 位姿有效判定 | θ < θ_max + \|k\| > 0.4 | **Z 相对锚点检查**（\|Z_center - Z_0\| < 50mm） |
| 边缘处理 | 无 | **弹球反弹**：统一向 C 收缩（不区分板边/FOV边） |
| 候选生成 | 固定 RX/RY/RZ ±10°/18°/25° | 板平面约束位置 + Z-range 过滤 + **FIM 增益排序** |
| 传感器倾斜判定 | 斜率 k（拟合，不稳定） | **Z_max - Z_min**（零拟合误差） |
| 不可观测方向处理 | 无 | **FIM 自动低分**（框架内处理） |
| 板边/FOV边区分 | 未考虑 | **不区分**——两者 N_pts 下降的效果相同，统一向 C 回拉 |

---

## B.5 关键差异：为什么不是随机行走

之前在 v5-v8 中的随机关节行走失败，因为：
- 关节空间扰动无法保证激光在板上 → 一旦丢失无法恢复
- 没有利用 2D 轮廓数据做反馈控制

Xiao 2022 框架的核心优势：**运动约束方程 + 传感器特征反馈 = 确定性保证**。每个位姿的可行性在被执行前已被约束方程验证。

---

## B.6 实现

| 文件 | 内容 |
|:--|:--|
| `auto_calib_node.py` (v9) | 约束方程 + 特征反馈 + 两阶段自动化 |
| `common/plane_calib.py` | 平面约束求解器 + FIM 计算 |
